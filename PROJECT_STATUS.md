# 과탐실 AI 탐구 플랫폼 - 현재 상태 및 인수인계

## 2026-09-13 UI·준비물 변경 GitHub 및 운영 반영 완료 / Codespaces 동기화 미완료

- 사용자 “응 반영해” 승인 후 제품 커밋 `8b0c7b9`를 GitHub main에 전송했다. 공개 후보 435파일 비밀값 패턴/로컬 비밀값 일치 0건, 변경 공백 검사 통과. GitHub 자동 검사 run `34748885454` success, 기존 Cloud Build의 운영 이미지 생성 success를 확인했다.
- 기존 Cloud Run에 새 이미지를 배포하고 준비 단계 DB 건강 응답 확인 뒤 트래픽 100%를 전환했다. Ready=True, 로그인/건강 응답 200, 지난 기록/문서 버전 비로그인 접근 403, 준비물 POST 비로그인 접근 403을 확인했다. 최초 준비물 검사는 지원하지 않는 GET을 사용해 405였으며 POST로 정정했다. 임시 배포 태그는 제거했다. 운영 식별자와 세부 증거는 공개 제외 work/deploy-ui-result.json에 보관한다.
- 운영 DB를 읽기 전용으로 재조회해 마이그레이션 0012, total_amount bigint를 확인했다. 복구한 1·2·3·4반 7조의 금액과 synced 상태 4건 모두 유지. 조회 시 성공 20건·실패 1건이며 실제 신청을 시험 전송하거나 수정하지 않았다. 임시 운영 DB 연결을 종료했다.
- Codespaces 직접 주소, 목록의 기존 작업실 링크, 재접속에서 GitHub 500/시간 초과가 발생했다. 목록에 미커밋 변경이 있다고 표시돼 작업실 초기화·재생성·강제 덮어쓰기는 하지 않았다. GitHub/운영 반영은 완료지만 기존 Codespace의 소스 동기화와 개발 미리보기 검증은 미완료다. 접속 복구 후 기존 변경부터 확인하고 main을 통합해야 한다. 아래 반영 미승인/미수행은 과거 상태다.

## 2026-09-13 PC·태블릿 화면 깨짐 수정 재개·로컬 검증 완료

- 준비물 복구 후 중단했던 UI 작업을 재개했다. 교사 문서 영역을 한 열로 배치하고 본문 읽기 폭을 제한했다. 긴 문서/URL 줄바꿈, AI 검토·이력 버튼 아래 안내문 간격, 모바일 이력 버튼 배치를 수정했다. 추가로 팀 대화의 긴 URL이 말풍선 밖으로 넘치는 문제를 확인해 줄바꿈을 적용했다. 권한·제출·복원 처리 로직은 변경하지 않았다.
- 운영 자격정보를 제외한 임시 PostgreSQL과 합성 계획서·보고서·이력·2,929자 URL로 실제 브라우저를 검증했다. 교사 1440/1024/768/390/360px에서 문서·대화·이력 영역의 가로 넘침 0건, 문제 안내문 3곳의 버튼 간격 10px를 확인했다. 태블릿·휴대폰 화면을 시각 확인하고, 360px에서 이력 비교창 열기/닫기 후 미저장 교사 피드백 유지도 확인했다. 학생 대화의 390px 긴 URL 줄바꿈을 확인했다. 실제 기기 검증이나 모든 학생 탭 검증 완료로 확대하지 않는다.
- 최종 Next 빌드와 TypeScript, 52경로 생성 통과. 첫 빌드는 검증용 standalone 서버의 파일 점유(EBUSY)로 실패했으나 두 검증 서버를 종료한 재실행이 통과했다. 기존 전체 588검사 결과를 유지하며 이번 추가 CSS는 실제 화면으로 검증했다. 검사 화면 크기를 원복했다.
- 변경은 현재 로컬 작업본에만 있다. Git 커밋·푸시·Codespaces 동기화·운영 배포는 미수행이다. 이전 푸시는 명시적 외부 반영 승인 부족으로 자동 승인 검토에서 거절된 이력이 있어, 완성된 변경을 보고한 뒤 별도 반영 지시를 받아 진행한다. 전면 UI/UX 재설계는 후속 작업이다.

## 2026-09-13 실제 준비물 실패 건 대조·복구 완료

- 사용자 후속 지시 “대조 후 복구해봐”에 따라 운영 신청과 실제 학교 Google Sheet를 대조하고 1·2·3·4반 7조 4건을 복구했다. 모두 활성 팀·활성 회차이며 더 최근 신청은 없고, 현재 7조 영역의 학생/품목 입력란이 비어 있음을 쓰기 직전에 확인했다. 9반 99조는 보관 팀이므로 실패 기록을 보존하고 전송하지 않았다.
- 복구 내용: 1반 1품목 4,900원(A38:J42), 2반 9품목 63,160원(A44:J53), 3반 9품목 32,810원(A46:J55), 4반 4품목 6,000원(A42:J46). 총 23품목 106,870원. 2·3반은 7조 소계 앞에 5행씩 추가했다. 현재 7조 영역이 존재하므로 조 자체를 중복 생성하지 않았다.
- 4반의 기존 불변 전송 위치를 보존하고, 옛 1·2·3반 신청에는 사용자 승인·현재 팀과 빈 시트 대조 근거를 명시한 복구 스냅샷을 만들었다. 원래 신청 내용·제출자·제출일·예산 승인 상태는 그대로 유지했다. 전송 배치와 중복 방지 표식을 먼저 고정하고, Google 커넥터로 적용한 뒤 모든 품목/수식/소계가 원문과 일치함을 읽어 확인한 건만 운영 DB에 synced 및 복구 감사 이력을 기록했다. 애플리케이션이나 DB 스키마를 새로 배포하지 않았다.
- 2반 종합 I59의 #REF! 및 잘못된 참조를 실제 조별 소계로 바로잡아 230,350원, 4반 종합 I52의 잘못된 참조를 바로잡아 157,760원을 확인했다. 다른 조의 입력 내용은 변경하지 않았다. 행 삽입에 따른 수식 이동과 명시한 종합 합계 수정을 제외한 네 탭의 기존 A:J 값/수식 차이 0건을 확인했다.
- 운영 재조회: synced 19건, failed 1건(보관된 9반 99조). 네 복구 건의 전송 예약 해제 및 원래 제출일 유지 확인. 네 반의 실제 Google Sheets 화면에서 품목과 소계/종합 표시를 확인했다. 기존 고정 열 너비는 유지해 긴 품명·링크는 셀 선택으로 전문을 확인하는 상태다.
- 대조 원문·배치·검증 증거는 Git 제외 work/material-audit에 보관하고 공개 문서에 학생 이름·학번·원문을 넣지 않았다. 임시 운영 DB 연결 종료, 브라우저 검사 크기 원복. 아래 ‘옛 실패 건 복구 미수행’은 이 작업 전 상태다. 예산 상한 제거·조 자동 생성·UI 개선 코드의 운영 배포는 계속 별도 미수행이다.

## 2026-09-13 준비물 실패 조사·예산 상한 제거 (로컬 구현·검증 완료)

- 실제 운영 DB를 읽기 전용으로 조회: 성공 15건·실패 5건. 1반 7조(8/31), 2반 7조(9/8), 3반 7조(9/6), 4반 7조(9/8), 보관된 9반 99조(8/30) 모두 조 입력 영역 탐색 실패 계열. 학생 이름·신청 원문을 DB 조사 출력에 포함하지 않았다. 운영 연결과 일치하는 원본 시트의 현재 1반·4반에는 7조 영역이 있어, 실패 당시 실제 누락/양식 불일치를 현재 상태만으로 확정하지 않는다.
- 4반 7조는 불변 전송 위치 기록이 있고 아직 전송 배치 미생성 상태이며 시트 전송 예약을 점유한다. 1·2·3반 및 9반 99조 옛 기록은 전송 위치·완료 기록이 없어 기존 자동 재전송 차단을 보존한다. 실제 재전송·운영 시트 수정은 하지 않았다. 임시 운영 DB 연결 종료.
- 구현: 학교 시트 전체 행에서 조를 찾고, 조가 없으며 검증된 원본 열/조 양식/종합 SUM 참조가 있을 때만 종합 앞에 영역을 삽입한다. 다른 조의 내용 대신 서식만 복사하고 품목·소계·종합 참조·중복 방지 표식을 하나의 배치로 기록한다. 부분/중복/다른 조를 가로지르는 영역은 차단한다.
- 예산 상한·초과 경고·승인 안내를 제거하고 도움말도 갱신했다. 단가/배송비의 임의 금액 상한을 제거하고 안전한 정수 총액만 허용한다. 합계 저장을 BIGINT로 넓히는 0012 마이그레이션과 화면/API 숫자 변환을 추가했다. 과거 승인 상태 기록은 보존한다.
- 검증 완료: 관련 8파일 62검사, 전체 103파일 588검사, 타입 검사, 52경로 빌드 통과. 분리된 실제 PostgreSQL 16에서 6,020,000,000원의 BIGINT 저장·화면 숫자 변환·전송 성공·동일 건 재시도 시 전송 1회 유지를 확인했다(`scripts/local-postgres-material-budget.mjs`, 실행 시 `--experimental-transform-types --import ./scripts/local-material-test-loader.mjs`). Google 전송은 합성 대체이며 운영 시트에 실제 쓰기를 검증한 것은 아니다.
- 처음 전체 검사는 빌드와 동시 실행으로 224초 소요됐으나 588검사 모두 통과했다. 시험 DB는 제한된 프로세스 권한으로 최초 시작 실패 후 허용된 로컬 실행으로 시작했고, Node 타입 변환 옵션을 적용한 최종 시험이 통과했다. 이전 UI 변경 3파일 보존. 커밋·푸시·운영 배포·옛 실패 건 복구는 수행하지 않았다. 적용 순서는 변경 검토 및 배포 승인 → 마이그레이션 포함 운영 반영 → 전송 위치 기록이 있는 4반 7조 확인/재전송 → 옛 기록의 신청·대상 시트 대조 후 별도 복구다. 현재 1반·4반의 8조 소계 표시는 D열에 있고 일부 종합 수식 참조도 불일치하므로 기존 원본 문제를 자동 정정했다고 주장하지 않는다.

## 2026-09-13 최신 기능 클라우드 통합 완료

- 최종 제품 커밋 `6bb070e`의 Linux 자동 검사 run `34745213287`도 success로 완료됐다. 아래 통합 진행·누락 정정은 해결 전의 이력이다. 최신 기능 통합, 클라우드 실행, 학생·교사 핵심 동작 확인이 완료됐으며 전면 UI 개편·운영 배포 완료를 뜻하지 않는다.

- 제품 통합 커밋 `cf192a7`을 GitHub main과 실제 Codespace에서 확인했다. 해당 커밋의 Linux 자동 검사 run `34743957991` 성공. 로컬 환경 분리 3개·103파일 581개 검사·52경로 빌드·타입 검사 및 실제 PostgreSQL 11개 검증 결과와 함께 판단한다.
- 합성 개발 자료로 학생·교사 지난 기록 검색, 대화/일지 필터와 날짜 상세를 확인했다. 학생·교사 계획서/보고서에서 과거 두 버전의 목적·방법 차이 2개를 실제 비교했다. 학생 비교창 A/B 교환, 390×844/768×1024 화면과 로그아웃도 확인했다. 교사 보고서 비교창을 닫은 뒤 미저장 피드백이 그대로 유지됨을 확인하고 시험 초안을 지웠다. 실제 학생 자료에는 쓰지 않았다.
- 미리보기 서버의 3000번 응답 중단을 확인했다. 같은 개발 폴더에서 `node scripts/cloud-dev.mjs dev`로 다시 실행해 Ready 및 교사 화면/API 복구를 확인했다. 패키지 재설치나 개발 DB 초기화는 하지 않았다. 중단 원인을 메모리 부족으로 확정할 근거는 없다.
- 교사 변경 이력에서 서버 UTC와 브라우저 한국 시간의 차이로 초기 화면 재생성 경고가 발생함을 확인했다. 이력 목록·복원 확인의 표시를 기존 비교창의 `versionTime`으로 통일했다. UTC/서울/뉴욕에서 같은 한국 시각을 확인했고 52경로 빌드·타입 검사와 관련 2파일 9검사가 통과했다. 보완 커밋 `6bb070e`를 실제 Codespace에서 확인하고 교사 화면을 새로고침해 한국 시간 표시와 초기 재생성 경고 해소를 확인했다. 개발 Codex의 ChatGPT 로그인도 유지된다.
- 운영 배포·실제 데이터 변경은 하지 않았다. 다음 제품 작업은 사용자 사진의 긴 URL 넘침·버튼 안내문 겹침·태블릿 공간 배분 문제를 먼저 재현하고 수정하는 것이다. 이번 합성 화면 검증을 해당 UI 문제 해결로 확대하지 않는다.

## 2026-09-13 최신 기능 통합 진행

- 기본 프로젝트와 최신 기능 worktree의 대상 파일 813개를 별도 백업하고 SHA-256 일치를 확인했다. 원본 worktree는 수정하지 않았다.
- 검색·두 버전 비교 관련 소스와 검증/배포 기록 38개를 현재 프로젝트에 가져왔다. 클라우드 컨테이너·검사 설정·개발 로그인 안내·상대 주소 로그아웃 수정은 유지했다. 아래 누락 정정의 미통합 표기는 이 작업 시작 전 상태다.
- 통합본의 실제 로컬 PostgreSQL 16 검증 11개 통과: 과거/현재 조합, 65개 동일 시각 커서 탐색, 문서·회차·팀 권한, 이전 팀원 역할, 조회 무변경, 완료 회차 보존본, 잘못된 원문, 조회 중 권한 회수. 전체 검사와 GitHub/Codespaces 반영은 진행 중이며 아직 완료로 판단하지 않는다.
- 로컬 전체 103개 파일·581개 검사와 환경 분리 3개 통과. 최초 빌드가 백업 폴더의 이전 코드를 타입 검사에 포함해 실패하여 tsconfig에서 output/work/tmp를 제외했다. 수정 뒤 타입 검사·52개 경로 빌드가 정상 종료했다. 제품 소스의 줄바꿈/파일 끝 빈 줄만 정규화했고 원본 기능은 유지했다. 근거: `output/cloud-integration/local-validation.log`, `final-build.log`, `postgres-validation.log`. 공개 후보 437개 파일의 로컬 비밀값 일치·인증 키 패턴 검사 0건, 변경 공백 검사 통과. 다음은 승인된 GitHub 전송과 Codespaces 실제 기능 확인이다.
- 사용자가 태블릿의 큰 빈 공간·작은 표시와 PC의 긴 URL 넘침·버튼 안내문 겹침 사진을 제공했다. 후속 UI/UX 방향: 최신 기능 통합 완료 → PC/태블릿 공통 화면 깨짐 재현·수정 → 교사 팀 상세 화면 시안 → 교사/학생 순차 적용·기기별 검증. 현재 요청은 우선 방향 정리이며 전면 UI 개편은 시작하지 않았다. 원문과 확정 권한/저장 동작을 보존한다.
- 실제 운영 백업 복구 훈련과 운영 서비스 계정 Sheet 종단 권한은 별도 미완료 상태다. 이번 통합에서 운영 배포나 실제 학생 자료 변경은 하지 않는다.
- 공개 전송 감사: 자동 승인 검토가 새 배포 문서의 내부 프로젝트/아티팩트 메타데이터를 이유로 첫 전송을 거절했다. 원본 배포 문서·스크립트는 로컬 백업에 보존하고 공개용 문서에서는 새 운영 식별자를 생략했다. 과거 운영 배포 스크립트 3개도 공개 변경에서 제외했다. 제품 코드·합성 검증 기능에는 영향이 없다.

## 2026-09-13 이전 완료 판정 정정 — 최신 기능 누락 확인

- 사용자가 공유한 `WORK_ASTRA 핸드오프 최종 점검` 대화를 대조한 결과, 아래 클라우드 이전 완료는 개발 도구·로그인·미리보기 동작에 한정한다. 최신 제품 소스 전체 이전 완료 판정은 철회한다.
- 별도 `b8c4` worktree에 79차 지난 기록 검색과 80차 두 버전 비교의 실제 구현·검증·배포 기록이 존재한다. 현재 기본 프로젝트와 GitHub/Codespaces의 `f4eb12e`에는 두 기능이 없다. 원본 작업은 보존돼 있으며 아직 통합하지 않았다.
- 운영 Cloud Run을 읽기 전용 재조회해 `[비공개 운영 기록에 보존]`가 최신 Ready 리비전이며 트래픽 100%임을 확인했다. 별도 worktree의 DEPLOYMENT_79.md·DEPLOYMENT_80.md와 일치한다. 아래 00030-vet가 현재 운영 최신이라는 취지의 기록은 오래된 정보다.
- 지난 기록 구현은 학생·교사 화면에 연결돼 있고 제목/본문 한글 검색, 전체/대화/일지 필터와 날짜 상세를 포함한다. 80차 전체 검사 로그 102파일·580개 통과를 확인했다. 이번 점검에서 해당 기능 전체를 다시 테스트한 것은 아니다.
- 실제 운영 백업 복구 훈련과 운영 서비스 계정의 Sheet 종단 권한 점검은 기존 문서상 미완료이며 이후 완료 근거를 찾지 못했다. 클라우드 이전 검사의 564개 통과로 이 범위를 완료했다고 판단하지 않는다.
- 다음 필수 조치: 최신 worktree의 변경을 보존·비교한 뒤 현재 클라우드 설정 및 로그아웃 수정과 통합하고 전체 재검증한다. 통합 전 이 개발본을 운영에 배포하면 운영의 최신 기능이 빠질 수 있다. 이번 요청에서는 상태 점검과 이 정정 기록만 수행했으며 소스 통합·운영 배포는 하지 않았다.

## 클라우드 개발환경 이전 완료 (2026-09-13)

- 최신 상태: 사용자 승인 후 최신 소스와 이전 설정을 GitHub main에 저장·전송했다(`87da89a`, 이미지 수정 `bf075de`, Codex/로그인 안내 `839a5f3`, 로그아웃 수정 `aa30445`). 아래의 미전송·승인 대기는 당시 진행 이력이다.
- 실제 Codespace 생성·컨테이너 구성·패키지 설치·개발 DB 초기화 성공. 최초 Node 이미지 태그 부재는 실제 배포된 `5.2.1-24-bookworm`으로 수정했다. 클라우드에서 Node 24.21.0, pnpm 11.19.0, PostgreSQL 16 연결과 운영 DB·Google·푸시 연결 없음 확인. 검증한 앱 소스는 `aa30445`다.
- 작업실과 미리보기 접속 주소는 사용자 대화에서 전달하며 공개 문서에는 넣지 않는다. Ports의 3000번 Private를 실제 확인했다. 교사·마스터 첫 화면과 학생 탐구 첫 화면 로그인 성공. 합성 학생 화면의 390×844 / 768×1024 브라우저 크기에서 전체 가로 넘침 없음 확인(실제 휴대폰·태블릿 전체 기능 검증은 아님).
- 클라우드에서 로그아웃이 내부 0.0.0.0 주소로 이동하는 문제를 발견해 현재 브라우저 주소의 `/login`으로 돌아오게 수정했다. 실제 학생 로그아웃 후 정상 미리보기 `/login` 도착 확인. 개발 로그인 화면에는 비밀번호를 표시하지 않는다.
- 최신 Linux 자동 검사: GitHub Actions run `34740865907`, commit `aa30445`, success. 환경 분리 3개, 100개 파일·564개 검사, 49개 경로 빌드와 타입 검사 통과. 로그아웃 회귀 1개가 추가됐다. 이전 `839a5f3` 검사는 새 검사로 대체되어 취소됐으며 최신 검사는 성공했다.
- Codex CLI 0.154.0을 실제 작업실에 설치하고 버전을 확인했다. 같은 버전을 Dockerfile에도 고정했다. 사용자가 장치 코드 인증을 활성화하고 직접 인증을 완료했으며 터미널의 Successfully logged in을 확인했다. 프로젝트 폴더에서 Astra·High로 실행해 화면의 `gpt-6-astra high`와 실제 한국어 응답까지 확인했다. `CLOUD_DEVELOPMENT.md`에 사용법을 기록했다. 이 데스크톱 대화가 클라우드 Codex 대화로 자동 이전되지는 않으므로 이 문서와 CURRENT_DECISIONS.md로 이어받는다.
- 운영 Cloud Run·Cloud SQL 배포/데이터 변경 없음. 미리보기 앱 자체의 실제 AI는 기본 꺼짐이며 학생용 AI 응답 검증은 하지 않았다. 개발을 지시하는 Codex 계정 연결과 응답은 완료했다. 이후 개발은 작업실의 Codex에 한국어로 요청하고 별도 미리보기에서 확인한다.

- 사용자 요청: 최신 노트북 소스를 보존해 GitHub Codespaces에서 개발하고 휴대폰·태블릿에서 미리보기를 확인한다. 기존 Cloud Run·Cloud SQL 운영은 유지한다.
- 변경 전 소스 407개를 `output/cloud-migration/source-before-20260913-134009/`에 복사하고 해시 일치를 확인했다. 기존 미커밋 기능 변경을 보존한다.
- `.devcontainer`에 Node 24·pnpm 11.19.0·PostgreSQL 16·한글 PDF 글꼴과 비공개 3000번 미리보기를 구성했다. 개발 DB는 전용 볼륨에 저장하며 운영 연결을 넣으면 실행기가 거절한다. `.github/workflows/validate.yml`은 검사만 실행하며 운영 배포는 하지 않는다.
- `scripts/cloud-dev.mjs`는 개발/검증 프로세스에서 운영 DB·Google Sheets·푸시 자격정보와 환경 파일 기본값을 비운다. 실제 AI는 별도 개발용 선택값으로만 켜며 검사에서는 항상 끈다. `.env.*`·임시 폴더·개발 세션 파일은 Git 제외다. 사용 안내는 `CLOUD_DEVELOPMENT.md`다.
- 현재 검증: 환경 분리 3개 검사 통과. 로컬 전용 PostgreSQL 16의 `science_dev`에서 실제 초기화 완료(마이그레이션 11개·합성 계정 4개). Windows `--import` 경로를 파일 URL로 보완했다. 공개 후보 414개 파일에서 로컬 비밀값 일치/주요 키 서명 0건(`output/cloud-migration/source-audit.json`); 이는 모든 개인정보에 대한 포괄적 인증이 아니다.
- 로컬 검증 완료: `cloud:check` 정상 종료(99개 파일·563개 검사, 49개 경로 빌드, 타입 검사; `output/cloud-migration/local-validation.log`). 개발 교사·학생의 실제 로그인과 각 첫 화면 HTTP 200 확인(`login-check.json`). 개발 서버는 검증 뒤 종료했다. 변경 공백 검사도 통과했다.
- 사용자가 GitHub 로그인을 완료했다. 원격 main은 여전히 `77b7044cc0988670b2eae1ce1de7fd1439fe4d6e`이며, 로컬 준비·검증을 마친 소스를 최종 저장·전송하는 단계다. 실제 Codespace 생성·Linux 설치·클라우드 미리보기는 아직 검증되지 않았다. 운영 변경은 수행하지 않았다.
- Git 저장 후보는 329개 변경(기존 기능 변경 포함)으로 확인했고 제외 대상 비밀 파일은 없다. 새 추적 파일의 끝 공백·빈 줄만 정리했다. 커밋 시도는 자동 승인 검토가 AGENTS.md의 전체 작업 완료 전 커밋 금지를 이유로 거절해 실행되지 않았다. 클라우드 생성에 필요한 선행 커밋·푸시의 규칙 예외를 사용자에게 요청했다. 예외 승인 전 커밋·푸시를 재시도하지 않는다.
- 사용자가 2026-09-13 “승인할게”라고 답해 이번 이전의 선행 커밋·푸시 예외를 명시 승인했다. 기존 GitHub 저장소에 최신 검증 소스를 반영한 뒤 Codespaces 생성·검증을 계속한다.

아래는 이전 기능 개발·배포 이력이다.

- 2026-09-11 두 버전 비교 운영 배포 완료: Cloud Build [비공개 운영 기록에 보존] SUCCESS, Cloud Run [비공개 운영 기록에 보존] Ready=True/트래픽 100%/임시 태그 없음. 전체 580테스트, 실제 PostgreSQL 11검증, 최종 학생·교사 합성 브라우저와 52경로 빌드, 배포 전후 각 11점검을 통과했다. 10:49:57 KST 오류 로그 0건이며 이미지 외 운영 설정은 동일하다. 현재/과거 두 버전 독립 선택과 완료 회차 비교를 제공하고 미저장 초안·기존 복원·제출본·이전 팀원 역할은 보존한다. 중간 DNS 대기 실패는 실제 Ready/트래픽 재조회로 확인해 중복 배포 없이 복구했다. 상세 근거와 제한은 DEPLOYMENT_80.md를 따른다. 아래 진행 중/미배포 표기는 당시 기록이다.

- 2026-09-11 두 버전 비교 구현·검증: 지정 worktree에서 공통 비교창, 두 조회 전용 API, 반복 읽기/읽기 전용 PostgreSQL 서비스, 항목·문장·표·역할 비교를 구현했다. 학생·교사 계획서/보고서와 완료 회차에 연결했으며 기존 저장·복원·제출 서비스를 변경하지 않았다. 전체 102파일 580테스트 통과, 실제 PostgreSQL 11검증 통과(동일 시각 65개 커서 탐색, 권한 회수, 조회 무변경, 완료 보존본 포함), 모바일 학생 2종과 교사 2종 브라우저 검증을 통과했다. 사용자 구현·배포 승인에 따라 최종 모바일 조정 빌드와 Cloud Build를 진행 중이다. 이 시점 운영은 기존 00033-kow 100%이며 배포 완료를 뜻하지 않는다. 테스트 병렬 실행의 자원 부족은 2 workers로 줄여 해소했다. 합성 테스트의 스키마·항목명 오기와 실행 중 standalone 폴더 잠금도 수정/해제했다. 최종 배포 결과는 DEPLOYMENT_80.md에 기록한다.

- 2026-09-11 두 버전 비교 설계: 사용자 후속 지시에 따라 최신 저장본을 필수 기준으로 두지 않고 같은 팀·문서·회차의 A/B를 각각 선택하도록 DOCUMENT_VERSION_DIFF_DESIGN.md에 구체화했다. 지정 worktree의 인수인계서와 현재 이력·저장·제출·회차·권한·초안 소스를 대조했다. 과거↔과거, 날짜/페이지 탐색, 현재본 표시 고정, 모바일 공통 비교창, 네 화면 및 완료 회차 진입, 보고서 필드/역할·옛 양식 한계와 검증 계획을 정리했다. start_next에서만 종료 이력을 만들고 finish_project는 문서 행을 보존하는 차이를 반영했다. 확정 요구와 UI/API/알고리즘 권장안을 구분했다. 문서 작업만 완료했으며 제품 코드·DB·운영·배포·Git은 변경하지 않았고 제품 테스트는 재실행하지 않았다. 다음은 사용자 구현 요청 시 해당 설계의 첫 기본 범위를 구현·검증하는 것이다.

- 2026-09-11 문서 버전 비교 인수인계: 사용자가 학생·교사 계획서/보고서에서 이전 버전과 최신 버전의 차이를 색·굵기 등으로 확인하는 기능을 요청하고 새 Work용 인수인계서 작성을 지시했다. `DOCUMENT_VERSION_DIFF_HANDOFF.md`에 현재 변경 이력 메타데이터/스냅샷 구조, 네 화면 진입점, 같은 회차 최신 서버 저장본 비교 권장안, 완료 회차·제출본·양식/표·역할·권한·초안 보존 경계와 검증 기준을 정리했다. 세부 권장안과 미확정 선택지는 구분했다. 이번 작업은 문서 작성만이며 제품 구현·테스트 재실행·운영 조회/배포·새 작업 생성·Git 커밋/푸시는 수행하지 않았다. 최근 배포 확인은 아래 79차 기록을 따른다.

- 후속 79차 지난 기록 보기 배포 완료(2026-09-10): `[비공개 운영 기록에 보존]`에 반영했고 Ready=True, 트래픽 100%, 임시 태그 제거를 확인했다. Cloud Build `[비공개 운영 기록에 보존]` SUCCESS, 이미지 digest `[비공개 운영 기록에 보존]`. 배포 입력 193개·비밀 패턴 0, 최신 자동 백업 성공, 이미지 외 런타임 설정 동일, 0% 단계/운영 공개 주소 각 9개 검사와 전환 직후 ERROR 이상 0건을 확인했다. 실제 학생 로그인·원문 변경·운영 Sheet 쓰기·외부 푸시·Git 초기화/커밋/푸시는 하지 않았다. 상세는 `DEPLOYMENT_79.md`를 따른다.

- 후속 79차 지난 기록 보기(2026-09-09, 로컬 구현·검증 완료): 학생·교사 대시보드에 기본 접힘형 `지난 기록 보기`를 추가했다. 제목·본문 한글 검색, 전체/대화/일지 필터, KST 날짜별·팀별·회차별 묶음, 대표 제목·첫 문장 미리보기와 날짜 상세의 AI 대화·우리끼리 대화·대면/보완 기록·개인 일지를 지원한다. 새 저장 구조나 원문 변경은 없고 기존 `messages`, `discussion_entries`, `experiment_journals`를 읽기 전용으로 모은다.
  - 학생은 현재 학년의 활성 팀에서 팀 대화와 본인 개인 일지만 볼 수 있고, 팀에서 제거·비활성화·보관된 뒤에는 접근할 수 없다. 교사는 현재 학년 과탐실 전체 팀과 본인이 담당하거나 마스터인 동아리 팀의 활성·완료 회차 및 보관 팀 기록을 볼 수 있다. 제거된 팀원의 과거 기록 자체와 기존 교사 공동 기록 원칙은 보존했다.
  - 합성 pg-mem 집중 5개(한글 검색·필터·KST 자정 경계·과거 회차·긴 본문 미리보기·학생 제거/복귀·교사 학급 공유/동아리 담당 범위), 전체 100개 파일·568개, 타입 검사와 Next 16.3.3 프로덕션 빌드 50경로가 통과했다. 304px 실제 로컬 학생·교사 화면에서 기본 접힘, 검색/필터, 빈 상태와 가로 넘침 없음도 확인했다.
  - 운영 배포·실제 학생 자료·운영 Google Sheet·Git 초기화/커밋/푸시는 수행하지 않았다. 현재 검색은 권한이 확인된 기존 원문을 서버 메모리에서 필터링하므로, 한 학년도 기록이 매우 커지면 DB 전문 검색/색인을 별도 설계해야 한다.

- 후속 78차 최종 배포 완료(2026-09-09): AI 계획서 지침 v2 보완을 `science-inquiry-platform-00030-vet`에 반영했다. Ready True/트래픽 100%, 임시 태그 제거, 배포 전후 runtime 설정 동일, 단계/운영 각 8개 검사 통과. Build 3b869658-843f-4203-9da6-0fe999e41a7f SUCCESS, 이미지 dd28f2d6e5e4c39c6accc63dbbb9a605001da84941578bd77dd4a11019b2e0ed. AI 9종·11답변 최종 표본 채점, 계획서 24개 회귀, 49경로 빌드 및 실제 Google 시험 파일 7항목 확인은 `FINAL_REVIEW_78.md`를 따른다. 아래 78차 단계 배포 중은 당시 진행 이력이다. 로컬 시험 DB 종료, Git 미변경. 운영 계정 Sheet 종단 인증은 IAM 권한 제한, 실제 운영 백업 복원 훈련과 새 전체 실기기 조건은 미완료로 남기며 사용자에게 범위를 알린다.

- 후속 78차(2026-09-09) 최종 확인 진행: 사용자에게 Android/iOS 태블릿·휴대폰의 기존 앱 형태 정상 사용을 확인받았다. 이를 새 전체 기기 검증으로 확대하지 않는다. AI 교사용 답변의 학생 이해도 단정 한 건을 찾아 학생/교사 계획서 지침 v2로 보완했고, 실제 합성 6회 재검증(문제 사례 3회), 계획서 회귀 5파일 24개, 타입 포함 49경로 빌드를 통과했다. 최종 AI 9종 11답변 표본 채점은 9점씩이며 모든 사례 반복 검증은 아니다. 별도 Google 시험 파일에서 실제 전송 요청·합계·행 확장·동일 요청 중복 차단·잘못된 요청의 원자적 실패 등 7항목을 확인했다. 운영 서비스 계정 토큰 발급은 IAM_PERMISSION_DENIED(403)로 막혔고 권한 확대는 하지 않았다. 운영 계정 종단/응답 유실·실제 운영 백업 복원 훈련은 미완료로 명시한다. `FINAL_REVIEW_78.md` 및 `output/test-infra/sheets-provider-78.json` 참조. 최종 v2 이미지 Build 3b869658-843f-4203-9da6-0fe999e41a7f 성공 후 무트래픽 단계 배포 중이며 운영 전환은 검증 후 기록한다. Git 초기화·커밋·푸시 없음.

마지막 갱신: 2026-09-09

이 파일은 실제 구현·배포 상태와 다음 작업을 기록하는 운영 인수인계 문서다. 최신 확정 결정은 `CURRENT_DECISIONS.md`, 과거 배경 설계는 `implementation_plan.md`에 둔다. 비밀번호, API 키, 서비스 계정 JSON 및 학생 실명·학번은 기록하지 않는다.

## 새 대화에서 가장 먼저 할 일

1. `AGENTS.md`를 읽는다.
2. `CURRENT_DECISIONS.md`를 읽는다.
3. 이 `PROJECT_STATUS.md`를 처음부터 끝까지 읽는다.
4. 현재 기능에 해당하는 `implementation_plan.md` 절만 읽고, 큰 단계 시작이나 충돌 때만 전체를 읽는다.
5. 의미 있는 구현·검증·배포가 끝날 때마다 이 파일의 상태와 다음 작업을 갱신한다.

## 교사의 현재 의도

- **후속 77차 최종 점검·배포 완료(2026-09-09): `science-inquiry-platform-00028-reh`, Ready=True, 트래픽 100%.** 76차의 계획서 AI 늦은 권한 회수 문제를 수정하고 전체 99개 파일·563개, 집중 17개, 실제 PostgreSQL 경합 20개·잠금 대기 10회, 타입 포함 49개 경로 빌드를 통과했다. 실제 이전 배포 이미지와 새 Linux 이미지의 합성 백업 복원/마이그레이션/구·신 재시작 후 기존 53개 테이블 열·행 해시 보존을 확인했다. 아래 77차 진행 중·76차 배포 보류는 당시 기록이며 현재 판정은 이 항목과 `DEPLOYMENT_77.md`를 우선한다.
  - 성공 Build `034497f8-8db5-4a2e-8884-3afe7a84f961`, 이미지 `sha256:119bb3df2e88d7e566643b8ac0d8c6fd0f074fd27533f12d8c5abe38346be0c7`. 운영 추가 백업 `1788924598987` SUCCESSFUL. 이미지 외 실행 spec·Cloud SQL 연결 보존. 0% 새 리비전과 100% 공개 주소에서 각각 8개 상태/권한 검사 통과, 배포 직후 새 리비전 ERROR 이상 0건. 임시 배포 태그 제거.
  - 기존 승인 OpenAI 키로 합성 자료만 사용한 실제 AI 13회·44,449토큰·웹 검색 0회. 입력 보완/중간 분석을 포함하며 최종 9종 주요 기대 동작을 정성 확인했다. 원본·범위·남은 읽기 부담/교사 판단은 `DEPLOYMENT_77.md` 참조. 실제 기기 E1·시험용 Sheet E2·정식 반복 품질 평가 및 운영 백업 복원 훈련은 미완료다. 전체 과제가 모두 완료됐다고 확대하지 않는다.
  - 로컬 시험용 PostgreSQL 종료. 운영 학생 계정 변경/원문 편집 시험·운영 Sheet 시험 쓰기·외부 푸시 시험·Git 초기화/커밋/푸시 없음. 다음은 실제 기기/승인된 시험용 Sheet 확인과 교사 수업 표본 확인이다.

- 후속 77차(2026-09-09) 진행: 사용자가 76차 결과 확인 후 최종 점검과 배포를 새로 승인했다. 계획서 AI의 스냅샷 생성·기존 결과 조회·최종 저장을 세션/회차/문서→사용자/팀→담당 배정 잠금과 현재 권한 확인 안에서 처리하도록 수정했다. AI 대기 중 잠금은 유지하지 않는다. 교사는 최신 제출본·철회 여부도 재확인한다. 계정·비밀번호 변경 필요·학년도·팀/담당·회차 경계를 보강했다.
  - 집중 2개 파일·17개 검사, 실제 PostgreSQL 학생/교사×권한 변경/저장 선행 20개·실제 잠금 대기 10회 통과. 76차 P1 재현을 수정 후 차단했다. 기존 기록 보존 확인. 전체 회귀·Linux 이미지·구버전/신버전 합성 복원 검증 진행 중이다. 운영 추가 백업 작업 완료(DONE), 실제 성공 백업 ID 최종 확인 예정. 기존 운영은 아직 00027-hvc다.

- 76차 최종 실행 확정: **98개 파일·550개 검사 통과**(`output/test-infra/final-review-tests-76-verified.log`, 572.60초). 타입 포함 49개 경로 빌드도 통과했다. 아래 계획서 AI 권한 P1은 별도 재현한 미수정 결함으로 남아 배포 보류다. 로컬 시험용 PostgreSQL은 종료했다.

- 후속 76차 최종 종합 점검(2026-09-09): **배포 보류**. `WORK_ASTRA_HANDOFF.md` 기준 변경 목록·핵심 경로·배포 포함 범위·회귀 근거를 점검했다. 진행 중 계획서 AI 검토에서 계정 비활성화/팀 소속 해제 후에도 결과가 저장·반환되는 P1을 실제 시험용 PostgreSQL에서 2개 사례로 재현했다. 아직 미수정이며, 이전의 로컬 안정화 전체 완료 판정을 대체한다. 다음 우선 작업은 생성 후 현재 권한을 저장과 같은 잠금 범위에서 확인하고 학생/교사·회차·재사용 경계를 검증하는 것이다.
  - 이번 보완: 일지 요청 실제 수신량 26MiB 제한(정상 사진 5장 유지), 추가 탭 계정/역할/학년도 및 사용자·팀 잠금과 원자적 저장, 일반/마스터 동아리 설정의 학년도 제한, 검증 파일 배포 제외. 과거 기록 보존. 집중 10개/20개 및 실제 PostgreSQL 8개·잠금 대기 4회 통과. 타입 포함 49개 경로 빌드 통과, 최신 의존성 감사 291개·알려진 취약점 0건. 최종 전체 회귀는 `FINAL_REVIEW_76.md`의 확정 결과를 따른다.
  - 상세 판정과 재현 근거는 `FINAL_REVIEW_76.md`, `ASTRA_REVIEW_FINDINGS.md` 상단을 우선한다. 실제 기기 E1, 시험용 Sheet E2, 실제 AI 품질 E3, 실제 과거 이미지/운영 복구 L2는 미완료다. 운영·학생 자료·실제 AI/Sheet/푸시·배포·Git 초기화/커밋/푸시는 수행하지 않았다.

- 후속 75차 L6 대화 알림/읽음 경계 마감(2026-09-09, 로컬 검사): 메시지 뒤 새로 들어온 팀원은 과거 대화 원문은 열람하지만 과거 메시지의 수신·읽음 대상에는 소급 추가되지 않고, 이후 메시지부터 수신 대상으로 고정되는 것을 확인했다. 메시지 시점에 없던 새 푸시 구독은 과거 발송 대기열에 추가되지 않으며, 발송 목적지 검사 중 구독의 소유자·키가 바뀌면 지문 재검사에서 실제 발송을 건너뛴다. 기존 원문·과거 팀 관계는 변경하지 않았다.
  - 집중 2개 파일·16개 검사 통과(discussion-boundaries-75.log), 타입 포함 49개 경로 빌드 통과(build-75.log), 관련 변경 공백 검사 통과. 제품 코드는 이미 이 경계를 지키고 있어 회귀 검사만 추가했다. 실제 공급자/기기 수신·잠금 화면 숨김·절전 복귀는 E1에 남긴다. 운영/외부 푸시/배포/Git 미변경.
  - L6의 독립 로컬 범위는 완료다. L1~L6 중 로컬에서 마감 가능한 구현·검사는 끝났으며, L2의 실제 과거 이미지/운영 복구 조건과 E1~E3는 외부 조건으로 남는다. 사용자의 지시에 따라 최종 종합 변경 검토와 배포 판단 직전에서 멈추고 Work Astra에 넘긴다.

- 후속 74차 L5 의미·범위 정리(2026-09-09, 로컬): 평가 대상 학생이 같은 학급·동아리 안에서 다른 팀으로 이동한 경우 과거 팀 평가가 새 팀의 진행률·평균·공개 의견·공개 대기 판정에 섞이지 않도록 현재 평가 세션으로 한정했다. 과거 응답은 교사용 역사에 원문 그대로 남긴다. 회차 AI 근거 목록에는 공식 준비물 신청을 포함하고 체험 신청을 제외했다. 이전 회차는 저장된 분석과 실제 저장된 학생 판단만 사용하며, 없는 판단을 추정하지 않는 기존 범위를 확인했다.
  - 이동 평가 집중 13개(evaluation-moved-target-74.log), 의미 범위 4개 파일·17개(semantic-scope-74.log), 학년도/서비스 직접 접근 8개 파일·137개(year-service-access-74.log), 시간 초과 묶음 6개 파일·28개 재실행(full-tests-74-retry.log)을 통과했다. 안정적인 단일 작업자 전체 96개 파일·536개(full-tests-74-serial.log)와 관련 변경 공백 검사를 통과했으며, 75차의 타입 포함 빌드 49개 경로를 최종 빌드 증거로 사용한다. 운영/실제 AI·Sheet·푸시/배포/Git 미변경.
  - L5 로컬 범위는 완료다. 실제 AI 답변의 교육 품질은 L4의 합성 평가표로 E3/Work Astra에서 별도 채점한다.

- 후속 73차 L4 AI 교육 품질 평가 준비(2026-09-09, 로컬 문서): 실제 학생 자료와 실제 AI 호출 없이 반복 사용할 `AI_QUALITY_EVALUATION.md`와 `AI_QUALITY_CASES.json`을 만들었다. 근거 충실성·탐구 구조·학생/교사 역할·안전/개인정보·명료성의 5개 항목, 치명적 실패 조건, 사례별 기대/금지 답변을 정의했다.
  - 합성 사례 9개는 단일 회차 종료, 한 회차의 여러 변인 비교, 연쇄 탐구, 학생 판단 없음, 검증하지 않은 최적값, 문서 속 지시문, 개인정보와 위험 요청, 학생 대필 방지, 교사 최종 판단 경계를 포함한다. 필수 7개 ID·중복·필수 필드·합성 자료 표시 검증 통과(ai-quality-cases-73.json). 제품 코드/운영/외부 AI 호출/배포/Git 미변경.
  - L4의 로컬 평가 준비는 완료다. 실제 모델 응답 채점 E3는 Work Astra가 최종 점검 때 수행하도록 남긴다. 다음 독립 로컬 작업은 L5 의미·범위 차이 대조이며, 최종 종합 점검 직전에서 멈춘다는 사용자 경계를 유지한다.

- 후속 72차 L3 비AI 오류 비노출 완료(2026-09-09, 로컬): 준비물·Google Sheets 전송 상태, 일지/사진, 푸시 구독, 공지, 학생·마스터 계정, 팀·동아리, 동아리 설정, 회차 관리, 건의·문의, 도움말과 PDF 경로를 명시 사용자 오류와 예상 밖 내부 오류로 구분했다. 사용자가 조치해야 하는 권한·충돌·입력·회차·시트 연결 안내는 유지하고, 일반 DB/외부 서비스 오류의 원문은 안전한 재시도 문구로 바꾼다. Google Sheets의 원문 오류도 준비물 sync_error에 저장하거나 화면에 노출하지 않는다.
  - 준비물·일지·푸시 집중 15개 파일·89개(non-ai-errors-71.log), 최종 전체 95개 파일·534개(full-tests-72.log), 타입 포함 49개 경로 빌드(build-72.log), 변경 파일 공백 검사 통과. API 44개 정적 재검사에서 직접 request.json 0개, 일반 Error.message 반환 후보 0개(security-surface-72.json). 운영/실제 AI·Sheet·푸시/배포/Git 미변경.
  - L3 로컬 범위는 완료로 갱신한다. 정적 후보 0은 전체 보안 인증이 아니며 운영 Linux 이미지 검증과 실제 외부 연동은 별도다. 다음 독립 로컬 작업은 L4 AI 교육적 품질 평가표와 합성 사례 준비다. 최종 종합 점검은 사용자 지시대로 Work Astra 직전에 넘기고 이 작업에서는 수행하지 않는다.

- 후속 70차 L3 문서 오류 비노출(2026-09-09, 로컬): 학생 계획서·보고서 저장/제출/항목 잠금과 교사 검토/이력 복원에서, 사용자가 조치할 수 있도록 서비스가 명시한 오류만 화면에 표시하게 했다. 다른 팀원이 작성 중이거나 회차·제출본이 바뀐 경우의 안내는 유지하고, 예상하지 못한 DB 오류의 연결 문자열·비밀값은 일반 재시도 문구로 대체한다. FormWriteConflict도 명시 사용자 오류 계층에 포함했다.
  - 합성 내부 연결 문자열 비노출과 정상 충돌 안내 보존을 추가했다. 집중 5개 파일·14개(document-errors-70.log), 최종 전체 95개 파일·532개(full-tests-70.log), 타입 포함 49개 경로 빌드(build-70.log), 변경 파일 공백 검사 통과. 정적 일반 Error.message 후보는 26개에서 19개로 줄었다(security-surface-70.json). 운영/외부 AI·Sheet·푸시/배포/Git 미변경.
  - L3 다음은 준비물·공지·계정/팀·동아리 설정 등 남은 비AI 19개 후보를 같은 기준으로 분류하는 것이다. 사용자의 지시에 따라 로컬 구현·검증·인수인계 자료 정리까지만 진행하며, 최종 종합 점검은 Work Astra가 수행하도록 그 직전에 멈춘다.

- 후속 69차 L3 AI 오류 비노출(2026-09-09, 로컬): UserFacingError와 userFacingMessage를 추가해 사용자가 조치할 수 있도록 코드가 명시한 오류만 그대로 응답하게 했다. AI 공통/학생 대화·탐구 방향, 학생·교사 계획서 AI 검토, 회차 AI 분석의 권한·현재 회차·준비 상태·작업 중/만료·재시도 안내를 명시 오류로 전환했다. 알려진 OpenAI 상태별 안내는 유지하고, 일반 Error의 DB 연결 문자열·비밀값 등은 "AI 답변을 만들지 못했습니다. 잠시 후 다시 시도"로 바꾼다. 회차 분석 학생 판단 API도 명시 오류만 통과시킨다.
  - 합성 내부 연결 문자열/비밀값 비노출과 명시한 회차 변경 안내 보존을 추가했다. 집중 4개 파일·11개(ai-error-safety-69.log), 최종 전체 94개 파일·530개(full-tests-69.log), 타입 포함 49개 경로 빌드(build-69.log), 변경 파일 공백 검사 통과. 정상 AI/DB/외부 호출은 하지 않았다. 일반 오류 후보 26개 전체가 아니라 AI 관련 우선 경로만 완료했다.
  - L3 다음은 계획/보고서/준비물/공지/교사 관리 등 비AI route의 일반 Error.message 후보를 명시적 사용자 오류로 구분하고 정상 충돌 안내를 보존하는 것이다. 운영/배포/Git 미변경. 사용자의 지시대로 로컬 수정·검증까지만 진행하며 최종 전체 점검은 Work Astra에 넘기기 직전에 멈춘다.

- 후속 68차 L3 JSON 입력 제한(2026-09-09, 로컬): Route Handler는 Server Action의 bodySizeLimit 증거가 아니라는 설치 문서를 확인하고 공통 readJsonBody를 추가했다. Content-Length가 없거나 거짓이어도 실제 스트림을 최대 1MiB까지만 읽고 초과 즉시 취소하며, 비정상 길이·잘못된 JSON·잘못된 UTF-8은 null로 반환해 기존 Zod 입력 안내로 안전하게 거절한다. 인증 확인 뒤 호출하는 기존 순서를 유지했다.
  - 직접 request.json을 쓰던 API 33개/42곳을 모두 공통 제한기로 전환했다. 유니코드 정상 입력, 잘못된 JSON/UTF-8, 선언 크기 초과 시 추가 읽기 없음/취소, Content-Length 없는 실제 초과 취소, 시험 API의 형식 오류/초과 요청 서비스 미호출·정상 생성·미인증 본문 미사용과 모든 Route Handler 무제한 직접 파싱 0건을 검증했다. 집중 3개 파일·8개(request-body-route-68.log), 최종 전체 93개 파일·528개(full-tests-68.log), 타입 포함 49개 경로 빌드(build-68.log), 변경 파일 공백 검사 통과. 현재 정적 목록은 security-surface-68.json.
  - 정상 최대 입력은 기존 문서/양식의 항목별 Zod 제한보다 충분한 공통 1MiB이며 명단 multipart 2MiB 제한은 별도 유지한다. 초과/형식 오류는 각 API의 기존 400 입력 안내를 사용하고 413 구분은 하지 않는다. 제품 로컬 변경이며 운영/외부 AI·Sheet·푸시/배포/Git 미변경. L3 남은 것은 일반 Error.message 내부 정보 비노출과 정상 충돌 안내 보존이다. 사용자의 지시에 따라 이후 로컬 작업/검증까지만 하고 최종 전체 점검은 Work의 Astra에 넘기기 직전에 멈춘다.

- 후속 67차 L3 의존성 보안 패치(2026-09-09, 로컬): Node의 SheetJS CDN 다운로드에서 SELF_SIGNED_CERT_IN_CHAIN을 확인했다. 시스템 신뢰 저장소를 사용하는 --use-system-ca로 정상 다운로드한 뒤 Next 16.3.3과 next>sharp 0.35.4 고정을 설치했다(dependency-update-67.log, dependency-sharp-67.log). 인증서/공급망 검사는 유지하고 설치 스크립트/전역 설정 변경은 하지 않았다. Dockerfile 의존성 단계에 pnpm-workspace.yaml 복사를 추가해 잠금의 override와 일치시켰다. 기존 SheetJS 0.20.3은 유지한다.
  - 291개 의존성 재감사 권고 0건(dependency-audit-67.json). 런타임 Next 16.3.3/sharp 0.35.4 및 합성 PNG 생성(dependency-runtime-67.json), 별도 설치 설정 복사본의 frozen/offline/lockfile-only 검사(frozen-lock-67.log), 전체 90개 파일·520개(full-tests-67.log), 타입 포함 49개 경로 빌드(build-67.log), 변경 파일 공백 검사 통과. 첫 버전 조회는 sharp/package.json 비공개 export 오류였고 공개 런타임 versions로 정정해 확인했다. Linux 컨테이너 전체 빌드나 운영 반영 완료로 확대하지 않는다.
  - SECURITY_REVIEW.md의 66차 실패 이력을 유지하고 최신 해소 결과를 앞에 추가했다. L3 의존성 부분은 로컬 완료이며 일반 JSON 실제 바이트 제한/형식 오류/내부 오류 비노출과 정상 사용자 충돌 안내 보존은 다음 작업이다. 운영/실제 AI·Sheet·푸시/배포/Git 미변경. L2 과거 이미지/운영 조건, L4~L6/E1~E3 및 최종 요구 대조는 계속 남는다.

- 후속 66차 L3 보안 감사(2026-09-09, 패치 미완료): 전체 잠금 파일 온라인 감사가 critical 2/high 1을 보고했다(dependency-audit-66-online.json). Next 16.3.2의 Windows/AVIF 권고와 선택 sharp 0.35.3의 관련 libheif 권고를 공식 자료와 대조했다. 수정 Next 16.3.3 및 sharp >=0.35.4가 필요하다. Windows 로컬과 운영 Dockerfile Linux 조건을 구분하며 운영 이미지/실제 공격 시험은 하지 않았다. 상세는 SECURITY_REVIEW.md.
  - Next 갱신은 최초 저장소 불일치 후 기존 .pnpm-store 지정/캐시 우선/동일 pnpm의 Node 실행기 변경에도 fetch failed로 중단됐다(dependency-update-66*.log). 공식 Next/SheetJS 다운로드 HEAD는 200으로, 전체 인터넷 불가라고 단정하지 않는다. package.json/잠금/실제 Next 16.3.2는 유지되며 패치 완료 아님. 공급망/인증서 검사는 끄지 않았다. 다음 우선 작업은 패키지 실제 다운로드 오류 분리 및 보안 갱신/재감사/회귀다.
  - API 44개 정적 목록 중 직접 JSON 읽기 33개/일반 Error.message 후보 26개(security-surface-66.json)를 기록했다. 명단 외 파싱 전 본문 제한과 명시적 사용자 안내/내부 오류 구분이 남는다. 정규식 후보 개수를 확정 취약점 수로 표현하지 않는다. 제품 변경 및 새 전체 테스트/빌드 없음(64차 520개/49개 경로 기준 유지). 운영/실제 AI·Sheet·푸시/배포/Git 미변경. 의존성 외 독립 로컬 작업이 있어 전체 목표 차단으로 처리하지 않는다.

- 후속 65차 L2 실제 PostgreSQL 호환성(2026-09-09, 검증 도구만 추가): 별도 합성 DB와 새 자식 프로세스의 실제 getDb로 정상 시작 전후, 미래 번호, 알려진 번호의 해시 변경/이름 변경, 초기 구조가 없는 미래 DB의 6개 검사를 통과했다(compatibility-postgres-65.log, postgres-compatibility-65.json). 각 거절은 같은 자식 프로세스에서 두 번 호출해 실패한 초기화 캐시/풀이 남지 않음을 확인했다. 정상 구조 58개 테이블 전체 행 개수/해시·517열/283제약/129인덱스가 각 시작 전후 동일했고, 초기 구조 없는 경우 마이그레이션 표 1개 외 테이블·초기 계정이 만들어지지 않았다. 각 자식 종료 후 다른 연결의 bootstrap 잠금 획득도 통과했다.
  - 이번은 초기 합성 자료를 가진 DB 검사이며, 풍부한 확정/공개 역사 자료의 백업·복원 증거는 63차를 유지한다. 임시로 바꾼 합성 마이그레이션 기록을 원복한 뒤 최초 전체 상태와 일치하고 정상 시작됨을 확인했다. 로컬 PostgreSQL 정상 종료, 검증 도구 공백 검사 통과. 제품 코드 미변경이므로 전체 90개 파일·520개/49개 경로 빌드는 64차 기준이다. 운영/실제 AI·Sheet·푸시/배포/Git 미변경.
  - L2 현재 앱의 미래 DB 거절 검증은 완료했으나 실제 과거 배포 이미지 공존과 운영 복구 조건은 미완료로 유지한다. 다음은 독립 로컬 잔여 L3 의존성 및 입력·오류 노출 감사다. L4~L6/E1~E3와 최종 요구 대조도 남아 전체 목표 완료로 처리하지 않는다.

- 후속 64차 L2 미래 DB 초기화 방어(2026-09-09, 로컬): 현재 앱이 모르는 9999 합성 마이그레이션을 무시하는 동작을 재현했다(migration-future-before-64.log, 새 1개 실패/기존 5개 통과). 읽기 전용 assertDatabaseCompatibility를 추가해 기본 SCHEMA_SQL/seed 전에 전체 적용 번호·이름·해시를 확인하며, 마이그레이션 직접 실행 시작 전 및 각 변경의 행 잠금 안에서도 검사한다. 알 수 없는 번호와 내용 불일치를 거절하고 기존 0001~0011 정의는 유지했다.
  - 새 DB 검사 무변경/미래 번호 거절/누락 잠금 테이블 미생성/이력 보존과 getDb 거절 시 schema·seed 미실행/잠금·풀 정리를 검증했다. 집중 3개 파일·12개(migration-future-final-64.log), 최종 전체 90개 파일·520개(full-tests-64-final.log), 타입 검사(typecheck-64-final.log), 타입 포함 49개 경로 빌드(build-64.log), 변경 파일 공백 검사 통과. 첫 전체 실행은 기존 초기화 mock의 새 export 누락 1개 실패(518개 통과)였고 mock 보완 및 중단 검사 추가 후 전체 재실행을 통과했다.
  - RECOVERY_CHECKLIST.md에 구버전 전환 조건과 한계를 추가했다. 새 보호 기능은 이미 배포된 과거 이미지나 이미 초기화한 앱에 소급되지 않는다. 다음은 실제 PostgreSQL에서 getDb 실패 전후 전체 데이터/구조 불변 및 호환 DB 정상 시작을 확인한다. 실제 과거 이미지 호환성/운영 복구 조건 및 L3~L6/E1~E3는 계속 미완료. 운영/실제 AI·Sheet·푸시/배포/Git 미변경, 이번 차수 PostgreSQL 기동 없음.

- 후속 63차 L2 합성 전체 DB 복구(2026-09-09, 로컬 검증): 승인 계획/확인 보고 고정본·당시 역할·제거 소속·확정/공개 시험·공개 평가·대화/읽음/푸시 대기열이 들어 있는 별도 DB를 pg_dump custom으로 백업하고 새 DB에 pg_restore 단일 트랜잭션으로 복원했다. 58개 public 테이블 전체 행/해시와 517개 열·283개 제약조건·129개 인덱스가 일치했다. public 시퀀스/함수/사용자 트리거/뷰는 0개였다. 별도 프로세스로 현재 getDb를 시작해도 전체 데이터/구조 불변, 공개 결과 조회 및 새 메시지 쓰기 후 역사 행/기타 자료 보존, 원본 DB 불변을 확인했다.
  - 첫 데이터 비교 실행(backup-restore-63.log) 후 구조 비교를 추가한 최종 실행도 통과(backup-restore-63-final.log, postgres-backup-restore-63.json). JSON에 백업 경로/SHA-256과 테이블·구조 요약을 기록했다. `RECOVERY_CHECKLIST.md`에 재실행/운영 복구 준비 순서와 한계를 작성했다. 제품 미변경이므로 전체 516개/49개 경로 기준은 62차 유지. 운영/실제 AI·Sheet·푸시/배포/Git 미변경. L2 일부 완료로 기록하고 다음은 구버전/미래 스키마 공존 검증이다. 운영 자체 백업/PITR/권한·비밀값/큰 데이터 복구 시간은 이번 검사로 보장하지 않는다.

- 후속 62차 L1 실제 푸시 연결 주소(2026-09-09, 로컬): 설치된 web-push의 HTTPS agent 지원/전달 경로를 확인하고 공지·대화 전송에 목적지별 Agent를 적용했다. 연결 시 DNS 응답을 검사한 바로 그 주소 목록을 소켓에 전달하며 별도 재조회하지 않는다. 예상 호스트 변경/사설·혼합 주소를 거절하고 단일/all·IPv4/IPv6 요청을 처리한다. 공용 IP 직접 주소는 기존 endpoint 검사로 제한하고 TLS 인증서/호스트 검증은 유지한다. agent는 요청 후 정리하며 proxy 옵션을 추가하지 않는다.
  - 공용→사설 주소 변화 5개, 검증 주소 직접 전달/호스트 불일치, 일시 DNS 오류 구분, 혼합/직접 사설 주소 차단, 실제 HTTPS 요청에서 로컬 수신 서버 연결 0건의 새 9개와 공지 발송 도중 DNS 변화 검사 1개 추가. 공지/대화 합성 전송에서 agent 전달도 확인했다. 초기 집중 3개 파일·26개(push-connection-62.log, 공지 추가 사례 전), 최종 전체 90개 파일·516개(full-tests-62.log), 타입 포함 49개 경로 빌드(build-62.log), 공백 검사 통과. 외부 공급자 연결 없이 모의 DNS 및 로컬 수신 서버만 사용했다. 운영/실제 푸시·AI·Sheet/배포/Git 미변경. L1 로컬 완료, 실제 기기/공급자 수신은 E1에 유지한다. 다음은 L2 합성 전체 DB 백업·복원 및 구버전/미래 스키마 경계다.

- 후속 61차 L1 공지 푸시 일시 DNS 오류(2026-09-09, 로컬 수정): 실제 sendPushForNotice에 합성 DNS 일시 오류/빈 응답을 주입해 정상 구독 전체 행이 삭제되는 두 상황을 재현했다(notice-dns-before-61.log). 공지 발송은 UnsafePushDestinationError일 때만 구독을 삭제하고, DNS 빈 응답도 일시 확인 실패로 분류한다. 일시 실패 후 원래 구독 전체 행 보존·다음 재발송 성공·사설 주소의 계속 거절을 확인했다. 대화 푸시도 공통 빈 응답 분류를 사용한다.
  - 새 3개 포함 집중 4개 파일·31개(notice-dns-after-61.log), 전체 89개 파일·506개(full-tests-61.log), 타입 포함 49개 경로 빌드(build-61.log), 공백 검사 통과. DNS/발송 모두 모의 주입이며 실제 외부 전송 없음. 운영/배포/Git 미변경. L1은 일부 완료로 갱신했다. 다음은 설치된 web-push가 지원하는 HTTPS agent 경로를 이용해 DNS 사전 검사와 실제 연결 사이 주소 변화가 사설망 연결로 이어지지 않도록 검증·보강하는 작업이다. L2~L6/E1~E3 및 전체 인수인계 잔여는 유지한다.

- 후속 60차 전체 잔여 대조(2026-09-09, 읽기/문서): `REMAINING_WORK.md`에 원래 인수인계 1~9번, F01~F17 근거 연결, G01~G07 누락 방지와 다음 완료 조건을 정리했다. 현재 코드·평가/업로드/AI/마이그레이션 검사 내용, 58차 전체 결과와 59차 실제 DB 결과를 대조했다. 전부 새로 재실행한 최종 완료 감사는 아니다. 남은 로컬 작업은 L1 공지 푸시 DNS/연결, L2 전체 복구/구버전, L3 의존성/입력·오류, L4 AI 평가 기준, L5 의미·범위 차이, L6 알림 최종 검토로 구체화하고 외부·기기 확인은 E1~E3로 구분했다.
  - 다음 실행은 L1: 기존 공지의 assertSafePushDestination 예외를 모두 구독 삭제로 처리하는 경로를 합성 DNS 실패로 재현한 뒤 필요한 수정/회귀를 수행한다. 새 대화 푸시의 일시 실패 보존 검사를 기존 공지로 확대 해석하지 않는다. 코드에서 확인한 위험 경로이며 재현 전 새 확정 오류로 번호 매기지 않았다. 문서의 최초 검토/현재 상태 혼동을 줄이는 안내를 추가했다. 제품·운영/외부 호출/배포/Git 미변경, 새 전체 테스트/빌드 없음(58차 502개/49개 경로 기준 유지). 전체 목표는 미완료이며 독립 로컬 작업이 있으므로 차단 상태가 아니다.

- 후속 59차 인수인계 7번 실제 푸시 대기열 검증(2026-09-09, 로컬): 과탐실/동아리 × 동시 처리/발송 전 임대 이어받기/발송 중 이전 소유자의 늦은 실패/구독 계정 변경/팀원 제거/구독 갱신 뒤 이전 주소 만료 응답/지연 재시도/시도 상한/유효 기간의 PostgreSQL 18개 통과(discussion-push-postgres-59.log, postgres-discussion-push-59.json). 0011 재실행 행 불변, 완료 후 중복 처리 0건, 원문과 갱신 구독 보존, 늦은 실패의 완료 상태 덮어쓰기 차단을 확인했다. 전송 중 응답 유실/임대 만료로 이미 전송된 알림의 재시도가 생길 수 있다는 기존 한계는 유지하며 외부 공급자 exactly-once를 주장하지 않는다.
  - API 연결 검사 추가: 저장 성공 후에만 after에 예약되고 응답 전 직접 발송하지 않음, 예약 작업의 일시 실패는 삼켜 대기열 복구에 맡김, 저장 실패는 예약하지 않음. 인증된 정기 경로 검사와 집중 2개 파일·9개(discussion-push-route-59.log) 통과. 제품 코드 미변경이므로 전체/빌드는 58차 88개 파일·502개/49개 경로 기준이며 새 API 검사 1개는 집중 결과로 구분한다. 변경 파일 공백 검사 통과, 로컬 PostgreSQL 정상 종료. 실제 AI·Sheet·푸시/운영/배포/Git 미변경.
  - 다음은 인수인계 1~9번 및 G01~G07의 잔여 근거를 한 번 대조해 완료/로컬 미완료/외부·기기 확인 필요로 정리하고, 남은 필수 로컬 안정화에 집중한다. 브라우저 실제 공급자 수신·OS 숨김/절전·추가 소속/동시 구독 등록의 모든 경합, 기존 평가 이동/과거 오염 응답·외부 연동/복구/최종 검토 등 전체 잔여 범위는 유지한다. 새 기능 확대를 완료 기준으로 삼지 않는다.

- 후속 58차 인수인계 7번 대화 푸시 연결(2026-09-09, 로컬): 0011 outbox에 메시지 저장 당시 구독 ID/수신자/구독 지문을 같은 트랜잭션으로 고정한다. peer 저장 응답 후 Next after에서 발송을 시도하고 인증된 기존 정기 요약 경로가 대기·만료 작업을 재시도한다. 구독 소유자·키·당시 소속의 현재 상태·활성/학년도·체험 여부·읽음 여부를 확인하며 DNS 확인 뒤 재검사한다. 전송에는 본문·이름 없는 일반 안내와 /inquiry 링크만 포함한다. 소유 토큰/60초 임대·10초 발송 제한·최대 5회/24시간·지연 재시도·같은 메시지 tag를 적용한다. 외부 수신 성공 후 응답 유실의 정확히 한 번 발송은 보장하지 않으며 같은 tag로 기기 표시 중복을 줄인다.
  - 합성 전송 14개: 정상/재시도/제거/재가입/읽음/구독 해제/계정 재연결/체험 송수신자/늦은 구독/만료/안전하지 않은 주소/일시 DNS 실패/동시 실행 통과. 첫 재가입 검사는 합성 학생 학급 누락으로 중단됐고 fixture 학급 지정 후 수정 없이 통과했다. 정상 구독은 네트워크/DNS 실패 때 유지하고 만료/명백히 안전하지 않은 주소만 동일 소유·키 조건에서 제거한다. 문서 원문 불변/재시도 중복 대기 기록 없음/동시 발송 소유권/인증 전 정기 발송 차단을 확인했다. 초기 집중 14개, 최종 집중 16개(discussion-push-58-final.log), 전체 88개 파일·502개(full-tests-58.log), 타입 포함 49개 경로 빌드(build-58.log), 공백 검사 통과.
  - 운영 배포/실제 AI·Sheet·푸시/스케줄 변경/Git 커밋·푸시 미실행. 다음은 0011 실제 PostgreSQL 적용·동시 임대/이어받기·구독 변경 경계와 실제 브라우저 요청 후 연결 검증이다. 푸시 공급자 실제 수신·실제 기기 절전·외부 연동/복구/남은 권한·평가 경계/최종 전체 요구 대조는 미완료다. 사용자가 진행 성격과 잔여량을 물어, 안정화 작업과 별도 새 기능(알림/읽음)을 명확히 구분하고 잔여를 기능 마감→전체 안정성 확인→배포 준비/새 승인 후 반영의 세 묶음으로 설명했다. 완료율·시간은 근거 없이 추정하지 않는다.

- 후속 57차 인수인계 7번 실제 DB/브라우저 읽음(2026-09-09, 로컬 검증): 과탐실/동아리 × 메시지 전송/읽음 × 제거/저장 선행의 PostgreSQL 8개 통과(discussion-read-race-57.log, postgres-discussion-read-race-57.json). 저장 선행 4개의 실제 잠금 대기를 관찰했고 제거 선행은 새 수신자 제외 또는 읽음 403, 저장 선행은 당시 소속/첫 읽음 보존을 확인했다. 모든 사례에서 새 소속으로 재가입해도 옛 메시지 읽음은 새로 기록하지 않으며 종료 소속/기존 원문을 보존한다. 0010 재실행 후 마이그레이션 전체 행 불변도 확인했다.
  - Chrome 768×1024 합성 사이트에서 미활성 패널 무전송, 화면 아래 미독, 표시 후 읽음, 대면 탭 무전송, 발신자 읽음 명단 표시, 타인 메시지 명단 미표시의 6개 통과(discussion-read-browser-57-final.log/json 및 PNG). 캡처를 직접 확인했다. 첫 실행은 동작 검사 후 캡처 경로의 공백 인코딩 오류로 중단됐고 fileURLToPath로 검증 도구를 정정한 새 합성 DB 실행과 구분한다. 도우미 종료 시 권한/PowerShell 오류 및 중복 기동 실패를 확인한 뒤 정확한 PID를 재확인해 종료·재기동했으며 최종 도우미와 로컬 PostgreSQL 모두 종료했다. 실제 기기/OS 숨김·절전과 전송 도중 새 수신자 추가 등 모든 경합을 검증한 것은 아니다.
  - 제품 미변경이므로 전체 검사/빌드는 56차의 87개 파일·488개/49개 경로 기준을 유지한다. 변경 도구 공백 검사 통과. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시 미변경. 다음은 현재 구독 기기에 메시지 내용·이름 없는 알림을 보내는 경로와 중복/실패 복구 연결이다. 기타 원래 잔여 범위는 유지한다.

- 후속 56차 인수인계 7번 읽음 API/화면 연결(2026-09-09, 로컬): 학생 본인의 읽음만 최대 100개 ID로 기록하는 read 요청을 추가했다. 세션→사용자/팀 잠금 안에서 현재 접근 권한과 메시지 회차/당시 소속 ID의 현재 활성 상태를 확인하고 첫 read_at만 기록한다. 완료 회차를 열람한 경우에도 원문과 AI 근거를 바꾸지 않는 별도 읽음 메타데이터만 기록한다. 발신 학생에게만 해당 날짜/회차 메시지의 수신·읽음 목록을 제공하며 다른 수신자/교사에게 명단을 보내지 않는다. UI는 활성 대화 탭·보이는 문서에서 화면에 교차하는 메시지 ID만 전송하고 재조회 때 실패한 읽음을 재시도한다. 본인 메시지에 읽음 n/N명과 읽은 사람 이름을 표시한다.
  - 서비스 통합에서 최초 읽음·중복 후 시각 불변·발신자 조회/수신자 목록 비공개·교사 대리 기록 거절·잘못된 회차 거절·제거 후 거절·재가입 후 이전 미독 메시지 0건/새 메시지 정상 읽음 확인. API에 회차 필수/빈 목록/100개 초과 거절과 ID 전달 검사 추가. 초기 집중 2개 파일·6개(discussion-read-56.log)는 API 새 검사 추가 전이며 최종 전체 87개 파일·488개(full-tests-56.log), 타입 포함 49개 경로 빌드(build-56.log), 공백 검사 통과. 운영/외부 AI·Sheet·푸시/배포/Git 미변경. 다음은 실제 PostgreSQL 0010 및 수신/읽음과 소속 변경 경합, 합성 브라우저의 화면 밖/숨김/탭 전환/발신자 표시 검증이다. 알림 발송·재시도는 아직 미연결이고 나머지 전체 잔여 범위도 유지한다.

- 후속 55차 인수인계 7번 메시지 수신 관계 기반(2026-09-09, 로컬): 0010 마이그레이션으로 메시지별 수신자/당시 team_members.id/읽음 시각을 저장할 테이블을 추가했다. 과거 메시지에는 수신자를 추정해 채우지 않는다. 새 peer 메시지 저장 트랜잭션에서 발신자를 제외한 현재 학년도 활성 학생의 소속 ID를 고정한다. 세션→수신 후보 사용자 정렬→관련 팀 순서로 잠그고, 잠금 이후 새 후보가 나타나면 409로 재시도를 안내한다. 동일 요청 재전송은 기존 수신 관계를 추가·교체하지 않는다. 실제 푸시 발송은 아직 연결하지 않았다.
  - 수신 관계 고정/발신자 제외/초기 읽음 없음/탈퇴 후 새 소속으로 재가입/재전송 불변/새 메시지에 새 소속 적용 검사 추가. 집중 3개 파일·15개(discussion-recipients-55.log), 전체 87개 파일·487개(full-tests-55.log), 타입 포함 49개 경로 빌드(build-55.log), 변경 파일 공백 검사 통과. 53/54차 집중 검사도 이번 전체 실행에 포함됐다. 운영 DB/배포/실제 AI·Sheet·푸시/Git 커밋·푸시 미변경. 현재 화면에는 아직 읽음 표시가 없으며 읽음 저장 API·화면 연결·발신자 조회/알림 발송과 재시도/실제 PostgreSQL 새 잠금·마이그레이션 검증이 남는다. 0010은 로컬 검증 환경에서만 적용됐다. 기타 평가 이동/오염 응답·권한·외부 연동/기기/복구/최종 정리 등 전체 잔여 범위도 유지한다.

- 후속 54차 G06 제거된 평가자 기록(2026-09-09, 로컬 검증): 과탐실/동아리에서 실제 서비스로 동료평가 3건을 제출하고 그중 한 평가자를 팀에서 제거하는 두 통합 검사를 추가했다. 제거 뒤 추가 제출은 403, 현재 진척/공개 대상에서는 제외되지만 제거 전에 제출한 평가는 교사 열람과 유효 응답 3건 집계에 남고 검토된 익명 의견도 기준 충족 시 공개됨을 확인했다. 공개 후 평가 원문 전체 행과 종료된 소속 이력은 불변이다. 이는 유효한 당시 평가의 보존이며 과거 학년도 계정이 잘못 연결된 데이터의 유효성을 인정한 것이 아니다.
  - 최초 실행은 열린 평가에서 의견 검토를 시도한 합성 상태 설정 오류 2개(evaluation-history-54.log)였고, 검토/재개 순서를 설정한 최종 관련 2개 파일·15개(evaluation-history-54-final.log)와 타입 검사(typecheck-54.log), 변경 파일 공백 검사 통과. 제품 미변경이므로 전체 검사/빌드는 51차 기준을 유지하며 새 2개는 집중 실행 결과다. 실제 PostgreSQL/AI/Sheet/푸시/운영 배포/Git 작업은 하지 않았다. 다음은 인수인계의 미완료 알림·읽음 요구와 기존 대화 구조를 대조해 남은 구현 범위를 구체화한다. 이동한 평가 대상의 집계 의미·잘못 연결된 과거 응답, 기타 권한 경계/외부 연동/기기/복구/최종 정리 등 전체 잔여 범위는 유지한다.

- 후속 53차 G02 시험 생성 중 원문 변경(2026-09-09, 로컬 검증): 기존 출제 전 초안 변경 검사에 더해, 합성 생성기 실행 중 계획서·보고서·보고서 역할·개인 일지 원문을 변경하는 통합 검사를 추가했다. 완성된 팀 문항/학생 두 명의 개인 문항에서 처음 전송한 출처 키·라벨·인용문이 정확히 유지되고, 학생별 역할/관찰 근거가 뒤섞이지 않으며, 변경된 일지 원문은 그대로 저장돼 있고 기존 계획 고정본/보고서 이력 전체 행은 불변임을 확인했다.
  - 관련 2개 파일·8개 통과(exam-source-during-53.log), 타입 검사(typecheck-53.log) 및 변경 파일 공백 검사 통과. 제품 미변경으로 전체 테스트/빌드 기준은 51차의 86개 파일·483개/49개 경로이며 이번 새 검사 1개는 집중 실행으로 별도 기록한다. 실제 AI/외부 연결/운영/배포/Git 커밋·푸시는 미변경. 이번은 준비 완료 뒤 원문 변경의 근거 보존 검증이며, 여러 자료를 준비하는 도중의 동시 변경이나 동아리 추가 탭 전체 경계를 검증한 것은 아니다. 다음은 과거 평가 응답 집계와 제거 팀원 기록 보존을 대조한다. 기타 권한 경계·외부 연동/기기/복구/채팅 알림·읽음/최종 정리 등 잔여 범위는 유지한다.

- 후속 52차 G02 시험 생성자 실제 권한 경합(2026-09-09, 로컬 검증): 과탐실 계정 중지/비밀번호 초기화, 동아리 중지/초기화/담당 해제 × 시작 전/생성 대기/권한 변경 선행/저장 선행의 실제 PostgreSQL 20개 통과(exam-teacher-race-52.log, postgres-exam-teacher-race-52.json). 저장 선행 5개에서 변경 요청이 실제 생성 트랜잭션의 잠금을 기다리는 것을 관찰했다. 시작 전 거절은 생성 호출·작업 0건, 대기/변경 선행은 403·시험 미생성·작업 failed, 저장 선행은 당시 시험/시험지/문항·작업 completed 보존을 확인했다. 모든 사례에서 권한 변경 후 재요청 거절, 추가 생성 호출 없음, 기존 시험 전체 행과 학생 소속 불변을 확인했다.
  - 생성자 잠금 추가 후 설정 발행/재발행과 시험 저장의 실제 DB 4개도 재검증 통과(exam-config-race-52.log, postgres-exam-config-regression-52.json). 합성 PostgreSQL 정상 종료/변경 파일 공백 검사 통과. 제품 코드는 변경하지 않고 검증 도구만 추가했으므로 전체 86개 파일·483개/타입 포함 49개 경로 빌드는 51차 기준을 유지한다. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 시험 생성 중 원문 근거 변경과 고정 출처 보존 범위를 대조한다. 과거 평가 응답 집계·외부 연동/기기/복구/채팅 알림·읽음/최종 정리 등 전체 잔여 범위는 유지한다.

- 후속 51차 G02 시험 생성자 권한(2026-09-09, 로컬): 계정 중지/비밀번호 초기화/동아리 담당 해제와 시작 전·생성 대기·완료 결과 재사용의 조합에서 권한 상실 뒤에도 시험을 생성·저장·재사용하는 13개를 재현했다(exam-teacher-before-51.log). 생성 시작 전에 현재 학년도·교사 역할·활성·비밀번호 변경 완료·필요한 담당/마스터 권한을 검사한다. 결과 저장 트랜잭션은 기존 사용자 잠금 집합에 생성자를 함께 포함해 순서를 유지하고, 계정과 담당 관계를 잠근 상태로 권한을 다시 확인한다. AI 생성 중 잠금을 유지하지 않는다.
  - 검증: 과탐실/동아리의 계정·비밀번호·담당 변경 × 시작/생성/재사용 15개와 마스터 담당 해제 후 생성/재사용 허용 2개, 총 새 17개 통과(exam-teacher-after-51-final.log). 초기 집중 4개 파일·46개(exam-teacher-after-51.log) 후 마스터 두 사례를 추가했고 최종 전체 86개 파일·483개(full-tests-51.log), 타입 포함 49개 경로 빌드(build-51.log), 변경 코드 공백 검사 통과. 시작 거절은 합성 생성 호출 0회, 생성 중 거절은 새 시험 없음, 재사용 거절은 기존 시험/시험지 불변, 학생 소속 보존을 확인했다. 이번은 테스트 DB 검증으로 실제 PostgreSQL 양쪽 선행 순서/잠금 대기는 다음에 확인한다. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 원문 근거 변경·과거 평가 응답 집계·외부 연동/기기/복구/채팅 알림·읽음/최종 정리 등 전체 잔여 범위는 유지한다.

- 후속 50차 G02 동아리 설정 발행/시험 저장 잠금(2026-09-09, 로컬): 시험이 동아리 행을 잠근 상태에서 이미 발행한 같은 설정의 재발행 요청이 설정 행을 먼저 잠그면, 시험 INSERT의 설정 외래 키 잠금과 서로 기다려 PostgreSQL 40P01 교착 상태가 발생함을 재현했다(exam-config-before-50.log, postgres-exam-config-before-50.json). 설정 발행에서 대상 동아리를 조회·잠근 뒤 설정 버전을 잠그도록 순서를 통일했다. 이미 발행한 버전은 기존대로 정상 업무 오류로 거절하며 시험 저장은 성공한다.
  - 검증: 새 버전 발행/같은 버전 재발행 × 시험/발행 선행의 실제 DB 4개에서 잠금 대기를 확인했고 수정 후 교착 상태 0건, 시험 저장 성공, 새 버전 발행 성공/재발행의 정상 거절을 확인했다(exam-config-after-50.log, postgres-exam-config-after-50.json). 생성 중 새 버전이 발행돼도 시험은 시작 시 선택한 config_version_id를 보존하고 기존 설정 definition은 불변이다. 합성 DB 정상 종료. 전체 85개 파일·466개(full-tests-50.log), 타입 포함 49개 경로 빌드(build-50.log), 변경 코드 공백 검사 통과. 이번 새 검증은 독립 실제 DB 스크립트이므로 자동 테스트 개수는 이전과 같다. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 시험 생성자의 계정/담당 권한 변경 경계다. 원문 근거 변경·과거 평가 응답 집계·외부 연동/기기/복구/채팅 알림·읽음/최종 정리 등 전체 잔여 범위는 유지한다.

- 후속 49차 G02 시험 대상 팀 확대(2026-09-09, 로컬): 생성 대기 중 처음 비어 있던 팀 또는 새 팀에 학생이 들어오면 기존 검사는 준비 당시 팀만 확인해 누락된 시험을 저장하는 두 상황을 재현했다(exam-scope-before-49.log). 결과 저장 시 학급/동아리 부모 행을 팀 생성과 같은 순서로 잠근 뒤 해당 범위의 모든 팀 세션과 관련 팀을 잠그고, 학생이 있는 활성 팀 집합과 준비 명단을 대조한다. 보관 팀도 잠금 대상에 포함해 복원/배정을 직렬화한다. 학생 없는 새 팀 자체는 시험 대상 변화로 처리하지 않는다. 잠금은 생성 완료 후 저장 트랜잭션 안에서만 유지한다.
  - 검증: 새 두 재현을 포함한 집중 3개 파일·31개(exam-scope-after-49.log). 실제 PostgreSQL에서 두 활동 × 빈 팀 배정/새 팀 생성·배정/학생 없는 새 팀 생성 × 양쪽 선행 순서 12개 통과(exam-scope-race-49.log, postgres-exam-scope-race-49.json). 새 대상이 먼저 생기면 409, 저장이 먼저면 실제 변경의 잠금 대기 후 당시 시험 보존, 빈 새 팀만 먼저 생기면 기존 시험 정상 저장을 확인했다. 기존 명단 변경 20개도 재검증 통과(exam-roster-race-49.log), 원문·소속 보존/합성 DB 정상 종료. 전체 85개 파일·466개(full-tests-49.log), 타입 포함 49개 경로 빌드(build-49.log), 변경 코드 공백 검사 통과. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 생성자 권한 변경 및 동아리 설정 발행과 시험 저장의 잠금/근거 경계다. 과거 평가 응답 집계와 나머지 외부 연동/기기/복구/채팅 알림·읽음/최종 정리도 계속 남는다.

- 후속 48차 G02 시험 명단 실제 동시 실행(2026-09-09, 로컬 검증): 과탐실/동아리 × 학생 제거·중지·이동·추가/팀 보관 × 변경 선행/저장 선행의 실제 PostgreSQL 20개를 통과했다(exam-roster-race-48-final.log, postgres-exam-roster-race-48.json). 생성 준비가 끝난 뒤 세션 잠금 직전에는 실제 관리 변경이 먼저 끝나게 하고, 결과 INSERT 직전에는 다른 연결의 실제 행 잠금 대기 10개를 관찰했다. 변경 선행은 409·시험 미생성, 저장 선행은 당시 학생/세션의 시험지·문항 보존을 확인했다. 계획서/보고서 전체 행 불변, 제거/이동/중지 소속 이력과 추가·이동 후 새 소속도 검증했다. 첫 실행은 합성 계획서/보고서의 컬럼 이름을 잘못 사용해 초기 자료 구성에서 중단됐고, 실제 form_data 구조로 정정한 최종 실행과 구분한다.
  - 검증 도구만 추가했으며 제품 코드 변경은 없다. 전체 85개 파일·464개/타입 포함 49개 경로 빌드는 47차 기준을 유지하고 이번 실제 DB 20개를 별도로 기록한다. 합성 PostgreSQL 정상 종료, 변경 파일 공백 검사 통과. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 처음 비어 있던 팀/새 팀이 생성 대기 중 대상에 들어온 경우의 전체 명단 일관성이다. 생성자 권한·근거 변경, 과거 평가 응답 집계, 외부 연동/기기/복구/채팅 알림·읽음/최종 정리 등 전체 잔여 범위는 유지한다.

- 후속 47차 G02 시험 생성 중 명단 변경(2026-09-09, 로컬): 생성 대기 중 학생 제거·중지·이동·추가/팀 보관 후에도 준비 당시 명단으로 시험이 저장되는 5개를 확인했다. 최초 5개 실패는 합성 팀 번호 중복이었으며 이를 제품 오류 재현으로 설명한 부분을 사용자에게 정정했다. 팀 번호를 고친 뒤, 현재 제품 파일을 되돌리지 않고 재검사 호출만 제외한 별도 검증 복사본(output/test-infra/exam-service-before-47.ts, vitest-exam-before-47.mjs)에서 5개 모두 실제로 시험 ID를 반환함을 재현했다(exam-roster-before-47-verified.log). 원래 before/after 및 full-tests-47.log의 팀 번호 오류는 최종 결과와 구분한다.
  - 수정/검증: AI 호출 완료 뒤 결과 저장 트랜잭션에서 팀 ID 순 세션→학생→관련 팀을 잠그고, 준비했던 각 팀의 활성 상태 및 현재 활성·표준·현재 학년도 학생 ID 목록이 동일한지 검사한다. 바뀌면 409로 재생성을 안내하며 시험 행을 만들지 않는다. 생성 대기 중 잠금은 없다. 정상 생성 1개와 다섯 변경의 거절/기존 소속 보존을 포함한 집중 4개 파일·61개(exam-roster-after-47-verified.log), 최종 전체 85개 파일·464개(full-tests-47-verified.log), 타입 포함 49개 경로 빌드(build-47.log), 변경 코드 공백 검사 통과. 이번은 테스트 DB/합성 생성기 검증이며 실제 PostgreSQL 양쪽 선행 순서·잠금 대기는 다음에 확인한다. 생성 시작 시 비어 있던 팀에 새 학생이 들어오거나 새 팀이 추가된 경우의 전체 대상 범위는 아직 검증하지 않았다. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 나머지 권한/근거/평가 집계/외부 연동/기기/복구/채팅 알림·읽음/최종 정리 범위도 계속 유지한다.

- 후속 46차 G06 시험 생성/학생 결과 학년도 분리(2026-09-09, 로컬): 현재 시험 생성 명단에 과거 계정이 포함되는 두 활동 2개와 결과 조회 14개 실패를 재현했다(exam-students-before-46.log). 결과 조회 중 6개는 과거 계정/팀으로 현재 시험 결과가 반환됐고, 과거 시험 8개는 결과 없음 대신 조회 예외가 발생했다. 생성 준비 명단에 현재 학생 학년도를 적용하고 공개 결과의 계정·팀·시험 학년도가 모두 현재일 때만 반환한다. 기존 시험/결과/소속 원문은 변경하지 않는다. 교사 시험 목록·과거 참여자 기록 열람은 이번 수정 범위가 아니다.
  - 검증: 두 활동 × 계정/팀/시험 현재·과거 조합 16개에서 기본/지정 팀 조회를 확인하고 합성 생성기 두 활동 2개로 현재 계정만 시험지 생성·과거 소속 불변을 확인했다. 집중 4개 파일·56개(exam-students-after-46.log), 정상 조회의 시험/팀 소속을 같은 활동으로 맞춘 최종 전체 84개 파일·458개(full-tests-46.log), 타입 포함 49개 경로 빌드(build-46.log), 변경 코드 공백 검사 통과. 이번은 테스트 DB·합성 생성기 검증이며 실제 AI/PostgreSQL 경합/기기 검증을 추가하지 않았다. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 생성 도중 학생 이동/중지와 결과 저장의 명단 일관성이다. 과거 평가 응답 집계, 추가 권한 경합, 외부 연동/기기/복구/채팅 알림·읽음/최종 정리 등 전체 잔여 범위는 유지한다.

- 후속 45차 G06 평가 학생/명단 학년도 분리(2026-09-09, 로컬): 과탐실·동아리에서 과거 학생/팀의 평가 화면 접근 6개와 현재 팀에 연결된 과거 계정이 교사 진척·학생 팀원 목록에 섞이는 2개를 재현했다(evaluation-members-before-45.log). 학생 문맥/저장 트랜잭션에서 계정·팀 학년도를 확인하고 동료평가 대상, 교사 피드백 대상, 공개 전후 명단 조회, 관리 진척/학생 팀원 목록에 현재 계정 학년도를 적용했다. 기존 과거 소속·평가 원문을 삭제하지 않는다.
  - 검증: 계정/팀 현재·과거 × 두 활동 8개와 현재 명단/동료평가 대상/교사 피드백/공개 대상 2개의 새 통합 검사 통과. 합성 동료평가의 확인 동의 필드 누락으로 발생한 400은 테스트 입력을 정정했고 최종적으로 과거 대상 403·교사 피드백 404·현재 학생 1명만 공개·과거 소속 불변을 확인했다(evaluation-members-after-45-final.log; 집중 4개 파일·43개). 기존 실제 PostgreSQL 평가 저장/공개·팀 변경 16개도 통과(evaluation-race-45.log, postgres-evaluation-race-after.json), 합성 DB 정상 종료. 전체 83개 파일·440개(full-tests-45.log), 타입 포함 49개 경로 빌드(build-45.log), 변경 코드 공백 검사 통과. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 시험 생성 명단과 학생 결과 조회다. 과거 평가 응답이 잘못 연결된 경우의 집계 의미, 추가 권한 변경 경합, 외부 연동/기기/복구/채팅 알림·읽음/최종 정리 등 잔여 범위는 완료로 간주하지 않는다.

- 후속 44차 G06 평가 관리 학년도 분리(2026-09-09, 로컬): 과거 과탐실 평가의 문항 수정과 두 활동의 시작·마감·재개/교사 피드백/의견 검토/공개가 처리되는 13개를 재현했다(evaluation-year-before-44.log). 공통 평가 관리 권한 검사에서 현재 학급인지 확인하고 담당 교사 동아리 검사도 현재 학년도를 요구한다. 현재 평가 동작과 기존 공개·버전 충돌 규칙은 유지한다. 일반 회차 열람 함수와 과거 원문/소속을 삭제하거나 변경하지 않는다.
  - 검증: 과탐실 문항 수정 2개 및 두 활동 × 현재/과거 × 상태/피드백/의견/공개 24개로 새 26개. 과거 거절 시 양식·회차·의견·공개본·세션·소속 전체 행 불변과 현재 정상 처리를 확인했다. 기존 평가/공개 경합/동아리와 집중 4개 파일·34개(evaluation-year-after-44.log), 전체 82개 파일·430개(full-tests-44.log), 타입 포함 49개 경로 빌드(build-44.log), 변경 코드 공백 검사 통과. 이번은 테스트 DB 검증으로 실제 PostgreSQL/기기/외부 연동을 추가 실행하지 않았다. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 현재 팀에 과거 계정이 연결된 경우의 평가 명단·공개 대상·학생 읽기/쓰기와 시험 생성/학생 결과 조회 경계다. 권한 변경 경합 및 외부 연동/기기/복구/채팅 알림·읽음/최종 정리 등 전체 잔여 범위는 유지한다.

- 후속 43차 G06 시험 관리 학년도/대상 일치(2026-09-09, 로컬): 과거 과탐실 시험의 문항 수정·추가·교정본 생성·결과 공개와, 과거 ID를 현재 시험으로 대체 조회한 뒤 확정/출력하는 오류를 재현했다. 문항 삭제 검사에서는 과거 문항이 변경된 뒤 모의 DB의 배점 연산 오류가 발생하는 것도 관찰했다. 시험 변경 권한 검사에 학급/동아리 현재 학년도를 적용하고, 개별 시험 조회 결과 ID가 요청 ID와 반드시 일치하도록 했다. 관리 목록의 기본 선택 동작은 유지하며 개별 작업에서 다른 시험을 대신 사용하지 않는다. 문항 삭제의 배점 매개변수를 정수로 명시했다.
  - 검증: 과탐실/동아리 × 현재/과거 × 문항 수정·삭제·추가/확정/교정/채점/공개/출력 새 32개. 최초 모의 DB 검사에는 정상 삭제 2개의 연산 오류가 섞였으며, 2→3문항으로 합성 조건을 변경해도 같은 오류가 남았다(exam-year-before-43.log/exam-year-before-43-final.log). 정수형 명시 후 집중 3개 파일·38개 통과(exam-year-after-43.log). 실제 PostgreSQL에서는 타입 추론 연산 자체가 정상임을 확인했고, 원래 2문항·2점 구성으로 두 활동의 현재 삭제/과거 거절 4개를 검증했다(postgres-exam-year-43-final.log, postgres-exam-year-43.json). 첫 실제 DB 실행은 Node의 TypeScript 변환 옵션 누락으로 중단됐으며 옵션을 추가한 최종 실행과 구분한다. 합성 DB 정상 종료. 전체 81개 파일·404개(full-tests-43.log), 타입 포함 49개 경로 빌드(build-43.log), 변경 코드 공백 검사 통과. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 시험 생성 명단/학생 결과 조회, 평가 회차·명단·공개·학생 조회의 나머지 학년도 경계와 기존 권한 경합/외부 연동/기기/복구/채팅 알림·읽음/최종 정리는 계속 남는다.

- 후속 42차 G06 동아리 소속 학년도 분리(2026-09-09, 로컬): 현재 동아리에서 과거 담당 교사/학생 계정으로 배정·탈퇴가 처리되는 6개와 담당 교사/마스터 관리 명단에 과거 학생이 섞이는 2개를 재현했다(club-year-before-42.log). 공통 동아리 교사 검사에 현재 학년도를 확인하고 배정/탈퇴는 기존 사용자·팀 잠금 안에서 학생 학년도를 검사한다. 관리 학생/팀원 목록에도 계정 학년도 조건을 적용했다. 현재 계정·동아리 정상 관리와 과거 요청 거절 시 사용자/소속/팀 전체 행 보존을 확인했다. 동일 학번의 다른 학년도 계정 중 현재 계정을 선택하는 기존 등록 경로는 정상임을 확인했으며 수정하지 않았다.
  - 검증: 담당/동아리/학생의 현재·과거 조합 × 배정·탈퇴 16개, 마스터/담당 목록 2개, 동일 학번 등록 1개로 새 19개. 기존 동아리 운영/설정/관리와 집중 4개 파일·41개(club-year-after-42.log), 전체 80개 파일·372개(full-tests-42.log), 타입 포함 49개 경로 빌드(build-42.log), 변경 코드 공백 검사 통과. 이번은 테스트 DB 검증이며 실제 PostgreSQL 경합/기기/외부 연동은 추가 실행하지 않았다. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 시험·평가의 개별 수정·공개 및 학생 결과 조회 학년도 경계다. 동아리 권한 변경과 관리의 동시 실행, 외부 연동/기기/복구/채팅 알림·읽음/최종 정리 등 전체 잔여 범위는 유지한다.

- 후속 41차 G06 비밀번호/마스터 계정 관리 학년도 분리(2026-09-09, 로컬): 학생 비밀번호 초기화 API, 공통 비밀번호 변경, 마스터의 교사·체험 계정 초기화/중지/복원이 과거 계정도 변경하는 8개 상황과 과거 마스터 권한 허용 1개를 재현했다(account-year-before-41.log). 현재 학년도 대상과 마스터만 허용하고 실제 비밀번호/상태 UPDATE에도 학년도 조건을 적용했다. 체험 학생 중지는 현재 학급·동아리의 소속과 팀장 관계만 종료해 과거 행을 보존한다. 기존 마스터 자기 계정/다른 마스터 상태 변경 제한은 유지한다.
  - 검증: 현재/과거 계정 관리 16개와 과거 마스터 거절·현재/과거 학급/동아리 소속 보존 1개를 추가했다. 초기 후속 검증에서 합성 club_members에 없는 id 컬럼을 사용한 테스트 오류를 실제 복합 키에 맞춰 정정했다. 최종 집중 3개 파일·23개(account-year-after-41-final.log), 전체 79개 파일·353개(full-tests-41.log), 타입 포함 49개 경로 빌드(build-41.log)와 변경 코드 공백 검사 통과. 이번은 테스트 DB 검증이며 실제 PostgreSQL 동시 실행·실제 기기 검증을 새로 수행하지 않았다. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 동아리 소속 관리와 시험·평가의 남은 학년도 경계이며 외부 연동/기기/복구/채팅 알림·읽음/최종 정리도 계속 남는다.

- 후속 40차 G06 학생/팀 관리 학년도 분리(현재 실행 환경 날짜 2026-09-09, 로컬): 현재 교사의 실제 관리 API에서 과거 학년도 학생 배정/제거/팀장 변경, 과탐실·동아리 팀 보관/복원, 학생 중지/복원 요청 9개가 처리되어 과거 행을 바꾸는 문제를 재현했다(management-year-before-40.log). 서비스에서 학생·팀의 현재 학년도를 확인하고 중지/복원 UPDATE에도 대상 조건을 적용했다. 배정/제거/팀장 검사는 기존 사용자·관련 팀 잠금 안에서 수행한다. 추가로 현재 학생의 배정·중지가 잘못 연결된 과거 소속/팀장 행까지 바꾸는 2개 상황을 재현해 정리 대상 역시 현재 학년도 팀으로 제한했다(management-year-history-before-40.log). 과거 자료 직접 열람이나 원본 계정·소속 삭제는 추가하지 않았다.
  - 검증: 현재/과거 학년도의 9개 관리 요청 조합 18개에서 현재 정상 처리·과거 400 거절 및 사용자/팀/소속/세션 불변, 현재 관리 시 과거 소속·팀장 기록 보존을 확인했다. 기존 팀/학생 관리·동아리/소속 경합과 집중 5개 파일·36개 통과(management-year-after-40-final.log). 실제 PostgreSQL의 판단 저장/권한 변경 30개와 요약/팀 보관·복원 3개도 재검증 통과(decision-access-40.log/summary-archive-40.log). 전체 78개 파일·336개 테스트(full-tests-40.log), 타입 포함 49개 경로 빌드(build-40.log), 변경 코드 공백 검사 통과. 합성 PostgreSQL 정상 종료. 이번 날짜 변경은 실행 환경 갱신을 따른 것이며 이전 실행 로그 날짜를 소급 변경하지 않았다. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 비밀번호 초기화·마스터 계정 관리, 동아리 소속/시험/평가의 나머지 학년도 경계와 기존 잔여 범위는 계속 남는다.

- 후속 39차 G06 학생 활동/팀 접근 학년도 분리(현재 실행 환경 날짜 2026-09-08, 로컬): getStudentActivities, getInquiryDataForUser의 기본/지정 팀 조회, assertActiveTeamMember에 계정·활동 학년도 조건이 없어 과거 소속 또는 잘못 연결된 학년도 간 소속으로 자료를 읽는 6개 상황을 재현했다(student-year-before-39.log; 현재 계정/현재 팀 2개는 정상). 과탐실/동아리 모두 계정과 팀의 학급·동아리 학년도가 현재 설정과 일치할 때만 학생 진입 경로에 허용한다. 교사의 과거 팀 조회 함수와 원문/소속 행은 변경하지 않는다.
  - 검증: 과탐실·동아리 × 현재/과거 계정 × 현재/과거 팀의 새 8개에서 활동 목록/기본·지정 팀 조회/소속 검사 일치, 현재 두 활동 정상 접근, 교사 과거 계획 원문 열람·소속/계획 행 불변을 확인했다. 기존 두 활동 공존·팀 보관/복원·계획 AI 최신성·회차 재분석 화면 데이터를 포함해 집중 5개 파일·22개 통과(student-year-after-39.log). 전체 77개 파일·318개 테스트(full-tests-39.log), 타입 포함 49개 경로 빌드(build-39.log), 변경 코드 공백 검사 통과. 이번은 테스트 DB 검증이며 새 실제 PostgreSQL/기기 검증은 없다. 운영/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. ID로 직접 요청하는 학생·팀 관리 변경, 나머지 동아리/시험/평가의 학년도 경계는 다음 범위다.

- 38차 원래 작업 전달: 사용자 승인에 따라 원래 작업 '주간 사용량 후 작업 계획 수립'(01a0714d-4de5-7e43-92ef-404b306168d1)에 보고서/상태 문서 경로, 완료 범위·310개 테스트/빌드, 잔여 범위와 운영 보류를 메시지로 전달했고 도구 성공 응답을 확인했다. 참고용 수신 확인만 요청했으며 제품 수정/검증/배포 중복 실행을 시작하도록 지시하지 않았다. 이전 open_in_codex의 대기 상태와 이번 실제 메시지 전달을 구분한다.

- 후속 38차 G06 로그인/교사 기본 목록 학년도 분리(현재 실행 환경 날짜 2026-09-08, 로컬): 현재 학년도 로그인 조회와 달리 기존 토큰 복원은 계정의 학년도를 확인하지 않았고, 교사 과탐실 기본 명단/활성·보관 팀 목록에 학년도 조건이 없어 과거 자료가 섞였다. 학생/교사/마스터의 동일 아이디·다른 학년도 계정 및 두 학년도 명단으로 실패 4개를 재현했다(year-access-before-38.log). 로그인 토큰 복원에 현재 설정 학년도를 확인하고 교사 기본 학생/비활성 학생/활성·보관 팀 목록과 팀 연결/인원 수를 현재 학년도로 제한했다. 원래 계정·팀·과거 소속 행을 삭제하거나 변경하지 않는다. 교사 과거 기록의 직접 열람 권한은 이번에 임의 변경하지 않았다.
  - 검증: 동일 아이디의 학년도별 계정 분리, 현재 토큰 허용/과거 토큰 거절, 현재 활성·비활성/보관 자료 유지·과거 제외, 학년도를 잘못 넘나드는 소속의 기본 목록/인원 집계 제외와 원본 소속 불변을 확인했다. 로그인·마스터 관련 집중 3개 파일·7개 및 명단·팀·학생 관리·동아리 관련 집중 5개 파일·18개 통과(year-access-after-38.log/year-dashboard-38.log). 전체 76개 파일·310개 테스트(full-tests-38.log), 타입 포함 49개 경로 빌드(build-38.log), 변경 코드 공백 검사 통과. 이번은 테스트용 DB 검증이며 새 실제 PostgreSQL/브라우저 검증으로 주장하지 않는다. 운영 설정의 학년도와 운영 데이터/실제 외부 연동/배포/Git 커밋·푸시는 미변경. 학생 활동 선택·팀 자료 접근과 ID로 직접 요청하는 관리 경로, 동아리·시험·평가의 학년도 조합은 계속 남는다.

- 후속 37차 마무리: 합성 검증 PostgreSQL 정상 종료와 문서 공백 검사 통과를 확인했다. 다음 우선 점검은 다른 학년도 계정·자료의 접근 분리이며, 이전 권한 검증의 미확인 조합은 삭제하지 않고 잔여 범위로 유지한다.

- 후속 37차 G01 학생 판단/최종 분석 동시 실행(현재 실행 환경 날짜 2026-09-08, 로컬 검증): 신규 판단/기존 판단 수정 × 판단 저장 선행/최종 결과 저장 선행/최종 생성 대기 중 판단 저장의 실제 PostgreSQL 6개를 통과했다(decision-final-37.log, postgres-decision-final-race.json). 앞선 두 순서는 실제 세션 행 잠금 대기를 확인했다. 판단 선행은 최종 근거에 최신 판단 포함, 최종 저장 선행은 판단 쓰기 거절, 생성 대기 중 변경은 옛 판단 근거를 원형 보존하되 isCurrent=false와 종료 거절·재분석 후 최신 판단 포함/정상 종료를 확인했다. 종료 후 판단 수정 거절, 계획서/보고서/기존 근거/첫 최종 분석 원문 불변도 단언했다. 제품 코드 변경이 없어 전체 75개 파일·306개 테스트/49개 경로 빌드는 36차 기준이며 이번 실제 DB 6개는 별도다. 운영/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 남은 권한 경로와 학년도/외부 연동·복구 범위를 정리해 점검한다.

- 후속 36차 최종 검증: 전체 75개 파일·306개 테스트(full-tests-36.log), 타입 포함 49개 경로 빌드(build-36.log), 변경 코드 공백 검사 통과. 아래 실행 중 표기는 이 결과로 대체한다. 실제 DB 권한 경합 30개는 자동 테스트 수와 별개다. 합성 PostgreSQL 정상 종료, 운영 미배포. 다음은 학생 판단과 최종 분석 동시 실행이며 이후 잔여 검증/최종 정리를 계속한다.

- 후속 36차 G02 학생 판단 저장 권한 경합(현재 실행 환경 날짜 2026-09-08, 로컬): 학생 비활성/비밀번호 초기화/팀 보관/팀 제거·이동 후에도 AI 제안의 신규 판단·판단 수정·동일 내용 재전송을 처리하는 15개 상황을 실제 DB에서 재현했다(decision-access-before-36-final.log, postgres-decision-access-before.json). 최초 검증의 생성 버전 0 가정은 실제 스키마의 초기 버전 1로 정정했으며 제품 문제와 구분한다. 판단 저장에서 세션→회차→학생→관련 팀 잠금 후 학생 역할·계정 활성·비밀번호 변경 완료·현재 팀 소속/활성 상태를 다시 확인한다. 기존 버전 충돌 및 같은 내용 재전송 규칙은 유지하며 팀장 전용으로 좁히지 않는다.
  - 검증: 신규/수정/동일 재전송 × 다섯 권한 변경 × 변경/저장 선행의 실제 DB 30개 통과(decision-access-after-36.log, postgres-decision-access-after.json). 변경 선행은 기존 판단 불변/신규 미생성, 저장 선행은 실제 변경의 행 잠금 대기를 관찰하고 정상 저장을 보존한다. 수정 시 최초 작성자와 버전 증가, 재전송 시 전체 행 불변, 기존 분석/근거 불변 및 권한 변경 후 재요청 거절을 확인했다. 현재 일반 팀원 허용/교사·비활성·비밀번호 변경 필요 계정 거절 새 4개와 기존 단일 회차 최종 종료/판단 보존을 포함한 집중 2개 파일·8개 통과(decision-access-unit-36.log). 전체 테스트/빌드는 실행 중이며 최종 결과 확정 전 통과로 간주하지 않는다. 이번 화면 변경/실제 AI·Sheet·푸시/운영 변경/배포/Git 커밋·푸시는 없다. 다음 검증은 학생 판단과 최종 분석의 동시 처리 및 남은 동아리 소속·학년도/외부 연동·복구 범위다.

- 후속 35차 최종 검증: 전체 75개 파일·302개 테스트(full-tests-35.log), 타입 포함 49개 경로 빌드(build-35.log), 변경 코드 공백 검사 통과. 아래 확인 중 표기는 이 결과로 대체한다. 실제 DB 권한 경합 32개와 분석 중복/인계 4개는 별도이며 자동 테스트 수에 합산하지 않는다. 이번에는 기존 역할 테스트의 분석 결과 재사용 검사를 강화했으므로 테스트 개수는 34차와 같다. 합성 PostgreSQL 정상 종료, 운영 미배포. 다음은 학생 판단 저장의 계정/소속 변경 경계다.

- 후속 35차 G02 AI 분석 권한 경합(현재 실행 환경 날짜 2026-09-08, 로컬): 분석 시작 전 권한 조회 뒤, AI 생성 대기 중, 결과 INSERT 직전에 교사 비활성화/비밀번호 초기화/팀 보관/담당 해제가 끝나도 중간·최종 분석이 저장되는 24개 상황을 실제 DB에서 재현했다(analysis-access-before-35-final.log, postgres-analysis-access-before.json). 최초 검증 스크립트의 작업 테이블명 오류는 본문 이후 검사 실패였으며 실제 ai_generation_jobs/created_by로 정정한 재현 결과와 구분한다. 분석 근거를 고정하는 시작 트랜잭션과 결과 저장 트랜잭션 각각에 세션→회차→교사→팀→필요한 담당 관계 잠금/현재 권한 재검사를 연결했다. AI 생성 대기 중 DB 잠금은 유지하지 않는다.
  - 검증: 시작 전 변경/생성 중 변경/결과 저장 전 변경/결과 저장 선행 × 중간·최종 × 네 변경의 실제 DB 32개 통과(analysis-access-after-35.log, postgres-analysis-access-after.json). 시작 전 변경은 근거/작업/생성 호출 0건, 생성 후 변경은 기존 근거를 남기되 새 분석 결과 없이 작업 실패로 정리, 결과 저장 선행은 실제 권한 변경의 행 잠금 대기와 정상 결과 보존을 확인했다. 모든 경우 세션/회차/계획서/보고서 원문 불변·권한 변경 후 재실행 거절 및 기존 근거/결과 불변을 검증했다. 기존 역할 범위 테스트에 담당/마스터의 완료 분석 재사용과 비담당/학생 거절을 추가해 집중 3개 파일·12개 통과(analysis-access-unit-35.log). 실제 분석 중복/만료 인계 4개도 재검증했다(analysis-ownership-35.log). 전체 테스트/빌드 최종 결과는 아직 확인 중이다. 실제 AI·Sheet·푸시/학생 자료/운영 변경/배포/Git 커밋·푸시는 없다. 학생 판단 저장의 소속·계정 재검사 경계는 다음 수정 대상으로 남는다.

- 후속 34차 최종 검증: 전체 75개 파일·302개 테스트(full-tests-34.log), 타입 포함 49개 경로 빌드(build-34.log), 변경 코드 공백 검사 통과. 아래 실행 중 표기는 이 결과로 대체한다. 실제 DB 권한 경합 16개와 기존 승인/회차 경합 8개는 별도 검증이며 자동 테스트 수에 합산하지 않는다. 합성 검증 PostgreSQL 정상 종료, 운영 미배포. 다음은 AI 분석 시작/완료 및 학생 판단 저장의 권한 변경 경계다.

- 후속 34차 G01/G02 회차 종료 권한 경합(현재 실행 환경 날짜 2026-09-08, 로컬): 사용자의 '나머지 일 하자' 지시로 재개했다. 합성 AI 응답의 필수 제안 형식을 정정한 뒤, 권한 사전 조회 이후 교사 비활성화/비밀번호 초기화/팀 보관/동아리 담당 해제가 끝나도 다음 회차 시작·전체 탐구 완료가 처리되는 오류 8개를 실제 PostgreSQL에서 재현했다(cycle-access-before-34-final.log, postgres-cycle-access-before.json). 종료 트랜잭션의 세션→회차 잠금 뒤 교사→팀→필요한 담당 관계를 잠그고 현재 권한을 재확인한다. 기존 동아리 담당/마스터 관리 범위를 유지한다.
  - 검증: 변경/종료 선행의 실제 DB 16개 통과(cycle-access-after-34.log, postgres-cycle-access-after.json). 변경 선행은 회차/세션/계획서/보고서/이력 불변, 종료 선행은 실제 권한 변경의 행 잠금 대기·정당한 종료와 과거 원문 보존을 확인했다. 근거 스냅샷과 분석 원본은 모든 경우 불변이며 후속 오래된 종료도 거절한다. 두 종료 방식의 담당/비담당/마스터/학생 범위 새 8개를 포함한 집중 검증 3개 파일·12개와 기존 승인/회차 전환 실제 DB 8개 재검증 통과(positive-review-34.log). 합성 PostgreSQL 정상 종료. 전체 테스트/빌드는 실행 중이며 결과 확정 전 통과로 간주하지 않는다. 이번 화면 변경/실제 외부 호출/운영 변경/배포/Git 커밋·푸시는 없다. 다음은 AI 분석 시작·완료 저장 및 학생 판단 중 권한 변화다.

- 사용자 현황 정리 요청으로 일시 중단(현재 실행 환경 날짜 2026-09-08): 후속 34차 회차 전환 권한 검증용 scripts/local-postgres-cycle-access-race.mjs를 작성했다. 최초 실행은 합성 AI 응답의 suggestions 배열이 비어 있어 스키마 검사에서 종료되었으며 권한 경합 본문은 실행되지 않았다(cycle-access-before-34.log). 제품 오류 재현/수정/통과로 기록하지 않는다. 재개 시 합성 응답 형식을 고친 뒤 실제 경합을 확인해야 한다. 이번 34차 제품 코드 변경은 없으며 마지막 완료 기준은 아래 33차 전체 294개 테스트/빌드다. 사용자는 작업이 길어져 비개발자용 전체 현황과 남은 일을 먼저 요청했다.

- 후속 33차 G01/G02 문서 복원 권한 경합(현재 실행 환경 날짜 2026-09-08, 로컬): 계획서/보고서 복원 권한 조회 이후 교사·학생 비활성화/비밀번호 재설정, 학생 제거·팀 이동·팀장 변경·팀 보관이 끝나도 복원이 저장되는 오류 16개를 실제 PostgreSQL에서 재현했다. 세션→회차→문서 잠금 뒤 복원자와 보고서에 복원될 역할 사용자, 관련 팀을 일관된 순서로 잠그고 현재 계정/소속/팀장 권한을 재검사한다. 비밀번호 변경 필요 계정도 거절한다. 보고서 역할 사용자는 팀보다 먼저 잠가 역할 외래 키 검사와 팀장 변경의 잠금 역전을 피한다. 활성 교사의 기존 공동 복원 범위와 보관 팀 복원은 유지하며 동아리 담당 제한을 새로 추가하지 않았다. 학생은 활성 팀의 현재 팀장만 복원한다. 과거 원문/역할과 승인 후 재검토 상태 규칙을 보존한다.
  - 검증: 수정 전 실제 DB 재현 16개(postgres-restore-access-before.json/restore-access-before-33.log), 수정 후 변경/복원 선행의 32개 통과(postgres-restore-access-after.json/restore-access-after-33.log). 변경 선행은 문서/역할/이력 불변, 복원 선행은 실제 행 잠금 대기 뒤 복원 이력과 원문 보존·권한 변경 완료·후속 복원 거절을 확인했다. 교사/학생의 보관 팀 복원 범위 테스트 2개를 추가해 해당 파일 4개 통과(restore-access-unit-33-final.log). 기존 복원/회차 전환 8개 재검증 통과(restore-cycle-33.log). 전체 74개 파일·294개 테스트(full-tests-33.log), 타입 포함 49개 경로 빌드(build-33.log), 변경 코드 공백 검사 통과. 화면 변경이 없어 새 브라우저/실제 기기 검증은 수행하지 않았다.
  - 검증 DB 종료 호출은 관찰 제한 시간을 초과했으나 재시작하지 않고 로그의 database system is shut down과 postgres 프로세스 종료를 확인했다. 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 회차 전환·AI 분석 중 권한 변화, 학생 문서 쓰기와 동아리 소속 변경 조합 및 G06/G07 잔여 검증이다.

- 후속 32차 G01/G02 교사 검토 권한 경합(현재 실행 환경 날짜 2026-09-08, 로컬): 교사 비활성화/임시 비밀번호 재설정/팀 보관 이후에도 계획 승인·보고서 확인 완료 또는 수정 요청이 저장되는 경로와 동아리 계획 검토 중 담당 해제 경로를 실제 PostgreSQL에서 14개 재현했다. 검토 트랜잭션의 세션→회차→문서 잠금 뒤 교사 계정과 팀 행을 잠그고 활성 교사·비밀번호 변경 완료·활성 팀을 재검사한다. 동아리 계획은 기존 담당/마스터 규칙을 유지하며 담당 관계 행도 잠근다. 보고서는 기존 공동 교사 검토 범위를 유지한다. 계획 검토도 공통 문서 회차 잠금을 사용한다. 이미 적법하게 끝난 검토·원문·이력은 보존한다.
  - 검증: 변경/검토 선행의 실제 DB 28개 통과(postgres-review-access-after.json, review-access-after-32.log). 변경 선행은 문서/검토 이력/수정 요청 알림 불변·미생성, 검토 선행은 실제 행 잠금 대기 후 검토와 알림 보존·변경 이후 재검토 거절을 확인했다. 새 8개 단위 검증은 학생 거절과 계획/보고서의 담당·마스터·공동 교사 범위를 대조했으며 집중 검증 3개 파일·14개 통과. 기존 긍정 승인/회차 전환 8개와 학생 수정/재제출 8개도 재검증했다(positive-review-32-final.log, positive-review-student-32.log). 전체 74개 파일·292개 테스트(full-tests-32.log), 최종 타입 포함 49개 경로 빌드(build-32-verified.log), 변경 코드 공백 검사 통과. 이번에는 화면 변경이 없어 새 기기 검증을 주장하지 않는다.
  - 실행 복구/검증 한계: 이전 실행 연결과 DB가 종료된 것을 확인한 후 합성 DB를 재시작했고, 정상 복구 완료를 확인한 뒤 검증했다. 최초 Node 서비스 로딩의 next/headers 확장자 오류는 검증 전용 local-review-test-loader.mjs로 실제 Next 엔트리를 연결해 해결했다. 추가 회차 검증의 DB 생성 시간 초과는 검증 본문 실행 전 실패이며 단독 재실행 통과 결과와 구분한다. 별도 피드백 검증 프로세스가 사용한 임시 페이지의 생성 타입이 빌드에 남아 해당 프로세스 종료를 확인한 뒤 생성 파일만 output/test-infra/generated-types-before-32에 보관했다. 보관본까지 타입 검사에 포함되던 문제는 .txt 확장자로 보존해 해결하고 최종 빌드를 통과했다. 제품 수정에 대한 자동 승인 검토의 최초 거절 뒤 CURRENT_DECISIONS.md의 후속 구조 수정 허용 지시와 실제 부분 적용 상태를 확인해 같은 수정 절차로 재검토를 받아 완료했다. 우회 실행이나 사용자 지시 없는 배포는 하지 않았다. 합성 검증 PostgreSQL 정상 종료. 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 G02 문서 복원·회차 전환·AI 분석 중 권한 변화와 동아리 소속 변경, G06/G07 잔여 검증이다. 이전 항목의 날짜·실행 이력은 현재 환경 시각에 맞춰 소급 수정하지 않는다.

- 계획서 우선 작업의 추가 검증 기록(이 세션): 교사 AI 확인 항목의 즉시 피드백 누적, 중복 반영 방지, 교사 수동 문장 보존, 제출본별 복구 초안, 실패 후 재전송과 최신 제출본 검사에 대한 검증을 수행했다. `scripts/teacher-plan-feedback-regression.mjs`의 로컬 합성 화면에서 API 요청을 모두 가로채고 768px 브라우저의 누적·수동 수정 보존·중복 방지·새로고침 복구·제출본 전환 격리·오래된 AI 경고·실패 후 초안 보존·명시적 전송 payload 일치를 통과했다. 제출 시각의 서버/브라우저 오전·AM 차이로 인한 hydration 오류를 재현해 한국 시간의 고정 숫자 표기로 수정했다. 단위 검증은 계획서 관련 3개 파일 8개를 통과한 후 재제출/다른 교사 충돌 검증 1개를 추가해 해당 2개 파일 7개 통과. 전체 실행은 74개 파일 중 73개 통과, 292개 테스트 중 보고서 학생 권한 1개 실패였으나 함께 갱신된 최신 작업본에서 해당 권한 파일 8개를 재실행해 모두 통과했다. 전체를 재실행한 성공으로 표기하지 않는다. 타입 검사는 통과했다. 별도 프로덕션 빌드는 다른 build 실행 잠금 때문에 시작되지 않아 이번 세션의 빌드 성공을 주장하지 않는다. 합성 검증용 임시 페이지를 삭제했고, 종료 대기한 이번 테스트/자식 서버 PID를 확인해 종료했다. 이 세션에서 운영 변경·실제 AI/Sheet/푸시·배포·Git 커밋·푸시는 실행하지 않았다. 다른 작업이 기록한 최신 배포/검토 이력은 그대로 보존한다.
- 2026-09-09 후속 31차 G01 승인과 학생 수정/재제출(로컬 검증): 실제 계획서/보고서 저장·제출·긍정 검토 서비스에서 학생 수정/같은 내용 재제출×학생/교사 선행의 8개 시나리오 통과(postgres-positive-review-student-race.json, positive-review-student-31-final.log). 모든 경우 실제 세션 행 잠금 대기를 관찰했다. 학생 선행은 교사가 본 제출 ID/보고서 버전이 달라져 승인 거절, 교사 선행은 승인 이력을 남긴 뒤 후속 학생 수정/재제출을 보존하며 다시 검토할 상태로 전환한다. 계획 수정 선행은 기존 pending 제출을 withdrawn으로 남기고 draft, 승인 후 수정은 이전 approved 제출을 보존하고 reapproval_required, 재제출은 새 제출 ID의 pending으로 남는다. 보고서 수정은 draft, 재제출은 submitted로 남고 오래된 확인 완료를 재실행해도 문서가 바뀌지 않는다. 기존 계획 고정 스냅샷 불변과 승인 이력 생성 건수도 검증했다. 최초 스크립트는 내용 변경 없는 재제출도 과거 pending을 withdrawn으로 바꾼다고 잘못 가정해 중단했다. 현재 구현과 확정된 '내용 수정 시 철회' 범위를 대조해 과거 상태 보존/새 최신 ID 검증으로 정정했으며 제품 동작은 변경하지 않았다. 제품 코드 미변경이므로 전체 73개 파일·283개 테스트/49개 경로 빌드는 29차 결과를 따른다. 이번 실제 DB 8개는 별도다. 스크립트 공백 검사 통과, 합성 PostgreSQL 정상 종료. 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 교사 계정/팀 보관/동아리 담당 변경 중 문서 검토 권한과 G02/G06/G07 잔여 검증이다.

- 2026-09-09 후속 30차 G01 계획 승인/보고서 확인 완료 경합(로컬 검증): 앞선 plan_review/report_review 검증은 수정 요청 분기였으므로 승인·확인 완료를 실제 서비스로 별도 검증했다. 실제 저장 수정→재제출 후 계획 approved/보고서 reviewed 처리와 다음 회차/탐구 완료를 양쪽 선행 순서로 교차한 8개 시나리오 통과(postgres-positive-review-race.json, positive-review-30.log). 세션 행 잠금 대기를 관찰했고 승인/확인은 저장하되 옛 분석에 의한 전환은 거절하며 최신 재분석 후 정상 전환함을 확인했다. 각 시나리오의 종료 후 오래된 승인 요청도 재실행해 거절·현재 계획서/보고서 및 이전 회차 변경 이력 불변을 확인했다. 계획 제출 고정 스냅샷 불변과 긍정 승인에서 수정 요청 알림 미생성도 단언했다. 새 결함이 발견되지 않아 제품 코드는 변경하지 않았으며 전체 73개 파일·283개 테스트/49개 경로 빌드는 후속 29차 결과를 따른다. 이번 실제 DB 8개를 단위 테스트 수에 합산하지 않는다. 변경 스크립트 공백 검사 통과, 합성 검증 PostgreSQL 정상 종료. 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 승인과 학생 수정/재제출의 교차 및 교사 계정/팀 보관/동아리 담당 변경 중 검토 권한, G02/G06/G07 잔여 검증이다.

- 2026-09-09 후속 29차 G01/G02 준비물 저장 권한 경합(로컬): API의 소속 확인과 서비스의 팀 조회 뒤 팀 제거/이동·학생 비활성화·팀 보관이 완료되어도 준비물 신청이 저장되는 오류를 실제 PostgreSQL에서 4개 재현했다. 준비물의 회차 확인·현재 계정/소속/팀 재검사·전송 대상 읽기·신청 저장을 세션→회차→사용자/관련 팀 잠금 아래 한 트랜잭션으로 묶었다. 저장 완료 후 외부 전송을 준비/실행하므로 네트워크 대기 중 잠금은 유지하지 않는다. 같은 내용의 완료 결과 재사용과 일반 재전송도 현재 권한을 확인한다. 연습 신청에 대한 교사 재시도 역시 계정/팀 권한을 확인한 뒤 미전송 안내만 반환하며 원문은 유지한다. 기존 공동 교사 조회/재전송 권한을 담당 교사만으로 축소하지 않았다.
  - 검증: 수정 전 실제 DB 재현 4개(postgres-material-access-before.json), 수정 후 네 변경×변경/저장 선행 8개 통과(postgres-material-access-after.json). 변경 선행은 저장 행/전송 준비/전송 모두 0건, 저장 선행은 변경의 실제 행 잠금 대기를 관찰하고 적법하게 저장된 원문·원래 제출자·완료 전송·과거 팀원 기록을 보존했다. 새 계정 검증 8개는 학생 신규 저장/완료 결과 재사용 및 교사의 일반/연습 재시도에 대해 비활성·비밀번호 변경 필요 상태를 차단하고 원문/미호출을 확인했다. 동아리 서비스 테스트의 합성 소속 누락은 실제 활성 소속을 추가해 정정했다. 기존 전송 완료/회차 전환 4개와 준비물 저장/회차 전환 4개도 실제 DB로 재검증했다(material-completion-after-29.log/material-cycle-after-29.log). 전체 73개 파일·283개 테스트, 타입 포함 49개 경로 빌드 및 변경 코드 공백 검사 통과(full-tests-29.log/build-29.log). 외부 전송은 합성 함수로 대체했으며 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 검증 DB 종료 호출의 관찰 제한 시간이 초과됐으나 재시작하지 않고 실제 상태를 조회해 서버가 실행 중이지 않음을 확인했다. 다음은 G01 계획 승인/보고서 확인 완료의 남은 경합, G02 동아리 소속 변경 조합 및 G06/G07 잔여 검증이다.

- 2026-09-09 후속 28차 G04 이전 미전송 신청 확인(로컬): 교사 화면이 최신 신청 한 건만 표시해 같은 회차의 오래된 미전송 신청을 찾기 어려웠다. 현재 팀/회차의 일반·불확실한 미전송 신청을 오래된 순서로 조회하고 최신 신청과 중복 없이 '이전 미전송 신청' 목록에 표시한다. 원래 품목·규격·링크·합계·신청 시각과 전송 오류를 펼쳐 확인하고 각 신청 ID로 재전송한다. 순수 연습 신청은 추가 미전송 목록에서 제외하되 최신 연습 표시를 유지하고, 완료 회차의 재전송 버튼은 숨긴다. 과거 성공 신청 전체 조회나 완료 회차의 재전송 허용으로 확대하지 않았다. 전송 위치가 불명확한 신청은 원래 서비스의 기록 대조 요구를 유지한다. 재전송 UI를 별도 컴포넌트로 분리해 연결 끊김·잘못된 JSON·전송 실패 뒤 버튼을 복구하고 중복 클릭·화면 이탈 후 응답을 처리한다. 실패 응답을 성공 안내로 표시하지 않는다.
  - 검증: 새 조회 테스트는 최신 synced와 오래된 pending/failed, 전송 대상이 남은 불확실한 demo의 포함·연습/다른 팀/다른 회차 제외·원문 불변을 확인했다. 첫 합성 회차 번호 0 제약 오류를 유효 번호로 정정한 뒤 통과했다. 전체 72개 파일·275개 테스트 및 타입 포함 49개 경로 빌드 통과(full-tests-28.log). 최초 브라우저 검증에서 새 날짜의 서버/브라우저 표기 차이에 따른 초기화 오류를 발견해 고정 한국 시간 문자열로 수정했고 최종 빌드(build-28-final.log)와 새 합성 DB 재검증을 통과했다. Chrome 768×1024에서 최신 성공/이전 미전송 2건 동시 표시·원래 품목/링크·네트워크/JSON/전송 실패 복구·정확한 이전 신청 ID 4회·합성 성공 후 미전송 1건 갱신·완료 회차 읽기 전용·브라우저 오류 0건을 확인하고 화면도 검토했다(material-pending-browser.json/png). 전송 응답 4개는 가로챈 합성이며 실제 외부 Sheet는 호출하지 않았다. 대상 불명 신청은 실제 API가 대조 요구로 거절하고 원문을 보존함을 확인했다. 최종 코드 공백 검사 통과, 지정 브라우저 자식/부모 종료 및 검증 PostgreSQL 정상 종료. 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 준비물 초기 저장의 소속/계정/팀 보관 변경 경계와 G01 승인, G02/G06/G07 잔여 검증이다.

- 2026-09-09 후속 27차 G01/G04 준비물 전송 결과/회차 종료(로컬): 회차 전환이 분석 근거를 확인한 뒤 미전송 신청을 검사하기 직전에 전송 완료가 저장되면 옛 분석으로 종료되는 오류를 다음 회차/탐구 완료 두 경우에서 실제 PostgreSQL로 재현했다. 전송 성공·실패 결과 저장에 세션→회차→신청 행 잠금과 현재 전송 작업 ID 검사를 적용했다. 외부 전송 대기 중에는 DB 잠금을 유지하지 않는다. 이미 저장된 동일 작업의 중복 응답은 상태를 다시 쓰지 않고 반환하며, 새 신청 작업이 시작된 뒤 도착한 옛 성공/실패 응답은 새 신청과 전송 작업 행을 변경하지 못한다.
  - 검증: 수정 전 재현 2개(postgres-material-completion-before.json), 수정 후 두 종료 방식×전환/전송 완료 선행의 실제 DB 경합 4개 통과(postgres-material-completion-after.json). 실제 행 잠금 대기를 관찰했고 옛 분석의 종료 거절·전송 완료 보존·최신 재분석 뒤 정상 종료·전송 작업 정리를 확인했다. 오래된 성공/실패 응답 2개를 추가한 준비물 집중 테스트 17개 및 전체 71개 파일·274개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과(material-results-27.log/full-tests-27.log/build-27.log). 전송 함수는 합성 응답으로 대체했으며 실제 Sheet 호출은 없다. 성공 결과의 실제 DB 경합 4개를 실패 응답의 모든 경합까지 검증한 것으로 확대하지 않는다. 검증용 PostgreSQL 정상 종료. 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 여러 과거 미전송 신청의 조회·재확인 경로와 준비물 초기 저장의 소속 변경 경계, G01 승인 및 G02/G06/G07 잔여 검증이다.

- 2026-09-09 후속 26차 G04 체험 준비물/회차 종료(로컬): 원래 제출자가 demo이고 저장된 전송 대상(sync_snapshot)이 없으며 synced가 아닌 신청만 연습 제출로 구분한다. 재전송을 누른 사람의 계정으로 구분하지 않으며, 일반 제출·전송 대상이 남은 불확실한 자료는 예외로 넘기지 않는다. 회차 종료는 이 연습 신청을 미전송 차단 대상에서 제외하되 원문/제출자/sync_status/synced_at을 변경하지 않는다. 학생·교사 화면과 새 회차 근거 스냅샷에는 연습 여부를 함께 표시하고 '연습 제출 · 시트 미전송'으로 안내하며 해당 신청의 교사 재전송 버튼은 숨긴다. 기존 고정 스냅샷에 없는 연습 여부를 소급해서 채우지 않는다. 또한 체험 계정이 일반 학생이 제출한 기존 신청을 덮어쓰지 못하게 했다. 계정 유형은 기존 생성/조회 규칙을 따르며 새 DB 마이그레이션이나 가짜 synced 상태를 추가하지 않았다.
  - 검증: 실제 PostgreSQL에서 다음 회차/탐구 완료 × 연습만/일반 미전송/전송 대상이 남은 불확실한 demo/연습+일반 혼합의 8개 검증 통과(postgres-material-practice.json). 연습만 전환 허용, 나머지 active 유지/전송 확인 요구, 원래 신청 모든 필드 불변, 교사 demo 재시도 미전송과 고정 이력의 연습 표시·pending 보존을 단언했다. 전송 작업 행은 생성되지 않았다. 새 단위 검증은 실제 조회 데이터의 연습 표시·가짜 synced 미생성과 demo의 일반 원문 덮어쓰기 거절을 확인했다. 전체 71개 파일·272개 테스트 및 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과(full-tests-26.log/build-26.log). 빌드 산출물+새 합성 DB의 Chrome 768×1024에서 실제 준비물 API에 demo 신청을 저장하고 학생/교사 연습 표시·연습 재전송 버튼 미표시·별도 일반 신청의 전송 대기/재전송 버튼 유지·원래 연습 행 불변/전송 작업 없음·브라우저 오류 0건을 확인했다(material-practice-browser.json, material-practice-student.png, material-practice-teacher.png). 학생 화면도 시각 검토했다. 외부 Sheet/AI는 호출하지 않았다. 검증 브라우저 자식의 경로/부모 확인 후 종료·부모 실행 종료와 PostgreSQL 정상 종료를 확인했다. 운영/실제 학생 자료/외부 푸시/배포/Git 커밋·푸시는 미변경. 다음은 G01/G04 일반 준비물의 저장/전송 완료와 회차 전환 잠금 범위, 여러 과거 미전송 신청의 재확인 경로 및 G02/G06/G07 남은 검증이다.

- 2026-09-09 후속 25차 F03 AI 질문/관심사 복구(로컬): ChatPanel은 전송 직전에 질문을 지우고 fetch/JSON 예외를 처리하지 않아 통신 실패 시 글과 버튼 상태를 복구하지 못하는 코드 경로가 있었다. 사용자+세션+회차별 sessionStorage에 질문·관심사·동일 재시도 ID를 입력/요청 시 즉시 보관하고 성공 응답을 확인한 뒤에만 질문과 해당 요청 정보를 정리한다. 주제 생성 중 추가 편집한 관심사는 보존한다. 메시지/탐구 방향/방향 선택 모두 실패 후 버튼을 해제하며, 화면 이탈 때 브라우저 대기만 취소하고 서버에서 진행된 요청은 같은 ID로 재시도한다. 이전 컴포넌트의 응답은 새 화면의 초안/알림을 변경하지 않는다. 저장소 사용이 실제로 실패할 때만 복사 안내를 표시하고 메모리의 글은 유지한다. 초안 복구는 같은 브라우저 탭의 보관 범위이며 닫힌 탭·다른 기기로의 동기화를 주장하지 않는다.
  - 검증: 전체 71개 파일·270개 테스트와 타입 포함 49개 경로 빌드 통과(full-tests-25.log/build-25.log). 새 빌드+별도 합성 PostgreSQL의 Chrome 768×1024에서 연결 끊김/잘못된 응답 이후 글·버튼 복구와 새로고침/탭 이동 후 동일 요청 ID, 성공 확인 후 질문 정리, 관심사 재시도·대기 중 추가 편집 보존·방향 선택 실패, 탭 이동 후 늦은 응답 무효화, 회차 변경 후 이전 초안/요청 보존과 새 회차 분리, 같은 탭의 다른 학생 로그인 초안 분리, 저장소 실패 시 메모리 유지/안내를 6개 시나리오로 검증했다(ai-chat-draft-browser.json/png). AI 쓰기 9개는 브라우저에서 가로챈 합성 응답이며 실제 외부 AI를 호출하지 않았다. 스크립트의 중복 alert 선택과 합성 회차의 NOT NULL 초기값 오류를 정정한 뒤 최종 통과했고 화면도 검토했다. 브라우저 오류 0건, 실제 태블릿/IME 검증은 별도다. 검증용 브라우저 자식 경로/부모 PID 확인 후 종료와 부모 실행 종료를 확인했고 로컬 PostgreSQL도 정상 종료했다. 운영/실제 학생 자료/외부 Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 G04 체험 계정 준비물의 전송 보류와 회차 종료의 연결, G01 승인/전송 남은 조합 및 G02/G06/G07 잔여 검증이다.

- 2026-09-09 후속 24차 공동 교사 권한/팀 보관 검증(로컬): 23차에 확인 대상으로 적은 교사 대화 조회의 담당 조건 부재는 결함이 아니다. CURRENT_DECISIONS.md의 2026-09-04 확정 범위는 기존 교사의 학생 기록 공동 조회를 유지하고 담당/마스터 제한은 동아리 운영 설정과 Sheet 연결 관리에 적용한다. 제품 권한을 임의 축소하지 않았다. 실제 대화 GET/교사 정리 POST 서비스와 합성 생성기로 담당·비담당·마스터의 공동 조회/정리 허용, 비담당 운영 설정 생성 거절/담당·마스터 허용, DB에서 비활성화·비밀번호 변경 필요 상태인 교사 거절(오래된 인증 객체도 차단), 다른 팀 학생/탈퇴 학생 접근 거절 및 탈퇴 후 교사용 원문·요약 불변의 7개 새 테스트 통과(discussion-access-24.log). 실제 PostgreSQL에서 보관 선행→생성 함수 미호출/교사 원문 보존/복원 후 생성, 생성 중 보관→늦은 요약 차단/복원·재시도 후 원문과 단일 요약 보존, 요약 INSERT 선행의 실제 팀 행 잠금 대기를 pg_blocking_pids로 관찰→보관/복원 후 고정 요약 불변의 3개 검증 통과(postgres-summary-archive.json, summary-archive-24.log). 제품 코드 변경이 없으므로 전체 263개 테스트/49개 경로 빌드는 23차 기준이며 이번 추가 7개 테스트와 실제 DB 3개를 별도로 기록한다. 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 검증용 PostgreSQL도 정상 종료했다. 다음은 F03 AI 대화/관심사 초안의 통신 실패·회차 변경 복구와 G01/G02/G04/G06/G07 남은 검증이다.

- 2026-09-09 후속 23차 G05 날짜별 요약 생성/조회 회차 분리(로컬): 마이그레이션 0009로 회차+활동 날짜 키의 cycle_discussion_days/summaries와 최초 원문 이행 기록을 추가했다. 예전 session+date 정리/작업 테이블은 수정·분류하지 않고 보존한다. 기존 활성 회차 원문은 실제 cycle_id와 한국 시간 날짜로 새 작업을 한 번 준비하며 자동 실행은 회당 최대 20개 회차 이행 후 활성 회차/팀의 대상 날짜만 처리한다. 요약 획득/원문 읽기/버전 고정은 세션→회차→팀 잠금 안에서 짧게 수행하고 모델 호출은 잠금 밖에서 한다. 완료 때 같은 회차 활성 상태·팀 상태와 작업 토큰을 다시 확인하므로 전환 뒤 늦은 요약은 저장되지 않는다. 생성 중 새 원문은 다음 버전으로 남고 만료 인계 뒤 이전 토큰의 저장/실패 처리는 새 작업을 덮어쓰지 않는다. AI 대화/학생 기록의 작업 표시도 명시적 회차를 사용한다. 조회 API와 교사 정리 API는 cycleId 필수이며 학생·교사 화면의 회차 선택으로 이전 원문/고정 요약을 읽기 전용 조회할 수 있다. 기존 형식 요약은 선택 날짜의 별도 읽기 전용 안내에서 원본 이력/근거를 보존한다. 완료 회차는 새 생성·대기 자동 재시도를 안내하지 않는다. 회차 분석은 새 회차 요약을 우선하고 해당 날짜의 예전 요약은 새 정리가 없을 때 기존 원문 검증을 거쳐 사용한다.
  - 검증: 실제 PostgreSQL의 0009 기존 행 보존/반복 적용, 같은 날 두 회차 생성·조회/완료 후 고정, 두 전환 방식의 생성 중 종료→늦은 결과 차단과 정리 선행→옛 분석의 전환 거절, 중복 요청/만료 인계, 한국 시간 자정 원문 이행/예전 형식 별도 조회의 8개 시나리오 통과(postgres-summary-cycle.json). 22차 원문/0008 보존 검증도 새 회차 요약 동작에 맞춰 3개 재검증했다(postgres-discussion-evidence-scoped.json); 과거 혼합 재현 결과 파일은 유지했다. 전체 70개 파일·263개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과(full-tests-23.log/build-23.log). 빌드 산출물+합성 PostgreSQL의 Chrome 768×1024에서 같은 날 두 회차 원문/요약 분리·이전 회차 읽기 전용·예전 형식 별도 표시·현재 회차로 돌아왔을 때 초안 보존·전송 실패 복구·완료 학생 교사 버튼 미표시·브라우저 오류 0건을 확인하고 화면도 검토했다(discussion-scoped-browser.json/png). 실제 태블릿/IME 검증으로 확대하지 않는다. 검증용 브라우저 자식 경로/부모 PID를 확인해 종료하고 부모 실행 종료를 확인했다. 검증용 PostgreSQL도 정상 종료했다.
  - 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음 우선 확인: assertDiscussionAccess의 교사 분기에서 동아리 담당/마스터 범위가 다른 교사 서비스와 동일하게 제한되는지 합성 권한 검증(현재 소스는 팀 존재/보관만 검사해 담당 조건이 없음). G05의 보관/복원 중 요약, 생성 시작 전후 팀 권한 변화 조합, F03 AI 초안/통신 실패 복구와 G01/G02/G04/G06/G07 남은 검증도 이어간다.

- 후속 22차 최종 결과: 전체 261개 테스트, 수정 후 타입 포함 49개 경로 빌드(build-22-final.log), 변경 코드 공백 검사 통과. 아래 '최종 빌드 확인 중'은 이 결과로 갱신한다. 실제 DB의 양방향 경합 12개와 별도 근거/이행 검증 3개를 완료했고 검증용 PostgreSQL도 정상 종료했다. 다음은 날짜별 요약의 생성/조회/완료 시점을 회차 단위로 분리하는 작업이다.

- 2026-09-09 후속 22차 G05 회차 분석의 대화 근거(로컬): 날짜만으로 요약을 회차에 귀속하던 조건을 제거하고 저장된 요약의 모든 원문 ID·내용·종류·활동 날짜와 인용 ID를 현재 회차 원문에 대조한다. 같은 날 두 회차가 섞인 요약, 출처가 없거나 바뀐 요약은 원본을 보존하되 해당 회차 분석 근거에서 제외한다. 실제 작성 회차가 확인되는 소급 날짜 메모는 시작 날짜로 잘라내지 않는다. 아직 요약되지 않은 학생 대화/대면/보완 원문과 원래 작성자·참여자·내용 확인 관계를 새 근거 스냅샷에 고정하고 내용 확인도 근거 해시에 반영한다. AI에는 종류/날짜/내용과 메모라는 귀속을 전달하고 계정 ID를 노출하지 않으며 인원수로 실제 참석·합의·역량을 추론하지 않도록 명시했다. 중간/최종 프롬프트 v3, 출력 스키마 v2. 새 점진적 마이그레이션 0008은 discussion_entries JSONB 열과 원문 회차 인덱스만 추가하며 과거 스냅샷의 없는 원문은 NULL로 남긴다. 별도 실제 PostgreSQL에서 이전 스키마를 합성 재구성해 모든 기존 스냅샷 필드 불변·0008 반복 적용, 실제 날짜별 요약의 같은 날 회차 혼합 재현→회차 분석 제외/현재 원문 보존, 요약 전 새 글의 해시 변경/옛 스냅샷 불변 3개 검증 통과. 기존 discussion_peer/meeting/confirm 경합 검증을 양쪽 선행 순서로 확장해 다음 회차/탐구 완료 12개 통과(20차의 전환 선행 6개를 포함하며 추가 6개). 전체 70개 파일·261개 테스트 통과. 최초 빌드는 Map 원문 종류의 타입 추론 오류로 중단했고 명시적 타입으로 정정 후 최종 빌드 확인 중. 결과는 output/test-infra/postgres-discussion-evidence.json, postgres-document-race-discussion_peer-discussion_meeting-discussion_confirm.json, full-tests-22.log, build-22-final.log. 날짜별 읽기 화면/생성 작업 자체는 아직 세션+날짜 단위이므로 회차별 작업 분리, 생성 중 회차 종료, 완료 회차의 날짜별 요약 고정과 과거 혼합 요약 표시 정책은 다음 작업이다. 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경.

- 후속 21차 최종 검증: 전체 69개 파일·257개 테스트 통과(output/test-infra/full-tests-21-final.log), 타입 포함 49개 경로 빌드 통과(build-21.log). 아래 '최종 회귀 확인 중'은 이 최종 결과로 갱신한다. 실제 DB 수락 검증 4개와 구분하며 운영에는 배포하지 않았다.

- 2026-09-09 후속 21차 F03 AI 대화/탐구 방향 회차 경계(로컬): 실제 PostgreSQL에서 회차 전환 뒤 AI 대화 답변이 완료 회차에 저장되는 오류를 다음 회차/탐구 완료 두 경우에서 재현했다. 탐구 방향의 최초 재현은 합성 응답의 output 누락으로 중단했으므로 재현 성공 건수에 포함하지 않는다. AI 진입·질문 저장·응답 저장/복구는 세션→활성 회차→학생/팀→작업 소유권 순서로 검사하며 현재 활성 소속과 표시 회차를 다시 확인한다. 외부 생성 대기 동안 잠금을 유지하지 않는다. 요청 키에 회차·정책 버전을 넣고 주제 제안 생성으로 승인 이후 진행 단계를 초기화하지 않는다. 대화 입력은 질문 저장 시점의 같은 회차 주제/최근 대화로 고정하고 회차 출처가 없는 기존 conversation_summary는 원본을 보존하되 AI 입력에서 제외한다. 두 API는 cycleId 필수, 화면은 회차별로 다시 열고 완료 회차의 질문/방향 선택을 막는다. 합성 제공자만 사용하는 실제 서비스 검증에서 대화/방향×다음 회차/탐구 완료 4개 통과, 먼저 저장된 학생 질문은 보존하고 늦은 결과는 차단했다. 결과: output/test-infra/postgres-ai-cycle-after.json, ai-cycle-before.log(대화 재현 2개), ai-cycle-after.log. 최초 전체 250개 테스트와 타입 포함 49개 경로 빌드 통과 후 표시 회차/API/소속 변경 검증을 추가해 최종 회귀 확인 중이다. 추가 검증의 공유 합성 소속 변경이 기존 테스트에 남던 문제는 테스트 순서와 finally 복원으로 수정했다. 로컬 PostgreSQL 정상 종료. 이번 AI 화면의 실제 브라우저/기기 검증은 아직 하지 않았다. 운영/실제 학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 G05 날짜별 요약/원문 회차와 F03 AI 대화 초안·통신 실패 복구, G01/G02/G04/G06/G07 남은 검증이다.

- 2026-09-09 후속 20차 F03 학생 대화/대면 기록 회차 경계(로컬): 실제 PostgreSQL에서 학생 대화가 회차 전환의 잠금을 기다린 뒤 완료 회차에 저장되는 오류를 다음 회차/탐구 완료 양쪽에서 재현했다. saveDiscussionEntry와 confirmMeeting은 세션→현재 회차→학생/팀 잠금 아래 표시 회차·활성 상태·현재 소속을 다시 확인한다. 대면 기록 참여자도 같은 학생/팀 잠금 범위에서 확인하고 원래 참여자 기록은 보존한다. 대화/대면 저장 및 내용 확인 API는 cycleId를 필수로 받고 UI가 표시 회차를 전달한다. 회차 전환 선행의 대화/대면 저장/내용 확인 6개 실제 DB 수락 검증 통과. 반대 순서에서 아직 정리되지 않은 대화의 분석 근거 반영은 G05와 함께 남는다. 대화 초안 키에 회차를 포함하고 회차 변경 시 편집 컴포넌트를 새로 열며 기존 회차 불명 초안은 복사 가능한 읽기 전용 안내로 보존한다. 완료된 학생 화면에는 쓰기/교사 정리 버튼을 표시하지 않고 교사만 명시적 정리를 실행한다. 전체 68개 파일·250개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과. 새 local-browser-server.mjs가 별도 합성 PostgreSQL DB와 빌드 산출물로 127.0.0.1:3108 서버를 열어 Chrome 768×1024에서 첫/둘째 회차 초안 분리·새로고침 복구·이전 형식 초안 표시·전송 실패 후 글 유지·요청 회차 ID·완료 학생 읽기 전용과 브라우저 오류 0건을 확인하고 화면도 검토했다. 이는 실제 태블릿/IME 검증은 아니다. 결과는 output/test-infra/postgres-document-race-discussion_*.json, discussion-cycle-browser.json/png, full-tests-20.log, build-20.log. 브라우저 서버는 Ctrl+C/Stop-Process에 응답하지 않아 경로/부모 PID를 재확인한 지정 자식 PID를 taskkill로 종료하고 부모 실행 종료를 확인했으며 PostgreSQL도 정상 종료했다. 운영/학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음은 F03의 탐구 방향 생성·AI 대화 요청/완료 회차 재검사 및 G05 날짜별 요약/원문 회차 경계, 나머지 G01/G02/G04/G06/G07 검증이다.

- 2026-09-09 후속 19차 G03 실제 중간/최종 분석 서비스 경합: scripts/local-postgres-document-race.mjs에 analysis_overlap/analysis_takeover 모드를 추가해 실제 requestCycleAnalysis와 PostgreSQL을 사용했다. 중간/최종 각각을 선행시키고 생성 응답을 멈춘 상태에서 다른 종류를 요청하면 생성 함수가 추가 호출되지 않고 busy로 거절된다. 중간 완료 뒤 최종 생성은 가능하며 최종 완료 뒤 중간 요청은 차단되고 최종 고정 결과는 재사용된다. 합성 작업의 만료 시각만 앞당겨 다른 종류가 인계받아 완료한 뒤 옛 응답을 풀어도 소유권/최종 상태 검사로 옛 결과가 저장되지 않는다. 새 결과 한 개만 남고 재요청은 같은 ID를 반환한다. 4개 실제 서비스 시나리오 통과, 결과는 output/test-infra/postgres-document-race-analysis_overlap-analysis_takeover.json. 17차 공통 소유권 8개와 구분하며 실제 외부 AI는 호출하지 않았다. 제품 코드 변경 없이 검증만 추가했으므로 전체 246개 테스트/빌드는 18차 기록을 따른다. 운영/학생 자료/외부 Sheet·푸시/배포/Git 커밋·푸시는 미변경. 다음 우선 확인은 F03의 탐구 방향 제안·AI 대화·우리끼리 대화의 명시적 회차와 저장/완료 시점 재검사다. 현재 AI 대화는 서비스 진입 때 active 회차를 조회하지만 API에 표시 회차 계약이 없고, 대화 기록 저장/모델 응답 완료 시 세션·회차 잠금 범위를 대조해야 한다. G01/G02 남은 조합과 G04~G07도 계속 남는다.

- 2026-09-08 후속 18차 G02 평가 저장/공개와 소속 경합 수정(로컬): 실제 평가 권한 사전 조회 뒤 팀 제거·이동·계정 비활성화가 끝나도 옛 세션에 평가가 저장되는 7개 오류를 scripts/local-postgres-evaluation-race.mjs로 재현했다. writeEvaluation은 평가 회차→세션→참여 학생→관련 팀을 잠그고 자기평가 작성자 또는 동료평가 작성자/대상자의 현재 활성 일반 계정·같은 팀·학급/동아리 범위를 저장 직전에 재검사한다. 공개도 세션을 학생/팀보다 먼저 잠가 일지/평가 저장과 역순 잠금을 피하며, 최초 후보 밖 새 학생이나 잠그지 않은 세션의 대상이 생기면 공개를 중단한다. 변경 선행 평가 미생성/저장 선행 변경 대기와 원래 평가 보존 14개, 공개 후보 조회 후 새 팀원 배정→공개 롤백→피드백 확인 후 재공개, 공개 선행→제거 대기→2명 고정 결과와 반복 공개 불변 2개, 총 16개 실제 DB 수락 검증 통과. 과거 팀 관계는 inactive/left_at으로 유지하며 평가를 삭제하지 않는다. 재현 7개/수락 16개 결과는 output/test-infra/postgres-evaluation-race-before.json / after.json. 전체 67개 파일·246개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과. 검증 서버 정상 종료. 운영/학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. G02의 모든 팀 보관·동아리 중복 소속·시험 대상 변경 및 일지/공개 교차 실행까지 완료한 것은 아니다. 다음은 G01의 승인/전송 남은 조합, G02 나머지 조합, G03 실제 중간/최종 분석 경쟁과 G04~G07/F03 남은 경계다.

- 2026-09-08 후속 17차 G03 AI 소유권 인계 수정(로컬): 실제 PostgreSQL에서 소유권 SELECT 직후 새 요청이 인계받아도 옛 단계 INSERT가 성공하는 문제와 같은 자원/다른 요청 키의 만료된 옛 작업이 계속 단계·완료 결과를 쓰는 문제를 재현했다. ownsAiJob에 FOR UPDATE를 추가해 짧은 쓰기 트랜잭션 동안 소유권을 고정하고 beginAiJob의 자원 잠금 아래 만료된 다른 요청의 processing 상태/토큰을 종료한다. 모델 생성은 잠금 밖에 유지한다. 실제 getDb/AI 작업 서비스를 사용하는 scripts/local-postgres-ai-race.mjs에서 같은/다른 요청 각각 최초 3중 획득(1개 시작·2개 busy), 인계 선행(옛 단계/완료 차단), 저장 선행(인계의 실제 행 잠금 대기·정상 단계 재사용), 저장 실패(롤백·새 소유자 정상 생성)의 총 8개 시나리오 통과. 최초 재현 4개와 수정 후 수락 8개는 output/test-infra/postgres-ai-race-before.json / after.json으로 구분한다. 새 단위 검증의 잘못된 id 열 조회는 실제 job_id로 고친 후 전체 67개 파일·246개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과. 실제 모델 호출/운영/학생 자료/외부 Sheet·푸시/배포/Git 커밋·푸시는 미변경. 공통 소유권 검사를 쓰는 계획 검토/대화/시험 저장에도 같은 트랜잭션 잠금이 적용되며 현재 호출부는 모두 쓰기 트랜잭션 안에서 검사함을 대조했다. G03의 실제 중간/최종 분석 서비스 경쟁, G02의 팀 변경/평가 공개, G01 남은 승인/전송 조합 및 G04~G07/F03의 나머지 검증은 여전히 미완료다.

- 2026-09-08 후속 16차 G01 계획서 제출/일지 및 준비물 경합 수정(로컬): 계획서 제출과 일지 저장을 다음 회차/탐구 완료와 양쪽 선행 순서로 교차한 8개 실제 PostgreSQL 검증을 통과했다. 기존 15차 28개와 별도 결과 파일에 보존한다. 준비물은 전환의 미전송 검사 직후 들어온 신청이 외래 키 대기 뒤 완료 회차에 pending으로 저장되는 결함을 두 전환 종류에서 재현했다. materials.ts의 저장 트랜잭션에 세션→회차 잠금 및 대기 후 active 재검사를 추가했고 화면/API에 cycleId를 필수로 연결했다. 내부 서비스는 이전 호출부를 위한 선택 인자를 유지하지만 학생 API는 회차 없는 요청을 거절하며 교사 재전송은 원래 신청 회차를 전달한다. 수정 후 준비물 양쪽 선행 순서 4개 통과: 전환 선행이면 새 신청을 만들지 않고, 저장 선행이면 pending 신청 때문에 과거 분석의 전환을 거절한다. 현재 누적 실제 DB 수락 검증 40개이며 material_race_reproduction의 2개는 수정 전 오류 재현으로 구분한다. 잘못된 회차의 저장/외부 전송 차단, API 회차 누락 거절/전달 검증 포함 전체 67개 파일·245개 테스트와 타입 포함 빌드, 변경 코드 공백 검사 통과. 결과는 output/test-infra의 postgres-document-race*.json, full-tests-16.log, build-16.log에 있다. 운영/학생 자료/실제 AI·Sheet·푸시/배포/Git 커밋·푸시는 변경하지 않았다. 다음은 제출 직후 승인/확인 완료 방향, 기존 준비물 수정·전송 완료 경합, 체험 pending 전환 정책(G04), G02 팀/평가 및 G03 AI 소유권 경합, F03 남은 AI/대화 회차 계약이다. 기존 자료의 임의 복구·삭제는 하지 않는다.

- 2026-09-08 후속 15차 G01 문서 저장/복원/제출/수정 요청 경합 확대: scripts/local-postgres-document-race.mjs를 실제 계획서 저장·계획서/보고서 복원·보고서 제출·계획서/보고서 수정 요청까지 확장했다. 보고서 저장을 포함한 7종 작업 × 다음 회차/탐구 완료 × 양쪽 선행 순서의 28개 실제 PostgreSQL 시나리오를 모두 통과했다. 전환 선행 시 낡은 문서 작업을 거절하고 이전 두 문서의 원문 이력 및 다음 빈 문서/완료 원문을 보존한다. 문서 작업 선행 시 새 작성 내용 또는 복원 원문과 재승인/초안/제출/수정 요청 상태를 보존하며 과거 분석의 전환을 거절한다. 계획서 복원의 주제 동기화와 보고서 복원의 과거 참여자 역할 원문도 확인했다. 수정 요청 알림은 작업 성공 때만 기존 회차의 해당 문서·팀에 저장되고 전환에 밀린 요청은 알림을 만들지 않는다. 기존 확정 상태에서 재제출/수정 요청/복원이 전환과 겹치는 실제 서비스 경로의 검증이며 계획서 제출, 제출 직후 승인/확인 완료, 일지·준비물 경합은 남는다. 결과는 output/test-infra/postgres-document-race.json. 제품 코드 변경 없이 합성 검증만 확대해 전체 242개 테스트/빌드는 13차 기록을 따른다. 실제 AI는 주입한 합성 응답을 사용하고 외부 자격·푸시 설정은 비웠다. 운영/학생 자료/외부 AI·Sheet·푸시/배포/Git 커밋·푸시는 미변경. G01 전체 및 G02~G07/F03의 남은 항목은 미완료다.

- 2026-09-08 후속 14차 G01 실제 보고서 저장/회차 전환 경합: 새 scripts/local-postgres-document-race.mjs에서 실제 앱 서비스와 독립 PostgreSQL 연결을 사용해 보고서 필드 저장↔다음 회차 시작, 보고서 필드 저장↔탐구 완료를 양쪽 선행 순서로 실행했다. 첫 작업이 세션/회차 잠금을 보유한 지점에 검증용 일시정지를 걸고 pg_stat_activity/pg_blocking_pids로 두 번째 앱 연결의 실제 Lock 대기를 확인했다. 전환 선행 시 이전 회차 저장은 대기 후 거절되고 다음 회차의 빈 문서/이전 원문 이력 또는 완료 원문을 유지한다. 저장 선행 시 변경 내용과 draft 상태가 유지되며 오래된 분석에 근거한 전환은 거절되고 현재 회차가 active로 남는다. 4개 시나리오 모두 통과, 결과는 output/test-infra/postgres-document-race.json. 분석 응답은 주입한 합성 함수이며 외부 AI를 호출하지 않았다. 검증 도구의 pg Pool callback 연결 API 누락과 현재 보고서 양식에 없는 필드명을 수정한 후 통과했으며 이 두 실패는 제품 오류가 아니다. 제품 코드는 변경하지 않아 전체 242개 테스트/빌드는 13차 기록을 따른다. 운영/학생 자료/외부 Sheet·푸시/배포/Git 커밋·푸시는 미변경. G01 중 계획서 저장·제출·검토·복원, 보고서 제출·검토·복원, 일지·준비물의 전환 교차 실행은 계속 남는다. G02~G07 및 F03 나머지 범위도 미완료다.

- 후속 13차 검증 환경 정리: Windows PowerShell에서 실제 정상 종료 후 프로세스 ExitCode가 비어 실패로 보고되는 관찰 문제를 확인했다. pg_ctl 핸들을 대기 전에 유지하도록 실행 도구를 수정하고 같은 Windows PowerShell 경로에서 시작·종료 모두 종료 코드 0으로 재검증했다. 검증용 PostgreSQL은 정상 종료 상태다.

- 2026-09-08 후속 13차 G07 동시 초기화·초기 자료 복구(로컬): 격리 PostgreSQL 16.15의 빈 DB에서 독립 연결 3개가 기존 SCHEMA_SQL을 동시에 실행하면 두 연결이 23505로 실패하는 것을 재현했다. 새 bootstrap-lock은 스키마 생성→마이그레이션→초기 자료 생성을 같은 앱 초기화 잠금으로 보호하며 실패 후 잠금을 해제하고 연결을 정리한다. 실제 getDb를 호출하는 앱 프로세스 3개로 학급 9개·합성 계정 4개·팀 1개·마이그레이션 7개가 한 번만 생성됨을 확인했다. 초기 계정 생성 실패 뒤 학급이 남아 재시작도 중복 오류로 실패하는 문제도 재현해 초기 자료 생성 전체를 단일 연결/트랜잭션으로 묶었다. 계정 생성 및 마지막 보고서 생성에 실제 DB 오류를 넣어 계정·학급·팀·회차가 모두 롤백되고 오류 제거 후 앱 3개가 정상 복구되는 것을 검증했다. 기존에 남아 있던 불완전 초기 자료를 자동 삭제·보정하는 변경은 아니다. scripts/local-postgres-bootstrap.mjs와 앱 프로세스/TS 로더 보조 스크립트로 재현하며 결과는 제외 폴더 output/test-infra에 남긴다. 전체 66개 파일·242개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과. 앞선 검증 출력의 관찰 기록이 유실되어 성공으로 추정하지 않았고, 추가 트랜잭션 수정 이후 전체 검증을 로그 파일에 보존해 최종 종료 코드 0을 확인했다. 운영 DB·학생 자료·외부 AI/Sheet/푸시·배포·Git 커밋/푸시는 변경하지 않았다. 다음은 G01의 실제 앱 문서 작업과 회차 전환 교차 실행, G02/G03 경합, G07의 구버전 앱 공존·알 수 없는 미래 마이그레이션·복구 및 나머지 F03 쓰기 회차 계약이다. G07 전체 완료는 아니다.

- 2026-09-08 후속 12차 실제 PostgreSQL 검증 환경·기초 전환 확인: PostgreSQL 공식 Windows 안내에서 연결한 EDB의 16.15 실행 파일을 배포/Git 제외 폴더 output/test-infra에 받아 필요한 bin/lib/share만 추출했다. Windows 서비스 등록 없이 127.0.0.1:55416, 별도 임시 계정/무작위 비밀번호/검증 클러스터로 실행한다. scripts/local-postgres.ps1은 전용 표식·메이저 버전 확인 후 시작/상태/종료하고 비밀번호를 출력하지 않는다. 초기 샌드박스 토큰 제한으로 pg_ctl 실행이 실패했으나 허용된 로컬 실행으로 해결했다. Start-Process가 서버 자식까지 기다리던 부분은 pg_ctl 프로세스만 기다리도록 수정했다. scripts/local-postgres-smoke.mjs는 운영 DATABASE_URL을 읽지 않고 고정 loopback/포트/전용 계정/표식을 확인한 뒤 이름이 codex_validation_으로 시작하는 새 합성 DB를 만든다. 실제 16.15에서 기존 0001~0005 DB에 보고서 원문/확인 상태를 넣고 독립 풀 3개가 0006/0007을 동시에 적용하여 전체 7개 기록·원문/시각/상태 보존·보고서 번호 0·새 AI 버전별 고유 제약 2개를 확인했다. 다른 연결의 실제 Lock 대기를 pg_stat_activity로 관찰하고 해제 후 반영 및 ROLLBACK 원상복구도 검증했다. 결과는 output/test-infra/postgres-smoke-result.json이며 서버는 정상 종료했다. 제품 코드는 이번에 변경하지 않아 전체 240개 테스트/빌드는 직전 11차 기록을 따른다. 운영 DB/학생 자료·외부 AI/Sheet/푸시·배포·Git 커밋/푸시는 변경하지 않았다. G01/G07 전체 완료는 아니다. 다음은 이 환경에서 실제 앱 저장/제출/검토/복원/일지/회차 전환 교차 실행, 빈 DB의 SCHEMA_SQL 포함 동시 초기화, 구버전 앱 공존 및 AI 작업 소유권 경합이다. 기존 스크립트의 0005 고정 기대값은 원래 복원 검증 이력용으로 보존하고 새 로컬 검증을 별도로 추가했다.

- 2026-09-08 후속 11차 F17 고정 문서 출제 근거(로컬): 출제 자료 수집에서 현재 계획서/보고서 초안·현재 주제·현재 역할을 직접 사용하는 경로를 제거했다. 같은 탐구의 모든 회차를 대상으로 승인 제출 스냅샷과 확인 보고서 이력, 회차 전환 시 보존한 승인/확인 원문을 선택하며 회차/고정본 ID를 출처 키에 포함한다. 세션→회차→문서 잠금 아래 현재 승인 상태만 남아 있는 기존 계획서는 legacy_capture로 보관하고 과거 승인 날짜를 만들지 않는다. 현재 확인 보고서는 본문·필드·원래 학생 ID별 역할·당시 양식을 기존 문서 이력에 내용 기반 ID로 고정해 반복 출제 시 재사용한다. 보고서 검토/전환 이력에도 앞으로 양식 정의를 보관한다. 이후 초안을 수정하거나 같은 문서 행을 다음 회차로 전환해도 이전 출제 근거와 개인 역할은 그대로다. 제거 학생의 역할은 교사용 원문에 유지하고 현재 시험 대상자의 개인 자료에는 포함하지 않는다. 익명 역할 alias를 현재 학생 순서에 연결하지 않으며 승인/확인 증거가 없는 문서를 AI 분석 결과로 추정 복원하지 않는다. 팀 근거가 없으면 실제 AI 호출 전 안내하고 팀 문항 수 0 선택을 허용한다. 화면에 전체 회차의 승인/확인 고정본 범위와 기존 자료 한계를 표시하며 작업 재사용 키는 출처 정책 v3로 구분한다. 합성 DB에서 두 회차 고정본·제출 대기 보고서 제외·승인 후 초안 변경 제외·역할 원문 보존, 실제 회차 전환 이력→출제 연결, 실제 생성 입력→저장 근거 일치와 제거 학생 제외를 검증했다. 전체 65개 파일·240개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과. F17의 로컬 수정/합성 검증은 완료했으며 실제 PostgreSQL의 동시 변경·역할/명단 경쟁은 G01/G02에서 검증한다. 운영 배포·실제 학생 자료·외부 AI/Sheet/푸시·Git 커밋/푸시는 하지 않았다. 다음은 G01~G07 및 F03의 남은 AI/준비물/대화 회차 계약이다. 현재 PATH에서 psql/postgres/pg_ctl/docker가 확인되지 않았고 기존 복원 검증 스크립트 2개를 찾았으므로 격리된 실제 DB 검증 환경부터 준비한다.

- 2026-09-08 후속 10차 최종 검증 완료: 학생 구성 검사 보강을 포함한 시험 관련 4개 파일·12개 테스트와 타입 포함 49개 경로 빌드를 다시 통과했다. 전체 237개 테스트 통과에 이어 최종 작업본도 확인했으며 아래 ‘재실행 중’ 표기는 이 기록으로 대체한다. F17 고정 문서 근거 연결과 G 항목은 계속 진행한다.

- 2026-09-08 후속 10차 F17 출처 검증(로컬): 시험 문항의 빈/알 수 없는/타 학생 출처를 첫 자료로 임의 대체하지 않고 거절한다. 입력 근거 키 중복도 거절하며 선택한 모든 근거를 전송 당시 문자열 그대로 저장해 세 번째 근거 누락과 700자 재절단을 제거했다. 실제 개인 자료가 전혀 없을 때 기존 표준 대체 문항은 ‘개인 기록 부족 · 표준 대체 자료’로 분리하고 생성된 제시문을 보존한다. 팀 근거가 없는 문항에는 이 예외를 적용하지 않는다. 팀/학생별 문항 수·학생 구성·근거를 완료 단계 저장 전에 검증하고 최종 시험 쓰기 전 재검증한다. 출처 정책 v2를 작업 재사용 키에 포함해 과거 정책의 완료 시험을 새 검증 완료로 간주하지 않는다. 알 수 없는 출처를 반환한 합성 AI 결과가 시험을 만들지 않고 같은 요청 재시도에서 다시 생성되는지, 입력 전체 문구/세 근거 보존과 개인 대체 자료 구분을 검증했다. 전체 64개 파일·237개 테스트와 타입 포함 빌드 통과 후 학생 구성 검사 보강을 포함한 최종 관련 검증/빌드를 재실행 중이다. F17의 승인 계획서·확인 보고서 고정본 및 과거 역할 연결은 아직 남는다. report의 teacher_review 이력은 검토 직전 원문/역할을 같은 트랜잭션에 남기지만 고정 양식 버전이 없고, 회차 분석의 과거 역할은 alias만 있으므로 현재 학생 순서에 맞춰 추정 연결하면 안 된다. 원문 입증과 고정본 연결을 다음 작업에서 마무리한다. 운영 배포·학생 자료·외부 AI/Sheet/푸시·Git 커밋/푸시는 하지 않았다.

- 2026-09-08 후속 9차 F17 출처 충돌 1차 수정(로컬): 시험 개인 일지의 근거 키에 회차 ID와 일지 원본 ID를 함께 넣고 교사용 근거 라벨에 회차·차시를 표시한다. 회차가 없는 이전 기록은 임의 배정하지 않고 ‘회차 미분류’로 구분한다. 동아리 추가 탭 근거 키도 제목 대신 설정 버전/응답 ID를 사용하고 제목 라벨에 기존 비식별화를 적용한다. 합성 DB의 같은 제목 두 회차·각 1차시를 AI 입력에서 별도로 선택해 저장 근거와 화면 제시문이 정확히 대응하고, 이후 일지 변경에도 생성된 시험 근거가 바뀌지 않는 것을 검증했다. 기존 출제·교정본·PDF·공개·동아리 회귀를 포함해 전체 63개 파일·231개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과. F17 전체 완료는 아니다. 다음은 현재 계획/보고서 초안 대신 승인/확인 당시 고정본 사용, 과거 회차 역할의 실제 학생 연결 보존, 존재하지 않는 AI 출처의 임의 첫 자료 대체 제거 및 입력/저장 근거 길이 일치다. 현재 evidenceFor는 미일치 출처를 첫 항목으로 대체하고 700자로 자르는 경로가 남아 있다. plan_submissions의 승인 스냅샷, document_revisions의 회차별 보고서 상태/역할, cycle_evidence_snapshots의 고정 보고서를 확인했으며 어떤 자료가 승인 당시 원문을 입증하는지 대조 후 연결해야 한다. 과거 역할 alias를 현재 학생 순서로 추정 연결하지 않는다. 운영 배포·실제 학생 자료·외부 AI/Sheet/푸시·Git 커밋/푸시는 하지 않았고 G01~G07도 계속 남는다.

- 2026-09-08 후속 8차 F15/F16 공지 대상·명단 가져오기(로컬): 전체/학급/팀 공지 푸시 수신자 조회에 일반 계정 조건을 추가해 체험 계정을 제외한다. 활성 계정이지만 팀에서만 제거된 학생은 전체/학급 공지는 수신하고 해당 팀 공지는 수신하지 않는 기존 범위를 유지한다. 명단 파서는 공급사 공식 tarball의 SheetJS 0.20.3으로 갱신하고 잠금 파일에 무결성 값을 고정했다. 인증 후 실제 업로드 스트림을 2MB+64KiB로 제한하고 개별 파일 2MB, 첫 시트 학생 1,000명(머리글 포함 1,001행)/32열, ZIP 256개 항목/실제 압축 해제량 합계 16MiB를 제한한다. 파싱은 환경변수를 비운 worker에서 수행하며 10초 종료 타이머와 V8 힙 예산을 둔다. 이는 OS 수준의 전체 메모리 격리를 뜻하지 않는다. 예상한 입력 오류만 안내하고 내부 예외·경로·명단 원문은 응답하지 않는다. 기존 비밀번호·팀 이력 보존 로직은 유지한다. 한글 XLS/XLSX, 1,000명 경계, 손상/확장자 위장 파일, 압축 해제량 허위 표시, 크기 헤더 누락/위조, 외부 발신 없는 수신자 범위와 기존 계정 재등록 회귀를 합성 검증했다. 시간 초과/worker 오류 종료는 모의 worker로 검증했다. 전체 62개 파일·230개 테스트와 타입 포함 49개 경로 빌드 통과. 최초 업로드 테스트의 송신용 FormData 스트림 취소 오류는 실제 수신 바이트 형태의 시험으로 수정했다. 배포 산출물 검사에서 worker용 CommonJS 파일 누락을 발견해 명단 경로의 파일 추적 포함 규칙을 추가한 뒤 다시 빌드하고, standalone 안의 0.20.3만 로드한 worker에서 한글 XLS/XLSX 처리를 통과했다. 운영 배포·실제 학생 자료/외부 AI/Sheet/푸시·Git 커밋/푸시는 하지 않았다. 다음은 F17 시험 근거의 고정본·회차 연결과 G01~G07 남은 검증이며 전체 의존성 감사 및 실제 PostgreSQL 0006/0007 전환은 아직 미완료다.

- 2026-09-08 후속 7차 F11 AI 결과 버전 분리(로컬): 새 마이그레이션 0007은 계획서/회차 분석의 중복 기준을 스냅샷·대상/분석 종류·프롬프트·스키마 버전으로 확장한다. 기존 결과는 덮어쓰지 않는다. 계획서 완료 결과 조회·최신 조회·저장과 회차 생성 재사용·작업 완료 조회는 현재 버전만 사용하며, 진행 중인 회차의 구버전 결과는 재분석 대상으로 표시하고 판단 저장/회차 전환/종료를 차단한다. 완료 회차 및 이전 분석 이력은 그대로 열람한다. 회차 분석은 개인정보 가림·실제 전송 근거 검사와 단일/여러 변인 탐구 지침을 포함하는 v2 프롬프트/스키마로 구분했다. 프롬프트만/스키마만 다른 구버전 각각에 대해 같은 스냅샷으로 새 결과 생성·현재 결과 재사용·옛 결과 원문 보존·구버전 판단/전환 차단을 합성 검증했다. 최초 전환 검증에서 메모리 DB의 자동 제약 이름이 예상과 다른 것을 확인해 정확한 이름으로 수정한 뒤 전체 재검증했다. 전체 58개 파일·216개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과. 실제 PostgreSQL의 0006/0007 전환과 구버전 서버 공존 검증은 아직 필요하다. 운영 배포·실제 학생 자료/외부 AI/Sheet/푸시·Git 커밋/푸시는 하지 않았다. 다음은 F15 전체 공지 체험 대상, F16 명단 파일 파서, F17 시험 근거와 G01~G07의 남은 검증이다.

- 2026-09-08 후속 6차 F09/F12 AI 입력·근거 경계(로컬): 회차 정의 전체(회차 이름 포함)에 기존 학생 식별정보 가림을 적용하고, 전송량 제한을 적용한 실제 근거 목록과 AI 요청 본문을 한 번 준비하여 생성/검증에 공유한다. 새로 생성한 강점·발견·제안·회차 비교 판단은 근거 ID가 최소 하나 있어야 하며, 원본에 존재하더라도 전송량 제한으로 제외된 근거를 인용하면 분석 저장을 거절한다. 과거 결과를 읽기 위해 기존 저장 형식의 파서는 유지한다. 합성 이름·번호·메일·전화번호가 전송 문자열에서 제거되는지, 빈 근거/미전송 근거 결과가 저장되지 않고 유효 근거만 저장되는지 검증했다. 전체 57개 파일·212개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과. 실제 AI·운영 학생 자료·Sheet/푸시·운영 배포·Git 커밋/푸시는 실행하지 않았다. F11 구버전 결과 재사용 분리는 아직 남아 있어 이번 기록은 기존 캐시의 재검증/교체 완료를 뜻하지 않는다. 다음은 계획서/회차 분석의 프롬프트·스키마 버전별 결과 구분과 관련 마이그레이션 검증이다.

- 2026-09-08 후속 5차 F07 과거 개인 일지·사진 열람(로컬): 완료/보관 회차에 학생 ‘이 회차의 내 일지와 사진’, 교사 ‘이 회차의 개인 일지와 사진’ 읽기 전용 영역을 추가했다. 명시적 회차 조회는 세션/팀 일치를 검사하며 학생은 활성 팀 관계·활성 계정 아래 본인 일지와 사진만 볼 수 있다. 새 회차 미승인/준비물 미신청이나 전체 탐구 종료가 이전 기록 열람을 막지 않는다. 현재 회차 편집·조회 진입 조건은 유지하고 이전 회차 저장을 허용하지 않는다. 제거된 학생의 접근은 차단하며 교사는 해당 과거 일지·사진을 보존하여 열람한다. 일지 편집기의 조회도 자기 회차 ID를 명시한다. 합성 서비스 검증에 회차 전환/최종 종료 후 본인 기록·사진, 다른 학생 사진 차단, 다른 팀 회차 거절, 과거 저장 거절, 제거된 학생 차단·교사 보존을 추가했다. 전체 56개 파일·211개 테스트, 타입 포함 49개 경로 빌드, Chrome 학생/교사 태블릿의 과거 기록·사진 렌더링과 읽기 전용 검증 통과. 최초 화면 검증은 개발 모드 중복 조회 가정과 수화 완료 전 클릭 문제를 고쳐 재검증했다. 운영 배포·실제 자료 변경·외부 AI/Sheet/푸시·Git 커밋/푸시는 하지 않았다. 다음은 남은 AI 입력/버전/근거 검사(F09/F11/F12), 푸시 대상(F15), 파일 가져오기(F16), 시험 근거(F17)와 G01~G07 검증이다.

- 2026-09-08 후속 4차 F06 한 회차 종료(로컬): 중간 분석 후 ‘이 회차에서 최종 분석’으로 넘어가 같은 회차에서 전체 탐구를 종료한다. 최종 고정 근거는 같은 회차의 중간 분석과 실제 저장된 판단을 포함하며 미응답을 강제하지 않는다. 이전 분석·판단은 분석 이력으로 읽기 전용 열람한다. 최종 분석 뒤 중간 분석/판단 수정/다음 회차 시작을 차단하며 최종 재분석은 허용한다. 분석 생성의 회차별 공통 작업 소유권, 세션→회차 잠금과 저장 직전 종료 여부 확인으로 늦은 결과가 종료 회차에 추가되는 경로를 보완했다. 현재 근거와 맞는 같은 종류의 과거 완료 결과를 다시 사용하는 경우 화면 최신성과 종료 판정도 일치시켰다. 학생 판단 0건/1건 각각 중간→최종→종료·두 번째 회차 없음·중간 이력 보존·중복 최종 결과 재사용을 합성 DB로 확인했다. 전체 56개 파일·211개 테스트, 타입 포함 49개 경로 빌드, 합성 Chrome 태블릿의 중간→최종 버튼·이전 학생 판단 열람과 기존 AI/판단 초안 회귀 검증 통과. 운영 배포·실제 학생 자료/외부 AI/Sheet/푸시·Git 커밋/푸시는 하지 않았다. 실제 PostgreSQL 경합과 AI 프롬프트/스키마 버전별 재사용·입력 경계(F09/F11/F12)는 별도 미완료다. 다음은 F07 이전 회차의 개인 일지·사진 열람과 나머지 인수인계 항목이다.

- 2026-09-08 후속 3차 최종 검증 완료: 최종 작업본의 타입 포함 49개 경로 운영 빌드와 변경 코드 공백 검사가 통과했다. 아래 진행 중 표기는 이 기록으로 대체한다. 전체 인수인계 목표는 아직 진행 중이며 운영 배포는 하지 않았다.

- 2026-09-08 후속 3차 보고서 검토 대상 보호(로컬): 새 번호 마이그레이션 0006은 reports.write_version을 추가하며 기존 원문·검토 상태를 보존한다. 보고서 필드/역할 저장·제출·복원·회차 전환·교사 검토가 번호를 증가시킨다. 교사 검토 API는 화면의 회차·검토 번호·상태·기존 피드백을 필수로 받고 같은 세션→회차→문서 잠금 아래 비교한다. 동일 내용의 재제출도 이전 요청과 구별하며 거절된 요청은 이력을 추가하지 않는다. 수정 요청 상태에서 추가 피드백 재전송은 허용하고 학생 재제출 전 확인 완료는 차단한다. 초안에는 검토 대상을 함께 보관하며 최신 보고서와 충돌하면 교사가 확인 후 명시적으로 초안을 이어 쓰게 한다. 이전 문자열 초안도 읽기·추가 편집·새로고침 복구를 보존하고 새 검토 대상에 자동 연결하지 않는다. 전체 55개 파일·209개 테스트와 타입 검사 통과. Chrome 합성 자료에서 다른 교사 저장 뒤 충돌 차단→확인→실제 로컬 API 재전송, 이전 초안 재복구, 늦은 응답·연결 실패 보존을 통과했다. 최종 빌드는 진행 중이며 완료 결과는 다음 기록을 따른다. 최초 시각 비교 방식은 폐기하고 단조 증가 번호로 구현했다. 기존 행 전환 시험에서 드러난 메모리 DB의 인덱스 기본값 반영 문제는 명시적 번호 초기화로 해결하고 원문 보존을 검증했다. 실제 PostgreSQL 전환·경합 검증과 구버전 서버 공존 배포 조건은 아직 남아 있다. 운영 DB/배포·외부 발신·Git 커밋/푸시 없음. 다음은 F06 같은 회차 중간→최종 분석과 이력 보존, F07 과거 일지 열람 및 나머지 인수인계 항목이다.

- 2026-09-08 후속 2차 로컬 수정: F13 보고서 피드백을 교사·팀·보고서·회차·양식별 탭 초안으로 보관하며 새로고침과 연결 실패 후 복구한다. 전송 중 새로 입력한 문장은 이전 성공 응답으로 삭제하지 않는다. F14 학생 AI 제안 판단도 입력별 토큰으로 이전 응답이 새 초안을 지우지 않게 했고 연결 실패/시간 초과 후 저장 버튼을 복구한다. F05 오래된 중간/최종 분석에는 같은 종류의 ‘최신 자료로 다시 분석’ 버튼을 제공하고 최신성 확인 전 전환/완료 차단을 유지한다. 기존 전체 53개 파일·206개 테스트와 추가 재분석 화면 2개 테스트, 타입 포함 49개 경로 빌드, Chrome 보고서 초안 복구·무자동전송·연결 실패 재시도·늦은 응답 보존 및 학생 판단 새로고침 복구를 통과했다. 기존 학생/교사 AI 화면 합성 검증도 통과했다. 최초 브라우저 시도는 합성 공지 팝업 때문에 실패하여 공지 응답을 격리한 후 재검증했다. 운영 배포·실제 학생 자료 변경·외부 AI/Sheet/푸시·Git 커밋/푸시는 하지 않았다. 보고서 검토 대상의 서버 기대값 보호와 동시 교사 충돌, F06 같은 회차 중간→최종 분석 및 이력 열람, F07 과거 일지 접근, 실제 PostgreSQL 경합과 나머지 AI/외부 경계는 남아 있다.

- 2026-09-08 후속 1차 수정 최종 검증: 최종 작업본은 53개 파일·206개 테스트와 타입 포함 49개 경로 운영 빌드를 통과했다. 기존 편집 Chrome 13개, 신규 회차 초안 격리 2개 통과. 주제 선택의 회차 조건 SQL은 합성 DB의 EXISTS 평가 오류를 수정한 뒤 전체 재검증했다. `git diff --check -- src scripts` 오류 없음. 배포는 하지 않았으며 현재 운영은 00027-hvc 그대로다. 다음 순서는 교사 보고서 초안/검토 대상 보호, 회차 재분석·같은 회차 최종 종료·과거 일지 열람, 실제 PostgreSQL 경합 검증이다.

- 2026-09-08 인수인계 후속 1차 기록 경계 수정(로컬): F01 완료 회차의 계획서·보고서 복원을 서버에서 차단하고, 복원/문서 저장/제출/잠금이 회차 전환과 같은 세션→회차→문서 잠금 순서를 사용하도록 보완했다. F03의 계획서·보고서 필드/역할/제출/복원/잠금과 주제 선택 API 및 일지 저장 API는 화면의 cycleId를 필수로 받아 오래된 회차 요청과 식별자 없는 구버전 요청을 거절한다. 내부 서비스의 기존 호출 호환용 선택 인자와 외부 API의 필수 조건은 구분한다. 보고서 제출의 검증·이력·상태·알림 해제는 같은 트랜잭션에 넣었다. F04의 계획서/보고서 초안 키와 컴포넌트 키에 사용자·회차·양식 버전을 포함했다. 회차 불명 기존 초안은 삭제하지 않고 별도 읽기 영역에서 확인·복사하게 하며 새 문서에 자동 반영하지 않는다. F08 교사 보고서는 고정된 실제 필드 정의로 맞춤 본문·표 열·선택/체크 값을 표시한다. 완료 회차 문서/이력 무변경, 동일 문서 ID·빈 기준값인 이전 회차 저장/제출/잠금·주제 선택 거절, API 회차 누락 거절·현재 회차 저장, 일지 이전 회차 거절을 합성 검증했다. Chrome 기존 편집 13개와 신규 계획서/보고서 회차 초안 분리·새로고침·옛 초안 보존 2개 통과. 실제 PostgreSQL 잠금 경합과 나머지 AI/준비물/대화 쓰기 회차 계약은 별도 후속 검증이며 G01 전체 완료를 뜻하지 않는다. 교사 보고서 초안 보존(F13 보고서 부분), 회차 진행/과거 일지(F05~F07), AI/외부 경계 등은 계속 남아 있다. 운영 배포·학생 자료 변경·외부 AI/Sheet/푸시·Git 커밋/푸시는 실행하지 않았다.

- 2026-09-08 피드백 재전송 복구 배포 완료: Cloud Build `89d13e93-b803-4349-9cac-7424ff7c28a8`, 이미지 `sha256:42e07552a4a6ba3de68bb21c826d1c09afe3f3ac4ace9994e38f24640a98be1f`로 `science-inquiry-platform-00027-hvc`를 배포했다. Ready=True·트래픽 100%, health/로그인 200, 새 리비전 ERROR 이상 0건을 확인했다. 이미지 외 실행 spec·환경/Secret 참조와 Cloud SQL 연결은 배포 전과 동일하다. 수정 요청 상태에서 AI 항목 반영·수동 편집·재전송이 가능하며 기존 제출본/회차/저장 내용 충돌 보호를 유지한다. 전체 203개 테스트, 타입 포함 운영 빌드, 합성 Chrome 재전송 시나리오 통과. 운영 학생 자료의 테스트·계정 변경·Git 커밋/푸시는 실행하지 않았다.

- 2026-09-08 수정 요청 후 피드백 추가·재전송 복구: 사용자가 기존처럼 수정 요청을 보낸 뒤에도 AI 항목을 반영하고 문장을 추가·수정해 다시 요청할 수 있도록 복구를 지시했다. 교사 화면, 검토 API 기대 상태, 검토 서비스, 교사 AI 제출본 조회에서 feedback 상태를 허용했다. 학생 재제출 전 승인 차단은 유지하고, 현재 제출본·회차·상태·저장 피드백 기대값 검사와 승인 취소 확인 규칙은 그대로다. 실제 로컬 API 테스트에 추가 피드백 저장·AI 기존 고정본 검토·오래된 교사 내용 거절·학생 재제출 후 이전 요청 거절을 추가했고 전체 203개 테스트 통과. Chrome 합성 계정에서 첫 요청 뒤 AI 항목 추가·수동 편집·동일 제출본 재전송 저장까지 통과했다. 운영 학생 대상 수동 시험은 요청하지 않았다. 태블릿 로그인은 사용자가 성공을 확인했고 PC 로그인 실패는 미확인으로 남긴다. 로컬 수정 완료이며 배포 결과는 다음 최신 항목에 기록한다.

- 2026-09-08 태블릿 로그인 문의 진단 중: 사용자는 기존 교사·학생 로그인에서 `아이디 또는 비밀번호가 맞지 않습니다.`를 보고했다. 운영 로그인 요청의 개인정보 없는 최근 상태 조회에서 새 리비전 00026-8hg의 200 응답 3건·401 응답 1건을 확인했으며 해당 성공 계정/기기가 사용자의 것인지는 확인하지 않았다. 로그인 API·화면·인증·학년도 상수·DB 초기화·실패 제한의 6개 파일 해시는 구조 검토 시작 기준과 동일하고 운영 학년도는 2026이다. 문서상 초기 teacher 계정은 비활성화돼 있으며 teacher1234는 로컬 기본 데모용이다. 실패한 실제 교사 아이디와 태블릿 접속 주소 확인을 요청했다. 원인은 아직 확정하지 않았고 비밀번호·계정 상태·운영 데이터·제품 코드를 변경하지 않았다.

- 2026-09-08 계획서 최소 수정 배포 완료: 성공한 Cloud Build `3e4cdc78-b040-4528-a248-b417cd646286`, 이미지 `sha256:6154bcb2439fe0818830bd7094c666f2dfbfb4039a6bad19ec1f44d432b20fa3`를 기존 서비스에 적용했다. 현재 `science-inquiry-platform-00026-8hg`, `Ready=True`, 트래픽 100%. 이미지 외 컨테이너/서비스 계정/환경·Secret 참조 등 실행 spec과 Cloud SQL 연결은 이전 리비전과 동일하다. `/api/health`(DB SELECT 1 포함), `/login`, `/manifest.webmanifest` 200, 비로그인 계획서 검토·AI 검토 API 403, 새 리비전 ERROR 이상 조회 0건. 실제 AI·Sheet·푸시 호출이나 운영 학생 자료 변경 검증은 하지 않았다. 최초 구조 검토 기준 제품 파일 대비 변경은 계획서 검토 관련 10개(테스트 포함)이며 검증 스크립트를 추가했다. DB 스키마/마이그레이션과 패키지 정의는 해당 기준과 동일하다. Git 커밋·푸시 없음. `ASTRA_REVIEW_FINDINGS.md`를 원래 작업에 열도록 요청해 `queued`를 받았고 자동 재개/메시지 전달 완료를 뜻하지 않는다. 실제 학생 기기 확인과 나머지 구조 개선은 다음 작업으로 남긴다. 아래 항목은 앞선 진행 기록이다.

- 2026-09-08 계획서 최소 수정 브라우저 검증 완료·배포 진행: 외부 자격정보를 빈 값으로 격리한 로컬 합성 DB에서 별도 Chrome 자동 검증을 수행했다. 기존 학생/교사 AI 화면 2개 시나리오 및 `scripts/teacher-plan-feedback-regression.mjs`의 AI 항목 누적·기존 문장 보존·중복 차단·자동 전송 없음·새로고침 초안 복구·네트워크 실패 후 초안 보존·재전송의 실제 로컬 저장을 통과했다. 768/390px 화면 가로 넘침 없음, `output/browser/teacher-plan-{tablet,mobile}.png` 확인. 최초 자동 검증은 화면 수화 전 값을 읽어 실패했으며 준비 완료 대기 후 통과했다(제품 수정 없음). 전체 203개 테스트·타입 검사 및 운영 빌드 49개 경로 통과. Windows 앱 제어는 URL 판독 실패로 중단했고 실제 기기/IME/절전은 확인하지 못했다. 사용자가 배포 및 별도 Chrome 검증을 승인했다. 기존 운영 00025-p9s와 이전 빌드의 프로젝트·저장소·소스 버킷 일치를 확인한 뒤 빌드 `3e4cdc78-b040-4528-a248-b417cd646286`를 시작했다. 최초 소스 업로드는 자동 승인 검토에서 미확인 목적지 사유로 거절됐으나 해당 읽기 전용 근거 확인 후 동일 운영 목적지 업로드가 허용됐다. 아직 새 운영 리비전 적용 완료를 뜻하지 않는다.

- 2026-09-08 수업 긴급 우선순위 정정: 사용자가 계획서 검토까지 정상 동작하는 데 필요한 최소 작업을 먼저 요청했다. 현재 작업본에 이미 추가된 교사 피드백 초안·항목별 `[피드백에 반영]`·제출본 기대값 검사 코드를 유지하고, 학생 AI 점검의 최신성 오판(F10)을 수정했다. 스냅샷 생성과 화면 조회가 같은 회차 정의로 해시를 계산하며 기존 고정본은 변경하지 않는다. 현재 저장본/교사 제출본에 맞는 AI 결과를 우선 조회해 늦은 옛 결과가 최신 검토를 가리지 않게 했다. `plan-review-readiness.test.ts`에서 저장 전후 최신성·회차 안내 변경·과거 고정본 보존, 기대값 누락/오래된 제출본 승인 거절, 제출→AI 검토→수정 요청→재제출→승인 흐름을 합성 DB와 실제 검토 API 처리 함수로 검증했다. 관련 4개 파일 10개 및 전체 51개 파일 203개 테스트, 타입 검사 통과. 별도 브라우저 검증은 끝까지 완료하지 못해 중단했으며 이번 화면·실제 기기 검증 완료를 뜻하지 않는다. 보고서·일지·회차 전환·시험 등 전체 구조 수정은 후속으로 남기고, 실제 AI·Sheet·푸시·운영 DB·배포·Git 커밋·푸시는 실행하지 않았다. 이번 수정은 로컬 작업본에만 있다.
- 2026-09-08 Astra 1번 전체 구조 검토 완료: 수정 75개·미추적 77개를 포함한 당시 현재 작업본을 검토하고 `ASTRA_REVIEW_FINDINGS.md`에 수정 필요 17개(F01~F17), 검증 부족 7개(G01~G07), 선택 개선 3개를 기록했다. 완료 회차 복원 차단, 검토한 제출본 식별, 회차별 저장·복구 초안 격리, 교사 피드백 초안 보존을 먼저 수정한다. 제품 관련 238개 파일의 전후 해시가 동일하며 제품 코드·운영 환경·외부 발신은 변경하지 않았다. 기존 49개 파일·198개 테스트와 타입 검사를 재실행해 통과했고, 검토 전용 합성 14개는 현 결함의 발생을 확인했다(수정 완료 증거 아님). 실제 PostgreSQL의 전체 경합·구버전 복구, 실제 학생 기기·AI 품질·Sheet·푸시 통합은 이번에 검증하지 않았다. 원래 작업 `01a0714d-4de5-7e43-92ef-404b306168d1`에 보고서 파일 열기를 요청해 `queued`를 받았으며 작업 간 메시지 전송·자동 재개는 하지 않았다. 다음 수정 우선순위와 이어갈 문장은 보고서 말미에 있다. 추가 배포·운영 변경·Git 커밋·푸시 보류를 유지한다.
- 2026-09-08 인수인계 완료: 새 Astra High 검토 작업의 시작 지시·범위·보고서 형식·원래 작업으로의 결과 전달 조건을 `REVIEW_NEXT_STEPS.md`에 정리했다. 새 작업 생성·메시지 도구 부재로 새 작업 시작과 자동 재개는 실행하지 못했다. 교사 AI 검토의 `[피드백에 반영]`은 누른 항목을 수정 요청 메시지 초안에 즉시 누적하고 교사가 검토 후 보내는 방식으로 확정했다. 별도 일괄 반영 방식은 폐기했으며 제품 구현은 아직 하지 않았다.
- 2026-09-08 최신 지시: 현재 리비전 `00025-p9s`는 이미 배포됐으나 Astra 전체 구조 검토를 먼저 받기로 했다. 추가 배포·운영 변경·Git 커밋·푸시는 보류한다. 검토와 수정 이후 사용자의 새 배포 지시가 필요하다.
- 2026-09-08 물로켓 사례 반영: 모든 탐구에 1차→2차를 강제하지 않는다. 단일 탐구·여러 변인 비교·연쇄 탐구 모두 지원하며, 실험 수를 Cycle 수로 간주하지 않는다. 한 회차 최종 종료와 이전 회차 없는 AI 분석을 검토 기준에 추가했다. 이번에는 결정·설계 문서에만 반영했으며 코드·운영 반영 완료를 뜻하지 않는다.
- 2026-09-07 문서 전체의 `임시 저장` 버튼으로 서버 저장·팀 공유·변경 이력을 남기되, 항목 이동이나 외부 사이트 방문 때문에 미저장 글이 사라져서는 안 된다고 명확히 정정했다. 작성 중인 글의 기기 복구용 초안은 유지한다.
- 2026-09-07 남은 분석·수정이 많으므로 전체 작업과 통합 검증을 마친 뒤 한 번에 배포하는 방향으로 정정했다. 중간 운영 배포는 보류한다.
- 2026-09-07 계획서 등에 글을 쓰다가 외부 사이트를 방문한 뒤 돌아오면 글이 사라지거나 입력이 막히는 오류의 실제 수정을 우선 진행하도록 요청했다. 오류 학생의 기기·브라우저는 아직 모른다.
- cycle은 과탐실과 동아리 모두에 적용한다. 회차별 문서·준비물·일지·대화와 중간/최종 AI 분석을 분리하는 구현은 `00025-p9s`에 배포했다. 탐구 형태 다양성에 관한 최신 정정은 추가 검토·수정 대상이다.
- `우리끼리 대화`의 같은 팀 새 메시지 알림과 메시지별 읽은 팀원 표시는 후속 과제로 등록했다. 현재 배포 준비 범위에는 포함하지 않고, 기존 변경 묶음의 운영 검증과 한 번의 배포를 마친 뒤 구현한다.
- 로그인과 팀 편성은 실제 태블릿에서 정상 동작함을 교사가 확인했다.
- 별도의 큰 시험 환경을 먼저 만드는 대신 수업 일정 순서대로 실제 기능을 계속 구현한다.
- 전체 학기 흐름을 학생과 교사 관점에서 끝까지 점검했고, 화면 캡처를 포함한 교사용·학생용 상세 사용서와 2쪽 빠른 안내서를 모두 제작했다.
- OpenAI 기능은 현재 배포에 연결된 기존 API 키를 계속 재사용한다.
- 계획된 기능 구현·검증·최종 배포가 모두 끝난 뒤 전체 소스를 Git에 저장한다. 중간 단계에서는 Git 초기화·최종 커밋을 하지 않는다.
- 최종 원격 Git 저장소는 `https://github.com/bocheol/Science-Inquiry-and-Experiment-Platform.git`이다. 전체 작업 완료 전에는 푸시하지 않는다.
- 시험 문항은 플랫폼 서버가 기존 OpenAI API로 생성하며, 교사가 별도 대화에서 만들어 복사하지 않는다.
- 시험 문항 수는 교사가 전체 공통·팀 공통·개인화별로 선택한다. 공통은 중립 자료, 팀 공통은 계획서·보고서, 개인화는 본인 일지·역할만 사용한다.
- 시험은 플랫폼에서 검토·수정하고 최종 인쇄용 PDF로 내보낸다. Word/DOCX는 실제 외부 편집 필요가 확인될 때만 후속 추가한다.
- 자기평가·동료평가는 2026-08-28 교사 합의를 완료했다. 기본 4인 팀을 기준으로 행동 기준 4단계와 `판단하기 어려움`, 핵심 4문항, 익명 의견 교사 검토, 유효 평가 3건 공개 기준을 사용한다.
- 첫 자기평가·동료평가 운영은 성적에 자동 반영하지 않고 학생 피드백과 교사 참고자료로 사용한다.
- 다음 큰 확장은 과탐실 전체 설정을 다시 만드는 작업이 아니라 동아리별 운영 설정이다. 과탐실은 현재 학기 양식을 유지하고 시험 기본안·문항과 확정본 교정 기능만 확장한다.
- 동아리별 계획서·보고서·준비물 Google Sheet·자기평가·동료평가·시험 기본안과 추가 탭을 교사가 플랫폼에서 설정하는 설계를 2026-09-04 확정했다.
- 기존 `teacher2` 계정에 마스터 관리자 권한을 부여하고, 마스터가 새 교사 계정과 교사용 학생 체험 계정을 안전하게 관리하는 기능을 운영에 반영했다. 동아리별 평가·시험·추가 탭 검토·Google Sheet 열 매핑도 함께 배포했다.

## 공개 배포 상태

- 서비스: `science-inquiry-platform`
- 공개 주소: `https://science-inquiry-platform-974188506094.asia-northeast3.run.app`
- Google Cloud 프로젝트: `chemistry-tutor-493405`
- 리전: `asia-northeast3` (서울)
- Cloud Run 최근 확인 리비전: `science-inquiry-platform-00027-hvc` (`Ready=True`, 트래픽 100%)
- Cloud SQL 인스턴스: `science-platform-db`
- 데이터베이스: `science_platform`
- PostgreSQL 16, 삭제 보호 활성화
- 자동 백업 보존: 180일
- Cloud Run은 공개 접속이지만 학생·교사 기능은 앱 로그인과 역할 검사로 보호한다.
- 최종 상태 확인 당시 `/api/health`, 로그인, manifest, 서비스 워커는 HTTP 200이었고 비로그인 마스터 계정·동아리 설정·평가·시험 API는 403이었다.
- 준비물 모바일 링크 처리, 문서 이력·복원, 자기평가·동료평가, 교사용 공식 도움말, 웹 푸시 알림, 동아리·대화 기록, 동아리별 운영 설정, 마스터 계정 관리, 시험 교정본, 글 보존·저장 충돌 방어, 공통 Cycle·계획서 AI 검토·중간/최종 분석까지 위 공개 리비전에 배포했다.
- 웹 푸시 고정 VAPID 공개 키·개인 키는 각각 별도 Secret Manager 항목으로 보관하고 Cloud Run 서비스 계정에 해당 비밀값만 읽는 권한을 부여했다. 키 값은 소스·문서·로그에 남기지 않았다.

## 계정 및 실제 데이터 상태

- 실제 학생 명단 282명을 1~9반에 등록했고, 교사용 학생 화면 체험 계정 4개를 별도 안전 팀에 추가했다.
- 학생 아이디는 `1 + 반 두 자리 + 번호 두 자리`의 5자리 학번이다.
- 학생은 최초 로그인 시 본인 비밀번호로 변경한다.
- 최초 임시 비밀번호의 한글 낱말은 두벌식 쿼티 키보드 영타로 변환해 발급한다.
- 학생 로그인 카드 PDF는 `output/pdf/2026-학생-임시-로그인-카드.pdf`에 있다.
- 체험 계정은 실제 학번·팀과 충돌하지 않으며 최초 로그인 후 비밀번호 변경 화면으로 이동하고, 변경 후 학생 화면에 진입하는 흐름을 운영 환경에서 확인했다. 체험 계정 로그인 정보는 이 문서나 소스에 기록하지 않는다.
- 2026-09-04 넥서스의 활성 2학년 학생 14명에게 새 영타형 임시 비밀번호를 일괄 재발급하고 전원 `must_change_password=true`, 저장 해시와 배부 파일 일치, 감사 기록 생성을 확인했다. 넥서스에 함께 참여하는 1학년 2명과 체험 계정은 대상에서 제외했다. 배부용 텍스트는 `output/credentials/넥서스-2학년-임시-로그인-2026-09-04.txt`에 있으며 배부 후 삭제해야 한다.
- 로그인 카드에서 반별 표본 계정을 시험했으며 서버 로그인과 비밀번호 변경 화면 진입이 정상 동작했다.
- 교사가 실제 태블릿에서도 학생 로그인과 팀 편성이 정상임을 확인했다.
- 교사 계정은 총 4개이며 전체 9개 학급을 공동 관리한다.
  - `teacher1`: 강석봉
  - `teacher2`: 김보철
  - `teacher3`: 김정원
  - `teacher4`: 원창연
- 초기 배포용 `teacher` 계정은 비활성화했다.
- 교사 임시 로그인 배부 파일은 `teacher-accounts.txt`이며 비밀번호 변경 완료 후 삭제해야 한다.
- 2026-08-30 활성 학생·교사 전원의 임시 비밀번호를 영타형으로 재발급했고, 개인 비밀번호를 이미 변경한 계정도 다음 로그인에서 다시 비밀번호를 변경하도록 설정했다.
- 명단의 조 번호는 최초 등록 당시 비어 있었다. 이후 교사가 화면에서 직접 팀을 만들고 배정 중이다.
- 현재 적어도 1반 1조가 생성된 것을 교사 화면에서 확인했다. 정확한 최신 팀 수는 고정값으로 기록하지 않는다.
- 학생 명단을 다시 올릴 때 기존 학생 비밀번호는 변경되지 않는다. `조 번호`를 채운 명단을 올리면 팀 생성·배정에 사용할 수 있다.

## 현재 구현 완료 기능

### 인증·계정

- 학생 학번 로그인
- 교사 로그인 및 전체 학급 공동 관리
- 최초 로그인 비밀번호 변경
- 교사의 학생 비밀번호 초기화
- 교사 대시보드의 학생 개별 추가·비활성화·복원
- 학생 비활성화 시 로그인·현재 팀 관계·팀장 권한만 종료하고 과거 기록 보존
- 학생 복원 시 기존 팀에 자동 재배정하지 않고 미배정 상태로 복원
- 학생 명단 Excel 일괄 등록
- 학생 임시 로그인 카드 출력용 데이터와 별도 PDF 생성
- 학생·교사 임시 비밀번호의 두벌식 쿼티 영타 생성

### 학급·팀

- 반별 팀 생성
- 학생 팀 배정·이동·제거
- 팀장 지정
- 제거된 팀원 관계를 삭제하지 않고 비활성 이력으로 보존
- 팀 생성 시 탐구 세션과 계획서 자동 생성
- 교사 전용 팀 보관·복원: 기본 목록과 학생 접근에서는 숨기되 팀 자료·팀원 관계·감사 기록 보존
- 팀 보관 전 `반 + 팀명` 확인 문구를 요구하고, 보관 팀은 학급별 별도 목록에서 복원

### 이론 탐구와 AI

- 팀 관심사 입력 후 AI 탐구 방향 3개 생성
- 탐구 방향 선택
- 팀 공유 AI 대화
- AI 응답 생성 중 추가 입력 잠금 및 순차 처리
- 최근 15개 대화와 대화 요약·선택 주제를 AI 문맥으로 사용
- 학생 실명과 학번 대신 `팀원 A`, `팀원 B` 형식의 가명을 AI에 전달
- 실제 웹 검색 출처 링크 수집
- 안전·스캐폴딩·정답 대필 방지 지시문 적용

### 탐구 계획서

- 학교 계획서 양식을 기반으로 한 팀 공동 계획서
- 항목별 저장 및 편집 잠금
- 제출, 교사 승인, 교사 피드백
- 승인 후 수정 시 재승인 필요 상태 전환
- 팀명·팀원 정보 자동 연결
- 승인 완료 후 교사 화면에서 승인·수정 요청 버튼을 기본 숨김
- 승인 상태를 수정 요청으로 되돌릴 때 `반 + 팀명` 확인 문구를 정확히 입력해야 하는 2단계 안전장치
- 화면 우회 요청도 서버에서 현재 승인 상태와 확인 문구를 다시 검증하며, 이미 승인된 계획서의 중복 승인 요청을 차단

### 준비물 신청

- 탐구 화면의 별도 준비물 탭
- 모바일 쇼핑 앱 공유 문구에서 URL 자동 추출
- 지원 쇼핑몰 모바일 상품 주소의 PC 주소 자동 변환과 변환 불가 모바일 주소 제출 차단
- 화면과 서버의 동일한 링크 검증, 외부 링크 확인 요청 없음
- 품명, 규격, 단가, 개수, 배송비, 총액 입력·계산
- 팀장·학번·조 정보 자동 연결
- 5만원 초과 경고 상태
- Google Sheets 학급 탭 동기화
- 실패 상태 저장 및 교사 재전송
- 같은 품목도 팀별 독립 기록

### 개인 실험 일지

- 승인된 탐구의 차시별 개인 일지 생성·수정·열람
- 날짜, 오늘 한 일, 관찰 결과, 느낀 점·궁금한 점 기록
- 차시당 사진 5장 첨부, 태블릿에서 큰 사진 자동 축소
- 작성자 학생과 교사만 목록·본문·사진 접근
- 브라우저 IndexedDB 임시 저장, 오프라인 저장 요청 보존, 연결 복구 시 자동 재전송
- 같은 차시 저장 시 기존 레코드를 갱신하며 별도 수정 이력은 생성하지 않음
- 제거된 학생은 학생 화면 접근이 차단되지만 과거 일지는 교사 화면에 보존
- 교사 팀 상세 화면에서 활성·제거 팀원별 과거 일지 열람
- 실제 계획서 승인과 준비물 신청 기록을 모두 확인한 뒤 학생 일지 접근 허용
- 잠긴 일지 탭을 누르거나 키보드로 활성화하면 부족한 조건과 다음 행동 안내

### 팀 최종보고서

- 교사가 제공한 HWP 원본을 다시 읽고 표지와 Ⅰ~Ⅵ 목차를 그대로 반영
- 연구주제, 팀명, 학급, 학번·이름, 팀장/팀원 구분, 팀원별 역할 표시
- 연구목적, 용어정의, 이론적 배경, 연구 계획·일정, 실험 과정·방법, 자료정리, 결과분석·고찰, 결론, 참고문헌, 부록 작성
- 항목별 저장, 4초 갱신, 편집자 표시와 35초 잠금 갱신으로 공동편집 충돌 방지
- 서로 다른 항목은 독립 행으로 저장해 동시 저장 시 내용 덮어쓰기 방지
- 제출, 교사 수정 요청·확인 완료, 수정 후 재제출 상태 전환
- 활성 팀원만 학생 화면 접근 가능, 제거된 팀원의 기존 역할 기록은 교사 화면에 보존
- 교사 대시보드에서 팀별 보고서 상태 표시

### 문서 변경 이력·복원

- 계획서와 팀 보고서의 변경 전 전체 상태를 최근 이력으로 보존
- 복원 직전 상태도 다시 이력으로 남겨 연속 복원 가능
- 복원 권한은 교사와 해당 팀의 현재 활성 팀장만 허용
- 일반 팀원·이전 팀장 복원 차단, 복원 후 재제출·재검토 상태 전환

### 교사 진척 대시보드·내보내기

- 전체 학급 또는 반별 팀 진척 요약: 팀 수, 계획 승인, 일지 작성 학생, 보고서 제출, 교사 확인 필요
- 팀별 탐구 주제, AI 질문 수, 계획서, 개인 일지 작성 인원·차시 수, 보고서, 최근 활동 표시
- `교사 확인 필요`와 `학생 진행 필요` 상태를 분리해 필터링
- 계획 승인 대기·재승인, 보고서 검토 대기, 준비물 시트 실패·예산 초과를 교사 확인 항목으로 집계
- 현재 활성 팀원만 일지 진척 분모·집계에 사용하고, 제거 팀원의 과거 원문은 기존 교사 팀 상세에서 계속 보존
- Excel은 `팀 진척`, `학생 일지 현황` 2개 시트로 제공하고 CSV는 팀 진척 요약을 제공
- 내보내기에는 일지 본문·사진·비밀번호를 포함하지 않으며, 스프레드시트 수식 삽입 문자열을 무력화
- 내보내기 API는 교사 역할만 접근 가능

### 시험·채점·결과 공개

- 교사가 전체 공통 0~5개, 팀 공통 0~5개, 개인화 0~3개 범위에서 총 1~10개 문항과 총점을 선택해 초안을 생성
- 플랫폼 서버의 기존 OpenAI Responses API와 구조화 출력을 사용하며 브라우저에는 API 키를 노출하지 않음
- 전체 공통은 실제 어느 팀의 자료도 그대로 쓰지 않고 새 중립 자료를 생성
- 팀 공통은 해당 팀의 승인 계획서·최종보고서만, 개인화는 해당 학생 본인의 일지·보고서 역할만 사용
- AI 입력에서 학급 전체의 학생 실명·학번을 제거하고, 제거된 팀원은 출제 대상·일지 자료에서 제외
- 문항별 출제 근거 스냅샷, 모범답안, 항목별 채점 기준, 평가 역량, 난이도 저장
- 교사 문항 수정·공통 문항 직접 추가·공정한 동일 슬롯 삭제·최종 확정
- 확정 전 출력 차단, 확정 후 학생별 A4 시험지와 교사용 답안 PDF 출력
- 교사 수동 채점·피드백 저장·학생별 결과 공개, 공개 전 학생 화면에서 문항과 점수 비공개
- 시험 확정 시 탐구 단계를 시험으로, 결과 공개 시 평가 단계로 전환

### 자기평가·동료평가

- 핵심 행동 문항 4개와 교사 선택 문항 최대 1개
- 문항별 관찰 가능한 4단계 기준, 동료평가 `판단하기 어려움`, 자기평가 `해당 활동 기회 없음`
- 1·2단계 동료평가의 교사용 관찰 근거 필수 입력과 의심 응답 표시
- 익명 의견 교사 승인·숨김·수정 검토
- 항목별 유효 숫자 평가 3건 이상일 때만 평균 공개, 기준 미달 시 교사 종합 피드백 제공
- 제거 학생 접근 차단과 원자료 교사 보존

### 교사용 공식 도움말

- 교사 대시보드에서 플랫폼 사용법 질문 가능
- 소스에 포함된 공식 안내만 키워드로 검색해 답변하며 외부 AI를 호출하지 않음
- 학생 개인정보·비밀번호·학생 자료의 조회·전달·변경 요청을 명시적으로 거절

### 전역 결과 알림·교사 건의 게시판

- 주요 서버 작업의 성공·실패를 자동으로 사라지는 전역 토스트 큐로 표시하고 연속 알림을 순서대로 보존
- 오류 토스트 `alert`, 성공·안내 토스트 `status`, 닫기 버튼과 모바일 안전 여백 적용
- 모든 교사가 함께 보는 내부 기능 건의·오류 문의·사용 문의 게시판
- 작성 교사·시각·분류·제목·내용·처리 상태 저장, 외부 AI·외부 서비스 전송 없음
- 학생 이름·학번·비밀번호·학생 자료 입력 금지 안내와 서버 차단, 학생·비로그인 접근 차단

### 공지·알림함과 일정

- 교사가 전체·특정 반·특정 팀을 대상으로 일반·중요 공지를 작성하고 수정·보관·복원
- 중요 공지는 학생 로그인 뒤 팝업으로 안내하고, 헤더와 공지함에 학생별 읽지 않은 공지 수 표시
- 선택한 시작일·종료일을 학생 공지함 월간 달력에 자동 표시
- 계획서·보고서 수정 요청 시 교사 피드백 문구를 알림으로 보존하고 해당 작성 탭으로 바로 이동
- 공지를 읽은 상태와 수정 요청을 처리한 상태를 분리하며, 해당 문서를 다시 제출하면 처리 필요 상태 자동 해제
- 학생 식별정보·비밀번호로 보이는 교사 공지 입력을 서버에서 차단하고 학생·교사 역할별 API 접근 분리
- 학생이 공지·알림함에서 기기별 웹 푸시 알림을 직접 허용·해제하고, 기존 구독 기기는 로그인 계정이 바뀌면 현재 학생에게 안전하게 재연결
- 알림 권한 거절 뒤 로그아웃·재로그인해도 `알림 다시 켜기` 버튼을 표시하고, 기기 설정 안내와 앱 복귀 시 최신 권한 재확인을 제공
- 교사가 공지 작성·수정 시 푸시 발송 여부를 선택하고 대상 구독 기기 수·성공·실패 결과를 확인
- 계획서·보고서 수정 요청은 해당 팀의 구독 기기에 중요 푸시로 자동 안내
- 잠금 화면 푸시에는 공지 본문·학생 정보를 넣지 않고 앱 내 공지·알림함 또는 처리 화면으로만 연결
- iOS·iPadOS는 홈 화면에 설치한 웹 앱에서만 푸시 허용을 안내하며, 만료된 구독은 전송 결과에 따라 자동 정리

### 운영·배포

- Cloud Run 공개 배포
- Cloud SQL 영구 데이터베이스
- Google Secret Manager를 통한 비밀값 주입
- Google Sheets 서비스 계정 연결
- 180일 자동 백업 및 삭제 보호
- 설치형 웹앱 manifest와 192/512/마스커블/Apple 아이콘, 기기별 설치 안내
- 로그인·비밀번호 변경·학생·교사 화면과 API의 `private, no-store` 응답 헤더

### 수업용 빠른 안내서

- 교사용 2쪽 안내서: 로그인, 팀 편성·팀장 지정, 진척 확인, 계획서 검토·승인, 수업 중 문제 대응, 개인정보 원칙
- 학생용 2쪽 안내서: 최초 로그인, 팀 확인, AI 탐구 방향 찾기, 공동 계획서 작성, 제출·수정, 안전·개인정보 확인
- 교사용·학생용 모두 배포용 PDF와 수정 가능한 DOCX로 제작
- 빠른 안내서는 제작 당시 수업 우선 기능만 설명하며, 새 평가·복원·도움말 기능은 최종 상세 사용서에서 반영한다.

### 교사용·학생용 상세 사용서

- 교사용 상세 사용서: 계정·팀 편성, AI 탐구·계획서·준비물, 일지·보고서·복원, 진척·시험, 평가, 공식 도움말·보안까지 전체 운영 흐름 수록
- 학생용 상세 사용서: 최초 로그인, 팀 확인, AI 탐구, 계획서·준비물, 일지·보고서·복원 권한, 시험 결과, 자기·동료평가와 문제 해결 수록
- 운영 로그인 화면과 익명 예시 화면 캡처를 사용했으며 실제 학생 이름·학번·비밀번호는 포함하지 않음
- 교사용 9쪽, 학생용 7쪽 A4 PDF와 수정 가능한 DOCX 제공
- 전 16쪽을 이미지로 렌더링해 글꼴·표·이미지·여백·잘림·불필요한 빈 면을 확인하고, 접근성 고위험 항목 0건 및 개인정보 메타데이터 제거 확인

### 학생 수업 설명용 PPT

- 학생용 상세 사용서와 익명 화면을 바탕으로 첫 로그인부터 자기·동료평가까지 10장으로 구성
- 첫 로그인·팀 확인, AI 사용 원칙, 계획서·준비물, 개인 일지·팀 보고서, 변경 이력, 시험 결과·평가, 개인정보·안전 순서로 설명
- 실제 학생 정보와 비밀번호는 포함하지 않았으며 각 슬라이드 발표자 노트에 사용한 내부 자료 출처를 기록

## 현재 OpenAI 연결

- 일반 팀 AI 대화: `gpt-5.6-luna`
- 출처 검색이 필요한 팀 AI 대화: `gpt-5.6-terra`
- 탐구 방향 3개 제안: `gpt-5.6-terra`
- 시험 문항 생성: `gpt-5.6-sol`
- API: OpenAI Responses API
- SDK: `openai` 패키지 7.5.0
- reasoning effort: `low`
- 웹 검색 도구: 학생이 출처·논문·선행연구·최신 자료·통계·링크 등을 명시적으로 요청한 팀 대화에만 활성화, `search_context_size: low`
- 탐구 방향 제안과 시험 문항 생성에는 웹 검색 도구를 열지 않는다.
- 일반 팀 대화 최대 출력: 900토큰
- 응답 저장 옵션: `store: false`
- 안전 식별자는 팀 ID를 SHA-256으로 변환한 값이며 학생 개인정보를 사용하지 않는다.
- 운영 로그에는 기능·요청/실제 모델·입력/출력/추론/캐시 토큰·웹 검색 호출 수·처리시간·안전한 오류 분류만 구조화해 기록하고 학생 발화와 팀 식별자는 기록하지 않는다.
- OpenAI API 키는 Cloud Run 서버의 Secret Manager에서만 주입하고 브라우저에 노출하지 않는다.
- 교사는 기존 API 키 재사용을 명시적으로 승인했다.
- 2026-09-03 결제 반영 후 기존 키로 학생 데이터 없는 Luna·Terra·Sol 최소 호출을 다시 실행했고, 세 모델 모두 지정 모델로 정상 완료되었다. 실제 수업 품질과 비용은 다음 수업의 익명 구조화 로그와 교사 표본 확인으로 점검한다.

## 준비물 Google Sheet

- 문서: `2026. 2학기 과학 탐구 실험 실험 준비물`
- 링크: `https://docs.google.com/spreadsheets/d/1Ia5xoZZDv3b4sVq3la8POFNE_QVHEuLhitS-YC_QBVg/edit`
- 탭: `양식`, `1반`~`9반`
- 작년 실제 데이터는 제거했고 각 반에 익명 예시 한 조만 남겼다.
- 실제 열과 수식 구조는 `implementation_plan.md`의 `확인된 실제 시트 구조`를 기준으로 한다.
- 실제 제출 테스트 시 운영 시트에 불필요한 시험 데이터를 남기지 않도록 주의한다.

## 아직 구현되지 않은 기능

- 기존 v9의 핵심 수업 운영 흐름은 구현했으나, 2026-09-07 재점검에서 학생용 계획서 AI 검토 연결이 미완성임을 확인했다. `implementation_plan.md`의 `PLAN_SUPPORT` 피드백 요구는 존재하지만 현재 `sendTeamMessage` 입력은 선택 주제·요약·최근 대화이며 계획서 본문을 자동으로 포함하지 않는다. 문서 전체 검토 UI·결과 보존도 없다. 이전의 `남은 미구현 기능 없음` 표현을 정정한다.
- 교사가 제출된 계획서를 AI와 함께 검토하는 전용 기능은 현재 미구현이다. 확인한 기존 문서·첨부 지침에서는 별도 기능으로 명시된 요청을 찾지 못했다. 현재 교사 계획서 검토는 교사의 직접 승인·수정 요청·피드백이다. 이 확인을 새로운 AI 기능 구현 승인으로 간주하지 않는다.
- v10 확정 범위의 핵심 기능은 구현·자동 검증·운영 배포를 완료했다. 실제 교사 계정으로 넥서스 준비물 설정 화면의 주소·복수 운영 탭·2행 열 제목 인식과 별도 시험용 Google Sheet 쓰기 연결까지 확인했다. 마스터 계정 화면과 시험 교정본 생성의 수동 운영 점검은 남아 있다.
- 임의 코드·자유 수식·외부 API·사용자 정의 권한 자동화는 확정 범위대로 제공하지 않는다. 준비물 합계는 플랫폼이 계산해 교사가 지정한 기존 열로 보낼 수 있다.
- 운영 탭과 `양식` 탭에는 테스트 데이터를 쓰지 않았다. 교사가 지정한 별도 `플랫폼연결시험` 탭에만 비식별 연결 확인 3행을 기록했다.

## 동아리별 운영 설정·마스터 계정 2차 구현 (2026-09-04, 배포 완료)

- 사용자 역할 문자열을 바꾸지 않고 `is_master`, `account_type`을 추가했다. 스키마 적용 시 활성 여부와 관계없이 `teacher2` 교사 행에 마스터 표시를 부여한다.
- 마스터 전용 계정 화면과 API를 추가했다. 새 교사·`demo-` 체험 학생 생성, 임시 비밀번호 1회 표시, 비활성화·복원·재발급을 지원하고 계정·과거 기록은 삭제하지 않는다.
- 동아리 담당 교사 테이블과 권한 검사를 추가했다. 새 동아리 생성자는 자동 담당자가 되며, 마스터는 담당 교사를 지정한다. 일반 교사는 배정된 동아리만 관리·설정한다.
- 계획서·보고서·준비물·자기평가·동료평가·시험·추가 탭 설정을 초안·발행·보관 버전으로 저장한다. 발행 확인에는 동아리 이름 재입력을 요구하고, 동시에 수정하면 낙관적 잠금으로 충돌을 알린다.
- 계획서·보고서 양식 편집기는 글·숫자·날짜·선택·확인·표 구성요소를 제공한다. 새 동아리 팀을 만들 때 당시 발행본 ID를 계획서·보고서에 고정하며 기존 팀과 작성 자료에는 소급하지 않는다.
- 추가 탭은 팀 공동/학생 개인, 저장만/교사 검토 요청을 선택해 발행할 수 있다. 발행된 탭은 학생 동아리 화면에 나타나며 서버가 현재 팀원·버전·항목을 다시 검증해 저장한다. 교사는 설정 화면에서 제출 내용을 읽고 피드백을 저장하거나 검토 완료로 전환하며 학생은 같은 탭에서 피드백을 확인한다.
- 준비물 설정에서 Sheet 주소, 시험용 탭, 운영 탭, 기존 연결/플랫폼 관리형을 지정한다. 기존 운영 탭의 첫 행 헤더를 읽어 제출 시각·팀·품명·단가·수량·배송비·합계·링크를 열별로 매핑한다. 필수 열 매핑 없이 기존 탭 설정을 발행하지 못하며, 관리형 빈 탭은 표준 헤더를 만든다.
- 발행된 동아리 준비물 설정은 학생 화면의 준비물 탭을 열고 교사가 매핑한 기존 열 또는 관리형 표준 열로 전송한다. 합계는 플랫폼에서 계산하며 체험 학생의 제출은 플랫폼에만 저장하고 Google Sheet에는 전송하지 않는다.
- 과탐실과 동아리 확정 시험에서 사유를 입력해 `교정본 v2`, `v3` 초안을 만들 수 있다. 문항은 새 ID로 복제하고 원본 확정 상태·PDF 근거·채점·공개 결과는 그대로 보존한다.
- 동아리 시험은 담당 교사가 발행한 시험 기본안에서 시작하고, 공통·팀·개인 문항을 생성·검토·수정·확정·PDF 출력·채점·공개한다. 동아리 계획서·보고서와 `시험 출제 자료로 사용`을 켠 추가 탭의 버전 고정 응답을 출제 자료로 포함하되 학생 식별정보는 비식별화한다.
- 동아리 자기·동료평가는 각각 발행된 설정을 한 회차에 별도 스냅샷으로 고정한다. 학생은 선택한 동아리 팀에서 평가하고, 담당 교사는 기존 검토·최소 3건 공개 기준·교사 종합 피드백·결과 공개 흐름을 그대로 운영한다.
- TypeScript 타입 검사, Vitest 26개 파일 76개 테스트, Next.js 16.3.2 프로덕션 빌드를 통과했다. 버전 불변성·담당자 권한·위험한 Sheet 설정 차단·필수 열 매핑·새 팀 설정 고정·추가 탭 제출 검토·동아리 평가 스냅샷·동아리 시험 PDF/교정본·기존 과탐실 회귀를 자동 검증했다.
- 스키마 변경 전 Cloud SQL 수동 백업 `1788491886530`을 생성해 `SUCCESSFUL` 상태를 확인했다.
- Cloud Run 리비전 `science-inquiry-platform-00022-6d8`로 배포하고 `Ready=True`, 트래픽 100%를 확인했다. `/api/health`, 로그인, manifest, 서비스 워커는 HTTP 200, 비로그인 마스터 계정·동아리 설정·평가·시험 API는 403, 새 리비전의 심각도 `ERROR` 이상 로그는 0건이었다.
- 건강 확인에서 운영 PostgreSQL 연결과 스키마 초기화가 통과했다. 기존 환경 설정 17개(Secret Manager 연결 6개 포함), 서비스 계정, 900초 요청 제한을 값 노출 없이 보존했다. 실제 학생 계정과 운영 Google Sheet에는 검증 데이터를 쓰지 않았다.
- 실제 넥서스 준비물 Sheet를 읽기 전용으로 확인해 `양식`, `석지우`, `우수인`, `홍린`, `안현주` 탭과 운영 탭의 2행 열 제목을 확인했다. 플랫폼의 `1조(석지우)`, `3조(우수인)`, `2조(홍린)`, `4조(안현주)` 팀 이름과 탭 이름이 정확히 대응한다.
- Google Sheet 연결 버튼이 현재 입력값을 먼저 자동 저장하도록 단순화하고, 쉼표로 구분한 복수 운영 탭, 1~10행 열 제목 자동 감지, 기존 조별 양식 구조, 팀 이름 괄호 기반 탭 선택을 지원했다. 대응 탭이 없거나 둘 이상이면 전송 전에 차단한다.
- `양식` 또는 운영 탭을 연결 시험 대상으로 쓰지 못하게 유지하면서 기존 연결 방식에서도 별도의 시험용 빈 탭을 만들 수 있게 했다. 넥서스 준비물 v1 발행본에는 제공된 Sheet 주소, 네 운영 탭, 시험용 탭 이름 `플랫폼연결시험`, 자동 감지된 조별 양식 구조와 2행 열 제목을 저장했다.
- TypeScript 타입 검사, Vitest 26개 파일 78개 테스트, Next.js 프로덕션 빌드를 통과했다. Cloud Run 리비전 `science-inquiry-platform-00024-2jl`로 배포해 트래픽 100%, `/api/health` 200, 새 리비전 심각도 `ERROR` 이상 로그 0건을 확인했다.
- 교사 확인 후 제공된 넥서스 Sheet에 `플랫폼연결시험` 탭을 새로 만들고 그 탭의 `A1:B3`에만 `플랫폼 연결 시험/정상`, `학생 데이터/사용 안 함`, 확인 시각을 기록했다. Google Sheets에서 탭 생성과 세 행을 다시 읽어 값이 일치함을 확인했으므로 서비스 계정의 쓰기 권한이 정상이다. `양식`, `석지우`, `우수인`, `홍린`, `안현주` 탭은 수정하지 않았다. 이후 교사 로그인 상태에서 준비물 설정이 `사용 중 v1`임을 확인해 실제 학생 제출 연결이 활성화된 상태다. `/api/health`는 200이었고 해당 리비전의 심각도 `ERROR` 이상 로그는 0건이었다.
- 상세 데이터 구조, 화면 흐름, 권한, 감사 기록, 단계별 구현 순서는 `implementation_plan.md`의 v10 절에 기록했다.

## 동아리와 대화 기록 구현 (2026-09-03, 배포 완료)

- 교사의 구현 승인에 따라 과탐실·동아리 활동을 분리하고, 기존 계정의 두 활동 중복 참여를 지원했다. 신규 2학년 등록, 팀 편성·팀장 지정, 참여 종료, 팀 보관·복원을 추가했다.
- 교사 대시보드 상단에서 과탐실·동아리를 전환한다. 과탐실은 반별, 동아리는 동아리별 접기·펼치기를 제공한다. 팀 확인에서 원문·AI 정리·탐구 흐름을 열람한다.
- AI 탐구 대화를 유지하면서 독립된 원격 텍스트 채팅, 대면 메모, 참여자 확인과 별도 보완 기록을 구현했다. 활동일과 입력시각을 구분하고 원문은 수정·삭제하지 않는다.
- 대면 메모는 원문 저장 직후 AI 정리를 시도한다. 원격 채팅·AI 대화는 한국 시간 다음 날 정리하며, 실패 기록은 작업 대기열에서 재시도한다. 중복 실행 잠금과 내용 버전 검사를 적용하고 과거 요약·근거 스냅샷을 보존한다.
- AI 요약은 논의·합의·질문·다음 할 일·AI 제안·기록된 활동을 구분하고 원문 근거를 연결한다. 학생 실명·학번·연락처를 가명으로 치환하고 성적·역량·생기부 문장을 자동 판정하지 않는다.
- 기존 공동 관리 교사 권한을 동아리에도 적용했다. 학생은 현재 활성 팀만 접근하며, 참여 종료·보관 후 과거 기록은 교사에게 남는다. 수업 팀 재배정과 동아리 참여 종료가 서로의 소속을 없애지 않는다.
- 동아리 일지는 계획 승인 후 사용한다. 기존 과탐실 준비물 Sheet 구조를 동아리로 추정·확장하지 않으며, 시험·자기·동료평가도 현재 과탐실 범위를 유지한다.
- 첫 버전 대화에는 텍스트만 사용한다. 기존 공지 웹 푸시는 유지하고 새 채팅에 사진·영상·푸시를 추가하지 않았다.
- 운영 버전이 로컬 기준보다 최신임을 확인해 원격에 반영된 커밋 649c5ac의 웹 푸시·팀 번호 수정 28개 파일을 보존하며 통합했다.
- TypeScript 검사, 23개 파일 69개 테스트, Next.js 프로덕션 빌드를 통과했다. 로컬 예시 계정으로 원격 메시지·대면 원문 저장, 실패 시 원문 보존과 재시도, 실제 Luna AI 정리 및 근거 연결을 확인했다. 학생·교사 390×844 화면의 가로 넘침이 없고 반별·동아리별 접기·펼치기, 교사 팀 확인에서 동일 원문·요약 열람을 확인했다. 실제 학생 자료와 Google Sheet에는 시험 데이터를 쓰지 않았다.
- Cloud Run 리비전 `science-inquiry-platform-00021-9jr` 배포와 `Ready=True`, 트래픽 100%를 확인했다. 건강 확인·로그인·manifest·서비스 워커는 HTTP 200, 비로그인 대화·동아리 관리·예약 정리·푸시 API는 403이었다. 새 리비전 심각도 ERROR 이상 로그는 0건이었다.
- 기존 비밀 연결 6개와 일반 설정 9개를 보존하고, 예약 호출 계정·대상 설정 2개를 추가했다. 장시간 일일 정리를 위해 요청 제한을 900초로 설정했다.
- Cloud Scheduler 작업 `science-daily-discussion-summaries`를 서울 리전에 연결했다. 5분마다 정리 대기 건을 확인하고, 한국 시간 다음 날의 원격·AI 대화와 실패한 대면 메모를 재시도한다. 전용 서비스 계정에는 이 Cloud Run 서비스 호출 권한만 부여했으며 서버가 서명·대상·계정 이메일을 검증한다.
- 실제 예약 호출 두 건에서 `daily_summary_batch` 성공과 처리 대상 0건을 확인했다. 운영 학생 자료를 시험용으로 만들거나 기존 대화를 일괄 재생성하지 않았다. 로컬 검증 서버는 종료했다.
- 최종 소스 저장 준비에서 최신 원격 기준 `649c5ac`과의 조상 관계·기존 스테이징 없음·193개 로컬 파일 해시 보존을 확인하고, 로컬 main 기준을 맞춘 뒤 이번 변경 44개를 스테이징했다. 이어진 커밋·푸시 명령은 자동 승인 검토가 원격 저장소로 코드 공개에 대한 명시적 승인 증거 부족을 이유로 실행 전에 차단했다. 신규 커밋·푸시는 실행되지 않았으며 운영 배포에는 영향이 없다. 사용자 확인 후 지정 GitHub 저장소로 최종 소스를 저장해야 한다.

## 글 유실 방지 수정 (2026-09-07, 로컬 검증 완료·미배포)

- 수정 전 브라우저에서 이전 항목의 저장 응답을 지연시킨 뒤 다음 항목을 작성하면 새 글이 빈 값으로 바뀌는 현상을 재현했다. 이전 저장 완료가 현재 편집 상태를 해제하고, 서버 조회 결과가 전체 입력값을 덮어쓰는 것이 확인된 원인이다.
- 계획서·보고서에 항목별 초안과 저장 버전을 분리했다. 늦은 저장 응답이나 조회 응답이 그 뒤 작성한 글을 덮어쓰지 않으며, 앱 내부 탭 이동 시에도 열린 편집기를 유지한다.
- 사용자·문서별 현재 탭의 sessionStorage에 초안을 입력 즉시 보관하고 새로고침·외부 페이지에서 뒤로 돌아오기 후 복구한다. 이 보관은 브라우저 탭의 세션 범위이며 모든 기기에 공유되는 영구 백업은 아니다.
- 네트워크 실패·요청 시간 초과를 표시하고 재저장할 수 있게 했다. 페이지 복귀·연결 복구 시 현재 항목의 편집 잠금을 갱신하고, 이전 항목의 늦은 잠금·저장 응답이 현재 포커스를 변경하지 않게 했다.
- 다른 기기나 팀원의 최신 수정과 충돌하면 공유 내용과 내 초안을 함께 보여주고 비교 후 선택하게 했다. 제출 전 남은 초안을 저장하며, 저장되지 않은 초안이 있으면 이력 복원을 먼저 실행하지 않는다.
- 서버 저장에는 기대한 기존 값 검사를 추가했다. 계획서의 서로 다른 항목 동시 저장은 최신 JSON에 병합하고, 보고서 항목·역할은 조건부 쓰기로 오래된 값의 덮어쓰기를 막는다. 편집 잠금 획득도 조건부 갱신으로 한 명만 성공하도록 수정했다.
- 계획서 변경 전 이력과 보고서 변경 이력·저장을 같은 트랜잭션에서 처리한다. 기존 학생·팀 권한과 제거된 팀원의 기록 보존 규칙은 유지하며 DB 스키마 변경은 없다.
- 전체 Vitest 28개 파일 89개 테스트, TypeScript 타입 검사, Next.js 프로덕션 빌드를 통과했다. 추가 테스트는 저장 응답 순서, 초안 복구, 서로 다른 항목 동시 저장, 오래된 값 충돌, 동일 값 재시도, 보고서 제목 초기값, 동시 잠금 획득을 검증한다.
- `scripts/editor-regression.mjs`의 브라우저 검증 11개를 통과했다. 기존 유실 재현 1개와 계획서·보고서 각각의 저장 중 추가 입력, 연결 실패·내부 탭·새로고침·재시도, 외부 탭 복귀·잠금 갱신, 같은 탭 외부 이동·뒤로 가기, 다른 기기 수정 충돌 5개씩이다. Chrome 768px 화면에서 가로 넘침과 브라우저 예외도 확인했다. 잠금 만료는 테스트에서 잠금을 해제해 모사했으며 실제 태블릿 절전·한글 키보드의 검증을 대신하지 않는다.
- 로컬 메모리 DB와 합성 계정만 사용했다. 운영 학생 자료·Google Sheet·비밀값은 변경하지 않았고, AI 호출·운영 배포·Git 커밋·푸시는 하지 않았다. 검증 서버는 종료했다.
- 주요 수정 파일: `src/components/use-document-editor.ts`, `src/components/document-conflicts.tsx`, `src/lib/document-drafts.ts`, 계획서·보고서 편집기/API/저장 서비스, `src/components/inquiry-workspace.tsx`, `src/lib/document-history.ts`.
- 후속 작업에서 운영 배포를 시작해 소스 업로드까지 진행했으나, 사용자의 전체 작업 후 일괄 배포 요청으로 로컬 배포 명령을 중단하고 Cloud Build `ad51f2d9-a7d9-4500-9f99-9b731caf49a9`를 취소했다. 빌드 `CANCELLED`, 최신 생성·준비 리비전 `science-inquiry-platform-00024-2jl`과 트래픽 100%를 확인했다. 글 유실 수정은 운영에 반영되지 않았으며 기존 운영 버전이 유지된다.

## 전체 구조 분석·1차 안정화 (2026-09-07, 로컬 수정·미배포)

- `ARCHITECTURE_REVIEW.md`에 A~K 구조 분석, 근거가 있는 우선 문제 10개, 과탐실·동아리 공통 cycle 제안, AI·데이터·보안 검토, Phase 0~4 작업 순서를 정리했다. 기존 서비스를 유지하면서 단계적으로 개선하는 방향이며, 전체 후속 구현이 완료된 것은 아니다.
- 개인 일지에서도 저장 요청 후 입력한 글이 이전 저장 응답으로 덮이는 문제를 합성 브라우저 시나리오로 재현하고 수정했다. 저장 중 추가 입력·사진을 보존하고, 서버 조회 실패 시에도 기기 초안을 복구하며, 재접속 시 최신 작성 내용을 전송한다. 내부 탭 전환 시 편집기를 유지하고 IndexedDB는 트랜잭션 완료를 확인한다. 기기 종료 직전의 비동기 저장이나 여러 기기의 동시 일지 편집까지 완전히 보장하지는 않는다.
- 9개 API 파일의 13개 처리 경로에 누락된 최초 비밀번호 변경 조건을 추가했다. 변경 전 계정의 요청 차단과 정상 교사 조회를 검증했다.
- 보관된 동아리 팀은 추가 탭 조회·저장도 차단한다. 기존 기록을 삭제하지 않고 팀 복원 후 다시 접근할 수 있음을 검증했다.
- 체험 학생이 제출한 준비물을 교사가 재전송해도 Google Sheet로 보내지 않도록 원래 제출자의 계정 유형을 검사한다. 기존 금액·예산 상태는 보존한다.
- DB 초기화가 일시적으로 실패하면 실패한 연결을 정리하고 다음 요청에서 다시 초기화한다. 동시 요청의 초기화 공유도 검증했다. 스키마와 운영 데이터는 변경하지 않았다.
- 자동 테스트 프로세스에서 운영 DB·AI·Google·푸시 자격정보 환경값을 격리했다. 실제 학생 자료 대신 합성 데이터를 사용하고 외부 전송은 대체 응답으로 검증했다.
- 전체 자동 테스트 32개 파일 106개, 타입 검사, 프로덕션 빌드가 통과했다. 계획서·보고서 브라우저 회귀 11개와 개인 일지 시나리오 3개도 통과했다. 브라우저 검증은 로컬 Chrome 768px이며 실제 학생 기기·운영 PostgreSQL·실제 외부 연동 검증을 대신하지 않는다.
- 일지 브라우저 재현은 `scripts/journal-regression.mjs`, 계획서·보고서는 `scripts/editor-regression.mjs`에 남겼다. 추가 서비스 회귀 검증은 `access-regression.test.ts`, `custom-tab-access.test.ts`, `material-demo-regression.test.ts`, `db/initialization.test.ts`에 있다.
- 이번 재개 작업에서 운영 배포·학생 데이터 변경·실제 AI 호출·Google Sheet 쓰기·Git 커밋·푸시는 하지 않았다. 위 수정과 앞선 계획서·보고서 글 유실 수정은 모두 미배포 상태다.

## 임시 저장 방식 정정 (2026-09-07, 로컬 수정·미배포)

- 계획서·보고서의 항목 이동, 선택/체크 변경, 행 삭제, 외부 사이트 방문으로 서버에 자동 저장하지 않는다. `임시 저장` 버튼이 해당 시점의 모든 변경 항목을 저장하며 요청 중 추가 입력은 다음 임시 저장에서 반영한다. 제출 전 미저장 변경이 있으면 먼저 임시 저장하도록 안내한다.
- 사용자의 후속 정정을 반영해 기기 복구용 초안은 계속 유지한다. 항목 이동·내부 탭 이동·외부 탭 복귀·같은 탭의 외부 이동 후 뒤로 가기·새로고침에서 글이 복구됨을 검증했다. 계획서·보고서는 기존 sessionStorage 범위이며 서버 저장과 구분된다.
- 일지도 `임시 저장` 버튼으로 서버에 전송하고, 연결 복구만으로 전송하지 않는다. 글·사진의 기기 복구용 IndexedDB 초안과 이전 일지 기록은 유지한다.
- 충돌 비교에서 `내 내용 사용`은 작성할 내용을 선택하는 동작이며 서버 반영은 임시 저장 버튼으로 실행한다. 기존 승인·복원 권한과 과거 변경 이력은 유지했다. 서버 내부는 항목별 저장이므로 여러 항목을 저장하면 변경 이력도 항목별로 남고, 중간 실패 시 일부만 저장될 수 있다. 실패한 항목과 추가 입력은 초안에 남아 다시 저장할 수 있다.
- 자동 테스트 32개 파일 106개, 타입 검사, 프로덕션 빌드가 통과했다. 갱신한 계획서·보고서 브라우저 시나리오 13개와 일지 3개도 통과했다. 새 시나리오는 버튼을 누르기 전 서버 쓰기 없음, 제출의 암묵적 저장 차단, 저장 버튼 시점의 여러 항목 값 고정, 미저장 초안 복구를 확인한다.
- 학생용 계획서 AI 피드백의 기존 요구와 현재 미완성 연결을 재확인해 위 미구현 목록과 구조 분석 보고서를 정정했다. 교사용 전용 AI 검토는 별도 미구현 상태다. 이번에 AI 호출·새 AI 기능 구현·운영 데이터 변경·배포·Git 커밋·푸시는 하지 않았다.

## 주제 선택·준비물 안정화 (2026-09-07, 로컬 수정·미배포)

- 주제 재선택 때 기존 승인 상태 유지·실험 단계의 탐색 단계 후퇴·다른 학생의 편집 잠금 무시를 합성 테스트로 재현했다. `selectTopic`을 AI 파일에서 계획서 서비스로 옮기고 일반 항목 저장과 같은 충돌 검사·변경 이력·재승인 규칙을 적용했다. 계획서와 탐구 세션의 주제는 기존 저장 트랜잭션에서 함께 갱신하며, 초기 단계만 탐색으로 진행한다. 새 화면은 기대한 기존 주제를 함께 보내 오래된 화면의 변경을 차단한다.
- 준비물 신청 ID가 같아도 팀·세션이 다르면 조회 후 검사와 DB 충돌 갱신 조건에서 거절한다. 새 신청의 팀과 탐구 세션 관계도 검사한다. 다른 팀 기록을 변경하거나 외부로 보내지 않는 테스트를 추가했다.
- 동아리 준비물은 요청에 고정된 설정 버전의 Sheet·탭·열 매핑으로 전송한다. 해당 발행본이 보관되었어도 기존 신청에 사용하며, 당시 버전이 없는 과거 신청은 최신 설정을 추정 적용하지 않고 확인을 요청한다. 새 신청만 현재 발행본을 고정한다.
- 교사 재전송은 원 제출자·제출 시각·금액·예산 승인 상태를 보존한다. 이미 성공한 동일 신청 ID·동일 품목은 외부 호출을 생략한다. JSON 객체의 속성 순서 차이는 다른 품목으로 판단하지 않는다. 이전 전송의 완료가 더 새 내용의 실패 상태를 성공으로 바꾸지 않도록 조건부 상태 갱신을 적용했다.
- 준비물 화면의 작성 항목과 신청 ID를 사용자·탐구별 현재 탭에 복구용으로 보관한다. 탭 이동·새로고침 후 복구하며, 연결 실패·응답 지연 뒤 제출 버튼을 다시 사용할 수 있다. 저장 중 추가 입력은 유지하고 화면 갱신 실패를 제출 실패와 구분한다. 입력만으로 외부 전송하지 않는다.
- 자동 테스트 34개 파일 122개가 통과했다. 추가 테스트는 주제 선택 8개와 준비물 8개다. 실제 Google Sheet 호출은 대체 응답으로 검증했고 운영 학생 자료·비밀값·DB 스키마는 변경하지 않았다. 실제 PostgreSQL 동시 실행은 별도 검증이 필요하다.
- 계획서·보고서 브라우저 회귀 13개, 일지 3개, 신규 준비물 3개로 총 19개 시나리오와 프로덕션 빌드를 통과했다. 로컬 개발 서버의 자동 생성 타입 파일 손상과 잠금 요청 실패는 서버 종료·해당 생성 파일 정리·재시작 후 재검증했다. 실제 소스의 타입 검사를 우회하거나 검사 범위를 줄이지 않았다. 로컬 검증 서버는 종료했다.
- 남은 준비물 과제는 동시 외부 전송의 순서 보장, 시트가 반영했지만 응답/DB 성공 기록이 유실된 경우의 중복 방지, 당시 팀 이름·목적 탭 고정, 자유 입력의 수식 해석 차단이다. 이번 성공 기록 재사용만으로 모든 중복 전송이 해결된 것으로 간주하지 않는다.
- 운영 배포·Git 커밋·푸시·실제 AI 호출은 하지 않았다. 기존 계획서·보고서·일지의 임시 저장 원칙과 기기 초안 보존은 유지한다.

## 추가 탭·평가 작성 내용 보존 (2026-09-07, 로컬 수정·미배포)

- 동아리 추가 탭과 자기·동료평가에 사용자·탐구/팀·설정 버전/평가 회차·평가 대상별 복구용 초안을 적용했다. 입력 직후 현재 브라우저 탭의 sessionStorage에 보관하며 내부 화면 전환·새로고침·외부 페이지 방문 후 돌아오기에 복구한다. 서버 저장은 추가 탭의 `임시 저장`/검토 요청, 평가의 기존 저장 버튼으로만 실행한다.
- 여기서 동료평가의 `팀원 전환`은 팀 편성 변경이 아니라 평가할 학생 A/B를 선택하는 동작이다. 대상별 의견·관찰 근거·단계·예외 사유를 분리해 보관하고 자기평가 서술형 2개도 복구한다. 직접 관찰 확인 체크는 저장·대상 전환 후 다시 확인한다.
- 저장 시점의 내용과 이후 입력을 분리했다. 저장 완료 시각이 달라졌다는 이유로 평가 폼을 새로 만들지 않으며, 이미 닫힌 이전 폼의 저장 완료도 새 폼에서 작성한 초안을 제거하지 못하게 했다. 초안이 없는 마감 평가에서는 제출하지 않은 기본 단계 값을 제출된 평가처럼 표시하지 않는다.
- 마감된 평가에는 서버에 제출한 내용만 읽기 전용으로 표시하고, 미제출 초안은 따로 보관해 교사가 같은 회차를 다시 열면 복구한다. 평가 회차나 대상이 달라지면 다른 초안을 사용한다. 평가 기준·익명 의견 공개·최소 3건·교사 권한·과거 기록 규칙은 변경하지 않았다.
- 조회/저장 요청에 30초 제한과 실패 후 재시도를 적용했다. 추가 탭은 조회 실패 시에도 초안을 보여주되 서버 상태를 다시 확인하기 전 저장을 막는다. 평가 조회 실패 후 재조회로 초안을 복구하고, 저장은 성공했으나 후속 조회만 실패한 경우를 별도로 안내한다. 오래된 평가 조회 응답은 현재 화면을 갱신하지 않는다.
- `scripts/form-draft-regression.mjs`의 합성 브라우저 시나리오 10개가 통과했다. 대상 전환·새로고침·외부 이동, 저장 중 추가 입력, 닫힌 이전 폼의 응답, 연결 실패·조회 실패·30초 시간 초과, 마감/재개·회차 격리를 검증한다. 기존 계획서·보고서 13개, 일지 3개, 준비물 3개도 통과해 총 29개다. 실제 학생 기기·한글 조합·절전 검증을 대신하지 않는다.
- 전체 자동 테스트 34개 파일 122개, 타입 검사, 프로덕션 빌드(45개 정적 페이지 생성)가 통과했다. 새 브라우저 검증은 평가·추가 탭 API를 합성 응답으로 대체하며, 실제 학생 자료·Google Sheet·AI에는 접근하지 않았다. 운영 DB 스키마·평가 저장 서비스는 변경하지 않았다.
- 남은 문제: 추가 탭·평가의 여러 기기/사용자 간 서버 동시 수정 검사와 팀 공동 추가 탭의 첫 저장 중복 생성 방지. 이번 복구용 초안은 현재 탭의 세션 범위이며, 브라우저 저장 차단/용량 부족 시 경고를 표시한다. 여러 기기 공유·브라우저 탭 종료 후의 영구 백업을 보장하지 않는다.
- 운영 배포·Git 커밋·푸시는 하지 않았다. 수정 검증 서버는 종료했다.

## 추가 탭·평가 동시 저장 충돌 방지 (2026-09-07, 로컬 수정·미배포)

- 추가 탭·자기평가·동료평가에 `write_version`을 추가하고, 화면에서 읽은 저장 번호와 현재 번호가 같을 때만 내용을 변경한다. 학생 저장 API는 `expectedVersion`을 필수로 받는다. 처음 작성하는 화면은 `null`, 기존 자료는 조회한 번호를 보내며, 번호 없는 이전 화면의 요청은 거절한다. 서로 다른 변경을 같은 번호로 보내면 하나만 성공하고 다른 쪽은 HTTP 409를 받는다.
- 팀 공동 추가 탭의 첫 기록 ID를 설정 버전·탐구 세션·개인 작성자 유무에서 결정한다. NULL을 포함한 기존 UNIQUE에만 의존하지 않고 기본 키 충돌로 중복 첫 저장을 막는다. 기존 ID와 기록은 유지한다. 이미 중복된 과거 기록은 삭제하거나 임의 병합하지 않고 조회·저장을 중단해 확인을 요청한다.
- 교사 추가 탭 검토와 동료평가 의견 검토도 저장 번호를 검사하고 변경 시 번호를 올린다. 오래된 원문에 대한 검토나 검토 전 번호를 가진 학생의 다른 내용 저장이 최신 기록을 덮어쓰지 않는다. 자기·동료평가 쓰기와 의견 검토는 짧은 트랜잭션에서 평가 회차를 잠근 뒤 마감 상태를 다시 확인한다. 평가 마감/재개 갱신과 같은 행 잠금을 사용한다.
- 이미 저장한 것과 같은 내용의 재시도는 기존 번호를 반환한다. 불필요한 내용 변경·제출 시각 갱신을 하지 않으며, 동료평가의 교사 의견 공개 검토 상태를 초기화하지 않는다. 다른 내용이면 명시적으로 최신 번호를 확인해야 한다.
- 학생 충돌 화면에서 최신 저장본과 내 초안을 항목별로 비교한다. `최신 저장 내용 사용` 또는 `내 작성 내용으로 계속`을 선택할 수 있으며, 선택만으로 서버에 쓰지 않는다. 내 내용을 반영하려면 저장 버튼을 누른다. 충돌 전 저장 번호도 복구용 초안과 함께 보관해 새로고침으로 검사를 우회하지 못한다. 번호가 없는 과거 초안은 보존하고 명시적 비교를 요구한다.
- 저장 중 추가 입력과 이미 닫힌 폼의 응답 보호를 유지했다. 성공한 저장의 새 번호를 이후 입력 초안에 연결하되 텍스트를 바꾸지 않는다. 다른 사용자의 자기평가나 다른 평가자가 작성한 동료평가 원문을 충돌 화면에 노출하지 않는다.
- `form-concurrency.test.ts`는 첫 저장 경쟁, 기존 번호 경쟁, 오래된/누락된 번호, 기존 번호 0 자료, 학생별 분리, 교사 검토 충돌, 동일 내용 재시도, 과거 중복 보존과 JSON 비교를 검증한다. `scripts/form-draft-regression.mjs`의 기존 10개와 새 충돌 4개, 총 14개 브라우저 시나리오가 통과했다. `scripts/evaluation-api-concurrency.mjs`로 대체 응답이 아닌 실제 로컬 API의 동시 저장·409·필수 번호·마감 차단 5개를 검증했다.
- 최종 자동 테스트 35개 파일 135개, TypeScript 타입 검사, Next.js 프로덕션 빌드가 통과했다. 새 서버 테스트 13개를 포함하며, 학생·교사의 권한과 기존 기능 테스트도 함께 통과했다. 개발 서버는 종료한 뒤 빌드했다.
- DB 변경은 세 테이블에 기본값 0인 정수 열을 추가하는 로컬 스키마 변경이다. 운영 DB에는 적용하지 않았다. 최종 배포 전 백업, 실제 PostgreSQL 16의 마이그레이션·동시 실행 검증, 구버전 쓰기 요청이 새 버전과 섞이지 않는 전환 절차가 필요하다. 이번 테스트는 pg-mem이므로 PostgreSQL 행 잠금·롤백 동작까지 실증한 것으로 간주하지 않는다.
- 이번 서버 충돌 방지는 추가 탭과 학생 자기·동료평가 및 해당 교사 검토에 적용했다. 개인 일지의 여러 기기 동시 수정, 교사 평가 종합 의견·결과 공개 전체의 경합, 과거 중복 자료의 운영상 정리, 준비물 외부 전송 순서·중복 방지, 팀 이동 트랜잭션은 별도 후속 과제로 남는다.
- 운영 배포·학생 자료 변경·Google Sheet 쓰기·실제 AI 호출·Git 커밋·푸시는 하지 않았다. 로컬 검증 서버는 종료했다.

## Goal 진행: 준비물 전송·팀 변경 (2026-09-07, 로컬 수정·미배포)

- Codex Goal은 준비물 전송, 팀 이동, 개인 일지의 다중 기기 충돌, 교사 평가 종합 의견·결과 공개 경합과 마지막 통합 검증까지의 안정화 묶음으로 활성 상태다. cycle·계획서 AI 검토·운영 배포는 이 Goal에 추가하지 않았다. Sol High 사용 권고는 전달했지만 모델 설정을 도구로 바꾸지는 않았다.
- 준비물에 전송 작업 ID, 당시 품목·팀 이름·팀장·Sheet·탭·열 연결·시각 스냅샷, 실제 전송할 고정 요청을 저장한다. 새로 발행한 동아리 설정이나 팀장 변경으로 이전 전송의 목적지가 바뀌지 않는다. 금액·예산 승인·원 제출 시각은 동일 신청 재시도에서 보존한다.
- Google Sheet 문서별 전송 예약을 DB에 둔다. 확인되지 않은 전송이 있으면 같은 고정 요청만 재시도하고 다음 신청은 차단해 기기 초안으로 유지한다. 예약은 시간이 지났다는 이유로 해제하지 않으며, 외부 호출 중 DB 트랜잭션을 계속 열어 두지 않는다. 성공 상태 기록과 예약 해제는 한 트랜잭션이다.
- 조별 양식의 행 추가·품목 입력·합계 수식·완료 확인용 이름 있는 범위를 하나의 Sheets batchUpdate로 묶는다. 같은 완료 ID를 다시 추가하면 배치 전체가 거절되는 API 규칙을 사용해 늦은 중복 요청의 행 추가·덮어쓰기를 막는다. 쓰기 응답이나 DB 성공 기록이 유실돼도 저장된 동일 요청과 완료 기록으로 재시도한다. 완료 확인용 이름 있는 범위에는 학생 식별정보를 쓰지 않는다.
- 일반 표 형식은 전송마다 append하지 않고 탐구별 이름 있는 입력 범위를 생성·재사용한다. 품목이 줄면 해당 범위의 연결 열만 비우며, 연결하지 않은 교사 열·서식은 쓰지 않는다. 텍스트는 stringValue, 숫자는 numberValue, 검증된 합계만 formulaValue로 보내 학생 입력을 수식으로 해석하지 않는다. 이전의 우회 전송 함수는 제거했다.
- 과거 신청에 위치·완료 기록이 없으면 이미 쓴 행을 추측하거나 재추가하지 않고 교사의 시트 대조를 요구한다. 일반 표에서 이런 신청이 남아 있으면 새 ID로도 우회 추가하지 않는다. 최종 배포 전 과거 신청의 대조·이행 절차가 필요하며, 운영 자료는 이번에 수정하지 않았다. 전송 확인 중에는 해당 시트의 행 구조나 플랫폼 완료 기록을 수동으로 지우지 않아야 한다. 영구적인 잘못된 연결/요청은 자동 폐기하지 않고 운영 복구 대상으로 남긴다.
- 과탐실 팀 이동·제거·팀장 지정과 탐구 문서 생성은 연결 하나의 트랜잭션으로 처리한다. 학생 이동 시 이전 수업 팀의 팀장 표시도 해제한다. 학생 행을 먼저 잠근 뒤 관련 팀을 ID 순으로 잠그는 규칙을 동아리 배정/종료·학생 비활성화·체험 계정 비활성화에도 적용한다.
- 명단 재업로드도 기존 학생·관련 팀을 일관된 순서로 잠그며 이전 수업 팀장 표시를 정리한다. 기존 비밀번호와 동아리 소속을 유지하고, 기존 팀의 실제 ID를 재사용하며 보관 팀에는 배정하지 않는다. 새 팀의 탐구·계획서·보고서 생성도 같은 트랜잭션에 포함한다. 팀원 기록은 삭제하지 않고 참여 종료 시각을 남기며 재참여는 새 관계로 보관한다.
- 준비물 서비스/외부 배치 합성 검사 19개와 새 팀 변경 회귀 6개가 통과했다. 전체 Vitest 37개 파일 151개, TypeScript 타입 검사, Next.js 프로덕션 빌드(45개 정적 페이지)가 통과했다. 외부 배치 검사는 Google 응답을 대체하며, pg-mem은 실제 PostgreSQL 행 잠금과 롤백 검증을 대신하지 않는다. 이 컴퓨터에는 PostgreSQL 서버/psql/Docker가 확인되지 않았다. UI 코드는 이번에 바꾸지 않았고 이전 브라우저 검사는 재실행하지 않았다.
- 로컬 스키마 추가: material_requests에 sync_snapshot, sync_operation_id, sync_batch 및 material_sheet_dispatch 테이블. 최종 배포 전 실제 PostgreSQL 16에서 이전 버전과의 이행·동시 실행을 검증해야 한다. 운영 DB·Google Sheet·학생 자료·AI·Git 커밋/푸시는 건드리지 않았다.
- Goal에서 이어갈 일: 개인 일지의 저장 번호·사진 충돌·기기 초안 비교, 교사 종합 의견 및 공개 스냅샷 경합, 관련 통합/브라우저 검증. 현재 Goal을 완료 처리하지 않는다.

## Goal 진행: 개인 일지 다중 기기 충돌 (2026-09-07, 로컬 수정·미배포)

- 개인 일지에 차시별 write_version을 추가했다. 첫 저장은 null, 기존 일지는 조회한 번호를 필수로 보내며, 같은 번호에서 서로 다른 본문·날짜·사진 목록을 저장하면 한 요청만 반영하고 다른 요청은 HTTP 409로 거절한다. 기존 행은 번호 0에서 시작하고 새 행은 1부터 시작한다.
- 첫 일지가 아직 없는 경쟁도 학생 행 잠금과 세션·학생·차시에서 결정한 고정 일지 ID로 직렬화한다. 본문 갱신, 사진 추가/삭제, 번호 증가를 한 트랜잭션으로 처리해 충돌 요청이 사진을 일부 변경하지 않는다. 같은 사진 client ID의 내용이 다르면 거절한다.
- 저장이 반영됐으나 응답을 받지 못해 같은 본문·사진을 재전송한 경우 기존 번호·수정 시각·사진 한 장을 그대로 반환한다. 성공 뒤 팀 이동이 겹쳐도 이미 저장된 결과를 다시 권한 조회하다 실패하지 않도록 트랜잭션에서 확정한 결과를 응답한다. 제거 학생의 기존 일지를 교사가 보는 규칙은 유지한다.
- IndexedDB 기기 초안에 서버 기준 번호를 함께 보관한다. 기기 초안 쓰기·삭제를 호출 순서대로 완료해 이전 비동기 쓰기가 새 저장 번호를 늦게 덮는 경합을 막았다. 입력·페이지 숨김만으로 서버 전송하지 않으며 기존 450ms 기기 복구 저장과 명시적 `임시 저장` 원칙을 유지한다.
- 외부 페이지에서 돌아오거나 화면을 다시 보일 때 작성 중인 차시의 최신 번호를 확인한다. 다른 기기 저장과 다르면 날짜·세 본문·사진 수를 나란히 보여준다. `최신 저장 내용 사용`과 `내 작성 내용으로 계속`은 서버 쓰기를 일으키지 않고, 내 내용을 선택한 뒤 다시 임시 저장해야 반영한다. 다른 기기에서 이미 삭제한 저장 사진은 브라우저 초안에 원본 파일이 없어 자동 복원하지 않고 제외 사실을 알리며 글과 새 사진은 유지한다.
- 저장 번호가 없던 이전 IndexedDB 초안도 기존 서버 일지가 있으면 비교를 요구한다. 반대로 서버 조회 자체가 실패했을 때는 기록된 기준 번호를 임의로 지우지 않으며, 작성 내용은 유지한 채 이후 복귀/저장에서 다시 확인한다. 저장 성공 응답 중 계속 쓴 내용은 새 기준 번호로 재연결해 다음 수동 저장에서 보낸다.
- 일지 서비스/저장 경쟁/API 합성 검사 11개와 로컬 Chrome 768×1024 화면 시나리오 5개가 통과했다. 화면 시나리오는 서버 응답을 합성하며 실제 학생 기기·절전·한글 조합 입력을 대신하지 않는다. pg-mem은 PostgreSQL의 행 잠금 대기를 재현하지 않으므로 실제 PostgreSQL 16 동시 저장 검증은 최종 배포 전 과제다.
- 로컬 스키마 추가: experiment_journals.write_version 정수 열(기본값 0). 운영 DB·학생 자료·AI·Google Sheet·Git·배포는 건드리지 않았다.
- Goal에서 이어갈 일: 교사 평가 종합 의견·결과 공개 스냅샷 경합과 마지막 전체 통합 검증. Goal은 계속 활성 상태다.

## Goal 완료: 교사 평가 공개 경합·전체 통합 검증 (2026-09-07, 로컬 수정·미배포)

- 교사 종합 피드백에도 write_version을 추가했다. 처음 저장은 null, 기존 자료는 화면에서 읽은 번호를 필수로 보내며, 같은 번호에서 서로 다른 문장을 저장하면 한 요청만 반영하고 다른 요청은 HTTP 409로 거절한다. 저장 성공 응답이 유실된 뒤 같은 문장을 재시도하면 번호와 내용을 다시 바꾸지 않고 기존 번호를 반환한다. 기존 행은 번호 0에서 명시적으로 이어서 수정한다.
- 익명 의견 검토도 평가 회차와 현재 의견을 트랜잭션 안에서 다시 잠그고 확인한다. 같은 승인 내용을 재시도하면 기존 번호를 반환하며, 다른 교사의 최신 검토나 공개 완료 결과를 오래된 화면이 덮어쓰지 않는다.
- 교사 화면의 익명 의견 가림 문장과 학생별 종합 피드백은 교사·평가 회차·대상별 브라우저 복구 초안으로 보관한다. 외부 페이지를 다녀오거나 새로고침해도 미저장 문장을 복원하고, 다른 교사의 저장 번호가 달라졌으면 최신 저장본과 내 초안을 나란히 비교한다. `내 작성 내용으로 계속`을 선택해도 서버에는 쓰지 않으며 각 저장 버튼을 눌러야 반영된다. 현재 탭에 미저장 검토 초안이 있으면 결과 공개 버튼도 실행하지 않는다.
- 학생 평가 저장, 익명 의견 검토, 교사 종합 피드백, 평가 마감·재개, 결과 공개가 모두 같은 평가 회차 행 잠금을 사용한다. 공개는 회차를 잠근 뒤 현재 활성 학생과 관련 팀을 학생→팀 공통 순서로 잠그고 명단을 다시 읽는다. 검토 대기 의견, 유효 평가 수, 승인 문장, 교사 피드백과 대상 탐구 세션을 한 트랜잭션에서 확정한다.
- 공개 조건을 하나라도 충족하지 못하면 공개 자료와 회차 상태를 함께 롤백한다. 성공하면 학생별 공개 스냅샷, 회차 published 상태, 당시 대상 팀의 탐구 완료 단계를 함께 반영한다. 같은 공개 요청을 다시 보내도 기존 스냅샷과 공개 시각을 바꾸지 않는다. 공개 후 종합 피드백·익명 의견·재개는 차단된다. 진행 중 회차가 과거 공개 회차보다 항상 먼저 선택되도록 회차 조회 순서도 결정적으로 고정했다.
- 새 `evaluation-publication-concurrency.test.ts`와 기존 평가·폼 충돌 검사를 합쳐 관련 자동 테스트 3개 파일 20개가 통과했다. 전체 Vitest 40개 파일 163개, TypeScript 타입 검사, Next.js 16.3.2 프로덕션 빌드(45개 경로)가 통과했다.
- 실제 로컬 평가 API에서는 학생 첫 저장·수정 충돌, 번호 누락, 마감 차단, 교사 피드백 첫 저장·수정 충돌, 동일 재시도, 중복 공개, 공개 후 변경 차단 7개 시나리오가 통과했다. Chrome 768×1024에서 교사 검토/피드백의 외부 방문 복구, 다른 교사 저장 비교, 선택만으로 서버 미반영, 미저장 상태 공개 차단, 명시적 저장 반영 4개 시나리오가 통과했다. 계획서·보고서 13개, 추가 탭·학생 평가 14개, 일지 5개, 준비물 3개도 다시 실행해 전체 브라우저 회귀 39개가 통과했다.
- 로컬 스키마 추가: evaluation_publications.write_version 정수 열(기본값 0). 합성 DB와 로컬 API 검증은 실제 PostgreSQL의 행 잠금 대기를 증명하지 않으므로, 최종 배포 전 백업·번호 있는 마이그레이션·PostgreSQL 16 동시 실행·구버전 요청 전환·과거 준비물 신청 대조가 남는다.
- 이 Goal의 로컬 분석·수정·통합 검증 범위는 완료했다. 운영 DB·학생 자료·실제 Google Sheet·AI·Git·배포는 건드리지 않았다. cycle과 학생/교사의 AI 계획서 검토는 확정된 후속 범위로 유지한다.

## Goal 완료: Cycle 전 공통 기반 (2026-09-07, 로컬 수정·미배포)

- 기존 `SCHEMA_SQL`을 삭제하지 않고 번호·이름·내용 해시·적용 시각을 기록하는 점진적 DB 마이그레이션 경계를 추가했다. `0001`은 기존 스키마 기준점, `0002`는 세션 취소·로그인 제한, `0003`은 복구 가능한 AI 작업 구조다. 같은 변경의 반복 실행, 적용 후 내용 변조 거절, 실패 전체 롤백과 같은 번호의 수정본 재시도, 번호 중복 거절을 합성 DB에서 확인했다. 기존 스키마로 시작한 DB에 새 열·테이블이 마이그레이션 뒤 생기는 것도 확인했다.
- 로그인 토큰에 계정별 세션 번호를 고정했다. 본인 비밀번호 변경과 교사·마스터 비밀번호 초기화는 번호를 올려 이전 기기의 모든 토큰을 취소하며, 본인 변경 요청에는 새 번호의 토큰을 다시 발급한다. 계정 비활성화와 운영용 비밀번호 재발급 스크립트도 같은 취소 규칙을 따른다. 이 변경을 처음 운영 반영하면 번호가 없는 기존 로그인 토큰은 무효가 되어 한 번 다시 로그인해야 한다.
- 로그인 실패 제한은 평문 아이디·IP를 저장하지 않는 해시 키로 관리하며, `학년도+아이디+접속망` 조합에서 10분 안에 8회 실패하면 15분 동안 제한한다. IP 하나만으로 학급 전체를 막지 않으므로 같은 학교 Wi-Fi의 다른 학생은 정상 로그인할 수 있다. 성공 로그인은 해당 조합의 실패 기록을 지운다.
- 푸시 구독 주소는 HTTPS·자격정보·호스트·사설/루프백/링크 로컬 주소를 검사한다. 호스트 이름은 등록 때와 발송 직전에 DNS를 다시 확인하며 내부 주소로 해석되면 저장 또는 발송하지 않는다. 과거의 안전하지 않은 구독은 발송하지 않고 제거한다.
- AI 탐구 대화·탐구 방향·시험 생성에 요청별 작업 기록, 자원별 소유 토큰과 만료 시각, 완료 결과 재사용을 적용했다. 대화 질문은 요청 ID에서 정한 같은 메시지 ID로 한 번만 보존한다. 탐구 방향은 세션 갱신과 작업 완료를 한 트랜잭션에서 확정한다. 시험은 공통·팀별 생성 결과를 단계별로 저장해 뒤 단계 실패 후 재시도 때 완료한 AI 단계를 다시 호출하지 않으며, 최종 시험과 작업 완료도 한 트랜잭션으로 확정한다. 만료 뒤 새 소유자가 이어받으면 이전 소유자는 새 결과를 덮어쓸 수 없다.
- 브라우저는 대화·탐구 방향·시험 생성 요청 ID를 실패 후 같은 입력으로 재시도하는 동안 유지한다. 서버는 완료 결과를 재사용하고 OpenAI 요청에는 같은 추적 ID를 보낸다. 외부 AI가 요청을 처리한 직후 서버가 결과 저장 전에 종료되는 극단적인 구간까지 제공자 수준의 정확히 한 번 비용을 보장하지는 않지만, 앱 안의 영구 잠금·중복 질문·완료 결과 재생성·시험의 완료 단계 재생성은 막는다.
- 최종 검증은 Vitest 46개 파일 189개, TypeScript 타입 검사, Next.js 16.3.2 프로덕션 빌드 45개 경로를 통과했다. 로컬 브라우저 회귀 39개(계획서·보고서 13, 추가 탭·학생 평가 14, 일지 5, 준비물 3, 교사 평가 4), 실제 로컬 평가 API 7개도 다시 통과했다. 새 실제 로컬 인증 검증에서 이전 토큰 403, 새 토큰 200, 8회 실패 뒤 429, 같은 접속망의 다른 학생 로그인 200을 확인했다.
- 모든 자동 검증은 운영 DB·AI·Google·푸시 자격정보를 비운 로컬 메모리 DB와 합성 데이터로 수행했다. 운영 PostgreSQL·학생 자료·Google Sheet·실제 AI·푸시 서비스·Git·배포는 건드리지 않았고 로컬 서버를 종료했다. 최종 배포 전에는 백업 복원, 실제 PostgreSQL 16 마이그레이션·잠금, 구버전 요청 전환, 과거 준비물 전송 대조가 여전히 필요하다.
- 다음 확정 구현 순서는 과탐실·동아리 공통 Cycle → Cycle/문서 버전에 고정된 제출 스냅샷 → 학생이 AI와 계획서를 검토하는 기능 → 교사가 제출 계획서를 AI와 검토하는 기능이다. 원본 계획서·보고서·Sheet 구조와 미확정 운영 규칙은 추측하지 않는다.

## Goal 완료: 공통 Cycle·계획서 제출본·학생/교사 AI 검토 (2026-09-07, 로컬 수정·미배포)

- 번호 있는 `0004` 마이그레이션으로 과탐실과 동아리가 함께 쓰는 탐구 회차, 계획서 문서 스냅샷, 제출 기록, 학생/교사 AI 검토 기록을 추가했다. 기존 탐구 세션은 내용·승인 상태를 바꾸지 않고 `기존 탐구 자료` 회차에만 연결하며, 과거 자료를 임의로 `1차 탐구 최종본`이라고 표시하지 않는다. 새 팀은 `1차 탐구` 회차로 시작한다.
- 교사 팀 화면에서 현재 회차의 이름과 시작일·종료일을 설정할 수 있다. 학급은 기존 공동 교사 원칙을 따르고, 동아리는 담당 교사와 마스터만 바꿀 수 있다. 이 설정은 계획서·보고서 내용이나 승인 상태를 변경하지 않는다. 학생과 교사 화면의 팀 머리말에는 같은 회차 안내가 표시된다.
- 계획서를 제출할 때 현재 서버 저장본, 당시 계획서 양식 정의와 버전, 당시 회차 이름·순서·기간·상태를 한 트랜잭션에서 읽기 전용 제출본으로 고정한다. 이후 학생이 제출 대기 중인 현재 계획서를 수정하면 해당 제출은 철회 상태로 남고 현재 문서는 다시 제출해야 한다. 과거 제출본의 내용은 바뀌지 않으며 교사 화면은 현재 작성본 대신 최신 고정 제출본을 표시한다.
- 새 기능 반영 전에 이미 제출·승인된 계획서는 과거 시점의 원문을 추정하지 않는다. 교사 AI 검토나 직접 검토를 처음 시작한 시점의 현재 저장본을 `기존 자료 고정본`으로 남기고 화면에 이 한계를 명시한다.
- 학생 계획서 화면에 `AI와 계획서 점검`을 추가했다. 작성 중인 기기 초안은 먼저 `임시 저장`해야 하며, AI는 그 특정 저장본을 근거로 잘된 점, 누락·모순·실행 가능성·안전·근거의 확인 질문과 짧은 개선 방향만 제공한다. 완성 문장을 대신 쓰거나 계획서를 자동 변경하지 않는다. 같은 저장본·프롬프트·출력 형식은 저장된 결과를 재사용하고 계획서나 회차 설정이 바뀌면 새 저장본으로 다시 점검한다.
- 교사 팀 화면에 `AI 검토 보조`를 추가했다. 새 제출은 제출 순간의 고정본만 사용하며, AI 결과와 별도로 교사의 피드백 입력·승인·수정 요청 권한을 그대로 유지한다. 동아리 담당 범위도 서버에서 다시 검사한다. 학생 실명·학번·연락처는 AI 입력 전에 가리고, 계획서 본문 속 명령은 자료로만 취급하도록 프롬프트를 분리했다.
- 학생/교사 검토는 각각 프롬프트 버전과 공통 출력 스키마 버전, 실제 모델, 근거 스냅샷, 요청자와 AI 작업 ID를 보존한다. 기존 AI 작업의 소유 토큰·만료·완료 결과 재사용을 적용해 같은 저장본의 중복 비용과 영구 잠금을 막는다. 실제 OpenAI 호출 직후 DB 저장 전에 서버가 종료되는 제공자 수준의 극단적 중복 가능성은 기존 한계와 같다.
- 최종 자동 검증은 Vitest 48개 파일 196개, TypeScript 타입 검사, Next.js 16.3.2 프로덕션 빌드 48개 경로가 통과했다. 합성 검증에서 기존 자료 무변경 연결, 회차 설정 권한·기간 검사, 제출본·회차 정의 불변성, 제출 뒤 수정 철회와 재제출, 학생 팀 권한, 학생/교사 AI 결과 재사용을 확인했다. Chrome 768×1024에서 회차·학생 AI 결과·교사 AI 보조 화면 2개와 기존 계획서·보고서 글 보존 13개 시나리오가 통과했다.
- 운영 DB·학생 자료·실제 OpenAI·Google Sheet·푸시·Git·배포는 건드리지 않았다. 실제 여러 회차를 시작·종료하며 새 계획서·중간보고서·준비물·일지 번호를 운영하는 확장은 회차별 승인, 중간보고서 실제 양식, 과거 회차 수정 범위, 준비물 재신청, 일지 번호 범위를 교사와 확정한 뒤 진행한다.

## Goal 완료: 다중 Cycle 전환·중간/최종 AI 분석 (2026-09-08, 로컬 수정·미배포)

- `0005` 마이그레이션으로 AI 대화·우리끼리 대화·문서 이력을 Cycle에 연결하고 일지 번호의 고유 범위를 Cycle별로 바꿨다. 회차별 고정 근거, 중간/최종 AI 분석, 학생의 선택적 판단 기록을 별도 테이블에 보존한다. 기존 자료는 `0004`가 만든 `기존 탐구 자료` Cycle에 연결하며 원문·승인 상태·Google Sheet 구조를 바꾸지 않는다.
- 교사는 승인된 계획서와 확인 완료 보고서가 있는 현재 Cycle에서 중간 또는 최종 분석을 선택한다. 선택한 분석 종류는 해당 Cycle에서 바꾸지 않으며, 분석 뒤 문서·일지·대화가 바뀌면 최신 고정본으로 다시 분석해야 전환할 수 있다. AI는 가설·변인·측정, 통제·교란, 오차, 반복·표본, 해석·일반화, 실행 가능성, 안전을 근거 ID와 함께 검토한다.
- 학생은 중간 분석의 제안 중 팀에 필요한 항목에만 수용·수정·선택하지 않음과 이유를 선택적으로 저장한다. 아무 판단도 저장하지 않아도 다음 Cycle을 시작할 수 있다. 저장한 판단만 다음 회차와 최종 분석의 이전 맥락에 포함하고 미응답 항목의 선택이나 이유는 추정하지 않는다. 판단 작성 중인 글도 사용자·분석·제안별 현재 브라우저 탭의 복구 초안으로 유지한다.
- 다음 Cycle 시작 시 이전 Cycle과 분석 근거는 읽기 전용으로 남기고 현재 계획서·보고서·잠금·역할을 새 Cycle의 빈 상태로 전환한다. 과탐실은 기존 고정 양식, 동아리는 시작 시점의 최신 발행 계획서·보고서 양식을 고정한다. 준비물 신청과 일지는 새 Cycle에 따로 저장하고 일지 차시 번호는 1번부터 다시 사용할 수 있다. 새 Google Sheet 탭이나 열은 추측해 만들지 않으며 기존 전송 규칙 안에서 Cycle별 대상을 구분한다.
- 전체 탐구 완료 뒤 학생·교사 편집·복원·준비물 재전송 화면을 읽기 전용으로 바꾼다. 완료 회차의 고정 계획서·보고서·당시 팀원 역할·준비물 신청과 AI 분석·저장된 학생 판단은 회차 기록에서 다시 볼 수 있다. 제거된 팀원의 원자료와 권한 이력은 기존 규칙대로 보존한다.
- AI 입력 전 실명·학번·연락처를 가리고 학생 작성물 속 명령을 지시로 실행하지 않는다. 분석별 근거 스냅샷·내용 해시·작업 ID·소유 토큰·모델·프롬프트/스키마 버전·완료 결과를 기록하고 같은 고정본은 재사용한다. 실제 OpenAI 호출 직후 DB 저장 전 서버가 종료되는 제공자 수준의 극단적 중복 가능성은 기존 한계와 같다.
- 합성 DB에서 중간 분석 재사용, 변경된 자료의 오래된 판단 차단, 선택 판단 1건만 있는 다음 Cycle 전환, 회차별 일지 1번 재사용, 이전 고정 문서 복원 차단, 최종 분석의 이전 판단 포함, 중복 완료 차단을 확인했다. 전체 Vitest 49개 파일 198개 테스트, TypeScript 타입 검사, Next.js 16.3.2 프로덕션 빌드 49개 경로가 통과했다. Chrome 768×1024에서 학생 판단 초안 새로고침 복구·명시적 저장, 학생/교사 Cycle 분석 화면과 계획서 AI 검토 화면을 확인했다.
- 검증은 운영 자격정보를 비운 로컬 메모리 DB와 대체 AI 응답으로 수행했다. 운영 PostgreSQL·학생 자료·실제 OpenAI·Google Sheet·푸시·Git·배포는 건드리지 않았고 로컬 서버를 종료했다. 실제 PostgreSQL 16의 `0004`·`0005` 마이그레이션과 행 잠금·구버전 요청 전환은 최종 배포 전에 검증해야 한다.

## PostgreSQL 복원 검증·통합 배포 완료 (2026-09-08)

- 운영 PostgreSQL 16의 수동 백업 `1788830618056`을 성공 상태로 확인하고 소형 임시 Cloud SQL 인스턴스에 복원했다. 운영 원본과 분리된 복원본에서만 마이그레이션과 합성 회차 전환을 실행했으며 검증 뒤 임시 인스턴스 삭제를 완료했다.
- 첫 복원 검증에서 여러 서버가 처음 기동하며 `schema_migration_lock` 테이블을 동시에 만들 때 PostgreSQL 내부 형식 이름 충돌이 발생하는 것을 재현했다. 실제 PostgreSQL 연결에서는 잠금 테이블 생성 전에 세션 advisory lock을 잡도록 수정하고, pg-mem 개발·테스트 경로는 기존 조건부 생성을 유지했다.
- 같은 백업을 다시 깨끗하게 복원해 실제 앱 초기화 순서를 3개 연결에서 동시에 실행했다. `0001~0005`가 한 번씩 적용됐고, 기존 탐구 세션 77개가 77개 `기존 탐구 자료` Cycle로 연결됐다. 사용자·팀·팀원 이력·세션·계획서·보고서·준비물·일지·AI 대화·우리끼리 대화 10개 보호 대상의 행 수와 내용 해시는 전후가 같았다.
- 합성 회차 전환 2건을 동시에 실행해 1건만 완료되고 늦은 요청 1건은 오래된 Cycle로 차단되는 것을 확인했다. 운영 배포 후 읽기 전용 감사에서도 `0001~0005` 5개, 세션 77개, Cycle 77개, 누락·잘못 연결된 자료 0건, 회차별 일지 중복 0건이었다. 운영 DB에는 합성 테스트 행을 만들지 않았다.
- 최종 후보는 Vitest 49개 파일 198개 테스트, TypeScript 검사, Next.js 16.3.2 프로덕션 빌드 49개 경로와 Cloud Build를 통과했다. 배포 이미지 digest는 `sha256:deafa659d0b2702c1766860e3756d638a89989be57614f478b9cd2260cac8a7e`이다.
- Cloud Run 리비전 `science-inquiry-platform-00025-p9s`로 한 번 배포하고 트래픽 100%를 확인했다. `/api/health`, 로그인, manifest는 HTTP 200이고 새 Cycle·계획서 AI API의 올바른 비로그인 쓰기 요청은 403이었다. 환경·Secret 참조 17개와 Cloud SQL 연결은 배포 전후 동일하며 새 리비전의 심각도 `ERROR` 이상 로그는 0건이었다.
- 배포 직전 최근 5분간 학생·교사의 쓰기 요청이 0건인 것을 확인하고 전환했다. 실제 OpenAI·Google Sheet·푸시 시험 호출, 학생 계정 로그인, Git 커밋·푸시는 하지 않았다.

## 다음 운영 순서

최신 우선순위(2026-09-08): 아래 기존 순서보다 Astra 전체 구조 검토를 먼저 수행한다. 단일/여러 변인/연쇄 탐구의 동등한 지원, 문서의 오래된 미완료 표기, 빠진 동시 실행·외부 연동 검증을 포함한다. 검토 → 필수 수정 → 검증 → 사용자 배포 지시 순서이며 지금은 추가 배포하지 않는다. 작업별 모델 추천과 인수인계 범위는 `REVIEW_NEXT_STEPS.md`에 기록한다.

1. 공통 Cycle·고정 제출본·학생/교사 계획서 AI 검토·실제 다중 Cycle 전환·중간/최종 분석의 PostgreSQL 복원 검증과 통합 배포를 완료했다. 학생 판단은 필요한 제안만 기록하는 선택 사항이다.
2. 실제 학생 기기에서 외부 사이트 복귀·한글 입력·절전 후 재개, 기존 토큰의 재로그인, Cycle 화면을 확인한다. 학생 자료를 시험 목적으로 만들거나 바꾸지 않는다.
3. 체험 학생 계정의 설치형 앱에서 푸시 허용·공지 수신·알림 클릭 이동·해제를 확인하고, 운영 학생이나 Google Sheet에는 시험 데이터를 남기지 않는다.
4. 다음 개발 과제로 `우리끼리 대화`의 같은 팀 새 메시지 푸시 알림과 메시지별 읽은 팀원 표시를 구현한다. 보낸 학생 제외, 현재 활성 팀원만 대상, 잠금 화면 개인정보 비노출, 제거 팀원의 과거 읽음 기록 보존을 기준으로 한다.
5. 다음 수업 뒤 `openai_usage` 구조화 로그에서 기능별 호출·토큰·웹 검색 횟수·지연을 집계하고 Luna 대화 품질과 Terra 탐구 방향 품질을 교사가 표본 확인한다.
6. `teacher2`로 마스터 계정 화면과 시험 교정본 생성을 1회 확인한다. 넥서스 준비물 Sheet의 별도 시험 탭 쓰기 확인과 실제 연결 v1 발행은 완료했다. 비밀번호를 추측·초기화하지 않는다.
7. 운영 중 발견되는 개선 요청은 학생 자료를 복제하지 않은 재현 환경에서 처리한다.

## 현재 수업 운영 흐름

```text
교사 명단 등록
→ 교사 팀 생성·학생 배정·팀장 지정
→ 학생 최초 로그인·비밀번호 변경
→ 교사 공지 배포·학생 공지함 및 일정 확인
→ 팀 관심사 입력
→ AI 탐구 방향 3개 제안
→ 팀 주제 선택·AI 이론 탐구 대화
→ 팀 탐구 계획서 공동 작성
→ 교사 피드백·승인
→ 준비물 신청·Google Sheets 반영
→ 개인 실험 일지
→ 팀 최종보고서 공동 작성·제출·교사 피드백
→ 교사 진척 대시보드·Excel/CSV 내보내기
→ 시험 생성·교사 검토·A4 출력·수동 채점·학생 결과 공개
→ 자기평가·동료평가 입력·교사 검토·결과 공개
```

## 검증 기록

- 타입 검사 통과
- Vitest 5개 파일, 11개 테스트 통과
- Next.js 16.3.2 프로덕션 빌드 통과
- Cloud Run 배포와 Cloud SQL 연결 통과
- 학생 282명 등록 확인
- 교사 4명 로그인 및 최초 비밀번호 변경 경로 확인
- 학생 카드의 반별 표본 계정 로그인 확인
- 교사가 실제 태블릿에서 로그인과 팀 편성을 확인
- 비로그인 교사 API 접근 403 확인
- 준비물 Google Sheets 구조와 서비스 계정 접근 확인
- OpenAI 주제 제안 및 스캐폴딩 대화는 로컬 라이브 테스트를 통과했으며, 실제 팀 기반 배포 사용은 수업 흐름에서 계속 확인한다.
- 개인 일지 서비스 권한 테스트 통과: 작성자 전용 목록, 다른 학생 사진 차단, 재전송 중복 방지, 제거 학생 접근 차단, 제거 학생 기록 교사 보존
- 개인 일지 추가 후 TypeScript 타입 검사 통과
- 로컬 브라우저 학생 흐름 통과: 일지 자동 임시 저장, 새로고침 후 복구, 사진 첨부·저장, 이전 차시 재열람
- 로컬 브라우저 교사 흐름 통과: 팀원별 개인 일지와 보호된 사진 열람
- 로컬 권한 통합 점검 통과: 작성자 일지 1건, 같은 팀 다른 학생 목록 0건·사진 403, 교사 목록·사진 200, 비로그인 교사 API 403
- 768×1024 태블릿 너비에서 교사 일지 화면의 가로 넘침 없음 확인
- Cloud Run 리비전 `science-inquiry-platform-00005-qbb` 배포 및 100% 트래픽 확인
- 배포 `/api/health` 200, 비로그인 학생 일지·교사 일지·일지 사진 API 모두 403 확인
- 배부용 학생·교사 표본 계정은 `/change-password`로 안내되며, 비밀번호 변경 전 새 일지 API 접근 403 확인
- 최종 리비전에서 심각도 `ERROR` 이상 Cloud Run 로그 없음
- 계획서 승인 상태 변경 안전장치 테스트 추가: 중복 승인 차단, 확인 없는 승인 취소 차단, 잘못된 팀명 차단, 정확한 확인 문구로만 수정 요청 전환
- 승인 안전장치 추가 후 TypeScript 타입 검사, Vitest 7개 테스트, Next.js 16.3.2 프로덕션 빌드 통과
- Cloud Run 리비전 `science-inquiry-platform-00006-92m` 배포 및 100% 트래픽 확인
- 새 리비전 `/api/health` 200, 비로그인 계획서 검토 API 403, 배포 직후 심각도 `ERROR` 이상 로그 없음
- 팀 최종보고서 서비스 테스트 통과: 항목 잠금 충돌, 서로 다른 항목 동시 저장, 필수 항목·팀원 역할 검사, 제출·수정 요청·재제출·확인 완료, 제거 학생 접근 차단과 역할 기록 보존
- 팀 최종보고서 추가 후 TypeScript 타입 검사와 Next.js 16.3.2 프로덕션 빌드 통과
- 로컬 브라우저 학생 흐름 통과: 학교 양식 전체 작성, 팀원별 역할 저장, 제출 상태 전환
- 로컬 브라우저 교사 흐름 통과: 제출 내용·역할 열람, 수정 요청과 보고서 상태 표시
- 768×1024 태블릿 너비에서 교사 보고서 화면의 전체 페이지 가로 넘침 없음 확인
- 배포 소스에서 `.env`·서비스 계정 JSON·교사 계정 파일·출력물을 제외하는 `.gcloudignore` 추가
- Cloud Run 리비전 `science-inquiry-platform-00007-tmm` 배포 및 100% 트래픽 확인
- 배포 `/api/health` 200, 비로그인 팀 보고서 API 403 확인
- 새 리비전에서 심각도 `ERROR` 이상 Cloud Run 로그 없음
- 교사 진척 집계 테스트 통과: 현재 활성 팀원만 일지 인원·차시 집계, 제거 팀원 기록 제외, AI 질문 수·계획서·보고서·준비물 확인 상태 일치
- Excel/CSV 테스트 통과: 학급 필터, 2개 Excel 시트, UTF-8 CSV, 일지 원문 제외, 수식 삽입 문자열 무력화
- 교사 진척 기능 추가 후 TypeScript 타입 검사, Vitest 6개 파일 13개 테스트, Next.js 16.3.2 프로덕션 빌드 통과
- 로컬 브라우저에서 전체·학급·확인 필요 필터, 요약 카드, 팀 표, 내보내기 API 200 응답과 화면 오류 없음 확인
- 로컬 비로그인 내보내기 API 403 확인
- Cloud Run 리비전 `science-inquiry-platform-00008-6xl` 배포 및 100% 트래픽, `Ready=True` 확인
- 새 리비전 시작 프로브 1회 통과, HTTP 200 요청 기록, 심각도 `ERROR` 이상 로그 없음
- 시험 출제 서비스 테스트 통과: 공통 2·팀 1·개인 1 구성, 학생별 동일 범주·총점, 다른 학생 일지 혼입 방지, 제거 학생 제외, 실명·학번 AI 입력 제거
- 시험 확정·PDF 생성·수동 채점·공개 테스트 통과, 공개 전 비노출과 학생별 결과 격리 확인
- A4 한글 시험지 2쪽 샘플을 렌더링해 글꼴·여백·문항 구분·답안 공간을 시각 확인
- 시험 기능 추가 후 TypeScript 타입 검사, Vitest 7개 파일 15개 테스트, Next.js 16.3.2 프로덕션 빌드 통과
- 로컬 브라우저 교사 시험 설정 화면과 학생 시험 결과 탭 확인, 화면 오류 없음
- Cloud Run 리비전 `science-inquiry-platform-00009-8cx` 배포 및 100% 트래픽, `Ready=True` 확인
- 새 리비전 `/api/health` 200, 비로그인 교사·학생 시험 API 403, 심각도 `ERROR` 이상 로그 없음
- 다음 배포부터 로컬 `.pnpm-store`를 업로드하지 않도록 `.gcloudignore`에 추가
- 교사용·학생용 빠른 안내서를 DOCX와 PDF로 생성하고 각 PDF가 정확히 2쪽인지 확인
- PDF 4쪽 전체를 PNG로 렌더링해 한글 글꼴, 표, 여백, 잘림 여부를 시각 확인
- DOCX는 `python-docx`로 열림 여부와 문단·표 구조를 확인했으며, 로컬에 LibreOffice가 없어 별도 이미지 렌더링 검증은 하지 못함
- 준비물 링크 단위 테스트 통과: 공유 문구 URL 추출, G마켓·옥션·11번가·스마트스토어·쿠팡 PC 주소 변환, 변환 불가 모바일 주소 차단, 비웹 스킴 차단
- 로컬 학생 화면에서 G마켓 공유 문구 자동 변환과 변환 불가 모바일 주소 오류 안내 확인. 운영 Google Sheet 제출은 실행하지 않음
- 계획서·보고서 변경 이력 권한 테스트 통과: 일반 팀원·이전 팀장 복원 차단, 현재 팀장·교사 복원 허용, 내용·역할 복구 확인
- 자기평가·동료평가 테스트 통과: 행동 기준 4단계, 예외 사유, 저단계 관찰 근거, 판단 보류 제외, 유효 평가 3건, 교사 검토 익명 의견 공개 확인
- 교사용 공식 도움말 테스트 통과: 공식 항목 검색과 학생 자료 조회·전달·변경 요청 거절 확인
- 이번 배포 후보 전체 TypeScript 타입 검사, Vitest 10개 파일 30개 테스트, Next.js 16.3.2 프로덕션 빌드 통과
- Cloud Run 리비전 `science-inquiry-platform-00010-d7w` 배포 및 100% 트래픽, `Ready=True` 확인
- 새 리비전 `/api/health` 200, 비로그인 교사용 도움말·평가 API와 학생 준비물 API 403 확인
- 운영 DB에서 새 평가 테이블 5개와 문서 변경 이력 테이블이 생성된 것을 읽기 전용으로 확인
- 교사용 학생 체험 계정 4개의 운영 로그인과 최초 비밀번호 변경 화면 진입 확인
- 새 리비전에서 최근 심각도 `ERROR` 이상 Cloud Run 로그 없음
- 교사 인증 배포 후 화면 점검은 현재 교사 비밀번호가 로컬 임시 배부 파일과 일치하지 않아 보류했다. 계정 비밀번호를 추측·초기화하지 않았으며 교사 실사용 계정으로 확인이 필요하다.
- 교사용·학생용 상세 사용서를 DOCX와 PDF로 생성하고, 운영 로그인 화면과 익명 예시 화면만 사용했는지 확인했다.
- 교사용 9쪽과 학생용 7쪽 전체를 PNG로 렌더링해 한글 글꼴, 표, 이미지, 여백, 잘림과 빈 면을 시각 확인했다. 학생용의 불필요한 빈 면은 수정 후 다시 렌더링했다.
- 상세 사용서 접근성 감사 결과 두 문서 모두 고위험 항목 0건이었다. 중간 항목은 실제 데이터 표가 아닌 한 칸짜리 안내 상자의 머리글 권고뿐이며, 데이터 표에는 반복 머리글을 적용했다.
- 상세 사용서 DOCX의 작성자·편집 이력 메타데이터와 임시 식별 속성을 정리했고, 비밀번호·실제 학생 식별정보가 포함되지 않았음을 확인했다.
- 2026-08-29 최종 TypeScript 타입 검사, Vitest 10개 파일 30개 테스트, Next.js 16.3.2 프로덕션 빌드를 다시 통과했다.
- 2026-08-29 최종 소스를 Cloud Run 리비전 `science-inquiry-platform-00011-484`로 다시 배포했고 `Ready=True`, 트래픽 100%를 확인했다.
- 새 리비전의 `/api/health`와 로그인 화면은 HTTP 200, 비로그인 학생 평가·교사 평가·준비물 제출·교사 도움말 API는 403이었으며, 배포 직후 심각도 `ERROR` 이상 로그는 없었다.
- 민감 파일과 체험 비밀번호 제외 검사를 통과한 전체 소스·익명 사용서를 지정 GitHub 저장소의 `main` 브랜치에 푸시했다.
- 2026-08-30 운영 DB에서 활성 학생 286명과 교사 4명의 비밀번호를 영타형 임시 비밀번호로 재발급하고 전원 `must_change_password=true`를 확인했다. 비활성 초기 교사 계정 상태는 유지했다.
- 새 학생 로그인 카드 PDF 36쪽에서 아이디·영타 비밀번호가 각각 282개이고 한글 비밀번호가 0개임을 확인했으며, 첫·마지막 쪽을 개인정보를 가린 상태로 렌더링해 배치·QR·안내문을 확인했다.
- 운영 서비스에서 9개 반 대표 학생, 체험 학생 4명, 교사 4명의 새 임시 비밀번호 로그인과 `/change-password` 이동을 확인했다.
- 임시 자격정보 변환 파일과 DB 프록시·로그인 검증 중간 파일은 검증 직후 삭제했다. 운영 Google Sheet에는 쓰지 않았다.
- 영타형 임시 비밀번호 변경 후 TypeScript 타입 검사, Vitest 10개 파일 31개 테스트, Next.js 16.3.2 프로덕션 빌드를 통과했다.
- Cloud Run 리비전 `science-inquiry-platform-00012-hks` 배포 및 100% 트래픽을 확인했다. `/api/health`와 로그인 화면은 HTTP 200, 비로그인 교사 도움말·학생 평가 API는 403이었으며 배포 직후 심각도 `ERROR` 이상 로그는 없었다.
- 학생 수업 설명용 PPTX 10장을 생성하고 전 슬라이드를 이미지로 렌더링해 제목 줄바꿈, 한글 글꼴, 화면 캡처 가독성, 잘림·겹침을 확인했다. 슬라이드 캔버스 넘침 0건, 발표자 노트와 `[Sources]` 블록 각 10개를 확인했다.
- 전역 토스트 큐, 실제 상태 기반 잠긴 단계 안내, 교사 팀 보관·복원, 내부 건의·문의 게시판 테스트를 추가했다. 학생·비로그인 게시판 차단, 정확한 팀 보관 확인 문구, 보관 자료 보존·학생 접근 차단·복원, 일지의 계획 승인·준비물 신청 이중 조건을 검증했다.
- 운영 DB를 읽기 전용 트랜잭션으로 확인한 결과 체험 학생 4명은 실제 학생과 분리된 안전 팀에만 속하며 학급·활성 팀원·팀장·탐구 세션·1~9반 시트 매핑과 준비물 신청 자격이 모두 유효했다. 운영 Google Sheet에는 쓰지 않았다.
- 데스크톱 1280×900, 태블릿 768×1024, 스마트폰 390×844에서 토스트 안전 배치, 연속 알림, 잠긴 일지 클릭·Enter 안내, 팀 보관 확인 입력, 교사 건의 게시판 반응형 배치를 확인했다.
- 2026-08-30 배포 후보 TypeScript 타입 검사, Vitest 15개 파일 39개 테스트, Next.js 16.3.2 프로덕션 빌드를 통과했다.
- 최종 후보를 Cloud Run 리비전 `science-inquiry-platform-00013-s7z`로 한 번 배포했고 `Ready=True`, 트래픽 100%를 확인했다.
- 새 리비전의 `/api/health`, 로그인 화면, manifest, 192·512 아이콘은 HTTP 200이었다. 로그인과 API에는 `private, no-store`가 적용되었고, 비로그인 교사 팀 변경·건의 게시판·학생 일지 API는 403이었다.
- 운영 로그인 화면을 스마트폰 폭에서 확인해 설치 안내와 manifest 연결, 가로 넘침 없음(본문 너비와 화면 너비 일치)을 확인했다. 새 리비전의 최근 심각도 `ERROR` 이상 Cloud Run 로그는 없었다.
- 민감 파일명·비밀값 서명 제외 검사를 통과한 54개 기능·문서·PWA 변경 파일을 기존 GitHub `main`에 반영했다.
- 2026-08-31 교사 대시보드의 학생 개별 추가·비활성화·복원을 구현했다. 비활성 학생은 로그인과 새 팀 배정이 차단되고 현재 팀 관계·팀장 권한만 종료되며 메시지 등 과거 기록은 보존된다.
- 학생 계정 관리 서비스 테스트 2개를 포함한 Vitest 16개 파일 41개 테스트, TypeScript 타입 검사, Next.js 16.3.2 프로덕션 빌드를 통과했다.
- 로컬 브라우저에서 학생 개별 추가, 임시 비밀번호 1회 표시, 학번 재입력 비활성화 확인, 비활성 목록, 미배정 복원, 390px 화면의 가로 넘침 없음을 확인했다.
- Cloud Run 리비전 `science-inquiry-platform-00014-jk2` 배포 및 트래픽 100%를 확인했다. `/api/health`와 로그인 화면은 HTTP 200, 비로그인 학생 관리 API는 403이었으며 새 리비전의 심각도 `ERROR` 이상 로그는 없었다.
- 2026-08-31 전체·반·팀 공지, 중요 팝업, 학생별 읽음 수, 날짜 공지 달력, 계획서·보고서 수정 요청 알림과 재제출 시 자동 처리 완료를 구현했다.
- 공지 대상 격리·읽음·수정 후 읽음 초기화·보관과 계획서·보고서 처리 알림 테스트를 포함해 Vitest 17개 파일 43개 테스트, TypeScript 타입 검사, Next.js 16.3.2 프로덕션 빌드를 통과했다.
- 로컬 브라우저에서 교사 팀 공지 작성, 학생 중요 팝업·읽음 숫자, 수정 요청 딥 링크, 읽음과 처리 필요 분리, 기간 공지의 달력 양일 표시를 확인했다. 390×844 학생·교사 화면에서 가로 넘침이 없었고, 화면 검증 중 발견한 서버·브라우저 날짜 표기 불일치와 날짜 입력 반영 문제를 수정했다.
- Cloud Run 리비전 `science-inquiry-platform-00015-vdl` 배포 및 트래픽 100%를 확인했다. `/api/health`와 로그인 화면은 HTTP 200, 비로그인 학생 공지·교사 공지 API는 403이었으며 새 리비전의 심각도 `ERROR` 이상 로그는 없었다.
- 운영 PostgreSQL에서 학생 공지 조회의 `SELECT DISTINCT` 정렬식이 선택 목록에 없다는 오류(`42P10`, 화면 오류 `825440413`)를 확인했다. 정렬 순위를 선택 목록의 별칭으로 명시해 공지·읽음 데이터 변경 없이 수정했다.
- 공지 기능 테스트 2개, TypeScript 타입 검사, Next.js 16.3.2 프로덕션 빌드를 다시 통과한 뒤 Cloud Run 리비전 `science-inquiry-platform-00016-5wk`로 핫픽스를 배포하고 트래픽 100%를 확인했다. `/api/health`와 로그인 화면은 HTTP 200이었고 새 리비전의 심각도 `ERROR` 이상 로그는 없었다.
- 2026-08-31 Cloud Run 요청 로그를 학생 입력 없이 집계했다. 팀 대화 450건 중 433건 성공·17건 503, 탐구 방향 제안 25건 중 22건 성공·3건 503이었다. 팀 대화 지연은 평균 6.76초·p50 6.03초·p95 13.93초, 탐구 방향은 평균 14.65초·p50 16.01초·p95 20.06초였다. 503 20건 중 16건은 100ms 이내여서 팀별 AI 잠금에 걸린 중복 요청으로 판단하고, 4건은 2초 이후 실패였다.
- 일반 팀 대화 Luna, 명시적 출처 검색 팀 대화 Terra+웹 검색, 탐구 방향 Terra, 시험 Sol로 기능별 모델을 분리했다. 탐구 방향의 사용되지 않는 웹 검색을 제거하고, 팀 대화는 출처·논문·최신 자료 등을 명시적으로 요구할 때만 웹 검색을 열도록 했다.
- 학생 발화와 팀 식별자를 제외한 `openai_usage`·`openai_failure` 구조화 로그를 추가했다. 기능·모델·입력/출력/추론/캐시 토큰·웹 검색 호출 수·처리시간만 기록하며 관련 Vitest를 포함해 전체 18개 파일 48개 테스트, TypeScript 타입 검사, Next.js 16.3.2 프로덕션 빌드를 통과했다.
- 기존 API 키로 학생 데이터 없는 Luna·Terra·Sol 최소 호출을 시도했으나 `credit_balance_exhausted` 429가 반환되어 모델별 실제 응답 품질 검증은 보류했다. 자동 충전 설정 직후 두 차례 재시도해도 결과가 같았으며, 키 값은 출력하거나 변경하지 않았다.
- 위 모델 라우팅 변경은 공지 핫픽스와 함께 Cloud Run 리비전 `science-inquiry-platform-00016-5wk`에 빌드되었다. 리비전 `Ready=True`, 트래픽 100%, `/api/health`와 로그인 화면 200, 비로그인 팀 대화·탐구 방향 API 403, 심각도 `ERROR` 이상 로그 0건을 다시 확인했다.
- 두 작업의 최종 소스를 다시 검증한 뒤 Cloud Run 리비전 `science-inquiry-platform-00017-r2j`로 명시적으로 배포했다. 전체 18개 파일 48개 테스트, TypeScript 타입 검사, 프로덕션 빌드가 통과했고 리비전 `Ready=True`, 트래픽 100%, `/api/health`와 로그인 화면 200, 비로그인 팀 대화·탐구 방향 API 403, 새 리비전의 심각도 `ERROR` 이상 로그 0건을 확인했다.
- 2026-09-03 결제 반영 후 학생 데이터 없는 최소 요청으로 `gpt-5.6-luna`, `gpt-5.6-terra`, `gpt-5.6-sol`을 확인했다. 세 요청 모두 지정한 실제 모델로 `completed` 응답을 반환했으며 기존 키 값은 출력하거나 변경하지 않았다.
- 2026-09-03 학생 기기별 Web Push 구독·해제, 계정 변경 시 구독 재연결, 전체·반·팀 대상 격리, 만료 구독 자동 제거, 공지·수정 요청 푸시 연결을 구현했다. 잠금 화면 페이로드에는 공지 본문·제목·학생 식별정보를 넣지 않는다.
- 푸시 기능 추가 후 TypeScript 타입 검사, Vitest 18개 파일 50개 테스트, Next.js 16.3.2 프로덕션 빌드를 통과했다.
- 로컬 학생 공지 화면에서 푸시 미설정 안내와 개인정보 보호 문구를 확인하고, 390×844 화면에서 가로 넘침 없음, 서비스 워커 HTTP 200·보안/무캐시 헤더, 비로그인 구독 API 403을 확인했다. 실제 푸시 수신은 운영 VAPID 설정·배포 후 체험 계정 기기에서 확인해야 한다.
- 고정 VAPID 키 쌍을 생성해 `science-platform-vapid-public`, `science-platform-vapid-private` Secret Manager 항목의 버전 1로 저장하고, Cloud Run 서비스 계정에 두 비밀값의 `secretAccessor` 권한만 추가했다. 키 값은 출력하거나 로컬 파일에 저장하지 않았다.
- 푸시 기능 소스를 Cloud Run 리비전 `science-inquiry-platform-00018-lvx`로 배포하고 `Ready=True`, 트래픽 100%를 확인했다. VAPID 공개 키·개인 키는 Secret Manager 버전 1, 연락 주체는 일반 환경값으로 연결되었다.
- 새 리비전의 `/api/health`, 로그인, manifest, 서비스 워커는 HTTP 200이었다. 서비스 워커는 JavaScript 콘텐츠 유형·`no-store`·동일 출처 CSP 헤더를 반환했고, 비로그인 푸시 구독·교사 공지 API는 403이었다. 새 리비전의 최근 심각도 `ERROR` 이상 로그는 0건이었다.
- 배포 후 기존 데이터베이스·세션·교사 초기 계정·OpenAI 비밀값 4개와 새 VAPID 비밀값 2개, Cloud SQL·학년도·Google Sheets 일반 환경 설정이 모두 보존된 것을 값 노출 없이 확인했다.
- 2026-09-03 알림 권한 거절 상태에서도 재로그인 뒤 `알림 다시 켜기` 버튼을 유지하고, 기기 설정 안내와 앱 복귀 시 권한 재확인을 추가했다. 앱은 브라우저·기기가 차단한 시스템 권한 창을 강제로 다시 열지 않는다.
- 재승인 흐름 테스트를 포함해 Vitest 19개 파일 54개 테스트, TypeScript 타입 검사, Next.js 16.3.2 프로덕션 빌드를 통과했다. 로컬 390×844 공지 화면에서 가로 넘침과 브라우저 오류가 없음을 확인했다.
- 개선 소스를 Cloud Run 리비전 `science-inquiry-platform-00019-xc6`로 배포하고 `Ready=True`, 트래픽 100%를 확인했다. `/api/health`, 로그인, manifest, 서비스 워커는 HTTP 200, 비로그인 푸시 구독·교사 공지 API는 403, 새 리비전의 심각도 `ERROR` 이상 로그는 0건이었다. 기존 비밀 연결 6개와 일반 환경 설정 9개도 값 노출 없이 보존을 확인했다.
- 2026-09-03 9반의 `교사용 체험팀` 특수 번호가 일반 팀의 다음 번호 계산에 포함되어 팀 생성 요청이 서버 검증에서 거절되던 문제를 수정했다. 일반 팀은 활성·보관 팀의 1~20번 중 비어 있는 첫 번호를 사용하고, 모두 사용 중이면 추가 버튼을 비활성화한다. 기존 팀·학생 자료는 변경하지 않았다.
- 팀 번호 계산 테스트를 포함해 Vitest 20개 파일 58개 테스트, TypeScript 타입 검사, Next.js 16.3.2 프로덕션 빌드를 통과했다. 운영 교사 화면에서 9반의 `+ 팀 추가` 버튼이 활성 상태임을 읽기 전용으로 확인했으며 실제 팀 생성은 실행하지 않았다.
- 수정본을 Cloud Run 리비전 `science-inquiry-platform-00020-7sw`로 배포하고 트래픽 100%를 확인했다. `/api/health`, 로그인, manifest, 서비스 워커는 HTTP 200, 비로그인 팀 생성 API는 403, 새 리비전의 심각도 `ERROR` 이상 로그는 0건이었다. 기존 비밀 연결 6개와 일반 환경 설정 9개도 값 노출 없이 보존했다.
- 배포 업로드에서 실제 자격정보를 생성·취급하는 교사 계정 생성 스크립트를 제외하도록 `.gcloudignore`를 강화했다.

## 개인정보·보안 주의사항

- 학생 실명·학번은 꼭 필요한 DB와 교사 화면에만 사용한다.
- OpenAI 프롬프트에는 실명·학번을 넣지 않는다.
- 개인 일지는 작성자와 교사만 접근한다.
- 제거된 팀원의 과거 기록은 교사용으로 보존하되 이후 팀 자료 접근은 차단한다.
- 비밀번호 원문은 발급 순간의 배부 자료 외에는 보존하지 않는다.
- `teacher-accounts.txt`, 학생 로그인 카드 PDF, `output/credentials/`의 임시 로그인 파일은 민감한 배부 자료다.
- `.env.local`, 배포 비밀값 파일, 서비스 계정 JSON을 출력하거나 문서에 복사하지 않는다.
- 실제 명단을 다시 가져오기 전에 기존 계정의 비밀번호를 바꾸지 않는 동작인지 확인한다.

## 주요 로컬 파일

- 제품·운영 결정: `implementation_plan.md`
- 최신 확정 결정: `CURRENT_DECISIONS.md`
- 에이전트 작업 규칙: `AGENTS.md`
- 현재 인수인계: `PROJECT_STATUS.md`
- 전체 구조 분석·우선순위·단계별 개선안: `ARCHITECTURE_REVIEW.md`
- 교사 진척 집계: `src/lib/teacher-data.ts`
- Excel/CSV 생성: `src/lib/teacher-export.ts`
- 교사 진척 화면: `src/components/teacher-dashboard.tsx`
- 학생 계정 관리: `src/lib/student-management.ts`, `src/app/api/teacher/students/route.ts`
- 공지·알림 서비스와 화면: `src/lib/notices.ts`, `src/components/notice-center.tsx`, `src/components/teacher-notice-manager.tsx`
- 웹 푸시: `src/lib/push-notifications.ts`, `src/lib/push-client.ts`, `src/components/push-notification-manager.tsx`, `src/app/api/push-subscriptions/route.ts`, `public/sw.js`
- 내보내기 API: `src/app/api/teacher/progress-export/route.ts`
- 시험 출제·공정성 서비스: `src/lib/exam-service.ts`, `src/lib/exam-ai.ts`
- 시험 PDF: `src/lib/exam-pdf.ts`, `src/app/api/teacher/exams/pdf/route.ts`
- 교사 시험 화면: `src/components/teacher-exam-manager.tsx`
- 학생 결과 화면: `src/components/exam-result-panel.tsx`
- 준비물 모바일 링크 처리: `src/lib/material-links.ts`, `src/components/material-form.tsx`
- 평가 서비스·화면: `src/lib/evaluation-service.ts`, `src/components/evaluation-panel.tsx`, `src/components/teacher-evaluation-manager.tsx`
- 문서 이력·복원: `src/lib/document-history.ts`, `src/components/document-history-panel.tsx`
- 교사용 공식 도움말: `src/lib/platform-guide.ts`, `src/components/teacher-help-chatbot.tsx`
- 학생 명단 원본: `C:/Users/user/Desktop/2026. 상당고 자료/교과/과탐실/학생 명단.xlsx`
- 계획서 원본: `C:/Users/user/Desktop/2026. 상당고 자료/교과/과탐실/2026. 과학과제연구 주제탐구 계획서 (1조 2100 강석봉, 2200 한용수 ← 각 조 및 조원 이름으로 변경).hwp`
- 보고서 원본: `C:/Users/user/Desktop/2026. 상당고 자료/교과/과탐실/2026. 동아리 조별 주제탐구 최종보고서 양식 (3100 강석봉, 3200 김정원) ← 팀원 학번, 이름으로 수정.hwp`
- 학생 로그인 카드: `output/pdf/2026-학생-임시-로그인-카드.pdf`
- 넥서스 2학년 임시 로그인 배부 파일: `output/credentials/넥서스-2학년-임시-로그인-2026-09-04.txt`
- 교사용 빠른 안내서: `output/manuals/2026-과탐실-교사용-빠른-안내서.pdf`, `output/manuals/2026-과탐실-교사용-빠른-안내서.docx`
- 학생용 빠른 안내서: `output/manuals/2026-과탐실-학생용-빠른-안내서.pdf`, `output/manuals/2026-과탐실-학생용-빠른-안내서.docx`
- 교사용 상세 사용서: `output/manuals/2026-과탐실-교사용-상세-사용서.pdf`, `output/manuals/2026-과탐실-교사용-상세-사용서.docx`
- 학생용 상세 사용서: `output/manuals/2026-과탐실-학생용-상세-사용서.pdf`, `output/manuals/2026-과탐실-학생용-상세-사용서.docx`
- 학생 수업 설명용 PPT: `output/presentations/2026-과탐실-학생용-사용법-안내.pptx`
- 상세 사용서 생성 스크립트: `scripts/build_detailed_manuals.py`

## 작업 시 주의

- 전체 구현·검증·사용서 제작 완료 후 2026-08-29 로컬 Git 저장소를 초기화했다. 파괴적인 되돌리기 명령을 사용하지 않는다.
- 민감 파일 제외와 스테이징 내용 검사를 통과한 최종 상태를 교사가 지정한 원격 저장소에 푸시했다.
- 학생 명단을 재업로드하면 이미 존재하는 계정의 비밀번호는 다시 표시되지 않는다.
- 학생 비밀번호 전체 초기화나 팀 자료 삭제 같은 대량 변경은 교사의 명시적 승인 없이 하지 않는다.
- 운영 Google Sheet에 테스트 데이터를 쓰거나 실제 팀을 시험 목적으로 생성하지 않는다.
- Cloud Run 배포 전에 로컬 타입 검사·테스트·프로덕션 빌드를 모두 통과시킨다.
