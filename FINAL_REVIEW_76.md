# 최종 종합 점검 — 2026-09-09

> 후속 77차에서 아래 P1을 수정·검증하고 사용자의 새 승인으로 배포했다. 최신 결과는 `DEPLOYMENT_77.md`다. 아래 보류 판정은 수정 전 점검 이력으로 보존한다.

**판정: 배포 보류.** WORK_ASTRA_HANDOFF.md에 따라 현재 미커밋·미추적 작업본의 변경 목록, 핵심 권한·저장 경로, 검증 근거와 배포 포함 파일을 점검했다. 아래 미해결 권한 문제 때문에 로컬 안정화 전체 완료라는 이전 판정을 갱신한다. 모든 변경 파일의 모든 행을 독립적으로 입증한 보안 인증은 아니다.

## 배포 전에 해결할 문제

### P1: 진행 중인 계획서 AI 검토가 권한 회수 후에도 저장·반환됨

`src/lib/plan-ai-review.ts`의 `runReview`는 생성 전 접근 확인 이후 AI 응답을 기다린다. 생성 후 저장 트랜잭션에서는 작업 소유권만 확인하고 현재 사용자·팀 권한을 다시 확인하지 않는다.

별도 PostgreSQL 16 합성 DB에서 학생 요청을 시작한 뒤 생성 콜백 안에서 ① 계정 비활성화 ② 팀 소속 비활성화를 각각 확정했다. 두 경우 모두 진행 중 요청이 검토 결과를 저장하고 호출자에게 반환했다. 다음 새 요청은 거절되므로, 문제는 진행 중 요청의 늦은 완료에 있다. 실제 모델·학생·운영 DB는 사용하지 않았다.

- 증거: `output/test-infra/plan-review-late-access-76.json`, `plan-review-late-access-76.log`.
- 재현 도구: `scripts/local-postgres-plan-review-access.mjs`. 이 도구의 성공은 **결함 재현 성공**이며 제품 정상 판정이 아니다. 최초 재현 시 소속 상태값을 잘못 지정한 시험 코드 오류를 바로잡은 뒤 두 사례를 재실행했다.
- 필요한 수정: 결과 저장과 현재 접근 권한 확인을 같은 잠금·트랜잭션 범위에서 처리하고 기존 권한 변경 경로와 잠금 순서를 맞춘다. 학생·교사 양쪽, 계정/소속/담당 변경, 회차 완료, 기존 결과 재사용 경로를 함께 검증한다. 이 항목은 이번 점검에서 **미수정**이다. 교사·회차 사례는 추가 확인 대상이며 이번 두 사례와 같은 재현 결과로 간주하지 않는다.

## 이번에 보완한 부분

| 항목 | 변경과 확인 |
|---|---|
| 일지 사진 업로드 | 전체 요청을 먼저 읽는 대신 실제 수신량을 최대 26MiB로 제한한다. 잘못되거나 거짓인 길이 선언도 처리하며 초과 시 읽기를 중단한다. 사진 최대 5장·장당 5MiB 규칙 유지. 집중 10개 검사 통과. 최대 크기 정상 입력 검사는 이미지 판별을 대체한 합성 검사다. |
| 추가 탭 접근·저장 | 현재 학생 상태·비밀번호 변경 필요 여부·역할·학생/동아리 학년도를 확인한다. 저장 시 사용자·팀 변경과 잠금을 맞추고 저장 및 감사 기록을 한 트랜잭션으로 처리한다. 과거 응답과 소속 기록을 보존한다. 관련 20개 검사 및 실제 시험용 PostgreSQL의 변경 우선/저장 우선 8개 사례 통과, 잠금 대기 4회 관찰. |
| 동아리 설정 학년도 | 일반 담당 교사와 마스터 모두 현재 학년도 사용자·동아리만 설정을 변경하도록 보완했다. 과거 설정 원문을 보존하는 회귀 검사를 추가했다. |
| 배포 자료 범위 | 검증 스크립트·테스트·운영 문서·합성 사례·에이전트 설정을 업로드/컨테이너 입력 제외 규칙에 추가했다. 별도 빈 gcloud 설정에서 업로드 목록 190개를 확인했고 해당 제외 대상 및 실제 환경 파일이 없었다. 실제 Linux 이미지 빌드는 별도 미실행이다. |

## 검증 증거와 한계

- 최초 전체 회귀: 96개 파일·537개 검사 통과(`final-review-tests-76.log`). 최종 수정 전 결과이므로 아래 수정 후 결과를 대신하지 않는다.
- 수정 후 전체 회귀: **98개 파일·550개 검사 통과**, 572.60초(`final-review-tests-76-verified.log`). 이는 기존 회귀와 이번 보완의 검사 결과이며 위 P1이 해결되었다는 뜻은 아니다. 중단/진행 정체/중복 정리된 `final-review-tests-76-final.log`와 `final-review-tests-76-rerun.log`는 통과 근거에서 제외한다.
- 타입 검사 포함 제품 빌드: 49개 경로 생성 성공(`final-review-build-76.log`). 로컬 `.env.local`을 로드했다는 빌드 안내가 있으므로 자격정보 없는 Linux 빌드 검증으로 확대하지 않는다. 운영 변경은 하지 않았다.
- 최신 패키지 감사: 291개 의존성, 알려진 취약점 전 등급 0건(`dependency-audit-76-retry.json`). 최초 조회는 `fetch failed`로 실패했고 재조회에 성공했다. 애플리케이션 자체의 권한 문제까지 없다는 뜻은 아니다.
- API 정적 목록: 44개 경로, 직접 JSON 파싱 0개·일반 오류 원문 응답 후보 0개(`security-surface-76.json`). 정적 탐지 범위에 한정한다.
- 집중 검사: `journal-upload-budget-76.log`, `custom-tab-access-after-76.log`, 실제 DB `postgres-custom-tab-access-76.json`.

## 실제 사용 조건에서 남은 확인

- E1: 실제 태블릿의 한글 입력, 절전/앱 종료 후 복귀, 여러 기기 충돌, 푸시 수신과 잠금 화면 숨김.
- E2: 승인된 시험용 Google Sheet의 실제 구조, 전송 응답 유실·재시도·영구 실패.
- E3: 실제 AI 응답의 교육 품질. 합성 응답은 이 항목의 통과 증거가 아니다.
- L2: 실제 과거 배포 이미지와 새 DB의 호환성, 운영 백업·복원 가능 여부 및 전환 순서. `RECOVERY_CHECKLIST.md`의 격리 복원·역사 자료 비교·전환 승인 절차를 따른다. 구버전 앱만 되돌리면 안전하다고 가정하지 않는다.

## 다음 순서

1. 위 P1을 수정하고 권한 회수와 늦은 완료의 동시 처리 검사를 통과시킨다.
2. 최종 변경본의 전체 회귀·타입 포함 빌드와 실제 Linux 배포 산출물을 확인한다.
3. E1~E3 및 L2 외부 조건의 증거를 확보하고 구체적인 배포·복구 계획을 제시한다.
4. WORK_ASTRA_HANDOFF.md의 지시에 따라 새 배포 승인을 받은 뒤 배포한다. 이후 로그인·역할별 접근·기존 기록 보존·연동 상태를 확인한다.

이번 작업에서는 운영 학생 자료·운영 Sheet를 시험에 사용하지 않았고 실제 AI·푸시 호출, 배포, Git 초기화·커밋·푸시를 하지 않았다.

검증에 사용한 로컬 전용 PostgreSQL은 종료했다. 최종 변경 파일 목록과 SHA-256은 `output/test-infra/final-review-inventory-76.json`에 보관한다.
