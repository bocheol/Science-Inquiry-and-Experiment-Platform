# 계획서·보고서 이전 버전 비교 — 새 Work 인수인계서

작성일: 2026-09-11

## 1. 요청과 현재 단계

사용자 요청: 학생용·교사용 대시보드의 계획서와 보고서에서 이전 버전을 선택하면 최신 버전과 달라진 내용을 빨간색·굵은 글씨 등으로 알아보기 쉽게 표시하고 싶다.

현재 단계는 **설계·구현을 이어가기 위한 인수인계**다. 이 문서를 작성하며 관련 소스를 읽었으며, 비교 기능의 제품 코드·API·DB는 아직 변경하지 않았다. 새 Work나 별도 작업은 생성하지 않았다.

사용자가 동의한 대화 방향은 기존 변경 이력을 활용하는 공통 읽기 전용 비교 화면이다. 아래 세부 UI와 알고리즘은 권장 설계이며 모든 세부사항이 개별 확정됐다는 뜻은 아니다. 새 작업은 이 기준으로 구체적인 설계를 제시하고, 사용자의 후속 요청 범위에 따라 구현한다.

## 2. 새 작업에 붙여 넣을 시작 요청

> 과탐실 플랫폼의 학생·교사용 계획서·보고서 이전 버전 비교 기능을 설계해 주세요. DOCUMENT_VERSION_DIFF_HANDOFF.md를 읽고 현재 소스와 대조해 구현안을 구체화해 주세요. 같은 탐구 회차의 최신 서버 저장본과 선택한 과거 기록을 비교하고, 변경된 항목을 먼저 보여 주며 추가·삭제·수정 부분을 색과 글꼴로 표시하는 모바일 우선 공통 화면이 목표입니다. 미저장 초안, 기존 복원 권한, 제출본 및 완료 회차 기록은 보존해야 합니다. 인수인계서의 권장안을 사용자 확정 요구와 구분하고, 기존 기능과 겹치는 부분을 활용해 주세요.

코드에 접근할 수 없는 Work라면 이 문서와 필요한 소스 파일을 함께 제공해야 한다. 문서만 보고 실제 구현이나 검증이 끝났다고 판단하지 않는다.

## 3. 작업 기준과 최근 완료 상태

- 로컬 작업 폴더: `[비공개 운영 기록에 보존]`
- 시작할 때 `AGENTS.md`, `PROJECT_STATUS.md` 전체와 `CURRENT_DECISIONS.md`를 확인한다. `implementation_plan.md`는 회차·문서 보존 및 양식 버전 관련 절부터 읽는다.
- Next.js 16.3.3이다. 코드 작성 전 설치된 `node_modules/next/dist/docs/`의 관련 가이드를 확인한다.
- 마지막 배포 기록(2026-09-10): `[비공개 운영 기록에 보존]`, Ready=True, 트래픽 100%. 이번 문서 작업에서 클라우드 상태를 새로 조회하지는 않았다. 근거는 `DEPLOYMENT_79.md`다.
- 공개 주소: [비공개 운영 기록에 보존]
- 직전 기능은 대화·개인 일지의 `지난 기록 보기`이며 운영 반영 완료다. 이번 문서 비교는 별도 기능이다.
- 직전 제품 검증은 전체 100개 파일·568개 테스트 및 타입·50경로 빌드 통과다. 비교 기능 검증 결과로 재사용하지 않는다.
- 작업 폴더에는 기존 수정·미추적 파일이 많다. 현재 Git HEAD만으로 운영 소스나 최신 기능을 재구성하면 안 된다. 관련 파일의 기존 변경을 보존한다.
- `CURRENT_TASK.md`는 2026-08-30의 오래된 작업 설명이다. 최신 의도·배포 판단은 상태 문서 상단과 이 인수인계서를 우선한다.
- 이번 요청은 인수인계서 작성이다. 앞선 `지난 기록 보기` 배포 승인을 새 비교 기능의 배포 승인으로 확대하지 않는다. Git 초기화·커밋·푸시 제한도 유지한다.

## 4. 소스에서 확인한 현재 구조

| 위치 | 확인한 역할과 구현 시 유의점 |
| --- | --- |
| `src/components/document-history-panel.tsx` | 학생·교사 공통 변경 이력 UI. 목록의 작업·작업자·시각과 복원 버튼만 제공한다. 과거 본문 조회·차이 표시 UI는 없다. |
| `src/lib/document-history.ts` | `document_revisions`에 변경 전 전체 스냅샷을 저장하고 이력 조회·복원을 처리한다. `getDocumentHistory`는 최근 30건의 메타데이터만 반환하고 원문은 반환하지 않는다. 이 함수 자체에는 사용자 권한 인자가 없다. |
| `src/components/plan-editor.tsx`, `report-editor.tsx` | 학생 계획서·보고서에서 공통 이력 패널 사용. 현재 팀장 여부로 복원 버튼을 제어한다. |
| `src/components/teacher-team-review.tsx`, `teacher-report-review.tsx` | 교사 계획서·보고서의 공통 이력 패널 진입점. 완료 회차는 복원을 막는다. |
| `src/lib/inquiry-data.ts` | 계획·보고서 및 해당 회차 이력 데이터를 화면에 연결한다. 보고서 `form_data`에 `report_fields` 값을 합친다. 조회 과정에 보고서 행 생성·만료 잠금 정리도 있으므로 새 읽기 전용 API에서 무심코 전체 함수를 재사용하지 않는다. |
| `src/lib/plan-snapshots.ts` | 계획서 저장본·제출본의 별도 고정 스냅샷, 양식·회차 정의와 내용 해시를 관리한다. 변경 이력과 제출본은 동일한 개념이 아니다. |
| `src/lib/cycle-workflow.ts` | 회차 종료 시 `cycle_completed` 문서 이력을 남긴다. 다음 회차에서 기존 문서 행을 재사용하므로 문서 ID만으로 비교 범위를 정하면 안 된다. |
| `src/lib/document-cycle.ts` | 쓰기·복원에 필요한 활성 회차 잠금/검사. 완료 회차 읽기까지 막는 권한 함수로 사용하지 않는다. |
| `src/components/read-only-form-fields.tsx`, `read-only-cycle-document.tsx`, `cycle-analysis-panel.tsx` | 읽기 전용 필드·표·완료 회차 고정 문서 표시 참고. 원문 보기 및 완료 회차 접근 동선에 활용할 수 있다. |
| `src/app/api/inquiry/plan/route.ts`, `report/route.ts` | 학생 저장·제출·복원 API. 현재 이 파일들에는 과거 본문 비교용 GET이 없다. |

스냅샷의 실제 차이:

- 계획서 변경 이력은 `formData`, `reviewStatus`, `teacherFeedback`를 저장한다. 일반 변경 이력마다 당시 양식 정의를 저장한다고 가정하지 않는다.
- 보고서 변경 이력은 `formData` 외에 `report_fields` 병합 결과, 상태·교사 피드백, 팀원 역할(`userId`, `description`), 선택적인 `configDefinition`을 포함한다.
- `document_revisions`는 `document_type`, `document_id`, `cycle_id`로 구분한다. 이전 회차와 현재 회차를 섞지 않는다.
- 이력의 시각·작업자는 해당 변경 작업을 기록한 시각·행위자다. 저장된 스냅샷은 주로 **그 작업 직전 상태**이므로 그 사람이 해당 과거 문장 전체를 작성했다고 표시하면 안 된다. 실제 제출 번호가 아닌 임의 `v1/v2` 번호도 만들지 않는다.

## 5. 권장 사용자 경험

1. 기존 변경 이력의 항목을 선택하면 `최신 저장본과 비교`를 열고 `당시 내용 보기`로 전환할 수 있게 한다. `이 상태로 복원`은 별도 버튼으로 유지한다.
2. 상단에 문서 종류, 팀·탐구 회차, 선택한 과거 이력의 시각·유형, 비교 대상의 저장 시각과 `이전 → 최신` 방향을 표시한다. 시각은 KST로 일관되게 표시한다.
3. 달라진 항목을 기본으로 보여 주고 `전체 항목 보기`를 제공한다. 상단에는 규칙 기반으로 `변경 3개: 연구 목적·실험 과정·팀원 역할`처럼 요약한다.
4. 추가는 초록색·굵게, 삭제는 빨간색·취소선, 수정은 이전/최신 내용을 대응시켜 표시하는 안을 권장한다. 색 외에도 `추가`, `삭제`, `수정` 문구와 구역 제목을 사용한다.
5. 모바일에서는 항목별로 `이전 내용`과 `최신 내용`을 위아래로 표시한다. 넓은 화면의 좌우 비교는 같은 데이터 모델을 사용한다.
6. 바뀐 단어나 구절만 강조하고 긴 본문 전체를 색칠하지 않는다. 차이가 없으면 `선택한 버전과 최신 저장본의 내용이 같습니다`를 표시한다.
7. 비교를 닫으면 작성 위치와 미저장 초안이 유지되어야 한다. 비교 열기/닫기/갱신으로 자동 저장·제출·복원·잠금 획득을 실행하지 않는다.

## 6. 비교 기준과 처리 범위

### 첫 구현에 권장하는 기준

- 같은 팀·문서 종류·회차의 선택 이력과 **조회 시점의 최신 서버 저장본**을 비교한다. 브라우저 미저장 초안은 제외하고 그 사실을 표시한다.
- 비교 중 팀원이 새로 저장해도 표시 중인 기준을 몰래 바꾸지 않는다. 반환된 저장 버전/시각/해시를 유지하고 `최신 내용으로 다시 비교`로 명시적으로 갱신한다.
- 완료 회차는 해당 회차의 최종 고정본과 비교한다. 다음 회차의 현재 문서 행을 기준으로 사용하지 않는다. 종료 이력·고정 스냅샷의 실제 보존 경로를 확인하고, 기준을 찾을 수 없으면 `비교 기준 자료가 없습니다`를 표시한다.
- 문서 내용과 보고서의 팀원 역할을 비교한다. 상태·교사 피드백의 변화는 본문 수정과 구분한다. 작성자·수정자 표시를 근거로 개인 기여도를 계산하지 않는다.
- 과거 기록 부족, 손상된 JSON, 양식 정의 부재를 빈 문서로 대체해 `모두 삭제됨`으로 오판하지 않는다. 가능한 원문은 남기고 비교 한계를 표시한다.

### 아직 세부 확정이 필요한 부분

- 교사 제출본 검토에서 `최신 제출본과 비교`를 첫 묶음에 포함할지 여부. 권장 첫 단계는 최신 저장본 비교이며 제출본 비교는 별도 선택지로 설계한다. 보고서가 계획서와 동일한 제출 스냅샷 테이블을 가진다고 가정하지 않는다.
- 모바일의 인라인 펼침/별도 상세 화면 선택. 기본 권장안은 기존 이력 안의 펼침형이다.
- 최근 30건 제한을 그대로 안내할지, 페이지를 더 읽을 수 있게 할지 여부. 전체 역사 열람을 약속한다면 현재 30건 제한을 해소해야 한다.
- 완료 회차의 과거 이력 진입점: 현재 학생 완료 문서는 편집기를 대체하는 별도 읽기 화면이므로 기존 패널만 수정하면 접근되지 않을 수 있다.

위 항목은 새 작업에서 현재 코드와 사용자 동선을 보고 제안할 설계 선택지다. 이미 결정된 기본 요구를 반복 확인하는 이유로 사용하지 않는다.

## 7. 구현 권장안

### 조회 서비스와 API

- 기존 스냅샷을 재사용하는 읽기 전용 서비스와 GET API를 추가한다. 경로 예: `/api/document-history/compare` — 제안 이름이며 현재 존재하는 API가 아니다.
- 입력은 문서 종류·문서 ID·회차 ID·이력 ID와 명시적인 기준 종류로 제한한다. 서버가 이력이 실제 해당 문서·회차에 속하는지 확인한다.
- 학생은 현재 활성 계정·학년도·팀 소속과 기존 문서 열람 조건을 확인한다. 일반 팀원의 비교 열람과 팀장 전용 복원은 구분한다.
- 교사는 기존 계획서·보고서 **열람** 권한을 따른다. 담당·마스터만 가능한 설정/승인 권한이나 79차 `지난 기록 보기`의 목록 제한을 문서 열람에 그대로 복사하지 않는다. 기존 공동 열람과 관리 권한을 소스·회귀 테스트로 대조한다.
- 사용자 권한 확인 없이 `getDocumentHistory` 또는 이력 ID 조회 결과를 API에 노출하지 않는다. 다른 팀·회차의 원문이나 존재 여부가 새 응답으로 드러나지 않게 한다.
- 두 비교 대상과 메타데이터는 일관된 DB 읽기 시점에서 가져온다. 쓰기 잠금을 오래 점유하지 않는다. 회차 전환 중 서로 다른 회차 데이터가 합쳐지지 않도록 검증한다.
- `private, no-store`, 안전한 오류 문구를 적용하고 본문을 URL·로그·외부 AI·분석 서비스에 넣지 않는다. 전체 과거 원문을 대시보드 초기 응답에 모두 싣지 말고 선택한 기록만 지연 조회한다.

### 비교 엔진과 렌더러

- 계획서·보고서 공통 순수 비교 함수와 공통 읽기 전용 컴포넌트를 권장한다. AI 호출 없이 두 저장 내용을 직접 비교한다.
- 양식 필드 ID로 항목을 대응시킨다. 과거/현재 필드 합집합을 사용해 삭제된 항목도 남긴다. 라벨 변경과 내용 변경을 구분한다.
- JSON 객체의 키 순서 차이는 변경으로 세지 않는다. 숫자 `0`과 미작성, boolean, 배열, 표, 문자열의 차이를 자료형에 맞게 처리한다. 저장 방식이 다른 JSON 문자열은 해당 필드 정의를 근거로 해석하고 무조건 빈 배열로 변환하지 않는다.
- 한글 본문은 단어/공백 단위 비교 후 바뀐 구간을 글자 단위로 세분화하는 방식을 검토한다. 유니코드 문자·이모지를 중간에서 자르지 않으며 줄바꿈과 원문은 보존한다.
- 표는 열 ID와 실제 행 식별자 유무를 먼저 확인한다. 안정적인 행 ID가 없으면 행 추가를 모든 다음 행의 수정으로 오판하지 않도록 내용 기반 정렬/대응을 검토한다. 중복 행처럼 대응이 모호하면 행 추가·삭제로 보수적으로 표시한다.
- 보고서 팀원 역할은 사용자 ID로 대응하고 이름이나 현재 팀원 배열의 순서로 맞추지 않는다. 제거된 팀원의 과거 역할을 삭제하지 않는다.
- HTML 문자열 삽입 대신 React 텍스트와 강조 요소로 렌더링한다. 매우 긴 입력은 계산량에 상한을 두고 항목 단위 이전/최신 표시로 전환해 브라우저 멈춤을 막는다.
- 새 DB 스키마 없이 구현 가능한지 먼저 확인한다. 과거 양식 정의가 없는 문제를 해결하기 위해 과거 자료를 추정해서 일괄 덮어쓰지 않는다.

## 8. 검증과 완료 기준

- 실제 학생 자료 대신 합성 자료를 사용한다.
- 텍스트 추가·삭제·수정·동일 내용, 한글·공백·줄바꿈·이모지·긴 본문, 숫자·확인값·다중 선택, 표의 행/열 추가·삭제·이동·중복, 필드 추가·삭제·이름 변경을 검증한다.
- 보고서 `report_fields` 병합과 역할 변경을 놓치지 않는지 확인한다. 과거 형식·잘못된 JSON·스냅샷 없음도 검증한다.
- 학생 일반 팀원/팀장, 제거·이동·비활성·보관·학년도 변경, 교사 공동 열람/담당/마스터의 기존 범위, 비로그인·다른 팀 이력 ID 요청을 검증한다.
- 같은 문서 ID의 다른 회차 이력, 완료 회차와 다음 회차, 계획서 저장본과 제출본을 혼합하지 않는지 확인한다.
- 비교 조회 전후 문서·이력·제출·승인·역할이 불변인지 확인한다. 조회를 위해 기존 쓰기 함수를 호출하지 않는다.
- 320~390px 휴대폰, 태블릿, PC에서 색·굵기·취소선·라벨·줄바꿈·표·긴 문장과 키보드 열기/닫기, 포커스 복귀를 직접 확인한다.
- 비교를 열고 닫거나 다른 기록을 선택한 뒤에도 미저장 초안·기기 복구 초안·편집 위치가 유지되는지 검증한다. 느린 응답이 새 선택 결과를 덮어쓰지 않아야 한다.
- 관련 기존 회귀 후보: `src/lib/document-history.test.ts`, `src/lib/document-cycle-regression.test.ts`, `src/lib/document-save-regression.test.ts`, `src/lib/teacher-review-access.test.ts`, `scripts/editor-regression.mjs`, `scripts/document-cycle-browser-regression.mjs`.
- 구현 후 필요한 테스트, `pnpm typecheck`, 전체 회귀와 `pnpm build`를 실행하고 결과·미확인 범위를 기록한다. 문서 작성만 한 이번 작업에서는 제품 테스트를 재실행하지 않았다.
- `PROJECT_STATUS.md`와 `CURRENT_DECISIONS.md`를 갱신한다. 배포 여부는 새 기능에 대한 후속 사용자 지시를 따른다.

## 9. 새 작업의 첫 산출물

현재 데이터로 가능한 비교 기준, 공통 화면의 간단한 배치, 변경할 파일/API, 권한·회차·미저장 초안 보존 방식, 필요한 검증을 담은 구현안을 제시한다. 이후 구현 요청을 받으면 계획서·보고서와 학생·교사 네 경로를 한 묶음으로 연결하고 검증한다.
