# DB 복구 확인 절차

> 78차 최신 운영 버전은 00030-vet, 직전 정상 트래픽 버전은 00028-reh이다. 계획서 AI 지침 v2만 추가 변경했고 DB 스키마·연결 설정은 바꾸지 않았다. 77차 성공 백업과 Linux 합성 복원 검증을 유지한다. 이번에 실제 운영 백업 복원 훈련을 새로 수행하지 않았으며 `FINAL_REVIEW_78.md`에 명시했다.

> 77차 배포 준비 추가 확인(2026-09-09): 운영 추가 백업 1788924598987 SUCCESSFUL. 실제 00027-hvc 이미지의 별도 합성 DB를 백업·복원하고 새 배포 이미지 적용/구버전 재시작/신버전 재시작 뒤 기존 53개 테이블의 기존 열·행 해시 보존을 확인했다. 성공 Build 034497f8-8db5-4a2e-8884-3afe7a84f961, `DEPLOYMENT_77.md` 참조. 실제 운영 백업 복원 훈련·전체 기능 동시 쓰기·운영 규모 복구 시간·PITR/IAM/네트워크 복구의 미검증 범위는 유지한다. 현재 배포는 00028-reh다.

2026-09-09 / 후속 63차. 운영 복구 실행 승인이 아니라, 검증 결과와 이후 복구 준비 절차다.

## 이번에 확인한 것

- PostgreSQL 16.15의 별도 합성 DB를 custom 형식으로 백업하고 다른 새 DB에 한 트랜잭션으로 복원했다.
- public의 58개 테이블 전체 행을 정규화하여 개수와 내용 해시로 비교했다. 빈 테이블도 포함하지만 아래 중요 자료는 실제로 채운 뒤 검사했다.
- 승인 계획서 고정본/제출 기록, 확인 보고서/당시 역할, 과거 팀 관계, 확정 시험/공개 시험 결과, 공개 평가 피드백, 메시지·읽음·푸시 대기열·마이그레이션을 보존했다.
- 517개 열·283개 제약조건·129개 인덱스를 비교했다. public의 시퀀스/함수/사용자 트리거/뷰는 0개였으므로 이 종류의 비어 있지 않은 객체 복원을 입증한 것은 아니다.
- 별도 프로세스에서 현재 앱을 복원 DB로 시작한 뒤에도 모든 테이블/구조가 같았다. 실제 서비스로 공개 시험/평가를 조회하고 새 메시지를 저장했다. 저장으로 바뀌어야 하는 대화 관련 테이블 외 자료와 원래 역사 행을 비교했다.
- 원본 합성 DB는 검증 종료 시에도 바뀌지 않았다. 실제 학생/운영/Google Sheet/AI/푸시에는 접근하지 않았다.

증거: `output/test-infra/postgres-backup-restore-63.json`, `backup-restore-63-final.log`. JSON의 `backupFile`과 `backupSha256`은 이번 합성 백업을 식별한다. 백업에는 합성 로그인 해시/구독 키도 들어 있으므로 배포 산출물로 사용하지 않는다.

## 같은 로컬 검증을 재실행할 때

1. `scripts/local-postgres.ps1 start`로 표식이 있는 로컬 전용 PostgreSQL을 시작한다. 127.0.0.1:55416 및 `codex_local` 조건이 다르면 진행하지 않는다.
2. 프로젝트에서 다음 검증 도구를 실행한다. 원본/복원 DB를 각각 새로 만들며 기존 DB를 삭제하거나 덮어쓰지 않는다.

   `node --experimental-transform-types --import ./scripts/local-review-test-loader.mjs scripts/local-postgres-backup-restore.mjs`

3. 종료 코드와 최종 JSON의 `restoreExact`, `structureExact`, `appBootstrapPreserved`, `restoredReadsAndWritePassed`, `sourceUnchanged`를 확인한다. 일부 출력만 보고 전체 성공으로 간주하지 않는다.
4. 검증 연결이 모두 닫힌 뒤 `scripts/local-postgres.ps1 stop`으로 종료한다.

비밀번호는 로컬 전용 자격 파일에서 읽어 하위 도구 환경에만 전달한다. 명령행·로그·문서에 복사하지 않는다.

## 운영 복구를 준비할 때의 순서

1. **사고 시점과 복구 목적을 확정한다.** 코드만 되돌릴 것인지, DB의 특정 시점으로 복원할 것인지 구분한다. 현재 버전·마이그레이션 번호·보존해야 할 최신 제출/결과를 먼저 기록한다.
2. **현재 상태도 별도로 보관한다.** 이전 백업으로 덮어쓰기 전에 사고 후 자료를 포함한 현재 DB를 보존한다. 사용할 백업의 시각·무결성·접근 권한을 확인한다.
3. **별도 격리 환경에 먼저 복원한다.** 실제 AI/Sheet/푸시·정기 작업을 끈다. 운영 DB 이름이나 연결 문자열을 로컬 검증 도구에 넣지 않는다. 실제 권한과 서비스 연결은 별도 운영 절차로 확인한다.
4. **복원본과 사용할 앱 버전의 호환성을 검사한다.** 원문/관계/확정·공개 결과/설정 버전·예약·영수증 및 스키마를 비교한다. 구버전 앱을 새 스키마에 연결해도 안전하다고 가정하지 않는다. 구버전/미래 마이그레이션 공존 검증은 아직 별도 미완료다.
5. **사용자 승인 후 전환한다.** 쓰기 중단 구간, 복구 시점 이후 자료의 처리, 접속 전환 방법과 되돌릴 기준을 구체적으로 정한 뒤 승인받는다. 운영에 합성 쓰기나 자동 시험 전송을 하지 않는다.
6. **전환 후 확인한다.** 읽기 전용 상태·로그인·권한·기존 문서/결과를 확인한다. 외부 연동 재활성화 전 예약·영수증과 푸시 대기열을 점검해 오래된 전송을 무심코 재실행하지 않는다. 장애 시 원본 보관본/전환 계획에 따라 처리한다.

## 아직 입증하지 않은 범위

### 앱과 DB 버전이 다를 때 (64차 로컬 보강)

현재 작업본은 앱 초기화 잠금 안에서 기본 스키마/초기 자료를 쓰기 전에 적용된 마이그레이션 전체를 읽는다. 알 수 없는 번호 또는 알려진 번호의 이름/내용 해시 불일치는 초기화를 거절한다. 마이그레이션 직접 실행도 시작 전과 각 변경의 잠금 안에서 다시 검사한다. 기존 0001~0011의 정의는 수정하지 않았다.

이 검사가 없는 과거 배포 이미지에 보호 기능이 소급 적용되지는 않는다. 이미 초기화를 끝낸 프로세스도 요청마다 DB 버전을 다시 검사하는 구조는 아니다. 따라서 앱만 과거 이미지로 전환하거나, 호환성을 확인하지 않은 구·신 버전이 함께 쓰도록 두지 않는다. 별도 복원본에서 실제 사용할 이미지의 읽기/쓰기와 보존 결과를 검증하고 전환 순서·구버전 종료 조건을 정해야 한다.

64차 메모리 DB 회귀에서 미래 번호를 무시하는 기존 동작을 재현했고, 수정 후 새 DB 무변경 검사/알 수 없는 번호 거절/마이그레이션 이력 불변/정상 적용·재실행·실패 후 재시도를 확인했다.

65차는 실제 PostgreSQL 16.15에서 새 자식 프로세스의 getDb로 6개 사례를 검증했다. 정상 시작 전후, 미래 번호, 해시 변경, 이름 변경, 원복 후 정상 시작, 초기 구조 없는 미래 DB가 대상이다. 각 거절은 두 번 재시도해 캐시/풀 정리를 확인하고, 자식 종료 후 다른 연결에서 초기화 잠금을 얻을 수 있었다. 정상 구조의 58테이블 전체 행 개수/해시·517열/283제약/129인덱스가 시작 전후 같고, 초기 구조 없는 경우 마이그레이션 표 1개 외 초기 테이블/계정이 생기지 않았다. 임시 합성 변경을 원복한 전체 상태도 처음과 같았다. 풍부한 확정/공개 역사 자료의 복원 검사는 63차 증거와 구분한다.

증거: `output/test-infra/compatibility-postgres-65.log`, `postgres-compatibility-65.json`. 표식이 있는 로컬 DB를 시작한 뒤 `node --experimental-transform-types --import ./scripts/local-review-test-loader.mjs scripts/local-postgres-compatibility.mjs`로 재실행하고 종료한다. 운영 연결을 받는 도구가 아니며 기존 DB를 삭제하지 않는다. 실제 과거 배포 이미지의 호환성과 구·신 프로세스 공존은 아직 별도 검증 대상이다.

Cloud SQL 자체 백업/PITR, 사용자·권한/IAM·Secret Manager·네트워크 복구, 큰 운영 데이터의 복구 시간, 복구 중 쓰기, 손상 백업, 운영 공급자 연동은 이번 로컬 검사로 보장하지 않는다. 운영 환경의 백업/복원 명령과 대상은 실제 환경 확인 및 승인 없이 추정해 실행하지 않는다.
