# 두 버전 비교 배포 결과 — 2026-09-11

**구현·배포 완료.** 사용자의 “제품 구현 및 배포 가자” 지시에 따라 지정 worktree의 제품을 기존 Cloud Run 서비스에 반영했다.

사이트: [비공개 운영 기록에 보존]

## 제공 기능

- 같은 팀·문서 종류·탐구 회차 안에서 A/B를 독립 선택한다. 최신 저장본↔과거뿐 아니라 과거↔과거, 역방향 비교도 가능하다.
- 학생·교사의 계획서·보고서 변경 이력과 완료 회차 기록에서 같은 비교창을 연다. 기본은 변경 항목이며 전체 항목·원문도 볼 수 있다.
- 추가는 녹색·굵기·밑줄, 삭제는 붉은색·취소선, 수정은 양쪽의 삭제·추가와 수정 배지로 표시한다. 한글·결합 이모지 단위 문장 비교, 표 행/셀, 저장된 사용자 ID에 따른 팀원 역할을 처리한다.
- 모바일에서는 A/B를 세로로 표시한다. 날짜 검색은 접힌 상태로 시작하고, 이력은 30개 커서 페이지로 계속 탐색한다. 긴 문서는 계산 예산을 넘으면 원문을 보존한 항목 비교로 전환한다.
- 현재 저장본은 조회한 상태로 고정한다. 다시 선택할 때 내용이 달라졌으면 409로 명시적으로 다시 불러오도록 안내한다.
- 비교창은 기존 편집기 위에서 열린다. 비교 자체는 저장·복원·제출 요청을 보내지 않으며 학생 초안과 교사 피드백 초안을 유지한다. 기존 복원 버튼·권한·미저장 방어는 유지한다.
- 제출본은 기존 제출본으로 보존한다. 교사의 검토 화면에 고정된 제출 내용과 비교창의 현재 서버 저장본을 혼동하지 않도록 별도 조회한다. 제출본을 새 버전 종류로 추가하는 확장은 이번 범위에 포함하지 않았다.

## 소스와 보존

공통 화면은 `document-version-comparison.tsx`, `document-diff-view.tsx`; 조회 서비스는 `document-version-reader.ts`; 순수 비교는 `document-diff.ts`다. 새 GET 경로는 `/api/document-history/versions`, `/api/document-history/compare`이며 모두 인증과 private/no-store 응답을 적용한다.

서비스는 같은 PostgreSQL 반복 읽기·읽기 전용 트랜잭션에서 양쪽 기록을 조회한다. 양쪽 기록의 문서·회차를 검증하며 조회 전후 활성 계정·현재 팀원·세션 버전을 다시 확인한다. 학생의 활성 회차 보고서 접근 조건과 기존 교사의 공동 검토 접근 범위를 유지한다.

보고서의 현재 내용은 form_data에 report_fields를 우선 반영한다. 제거된 팀원의 저장된 역할도 남긴다. 완료 회차는 문서 행이 그 회차에 남아 있으면 그 행, 다음 회차로 전환된 경우에는 해당 회차의 cycle_completed 기록을 사용한다. AI 분석의 익명 역할을 원래 사용자로 추정하지 않는다.

DB 스키마·마이그레이션·기존 저장/제출/복원 서비스 변경은 없다. 잘못된 원문은 빈 문서로 바꾸지 않고 확인 필요와 원문으로 표시한다. 옛 양식 정의가 없으면 현재 고정 항목명은 참고임을 알리고 알 수 없는 키·값을 보존한다.

## 검증

- 전체 회귀: **102개 파일·580개 테스트 통과**. 새 순수 비교/API 테스트 12개 포함. 병렬 수를 2로 제한해 완료했다.
- 실제 PostgreSQL 16.15: **11개 검증 통과**. 과거/현재 조합, 동일 마이크로초 시각의 65개 기록 누락·중복 없는 탐색, 날짜·커서 경계, 보고서 필드 우선순위, 제거된 팀원의 역할, 문서/회차 바꿔치기 차단, 활성 보고서 조건, 보관 팀 권한, 조회 무변경, 두 종류의 완료 보존본, 잘못된 원문, 조회 도중 팀원 권한 회수를 포함한다.
- 최종 프로덕션 빌드: TypeScript 통과, **52개 경로** 생성. 업로드 이후 201개 제품 입력 파일의 해시 변경 0개.
- 최종 Chrome 합성 브라우저 검증: 학생 계획서/보고서와 교사 공통 화면 통과. 360px 모바일 및 320/768px 오버플로 확인, 1280px 교사 화면, 과거↔과거 선택·교환·강조, Escape/뒤로 가기, 재접속 초안 복구, 현재 저장본 409/명시적 새로고침, 교사 피드백 초안, 문서 저장 요청 0건, 완료 회차 진입을 확인했다.
- 배포 전 0% 리비전과 배포 후 공개 주소에서 각각 **11개 점검 통과**. health/login/manifest/sw는 200, 새 비교 GET 2종과 기존 관리/AI/지난 기록 경로의 미인증 요청은 403.
- 새 리비전 ERROR 이상 로그는 2026-09-11 10:49:57 KST 조회 시 0건. 이는 확인 시점의 결과다.

증거: `output/test-infra/document-version-vitest-limited.log`, `document-version-postgres.json`, `document-version-browser.json`, `document-version-build-final.log`, `document-version-upload-manifest.json`, `deploy-stage-80.json`, `deploy-staged-smoke-80.json`, `deploy-activate-80.json`, `deploy-live-smoke-80.json`, `deploy-error-count-80.json`.

## 운영 반영

- Cloud Build: `[비공개 운영 기록에 보존]`, SUCCESS
- 입력: 제품 파일 201개, 약 1.4 MiB. output/테스트/스크립트/문서/환경·비밀 파일 제외, 실제 비밀키 패턴 후보 0개.
- 이미지: `[비공개 운영 기록에 보존]`
- 리비전: `[비공개 운영 기록에 보존]`, Ready=True, 트래픽 100%, 임시 태그 제거 완료.
- 이전 정상 리비전: `[비공개 운영 기록에 보존]`. 이미지 외 런타임 spec 해시 동일.

## 중간 문제와 확인 범위

초기 전체 테스트와 빌드의 동시 실행에서 자원 부족으로 테스트 제한 시간이 초과되어 해당 테스트 프로세스만 종료한 뒤 2 workers로 전체 통과했다. 합성 fixture의 inactive 상태·report_fields 컬럼·화면 항목명 및 승인된 계획서의 피드백 진입을 실제 소스에 맞게 고쳤다. 최종 재빌드는 로컬 검증 서버가 standalone 폴더를 점유해 한 차례 실패했고, 해당 서버를 종료한 뒤 성공했다. 업로드 감사의 password 이름 규칙은 정상 비밀번호 제품 소스를 잘못 분류하여 실제 제외 경로와 비밀키 패턴 검사로 수정했다.

0% 배포의 Google API 완료 대기 중 DNS 연결이 끊겼다. 재배포하지 않고 실제 서비스 조회로 00036-juh의 Ready, 기존 00033-kow 100%, 신규 태그 0%, 이미지와 런타임 동일성을 확인해 배포 기록을 복구했다. 이후 점검·전환·태그 제거가 정상 완료됐다.

실제 학생 원문이나 운영 계정으로 기능 테스트하지 않았고 Google Sheet·외부 푸시·AI에 시험 자료를 전송하지 않았다. 실제 iOS/Android 기기의 터치·키보드와 매우 큰 운영 이력의 검색 성능은 이번 합성 Chrome 검증 범위를 벗어난다. Git 초기화·커밋·푸시는 하지 않았다.
