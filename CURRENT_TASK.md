# 완료된 작업: 사용성·교사 운영 보완 및 최종 배포

마지막 갱신: 2026-08-30

완료 상태: 2026-08-30 구현·검증·운영 배포 완료. 운영 리비전과 Git 반영 결과는 `PROJECT_STATUS.md`를 따른다.

## 시작할 때

1. `AGENTS.md`, `CURRENT_DECISIONS.md`, `PROJECT_STATUS.md` 전체를 읽는다.
2. `implementation_plan.md`에서는 팀·준비물·일지·보고서·교사 대시보드 관련 절만 읽는다. 충돌이 있을 때만 전체를 읽는다.
3. `git status`와 `git diff`를 확인하고 아래 미완성 변경을 삭제하거나 덮어쓰지 않는다.

## 반드시 보존할 미배포 변경

- 설치형 웹앱(PWA): manifest, 192/512/마스커블/Apple 아이콘, 로그인 화면의 기기별 설치 안내
- 인증 화면과 API의 `no-store` 헤더
- PWA 관련 테스트와 아이콘 생성 스크립트
- 학생 수업 설명용 PPT 제작 사실을 반영한 `PROJECT_STATUS.md` 변경
- 운영 Cloud Run은 기존 리비전 `science-inquiry-platform-00012-hks`, 트래픽 100% 상태다. 새 버전 배포는 사용자 요청으로 중단했다.

## 구현 요구사항

### 1. 전역 결과 알림

- 주요 버튼의 서버 작업이 성공하거나 실패하면 자동으로 사라지는 토스트를 표시한다.
- 예: `탐구 방향이 설정되었습니다.`, `준비물이 신청되었습니다.`
- 일정 행 추가, 준비물 품목 행 추가처럼 화면 안에서 행만 늘리는 동작에는 토스트를 표시하지 않는다.
- 기존 인라인 오류와 중요한 경고는 접근성상 필요하면 유지한다.
- 연속 작업의 알림이 서로 덮이지 않도록 처리하고 키보드·스크린리더·모바일 배치를 고려한다.

### 2. 아직 열 수 없는 단계 안내

- 학생이 잠긴 실험 일지·팀 보고서 등의 버튼이나 탭을 누르면 현재 부족한 조건과 다음 행동을 토스트로 알려 준다.
- 특히 실험 일지는 탐구 계획 승인과 준비물 신청 완료 여부를 실제 상태에서 확인해 정확히 안내한다.
- 화면 안내가 기존 단계·권한 및 서버 검사를 우회해서는 안 된다.

### 3. 교사의 팀 보관·복원

- 교사만 팀을 정리할 수 있게 한다.
- 하드 삭제하지 않고 보관/비활성화하여 과거 팀 자료, 제거 팀원의 기록, 감사 가능성을 보존한다.
- 기본 팀 목록에서는 보관 팀을 숨기고, 교사가 별도 목록에서 확인·복원할 수 있게 한다.
- 보관 전 명확한 확인 절차와 서버 역할 검사를 둔다.
- 운영 실제 팀을 자동화 시험 대상으로 보관하지 않는다.

### 4. 제작자에게 기능 건의 및 문의

- 교사 대시보드에 내부 공동 게시판 형태로 구현한다. 외부 AI나 외부 서비스로 전송하지 않는다.
- 모든 교사가 내역을 보고 새 글을 등록할 수 있어야 한다.
- 작성자 교사, 작성 시각, 분류, 제목, 내용, 처리 상태를 저장한다.
- 학생 개인정보·비밀번호·학생 자료를 입력하지 말라는 화면 안내와 서버 검증을 둔다.
- 학생과 비로그인 사용자의 접근을 서버에서 차단한다.

### 5. 체험 학생의 준비물 신청 연결 확인

- 운영 DB에서 체험 계정의 학급·팀·팀장·탐구 세션·준비물 신청 자격·반별 시트 매핑을 먼저 읽기 전용으로 확인한다.
- 운영 Google Sheet에는 시험 데이터를 쓰지 않는다.
- 체험 계정만의 문제라면 실제 학생과 충돌하지 않는 안전한 체험 팀/반 매핑으로 준비물 신청을 사용할 수 있게 한다.
- 실제 학생에게도 공통인 문제라면 공통 원인을 수정한다.
- 자격정보와 학생 식별정보를 문서·소스·최종 응답에 노출하지 않는다.

## 검증과 마무리

- 기능별 DB·서비스·화면·권한 테스트를 추가한다.
- 데스크톱·태블릿·스마트폰에서 토스트와 교사 화면을 확인한다.
- 타입 검사, 전체 테스트, 프로덕션 빌드를 통과시킨다.
- 의미 있는 구현·검증 후 `PROJECT_STATUS.md`와 `CURRENT_DECISIONS.md`를 갱신하되 비밀번호·키·학생 식별정보를 쓰지 않는다.
- PWA와 위 기능이 모두 끝난 뒤 기존 Cloud Run 서비스에 한 번만 배포한다.
- 운영 주소, manifest, 아이콘, 보호 API, 새 기능 권한, 오류 로그를 검증한다.
- 운영 Google Sheet에 시험 데이터를 남기지 않는다.
- 배포 전에는 커밋·푸시하지 않는다. 최종 검증 후 민감 파일 제외를 확인하고 기존 `main` 원격에 반영한다.
