# Astra 전체 구조 검토 결과

> **78차 최종 판정:** 계획서 AI의 문서 기재 → 학생 이해도 단정 표현을 지침 v2로 보완하고 실제 문제 사례 3회 등 6회 재호출했다. 최종 9종·11답변 표본은 채점 기준 통과. 집중 회귀 24개/49경로 빌드 및 실제 Sheet 부분 7항목 통과. 00030-vet Ready/100%, 배포 전후 각 8개 검사 통과. E2 운영 계정 종단 인증은 IAM 권한 부족으로 미완료, 운영 백업 복원 훈련/새 전체 실기기 시나리오는 제한 유지. 최신 상세는 `FINAL_REVIEW_78.md`.

> **77차 최신 판정: P1 수정 및 승인된 배포 완료.** 전체 563개·실제 DB 20개·49경로 빌드·Linux/실제 이전 이미지 합성 보존 검사 통과. 운영 00028-reh Ready/100%, 추가 백업 성공, 배포 전후 각 8개 공개 상태/권한 검사 통과, 직후 ERROR 이상 0건. 실제 AI 9종 주요 동작을 합성 표본으로 정성 확인했다. E1·E2 및 반복 품질/운영 복원 훈련은 남는다. `DEPLOYMENT_77.md`가 최신 결과이며 아래 76차 보류는 수정 전 이력이다.

76차 최종 실행 확정: **98개 파일·550개 검사 통과**(`final-review-tests-76-verified.log`, 572.60초). 알려진 P1은 별도 재현 도구에서 확인한 미해결 결함으로, 전체 회귀 통과와 구분한다. 로컬 시험용 PostgreSQL 종료 완료.

> **후속 76차 최종 종합 점검: 배포 보류.** 진행 중인 계획서 AI 검토가 계정 비활성화/팀 소속 해제 후에도 저장·반환되는 P1을 실제 시험용 PostgreSQL에서 2개 사례로 재현했다. 아직 미수정이며 이전의 로컬 안정화 전체 완료 판정을 대체한다. 일지 수신 크기 제한, 추가 탭 현재 권한/동시 저장, 동아리 설정 학년도, 배포 제외 규칙은 보완했다. 집중 10개/20개와 실제 DB 8개 통과, 타입 포함 49개 경로 빌드, 최신 의존성 감사 291개 취약점 0건. 수정 후 전체 회귀 결과는 최종 보고서에 기록한다. E1~E3·L2 외부 확인은 남아 있다. 상세 재현·증거·범위와 다음 순서는 `FINAL_REVIEW_76.md`를 우선한다. 운영·실제 AI/Sheet/푸시·배포·Git 미변경.

> 후속 75차 L6 대화 알림/읽음 경계 마감: 새 팀원은 과거 대화 원문을 열람하되 과거 메시지 수신·읽음 대상으로 소급되지 않고 이후 메시지부터 포함된다. 메시지 뒤 등록한 구독은 과거 대기열에 추가되지 않으며, 목적지 검사 중 구독 소유자·키 변경은 지문 재검사에서 발송을 건너뛴다. 집중 2개 파일·16개와 타입 포함 49개 경로 빌드/공백 검사 통과(discussion-boundaries-75.log, build-75.log). 제품 코드 변경 없이 회귀 검사만 보강했다. 실제 기기/공급자 확인은 E1, 최종 종합 점검은 Work Astra에 남긴다.

> 후속 74차 L5 의미·범위 마감: 같은 학급·동아리 내 팀 이동 학생의 과거 동료평가를 현재 팀 진행률·평균·공개 의견·공개 대기 판정에서 제외하고 교사용 역사 원문은 보존했다. 회차 AI 근거에는 공식 준비물 신청을 추가하고 체험 신청은 제외했다. 이전 회차는 저장 분석과 실제 저장 판단만 사용하며 누락 판단을 추정하지 않는 범위를 확인했다. 이동 평가 13개, 의미 범위 17개, 학년도/직접 접근 137개, 시간 초과 묶음 재실행 28개, 안정적 전체 96개 파일·536개 및 타입 포함 49개 경로 빌드/공백 검사 통과. 운영/외부 호출 없음. 실제 모델 품질 E3와 최종 종합 점검은 Work Astra에 맡긴다.

> 후속 73차 L4 AI 품질 평가 준비: 실제 AI·학생 자료 없이 사용할 5항목 평가표와 합성 사례 9개를 만들었다(AI_QUALITY_EVALUATION.md, AI_QUALITY_CASES.json). 단일 회차/여러 변인/연쇄 탐구/판단 없음/검증하지 않은 최적값/삽입 지시/개인정보·위험/학생 대필/교사 판단 경계를 포함하며 필수 사례·형식 검증 통과(ai-quality-cases-73.json). 제품 코드·운영·외부 호출 없음. 실제 응답 채점 E3와 최종 종합 점검은 Work Astra에 맡긴다.

> 후속 72차 L3 비AI 오류 비노출 완료: 준비물/시트 전송 상태·일지/사진·푸시·공지·계정/팀/동아리 설정·회차·건의·도움말·PDF까지 명시한 정상 안내만 표시하고 예상 밖의 DB/외부 서비스 오류 원문은 일반 문구로 대체한다. Google Sheets 오류 원문도 sync_error에 남기지 않는다. 준비물·일지·푸시 집중 15개 파일·89개, 전체 95개 파일·534개, 타입 포함 49개 경로 빌드/공백 검사 통과(non-ai-errors-71.log, full-tests-72.log, build-72.log). API 44개 정적 목록의 직접 JSON 0/일반 Error.message 후보 0(security-surface-72.json). 운영/외부 호출 없음. 정적 0을 전체 보안 인증으로 확대하지 않으며 다음은 L4다. 최종 종합 점검은 Work Astra에 맡긴다.

> 후속 70차 L3 문서 오류 비노출: 계획서·보고서 저장/제출/잠금과 교사 검토/복원에서 UserFacingError로 명시한 정상 안내만 표시하고, 예상하지 못한 DB 오류의 연결 문자열·비밀값은 일반 재시도 문구로 대체한다. FormWriteConflict도 같은 명시 오류 계층에 포함했다. 합성 비밀값 비노출/충돌 안내 보존, 집중 5개 파일·14개, 전체 95개 파일·532개, 타입 포함 49개 경로 빌드/공백 검사 통과(document-errors-70.log, full-tests-70.log, build-70.log). 일반 Error.message 정적 후보는 26개에서 19개로 감소(security-surface-70.json). 운영/외부 호출 없음. 나머지 비AI 후보는 계속 로컬 정리하고 최종 종합 점검은 Work Astra 직전에 넘긴다.

> 후속 69차 L3 AI 오류 비노출: 명시적 UserFacingError만 화면에 통과시키고 알려진 OpenAI 상태 안내는 유지했다. 학생 대화/탐구 방향, 학생·교사 계획서 AI 검토, 회차 AI 분석의 조치 가능한 안내를 표시하되 예상하지 못한 일반 오류의 연결 문자열/비밀값은 일반 재시도 문구로 대체한다. 학생 회차 판단 API도 같은 규칙 적용. 집중 11개, 전체 94개 파일·530개·타입 포함 49개 경로 빌드/공백 검사 통과(ai-error-safety-69.log, full-tests-69.log, build-69.log). 운영/외부 호출 없음. 비AI 일반 오류 후보는 다음 로컬 작업이며 최종 전체 점검은 Work Astra에 맡긴다.

> 후속 68차 L3 입력 제한: 33개 API/42곳의 직접 request.json을 공통 1MiB 실제 스트림 제한기로 전환. 길이 헤더 유무/거짓과 무관하게 초과 즉시 취소, 잘못된 JSON/UTF-8 안전 거절, 인증 후 읽기 유지. 집중 8개, 전체 93개 파일·528개·타입 포함 49개 경로 빌드/공백 검사 통과(request-body-route-68.log, full-tests-68.log, build-68.log). 정적 재검사에서 Route Handler 직접 request.json 0건(security-surface-68.json). 기존 명단 2MiB multipart 별도 유지. 운영/외부 호출/배포 없음. 내부 Error.message 비노출은 다음 로컬 작업이며 최종 전체 점검은 사용자 지시에 따라 Work Astra에 맡긴다.

> 후속 67차 L3 보안 패치: 기존 다운로드 실패는 SheetJS CDN의 Node 인증서 신뢰 차이로 확인하고 시스템 신뢰 저장소 사용으로 해결했다(검증 비활성화 없음). Next 16.3.3/하위 sharp 0.35.4 고정 설치, Dockerfile에 workspace 설정 포함. 291개 재감사 권고 0건(dependency-audit-67.json), 실제 버전/합성 PNG·분리된 frozen lock 검사, 전체 90개 파일·520개·타입 포함 49개 경로 빌드/공백 검사 통과. 이전 SheetJS 고정 유지. 운영/외부 호출/배포 없음. 입력·오류 처리 및 기타 원래 잔여는 미완료이며 감사 0건을 전체 안전 인증으로 표현하지 않는다.

> 후속 66차 L3 감사: 잠금 파일 온라인 감사 critical 2/high 1(Next 16.3.2 Windows/AVIF, 선택 sharp 0.35.3 libheif)을 공식 권고와 대조했다. Next 16.3.3/sharp >=0.35.4 갱신 필요. 설치 시도는 저장소 지정 이후에도 다운로드 실패로 중단돼 현재 버전 유지/미수정이다. SECURITY_REVIEW.md에 권고·환경 조건·실패 로그·후속 검증을 기록했다. 정적 API 44개 중 JSON 읽기 33개/일반 오류 문구 후보 26개를 목록화했으며 입력 제한/오류 구분 구현은 미완료. 제품·운영 미변경, 전체 520개/49개 경로 기준 유지. 다음은 보안 패키지 다운로드 진단과 수정이며 전체 목표는 활성 유지.

> 후속 65차 L2 실제 PostgreSQL(2026-09-09): 새 자식 프로세스의 실제 getDb로 정상 전후/미래 번호/해시·이름 불일치/초기 구조 없는 미래 DB 6개 통과(compatibility-postgres-65.log, postgres-compatibility-65.json). 거절 두 번 후 초기화 캐시 없음, 58테이블 전체 행·517열/283제약/129인덱스 불변, 구조 없는 경우 표 1개만 유지, 잠금 해제 및 정상 상태로 원복 후 시작 확인. 초기 합성 DB 검사이며 확정/공개 역사 복원 증거는 63차와 구분한다. 제품 미변경, 전체 520개/49개 경로는 64차 유지. 로컬 DB 종료/운영·외부 미변경. 실제 과거 이미지 공존/운영 복구는 남기고 다음은 L3 독립 감사다.

> 후속 64차 L2 미래 DB 방어(2026-09-09, 로컬): 모르는 마이그레이션을 무시하는 실패를 재현한 뒤 기본 스키마/seed 전 읽기 전용 호환 검사 및 마이그레이션 잠금 아래 재검사를 적용했다. 기존 번호 정의 불변. 새 DB 무변경·미래 번호/내용 불일치 거절·이력 보존·getDb 실패 시 쓰기 미실행/연결 정리 검증. 집중 12개, 최종 전체 90개 파일·520개(full-tests-64-final.log), 타입 및 49개 경로 빌드/공백 검사 통과. 첫 전체의 초기화 mock export 누락 1개는 보완 후 재실행 통과로 구분한다. 실제 PostgreSQL 초기화 전후 불변 및 과거 이미지 공존은 아직 미완료. 과거/이미 초기화된 앱에 보호가 소급되지 않음을 RECOVERY_CHECKLIST.md에 기록했다. 운영/외부 호출/배포 없음.

> 후속 63차 L2 합성 복구(2026-09-09): 중요 역사·확정/공개 결과·새 대화 기록을 채운 실제 PostgreSQL 백업→별도 DB 복원에서 58개 테이블 전체 행과 517열/283제약/129인덱스 일치. 현재 앱 초기화 불변/결과 읽기·새 쓰기/역사 행·원본 DB 보존 확인(backup-restore-63-final.log, postgres-backup-restore-63.json). RECOVERY_CHECKLIST.md 작성. 제품/운영 미변경, 전체 검사 기준 62차 유지. L2는 일부 완료이며 구버전/미래 스키마 공존·운영 복구 조건은 남는다.

> 후속 62차 L1 연결 검증(2026-09-09, 로컬): 공지/대화에 검증된 DNS 결과를 소켓으로 직접 전달하는 목적지별 HTTPS agent를 적용했다. 공용→사설 변화·혼합/직접 사설 주소·예상 호스트 불일치 거절과 일시 오류 구분, 실제 HTTPS의 로컬 수신 연결 0건, 양쪽 발송 agent 전달/공지 연결 오류 처리를 검증했다. 새 10개 포함 최종 전체 90개 파일·516개·타입 포함 49개 경로 빌드/공백 검사 통과(full-tests-62.log/build-62.log). TLS 검증 유지/외부 발송·운영 배포 없음. L1은 로컬 완료로 갱신하고 실제 공급자/기기 수신은 E1에 남긴다. 다음 L2 전체 복원/구버전 경계 및 전체 잔여 범위 유지.

> 후속 61차 L1 공지 DNS 오류(2026-09-09, 로컬): 일시 DNS 오류/빈 응답에서 정상 구독 삭제 2개를 재현하고 명백히 안전하지 않은 주소만 삭제하도록 수정했다. 구독 전체 행 보존/재시도 성공/사설 주소 거절 새 3개, 집중 31개·전체 89개 파일 506개·타입 포함 49개 경로 빌드/공백 검사 통과(notice-dns-before-61.log, notice-dns-after-61.log, full-tests-61.log, build-61.log). 실제 DNS/푸시·운영 배포 없음. 실제 연결 단계 DNS 주소 변화 방지는 아직 남아 L1 일부 완료로 기록한다.

> 후속 60차 전체 잔여 대조(2026-09-09): 현재 완료 조건/순서는 `REMAINING_WORK.md`에 정리했다. 원래 1~9번/F01~F17/G01~G07을 유지하며 로컬 미완료 L1~L6와 외부·기기 E1~E3를 구분했다. 기존 공지 푸시의 일시 DNS 실패 시 구독 삭제 경로는 다음 재현 대상이다. 새 확정 오류나 전체 완료로 판정한 것은 아니다. 제품/운영 미변경, 전체 검사는 58차 기준이다.

> 후속 59차 인수인계 7번 실제 대기열 검증(2026-09-09, 로컬): 두 활동 × 동시 처리/이어받기 전후/구독 변경/제거/갱신 후 만료 응답/재시도/횟수·시간 상한 PostgreSQL 18개 통과. 0011 재실행 불변, 오래된 소유자의 늦은 실패가 완료 상태를 덮어쓰지 않음, 갱신 구독/원문 보존 확인(discussion-push-postgres-59.log, postgres-discussion-push-59.json). 저장 성공 후 after 예약/예약 실패 복구/저장 실패 미예약 및 인증 정기 경로 집중 9개 통과(discussion-push-route-59.log). 제품 미변경으로 전체 502개/49개 경로 기준은 58차 유지. 합성 DB 종료/실제 외부 발송 없음. 공급자 exactly-once·실제 기기 수신 및 기타 원래 잔여 범위 완료를 주장하지 않는다. 다음은 전체 요구별 잔여 근거 재대조다.

> 후속 58차 인수인계 7번 대화 알림(2026-09-09, 로컬 구현): 0011 구독 지문 고정 outbox·응답 후 발송·인증된 정기 경로 재시도 연결. 당시/현재 소속·구독 소유/키·계정/체험/읽음 검사, 일반 안내만 전송, 임대/소유권·최대 시도/유효 기간·안전 주소/일시 실패 구분 적용. 합성 14개(초기 fixture 학급 누락 정정), 최종 집중 16개·전체 88개 파일 502개·타입 포함 49개 경로 빌드/공백 검사 통과(discussion-push-58-final.log, full-tests-58.log, build-58.log). 실제 PostgreSQL 0011/발송 임대 경합·공급자/기기 검증은 남는다. 외부 성공 응답 유실 시 정확히 한 번 발송은 보장하지 않으며 동일 tag로 표시 중복을 줄인다. 운영 미배포/실제 발송·스케줄 변경 없음. 새 기능과 기존 안정화 잔여를 구분하고 전체 완료로 간주하지 않는다.

> 후속 57차 인수인계 7번 실제 읽음 검증(2026-09-09, 로컬): 두 활동 × 전송/읽음 × 제거/저장 선행 PostgreSQL 8개(실제 잠금 대기 4개), 0010 재실행 불변, 제거/재가입 후 옛 수신·읽음·원문 보존 확인. Chrome 768×1024 합성 화면의 활성 패널/스크롤/대면 탭/발신자 명단 6개 통과. 최초 캡처 경로 오류는 검증 도구에서 정정 후 새 합성 DB로 재실행했다(discussion-read-race-57.log, postgres-discussion-read-race-57.json, discussion-read-browser-57-final.log 및 PNG). 도우미/합성 DB 종료, 제품 미변경으로 전체 488개/49개 경로 기준은 56차 유지. 실제 기기 숨김·절전/새 수신자 추가 경합·푸시 연결 등은 남고 운영 미배포/외부 발송 없음.

> 후속 56차 인수인계 7번 읽음 연결(2026-09-09, 로컬): 본인·해당 회차·당시 소속의 현재 활성 상태를 잠금 아래 검사하는 read API, 발신자만 받는 읽음 명단, 활성/보이는 대화 화면에서 교차한 메시지만 읽음 전송, 본인 메시지 읽음 n/N명 표시를 연결했다. 원문/AI 근거와 읽음 메타데이터를 분리했다. 중복 시각 보존/제거·재가입 경계/교사 대리 거절/회차와 목록 제한 검증 및 전체 87개 파일·488개, 타입 포함 49개 경로 빌드/공백 검사 통과(full-tests-56.log/build-56.log). 실제 PostgreSQL 새 경합·브라우저 표시 검증, 푸시 연결은 남는다. 운영 미배포/외부 발송 없음, 전체 잔여 범위 유지.

> 후속 55차 인수인계 7번 수신 관계 기반(2026-09-09, 로컬): 메시지별 읽음/알림에 필요한 수신 당시 소속 ID 저장을 0010 및 peer 저장 트랜잭션에 추가했다. 과거 메시지 자동 수신자 생성 없음, 발신자 제외, 재전송 후 수신 관계 불변, 탈퇴/재가입의 소속 ID 구분 검증. 집중 15개·전체 87개 파일 487개·타입 포함 49개 경로 빌드/공백 검사 통과(discussion-recipients-55.log, full-tests-55.log, build-55.log). 읽음 API/화면/알림 발송은 아직 연결되지 않았고 실제 PostgreSQL 새 경합 및 마이그레이션 검증도 남는다. 운영 미배포/외부 발송 없음. 기존 전체 잔여 범위 유지.

> 후속 54차 G06 과거 평가자 기록(2026-09-09, 로컬): 과탐실/동아리에서 평가 제출 후 평가자 제거의 두 통합 검사 추가. 제거 전 평가의 교사 열람·유효 3건 집계/검토 의견 공개를 유지하고, 제거 학생의 새 제출은 403 및 현재 진척/공개 대상 제외, 기존 평가 전체 행/종료 소속 이력 불변을 확인했다. 최초 열린 평가에서 의견 검토를 시도한 합성 상태 오류 2개는 정정했으며 최종 관련 15개와 타입/공백 검사 통과(evaluation-history-54-final.log, typecheck-54.log). 제품 미변경/미배포. 이동한 평가 대상·잘못 연결된 과거 응답 전체를 검증한 것은 아니며, 다음 대화 알림·읽음 및 기존 잔여 범위는 계속 남는다.

> 후속 53차 G02 시험 생성 중 원문 변경(2026-09-09, 로컬): 합성 생성기 실행 중 계획서/보고서/역할/일지를 바꿔도 팀 문항과 학생별 개인 문항의 출처 키·라벨·인용문이 처음 사용한 내용 그대로 보존되는 새 통합 검사 통과. 기존 계획 고정본/보고서 이력 전체 행 불변과 변경된 일지 저장 유지도 확인했다. 관련 2개 파일·8개(exam-source-during-53.log), 타입 검사(typecheck-53.log), 공백 검사 통과. 제품 변경/배포 없음. 전체 검사·빌드는 51차 기준을 유지하며 새 검사 1개는 집중 실행 결과로 구분한다. 준비 도중의 다중 자료 변경·동아리 추가 탭까지 모두 검증한 것은 아니다. 다음 평가 집계/과거 팀원 기록 및 기존 전체 잔여 범위를 계속 확인한다.

> 후속 52차 G02 시험 생성자 실제 경합(2026-09-09, 로컬): 두 활동의 계정/비밀번호/담당 변경 × 시작/생성 대기/변경 선행/저장 선행 실제 DB 20개와 설정 발행 잠금 회귀 4개 통과(exam-teacher-race-52.log, postgres-exam-teacher-race-52.json, exam-config-race-52.log). 저장 선행 5개의 실제 잠금 대기, 시작 거절 시 호출/작업 0건, 늦은 저장 거절 시 failed/시험 미생성, 정상 저장 시 completed/당시 시험 보존을 확인했다. 권한 변경 후 재요청 거절·추가 호출 없음·기존 시험/소속 불변도 검증했다. 제품 미변경이므로 전체 테스트/빌드는 51차의 483개/49개 경로 기준이다. 합성 DB 정상 종료, 운영 미배포. 원문 근거 변화 및 기존 전체 잔여 범위는 유지한다.

> 후속 51차 G02 시험 생성자 권한(2026-09-09, 로컬): 교사 중지·비밀번호 초기화·담당 해제 후 생성/저장/완료 결과 재사용 13개를 재현했다. 시작 전 권한 검사와 저장 시 생성자·담당 관계 잠금 아래 재검사를 적용했다. 새 17개에서 시작 시 생성 호출 차단/늦은 저장 거절/기존 시험과 소속 보존·마스터 권한 유지 확인. 최종 전체 86개 파일·483개, 타입 포함 49개 경로 빌드/공백 검사 통과(exam-teacher-before-51.log, exam-teacher-after-51-final.log, full-tests-51.log/build-51.log). 이번은 테스트 DB 검증이며 실제 PostgreSQL 동시 권한 변경 순서는 다음 대상이다. 운영 미배포, 전체 잔여 범위 유지.

> 후속 50차 G02 설정/시험 잠금(2026-09-09, 로컬): 시험 저장과 이미 발행한 설정의 재발행 요청 사이에서 버전→동아리/동아리→버전 잠금 역전으로 실제 PostgreSQL 교착 상태 1건을 재현했다. 발행을 동아리→버전 순서로 통일하고 새 발행/재발행 × 양쪽 순서 4개에서 교착 상태 0건·정상 시험 저장·재발행의 정상 거절을 확인했다(exam-config-before-50.log/exam-config-after-50.log 및 postgres-exam-config-before-50.json/after-50.json). 시험의 시작 시 설정 버전 ID와 원래 definition을 보존한다. 전체 85개 파일·466개/타입 포함 49개 경로 빌드·공백 검사 통과(full-tests-50.log/build-50.log). 합성 DB 정상 종료, 운영 미배포. 생성자 권한과 원문 근거 변화 및 기존 전체 잔여 범위는 유지한다.

> 후속 49차 G02 시험 대상 확대(2026-09-09, 로컬): 생성 중 빈 팀/새 팀에 학생이 추가된 두 누락 상황을 재현하고 전체 활동 범위의 부모→세션→학생→팀 잠금 및 활성 수험 팀 집합 검사를 적용했다. 학생 없는 새 팀은 정상 허용한다. 집중 31개, 실제 DB 대상 확대 양쪽 순서 12개와 기존 변경 회귀 20개, 전체 85개 파일·466개 및 타입 포함 49개 경로 빌드/공백 검사 통과(exam-scope-after-49.log, exam-scope-race-49.log, exam-roster-race-49.log, full-tests-49.log/build-49.log). 합성 DB 정상 종료, 운영 미배포. 생성자 권한·동아리 설정 발행과 저장/근거 경계 및 기존 잔여 범위는 계속 검증한다.

> 후속 48차 G02 시험 명단 실제 경합(2026-09-09, 로컬): 두 활동 × 다섯 명단 변경 × 양쪽 선행 순서 20개 통과(exam-roster-race-48-final.log, postgres-exam-roster-race-48.json). 저장 선행의 실제 잠금 대기 10개, 변경 선행의 409/시험 미생성, 저장 선행의 당시 시험지·문항 보존과 계획/보고서/소속 이력 보존을 확인했다. 초기 합성 문서 컬럼 오류는 form_data로 정정했으며 최종 검증과 구분한다. 제품 코드 변경 없이 검증 도구만 추가했으므로 전체 테스트/빌드는 47차의 464개/49개 경로 기준이다. 합성 DB 정상 종료, 운영 미배포. 시작 시 비어 있던/신규 팀의 대상 포함 및 생성자 권한·근거 변경 등 잔여 범위는 계속 남는다.

> 후속 47차 G02 시험 생성 명단(2026-09-09, 로컬): 최초 5개 실패는 합성 팀 번호 중복으로 정정했다. 번호 수정 후 재검사 호출을 제외한 별도 검증 복사본에서 생성 중 제거·중지·이동·추가/팀 보관 5개가 시험 저장으로 이어짐을 확인했다(exam-roster-before-47-verified.log). 수정본은 AI 호출 후 세션→학생→팀 잠금 아래 준비된 팀의 현재 명단을 재검사하고 변경 시 409로 거절한다. 집중 61개, 최종 전체 85개 파일·464개와 타입 포함 49개 경로 빌드/공백 검사 통과(exam-roster-after-47-verified.log, full-tests-47-verified.log/build-47.log). 초기 실패 로그와 최종 결과는 혼동하지 않는다. 실제 PostgreSQL 양쪽 선행 순서·잠금 대기, 처음에 비어 있던/신규 팀의 대상 포함은 남는다. 운영 미배포이며 전체 잔여 범위는 유지한다.

> 후속 46차 G06 시험 생성/학생 결과(2026-09-09, 로컬): 과거 계정의 생성 명단 포함 2개, 과거 계정/팀의 현재 결과 조회 6개, 과거 시험 결과 조회 예외 8개를 재현했다. 생성 대상 계정과 공개 결과의 계정·팀·시험 학년도를 현재로 제한하고 원래 소속/시험/결과를 보존한다. 합성 생성기를 포함한 새 18개, 집중 56개, 전체 84개 파일·458개와 타입 포함 49개 경로 빌드/공백 검사 통과(exam-students-before-46.log, exam-students-after-46.log, full-tests-46.log/build-46.log). 실제 AI/기기/새 PostgreSQL 검증은 없으며 운영 미배포. 생성 중 명단 변경과 저장의 일관성 및 기존 전체 잔여 범위는 미완료다.

> 후속 45차 G06 평가 학생/명단(2026-09-09, 로컬): 과거 계정·팀의 평가 접근 6개와 현재 명단 혼합 2개를 재현했다. 학생 조회·저장 및 동료/피드백/공개/진척 대상에 현재 학년도를 적용하고 과거 소속 행은 보존한다. 새 통합 10개 포함 집중 43개, 실제 PostgreSQL 평가/팀 변경 회귀 16개, 전체 83개 파일·440개와 타입 포함 49개 경로 빌드/공백 검사 통과(evaluation-members-after-45-final.log, evaluation-race-45.log, full-tests-45.log/build-45.log). 합성 입력의 확인 동의 누락은 테스트 오류로 별도 정정했다. 운영 미배포. 다음은 시험 생성 명단/학생 결과 조회이며 과거 응답 집계·추가 경합 등 기존 잔여 범위는 유지한다.

> 후속 44차 G06 평가 관리(2026-09-09, 로컬): 과거 평가의 문항·상태·피드백·의견·공개 변경 13개를 재현하고 학급 및 담당 동아리의 현재 학년도 조건을 적용했다. 과거 거절 시 양식/회차/의견/공개본/세션/소속 행 불변을 확인했다. 새 26개 포함 집중 34개, 전체 82개 파일·430개와 타입 포함 49개 경로 빌드/공백 검사 통과(evaluation-year-before-44.log, evaluation-year-after-44.log, full-tests-44.log/build-44.log). 이번은 테스트 DB 검증이며 실제 DB 권한 변경 경합을 새로 검증한 것은 아니다. 운영 미배포. 현재 팀에 연결된 과거 계정의 명단·공개 대상·학생 읽기/쓰기, 시험 생성/학생 결과 조회 및 기존 잔여 범위는 별도로 남는다.

> 후속 43차 G06 시험 관리(2026-09-09, 로컬): 과거 시험의 수정·추가·교정·공개와 과거 ID 요청이 현재 시험으로 바뀐 채 확정/출력되는 문제를 수정했다. 변경 대상 학년도와 개별 조회의 요청/결과 ID 일치를 검사한다. 문항 삭제의 정수형 매개변수는 모의 DB 연산 문제를 해소하며 실제 PostgreSQL에서 원래 2문항·2점 구성의 삭제/과거 거절 4개를 확인했다. 과탐실/동아리 × 현재/과거 × 8개 작업 새 32개 포함 집중 38개, 전체 81개 파일·404개 및 타입 포함 49개 경로 빌드/공백 검사 통과(exam-year-after-43.log, postgres-exam-year-43-final.log, full-tests-43.log/build-43.log). 초기 모의 DB 삭제 오류와 실제 DB 실행 옵션 누락은 PROJECT_STATUS에 별도 기록했다. 운영 미배포. 시험 생성 명단·학생 결과 및 평가의 학년도 경계, 기존 잔여 범위는 완료로 간주하지 않는다.

> 후속 42차 G06 동아리 소속(2026-09-09, 로컬): 과거 담당/학생 계정의 현재 동아리 배정·탈퇴 6개와 관리 명단 혼합 2개를 재현했다. 교사/학생 학년도 검사와 관리 목록 계정 조건을 적용해 거절 시 기존 행을 보존한다. 배정/탈퇴의 학생 검사는 기존 잠금 안에서 수행한다. 동일 학번의 현재 계정 등록은 기존 정상 동작을 확인했다. 새 19개 포함 집중 41개, 전체 80개 파일·372개 및 타입 포함 49개 경로 빌드/공백 검사 통과(club-year-before-42.log, club-year-after-42.log, full-tests-42.log/build-42.log). 테스트 DB 검증으로 실제 DB 권한 경합·기기 검증 완료를 의미하지 않는다. 운영 미배포. 다음은 시험·평가의 개별 수정/공개/학생 조회 학년도 경계이며 기존 잔여 범위도 계속 남는다.

> 후속 41차 G06 비밀번호/마스터 관리(2026-09-09, 로컬): 과거 계정 초기화·중지·복원 8개 및 과거 마스터 허용 1개를 재현하고 현재 학년도 검사와 UPDATE 대상 제한을 적용했다. 체험 학생 중지 시 현재 학급/동아리 소속만 종료하며 과거 소속·팀장 행은 보존한다. 현재/과거 계정 및 역사 보존 새 17개 포함 집중 23개, 전체 79개 파일·353개와 타입 포함 49개 경로 빌드/공백 검사 통과(account-year-before-41.log, account-year-after-41-final.log, full-tests-41.log/build-41.log). 중간 합성 테스트의 club_members 키 오류는 정정했으며 제품 오류와 구분한다. 이번은 테스트 DB 검증으로 실제 PostgreSQL 경합·기기 검증은 추가하지 않았다. 운영 미배포. 동아리 소속·시험/평가의 나머지 학년도 경계 및 기존 잔여 범위는 유지한다.

> 후속 40차 G06 관리 요청 학년도 분리(현재 실행 환경 날짜 2026-09-09, 로컬): 과거 학생/팀의 직접 ID 관리 요청으로 배정·제거·팀장·중지·복원·보관이 처리되는 9개 API 오류와 현재 학생 관리가 과거 소속/팀장 기록까지 바꾸는 2개 상황을 재현했다. 서비스의 대상 검사와 정리 UPDATE 범위를 현재 학년도로 제한해 과거 계정/팀/소속을 보존한다. 현재/과거 관리 요청 18개 포함 집중 36개, 실제 DB 판단/권한·요약/보관 회귀 33개, 전체 78개 파일·336개 테스트와 타입 포함 빌드/공백 검사 통과(management-year-before-40.log, management-year-history-before-40.log, management-year-after-40-final.log, full-tests-40.log/build-40.log). 합성 DB 정상 종료, 운영 미배포. 비밀번호·마스터 관리 및 동아리/시험/평가의 나머지 학년도 검증은 별도 남는다.

> 후속 39차 G06 학생 학년도 접근(현재 실행 환경 날짜 2026-09-08, 로컬): 과탐실·동아리 학생 진입 경로에서 과거 계정/팀 자료 접근 6개 상황을 재현했다. 활동 목록·기본/지정 팀 조회·소속 검사에 현재 계정 및 학급/동아리 학년도를 확인한다. 새 8개 및 기존 활동 공존·보관/복원·AI 최신성 관련 집중 22개, 전체 77개 파일·318개 테스트와 타입 포함 빌드/공백 검사 통과(student-year-before-39.log/student-year-after-39.log, full-tests-39.log/build-39.log). 교사 역사 열람과 원래 계획/소속 행은 보존한다. 테스트 DB 검증으로 실제 PostgreSQL 동시 실행/기기를 새로 확인한 것은 아니다. 운영 미배포. 학생·팀 관리 쓰기 및 동아리/시험/평가의 남은 학년도 조합은 계속 검증한다.

> 후속 38차 G06 학년도 분리(현재 실행 환경 날짜 2026-09-08, 로컬): 과거 학년도 토큰이 유효하고 교사 기본 명단/팀 목록에 과거 자료가 섞이는 오류 4개를 재현했다. 토큰 복원과 교사 기본 목록·팀 연결·인원 집계에 현재 학년도 조건을 적용하며 과거 계정/팀/소속 원문을 보존한다. 현재 동일 아이디 계정의 정상 로그인과 기존 명단·팀·학생 관리 검증 통과. 전체 76개 파일·310개 테스트, 타입 포함 49개 경로 빌드와 공백 검사 통과(full-tests-38.log/build-38.log). 이번은 테스트 DB 검증으로 실제 PostgreSQL 동시 실행/실제 기기를 새로 확인한 것은 아니다. 교사 역사 자료 직접 열람 권한은 임의 축소하지 않았으며 학생 활동/팀 자료·관리 요청·동아리/시험/평가의 학년도 조합은 남는다. 운영 미배포.

> 후속 37차 G01 판단/최종 분석 경합(현재 실행 환경 날짜 2026-09-08, 로컬 검증): 신규/수정 판단과 최종 분석의 세 가지 선행 순서 6개 통과. 실제 잠금 대기, 판단 선행의 최신 근거 포함, 최종 저장 선행의 후속 판단 거절, 생성 중 판단 변경의 오래된 결과 표시·종료 거절·재분석 후 정상 종료를 확인했다. 과거 판단/근거/최종 결과와 계획서·보고서를 보존한다. 증거는 output/test-infra/postgres-decision-final-race.json 및 decision-final-37.log. 제품 코드 미변경이며 전체 테스트/빌드는 36차 기준이다. 운영 미배포. 나머지 권한/학년도/외부 연동/복구 검증은 별도 남는다.

> 후속 36차 최종 검증: 전체 75개 파일·306개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과(full-tests-36.log/build-36.log). 실제 DB 권한 경합 30개는 별도다. 합성 PostgreSQL 정상 종료, 운영 미배포. 학생 판단과 최종 분석 동시 실행 및 잔여 범위는 계속 검증한다.

> 후속 36차 G02 학생 판단 권한 경합(현재 실행 환경 날짜 2026-09-08, 로컬): 비활성/비밀번호 초기화/보관/팀 제거·이동 후에도 신규 판단·수정·동일 재전송이 처리되는 실제 DB 상황 15개를 재현했다. 세션·회차 뒤 학생·관련 팀을 잠그고 현재 학생 계정/소속을 재검사하며 팀장 전용 제한은 추가하지 않았다. 양쪽 선행 순서의 실제 DB 30개, 역할/계정 검증 추가 4개와 기존 단일 회차 흐름 포함 집중 8개 통과. 기존 판단/분석/근거 및 최초 작성자를 보존하고 권한 변경 후 재요청을 거절한다. 증거는 output/test-infra/postgres-decision-access-before.json 및 postgres-decision-access-after.json, decision-access-unit-36.log. 최초 검증의 버전 0 가정을 실제 초기 버전 1로 정정했다. 전체 테스트/빌드와 종료 결과는 PROJECT_STATUS.md를 따른다. 운영 미배포. 판단과 최종 분석의 동시 실행 및 잔여 검증은 계속 남는다.

> 후속 35차 최종 검증: 전체 75개 파일·302개 테스트와 타입 포함 49개 경로 빌드, 변경 코드 공백 검사 통과(full-tests-35.log/build-35.log). 실제 DB 32개 및 중복/만료 인계 4개는 별도 검증이다. 기존 역할 테스트의 분석 재사용 검사를 강화했으므로 자동 테스트 개수는 34차와 같다. 검증 PostgreSQL 정상 종료. 운영 미배포이며 학생 판단 저장 권한 검증은 남는다.

> 후속 35차 G02 AI 분석 권한 경합(현재 실행 환경 날짜 2026-09-08, 로컬): 분석 시작/생성 대기/결과 저장 직전 계정·비밀번호·보관·담당 변경 후에도 중간/최종 결과가 저장되는 실제 DB 오류 24개를 재현했다. 시작과 결과 저장의 각 트랜잭션에서 현재 교사/팀/담당 권한을 잠금 아래 재확인하고 생성 대기 중에는 잠금을 유지하지 않는다. 실제 DB 수락 32개, 중복/만료 인계 회귀 4개, 완료 분석 재사용의 기존 역할 범위를 포함한 집중 12개 통과. 시작 거절 시 생성/근거 미생성, 생성 중 권한 상실 시 늦은 결과 차단, 저장 선행 시 적법한 결과 보존을 확인했다. 원문/기존 근거/결과는 보존한다. 증거는 output/test-infra/postgres-analysis-access-before.json 및 postgres-analysis-access-after.json, analysis-ownership-35.log. 전체 테스트/빌드는 PROJECT_STATUS.md 최신 결과를 따른다. 외부 AI/운영 미변경. 학생 판단 저장 중 소속·계정 변화는 다음 대상이다.

> 후속 34차 최종 검증: 전체 75개 파일·302개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과(full-tests-34.log/build-34.log). 실제 DB 권한 경합 16개와 기존 승인/회차 경합 8개를 별도로 확인했으며 합성 검증 DB 정상 종료. 운영 미배포. 다음은 AI 분석 및 학생 판단 저장 중 권한 변화다.

> 후속 34차 G01/G02 회차 종료 권한 경합(현재 실행 환경 날짜 2026-09-08, 로컬): 교사 계정/비밀번호/팀 보관/담당 변경 이후에도 회차를 종료하는 실제 DB 오류 8개를 재현했다. 세션·회차와 교사·팀·담당 관계를 종료 트랜잭션에서 잠그고 현재 권한을 재확인한다. 양쪽 선행 순서 16개, 기존 승인/회차 전환 8개와 담당·마스터·비담당·학생 범위 추가 8개 통과. 변경 선행 시 원문/회차/이력 불변, 종료 선행 시 정당한 종료·과거 원문과 분석 보존을 확인했다. 근거는 output/test-infra/postgres-cycle-access-before.json 및 postgres-cycle-access-after.json, positive-review-34.log, cycle-access-unit-34.log. 최초 합성 응답 형식 실패와 실제 오류 재현을 구분했다. 전체 테스트/빌드 최종 결과는 PROJECT_STATUS.md를 따른다. 운영 미변경. AI 분석·학생 판단 중 권한 변화 및 나머지 검증은 남는다.

> 후속 33차 G01/G02 복원 권한 경합(현재 실행 환경 날짜 2026-09-08, 로컬): 계정/비밀번호/소속/팀장/보관 변경 후에도 계획서·보고서 복원이 저장되는 오류 16개를 실제 DB에서 재현했다. 복원 트랜잭션에서 사용자→관련 팀 잠금과 현재 권한 재검사를 적용하고 보고서의 과거 역할 사용자도 같은 순서로 잠근다. 교사의 기존 공동·보관 팀 복원 범위와 과거 원문/역할, 재검토 규칙은 보존한다. 양쪽 선행 순서 32개, 기존 복원/회차 전환 8개, 보관 팀 역할 범위 추가 2개와 전체 294개 테스트·타입 포함 49개 경로 빌드 통과. 증거는 output/test-infra/postgres-restore-access-before.json 및 postgres-restore-access-after.json, restore-cycle-33.log, full-tests-33.log/build-33.log. 검증 DB의 종료 관찰 시간 초과 뒤 실제 정상 종료를 확인했다. 운영/외부 연동 미변경. 회차 전환·분석 중 권한 변화와 학생 문서/동아리 소속 변경, G06/G07은 계속 남는다.

> 후속 32차 G01/G02 검토 권한 경합(현재 실행 환경 날짜 2026-09-08, 로컬): 계정 비활성/비밀번호 재설정/팀 보관 및 동아리 계획 담당 해제 후에도 검토가 저장되는 실제 DB 오류 14개를 재현했다. 문서 회차 잠금과 교사/팀/필요한 담당 관계 잠금 아래 권한을 재확인하도록 수정했다. 계획의 담당/마스터와 보고서의 공동 교사 범위를 유지한다. 양쪽 선행 순서의 실제 DB 28개, 기존 승인·회차/학생 쓰기 16개 재검증, 새 역할 범위 테스트 8개와 전체 292개 테스트·최종 빌드 통과. 권한 변경 선행은 문서/이력/알림을 변경하지 않고, 검토 선행은 정당한 검토·원문을 보존한 뒤 권한 변경을 처리한다. 증거는 output/test-infra/postgres-review-access-before.json 및 postgres-review-access-after.json, full-tests-32.log/build-32-verified.log. 초기 복구·검증 로더·임시 생성 타입·자동 승인 재검토 경위는 PROJECT_STATUS.md에 구분 기록했다. 운영/실제 외부 연동 미변경. 문서 복원·회차 전환·분석 중 권한 변화와 G06/G07은 별도 남는다.

> 후속 31차 G01 승인/학생 쓰기 교차(2026-09-09, 로컬 검증): 계획서/보고서×수정/재제출×양쪽 선행 순서 8개를 실제 DB와 서비스로 검증했다. 학생 선행은 오래된 제출 ID/보고서 버전에 대한 승인 거절, 교사 선행은 승인 이력을 보존하고 후속 수정·재제출을 재검토 상태로 유지한다. 기존 계획 고정본 불변·승인 이력 건수·오래된 승인 재실행 거절도 확인했다. 내용 변경 없는 재제출의 옛 pending 상태까지 철회한다고 가정한 최초 검증을 정정하고, 기존 이력 보존/새 최신 제출 ID를 검증했다. 제품 코드는 변경하지 않았다. 결과는 output/test-infra/postgres-positive-review-student-race.json 및 positive-review-student-31-final.log. 전체 테스트/빌드는 29차 기준, 실제 DB 8개는 별도다. 운영/외부 연동 미변경. 검토 중 계정/팀/동아리 권한 변화는 다음 검증이다.

> 후속 30차 G01 긍정 승인 경합(2026-09-09, 로컬 검증): 기존 수정 요청 경합과 구분해 계획 승인/보고서 확인 완료×다음 회차/탐구 완료×양쪽 선행 순서 8개를 실제 PostgreSQL과 실제 서비스로 검증했다. 실제 행 잠금 대기·승인 저장·옛 분석 전환 거절·최신 재분석 후 전환·종료 후 오래된 승인 거절 및 문서/과거 이력/계획 제출 고정본 보존을 확인했다. 수정 요청 알림은 생성되지 않았다. 증거는 output/test-infra/postgres-positive-review-race.json 및 positive-review-30.log. 제품 코드 변경이 없어 전체 테스트/빌드는 29차 결과를 유지하며 실제 DB 8개는 별도다. 운영/외부 AI·Sheet 미변경. 승인과 학생 수정의 교차, 검토 중 교사/팀/동아리 권한 변경은 별도 남는다.

> 후속 29차 G01/G02 준비물 권한 경합(2026-09-09, 로컬): 소속/계정/팀 변경이 끝난 뒤 신청이 저장되는 오류를 실제 DB에서 4개 재현했다. 현재 회차·사용자/팀 잠금 안에서 권한과 저장 당시 전송 대상을 확인한 뒤 신청을 저장하고 외부 준비/전송은 커밋 이후 실행한다. 양쪽 선행 순서 8개에서 변경 후 쓰기/전송 차단과 먼저 저장된 원문/과거 소속 보존을 검증했다. 비활성·비밀번호 변경 필요 계정의 신규/완료 재사용/일반·연습 재시도 8개, 기존 회차/준비물 경합 8개 재검증, 전체 283개 테스트와 타입 포함 빌드 통과. 실제 외부 Sheet는 호출하지 않았다. 증거는 output/test-infra/postgres-material-access-before.json 및 postgres-material-access-after.json, full-tests-29.log/build-29.log. 운영 미변경. 동아리 소속 변경과 다른 문서 승인/확인·학년도 경계는 별도 남는다.

> 후속 28차 G04 미전송 신청 조회/복구(2026-09-09, 로컬): 최신 신청만 보여 오래된 미전송 신청이 가려지는 경로를 수정했다. 같은 팀/회차의 미전송 신청을 별도로 표시해 원래 품목·규격·링크·시각을 확인하고 해당 신청 ID로 재전송한다. 연습/다른 팀/다른 회차를 섞지 않고 완료 회차는 읽기 전용으로 유지한다. 연결·JSON·전송 실패 뒤 버튼을 복구하며 실패를 성공으로 안내하지 않는다. 전체 275개 테스트와 최종 빌드 통과. 새 날짜 표기의 브라우저 초기화 오류를 실제 검증에서 발견·수정한 뒤 Chrome 768×1024의 이전 신청 선택/오류 복구/목록 갱신/완료 읽기 전용을 재검증했고 브라우저 오류 0건이다. 실제 API의 전송 대상 불명 기록 대조 거절·원문 보존도 확인했다. 결과는 output/test-infra/material-pending-browser.json/png 및 full-tests-28.log/build-28-final.log. 실제 Sheet/운영 미변경. 초기 저장 시 소속 변경과 실제 외부 전송·불확실 기록 대조는 별도 남는다.

> 후속 27차 G01/G04 전송 결과 경계(2026-09-09, 로컬): 준비물 전송 완료가 회차 전환 검사 사이에 저장되어 옛 분석으로 종료되는 오류를 실제 DB에서 2개 재현했다. 성공/실패 결과 저장에 세션→회차→신청 잠금과 현재 작업 ID 재검사를 적용했다. 두 종료 방식과 양쪽 선행 순서의 실제 DB 경합 4개에서 옛 분석 종료 차단·최신 재분석 후 정상 종료를 확인했다. 새 신청 뒤 늦게 온 옛 성공/실패 응답의 원문/작업 행 불변 테스트도 통과했다. 전체 71개 파일·274개 테스트와 타입 포함 49개 경로 빌드 통과. 재현/수락 증거는 output/test-infra/postgres-material-completion-before.json 및 postgres-material-completion-after.json에 구분 보존했다. 전송 함수는 합성이며 실제 Sheet/운영은 미변경. 여러 과거 미전송 신청의 재확인과 저장 시 소속 변경, 다른 G01/G02/G06/G07 검증은 남는다.

> 후속 26차 G04 체험 신청 종료 경계(2026-09-09, 로컬): 원래 demo 제출·전송 대상 없음·미전송 조건으로만 연습 신청을 구분해 회차 종료를 막지 않도록 했다. 일반/불확실한/혼합 미전송은 계속 차단하고 신청 원문/전송 상태는 보존한다. 체험 계정의 일반 학생 원문 덮어쓰기도 막았다. 실제 DB 8개, 전체 272개 테스트와 빌드 통과. 합성 브라우저에서 실제 demo 신청 API, 학생/교사 연습 표시와 재전송 미표시·일반 재전송 유지 확인. 운영/외부 Sheet 미변경. 일반 준비물 전송 완료 경합 및 나머지 G04는 남는다.

> 후속 25차 F03 AI 초안 복구(2026-09-09, 로컬): 질문을 전송 전에 지우고 통신/JSON 예외 뒤 버튼을 풀지 못하는 경로를 수정했다. 질문·관심사·재시도 ID를 사용자/세션/회차별 브라우저 탭에 보관하고 성공 확인 후 정리하며, 이전 화면의 늦은 응답이 새 초안을 바꾸지 못하게 했다. 전체 270개 테스트·빌드 통과. Chrome 768×1024의 6개 시나리오에서 네트워크/응답 오류·새로고침·탭/회차/사용자 변경·추가 편집·저장소 실패 복구를 확인했다. 실제 외부 AI 호출/기기 검증은 아니며 운영 미배포다. 다음은 체험 계정의 준비물 전송 보류와 회차 종료 연결 및 잔여 검증이다.

> 후속 24차 공동 교사/보관 검증(2026-09-09, 로컬): 23차의 담당 교사 조건 확인 대상은 확정 권한에 대조해 결함 아님으로 판정했다. 기록 공동 조회는 유지하고 동아리 운영 설정만 담당/마스터로 제한한다. 공동 교사 조회·정리, 설정 권한 분리, 비활성/비밀번호 변경 필요 교사, 다른 팀/탈퇴 학생 및 과거 기록 보존의 새 테스트 7개 통과. 실제 PostgreSQL의 요약/팀 보관·복원 3개 시나리오도 통과했고 저장 선행 시 보관의 행 잠금 대기를 관찰했다. 제품 코드 미변경, 전체 테스트/빌드는 23차 기준. 다음은 AI 대화 초안/통신 실패 복구다.

> 후속 23차 G05 날짜별 요약 분리(2026-09-09, 로컬): 0009의 회차별 작업/요약 저장소와 원문 기준 이행을 추가해 생성·조회·완료 저장에 명시적 회차를 연결했다. 예전 날짜별 정리는 원형 그대로 별도 읽기 전용 표시한다. 세션/회차/팀 잠금 아래 입력 버전을 고정하고 생성 후 같은 활성 회차/소유 토큰을 다시 확인한다. 실제 DB 8개 시나리오 및 이전 근거/이행 3개 재검증, 전체 263개 테스트와 빌드 통과. Chrome 768×1024에서 회차 선택·같은 날 원문/요약 분리·과거 읽기 전용·예전 형식 표시·초안/실패 복구를 확인했다. 다음은 교사 대화 조회/정리의 동아리 담당 권한 검증과 나머지 경계다. 운영 미배포.

> 후속 22차 최종 검증: 전체 70개 파일·261개 테스트, 타입 포함 49개 경로 빌드와 변경 코드 공백 검사 통과. 실제 DB 경합 12개 및 근거/이행 검증 3개 완료, 로컬 PostgreSQL 정상 종료. 날짜별 요약 작업/조회 분리는 다음 단계이며 운영 미배포.

> 후속 22차 G05 분석 근거 수정(2026-09-09, 로컬): 실제 날짜별 요약이 같은 날의 두 회차 원문을 합치는 현상을 재현했다. 회차 분석은 요약의 모든 원문 ID/내용/종류/날짜와 인용을 대조해 혼합·불명확한 요약을 제외하고 원본은 보존한다. 요약되지 않은 학생 대화·대면/보완 기록과 내용 확인을 근거에 고정해 새 기록 이후 옛 분석으로 종료되는 문제를 막았다. 원문 보존 열을 추가하는 0008의 기존 스냅샷 보존/반복 적용 등 실제 DB 3개 검증, 대화/대면/확인과 회차 전환의 양방향 경합 12개 통과. 전체 261개 테스트 통과, 최종 빌드는 PROJECT_STATUS.md를 따른다. 날짜별 요약 생성/조회 자체의 회차 분리는 아직 남아 있으며 이 수정으로 G05 전체가 완료된 것은 아니다. 운영 미배포.

> 후속 21차 최종 검증: 전체 69개 파일·257개 테스트와 타입 포함 49개 경로 빌드 통과. 실제 PostgreSQL 회차 변경/늦은 AI 응답 4개 수락 검증 통과. 로컬 검증 DB 서버 정상 종료, 운영 미배포.

> 후속 21차 AI 대화/탐구 방향 회차 경계(2026-09-09, 로컬): 대화의 전환 이후 늦은 저장 오류 2개를 실제 DB에서 재현했다. AI 진입·질문/응답 저장·복구 시 세션/회차/학생/팀과 작업 소유권을 같은 쓰기 트랜잭션에서 확인하고 API에 표시 회차를 필수로 연결했다. 주제 제안은 진행 단계를 초기화하지 않으며 회차가 불명확한 옛 요약은 원본 보존 후 대화 AI 입력에서 제외한다. 대화/방향 각각 다음 회차/탐구 완료의 실제 DB 4개 검증 통과. 처음 방향 재현은 합성 응답 형식 오류로 완료되지 않았으며 수정 후 수락 검증과 구분한다. 전체 회귀 최종 결과는 PROJECT_STATUS.md를 따른다. AI 화면 실제 기기와 초안/통신 실패 복구, 날짜별 요약 경계는 남는다. 운영 미배포.

> 후속 20차 학생 대화 경계 수정(2026-09-09, 로컬): 전환 대기 뒤 완료 회차에 대화가 저장되는 오류를 실제 DB에서 재현해 대화/대면 기록/내용 확인에 세션·회차 잠금과 표시 회차 검사를 적용했다. 전환 선행 6개 실제 DB 검증, 전체 250개 테스트와 빌드 통과. 대화 초안을 회차별로 분리하고 이전 형식 초안은 읽기 전용 복구 안내로 보존했다. 빌드 산출물+별도 합성 DB의 Chrome 768×1024에서 초안 복구/분리·실패 시 글 보존·표시 회차 전송·완료 학생 읽기 전용을 확인했다. 실제 기기 검증으로 확대하지 않으며 운영 미배포다.

> 후속 19차 실제 분석 서비스 검증(2026-09-09): 실제 requestCycleAnalysis에서 중간/최종 선행 순서의 중복 요청과 만료 인계 후 늦은 결과를 검증한 4개 시나리오 통과. 진행 중 다른 종류는 생성하지 않고, 최종 이후 중간 분석을 차단하며, 인계받은 새 결과만 저장/재사용한다. 생성 응답은 주입한 합성 함수다. 제품/운영 미변경이며 다음은 F03의 나머지 AI/대화 회차 쓰기 경계다.

> 후속 18차 최종 검증: 실제 평가 저장/공개 경합 16개, 전체 67개 파일·246개 테스트와 타입 포함 49개 경로 빌드 통과. 검증용 PostgreSQL은 정상 종료했고 운영에는 배포하지 않았다.

> 후속 18차 평가 권한 경합 수정(2026-09-08, 로컬): 평가 권한 조회 뒤 팀 제거/이동·계정 비활성화가 끝나도 자기/동료평가가 저장되는 7개 실제 DB 오류를 재현했다. 저장 트랜잭션에서 세션→참여 학생→팀 잠금과 현재 소속/계정/평가 범위 재검사를 수행한다. 공개도 같은 세션 우선 순서로 잠그며 최초 후보에 없거나 잠그지 않은 세션의 대상이 생기면 공개를 중단한다. 양쪽 순서의 평가 저장 14개와 공개 중 새 팀원/공개 후 제거·반복 공개 보존 2개, 총 16개 실제 DB 검증 통과. 전체 회귀/빌드 최종 결과는 PROJECT_STATUS.md 최신 기록을 따른다.

> 후속 17차 최종 검증: 같은/다른 요청의 최초 3중 획득도 각각 1개 시작·2개 busy로 확인해 실제 AI 작업 경합 검증은 총 8개다. 전체 67개 파일·246개 테스트와 타입 포함 49개 경로 빌드 통과. 아래 6개 표기는 추가 획득 검증 전 기록이며 ‘최초 동시 자원 획득 미확인’은 이번 검증으로 해소했다. 실제 중간/최종 분석 서비스 경쟁과 나머지 검토 범위는 남는다. 운영 미배포.

> 후속 17차 AI 소유권 경합 수정(2026-09-08, 로컬): 실제 PostgreSQL에서 소유권 검사와 단계 INSERT 사이의 인계 및 다른 요청 키로의 인계 후 옛 작업 권한 잔존을 재현했다. 쓰기 트랜잭션의 소유권 조회는 작업 행을 잠그며 같은 자원의 만료된 다른 요청은 새 요청 획득 시 권한을 종료한다. 인계 선행/저장 선행/저장 실패 × 같은/다른 요청의 6개 실제 DB 검증 통과. 인계 전 정상 완료 단계의 재사용은 유지하며 외부 모델 호출은 하지 않았다. 전체 회귀/빌드 최종 결과는 PROJECT_STATUS.md 최신 기록을 따른다.

> 후속 16차 최종 검증: 전체 67개 파일·245개 테스트와 타입 포함 빌드, 변경 코드 공백 검사 통과. 수정 전 결함 재현과 수정 후 수락 검증을 구분해 보존했으며 운영에는 배포하지 않았다.

> 후속 16차 로컬 수정·실제 DB 검증(2026-09-08): 계획서 제출·일지 저장의 추가 8개 경합을 통과했다. 준비물에서는 회차 전환 중 들어온 신청이 외래 키 대기 후 완료 회차에 pending으로 저장되는 실제 오류를 start_next/finish_project 양쪽에서 재현했다. 저장 트랜잭션에 세션→회차 잠금과 대기 후 active 재검사를 추가하고 화면/API에 명시적 회차 ID를 연결했다. 수정 후 준비물의 양쪽 선행 순서 4개 검증 통과. 과거 오류 재현 모드는 수락 기준이 아니며 별도 이름으로 보존한다. 제품은 로컬만 수정했으며 전체 회귀/빌드의 최종 결과는 PROJECT_STATUS.md 최신 기록을 따른다.

> 후속 15차 실제 DB 경합 확대(2026-09-08): 계획서/보고서 저장·복원, 보고서 제출, 두 문서의 교사 수정 요청과 다음 회차/탐구 완료를 양쪽 순서로 실행한 28개 시나리오 통과. 이전 회차 요청 거절, 선행 작성·복원 내용 보존, 낡은 분석의 전환 차단, 원문/역할 이력 및 수정 요청 알림의 문서·팀 연결을 확인했다. 제품/운영 미변경. 계획서 제출·제출 직후 승인/확인 완료·일지·준비물 등의 남은 경합은 계속 검증 대상이다.

> 후속 14차 실제 DB 경합 검증(2026-09-08): 보고서 필드 저장과 다음 회차 시작/탐구 완료를 양쪽 선행 순서로 교차한 4개 시나리오를 통과했다. 실제 앱 연결의 Lock 대기를 관찰하고 전환 후 이전 회차 쓰기 거절, 저장 후 낡은 분석에 근거한 전환 거절, 이전 원문·현재 저장 내용·다음 빈 문서 보존을 확인했다. 제품/운영 미변경이며 G01의 제출·검토·복원·일지·준비물 등 나머지 조합까지 입증한 것은 아니다.

> 후속 13차 로컬 수정·실제 DB 검증(2026-09-08): 빈 DB 동시 SCHEMA_SQL 생성의 23505 충돌을 재현하고 스키마→마이그레이션→초기 자료까지 앱 초기화 잠금으로 보호했다. 실제 앱 프로세스 3개가 중복 없이 초기화된다. 초기 계정 생성 실패 뒤 학급만 남아 재시작이 막히던 문제도 재현해 초기 자료 생성을 트랜잭션으로 묶었다. 계정 생성 및 마지막 보고서 생성에 오류를 넣어 전체 초기 자료 롤백과 재시작 복구를 실제 PostgreSQL 16.15에서 확인했다. 전체 242개 테스트·타입 포함 49개 경로 빌드 통과. 운영 미변경이며 기존 불완전 DB의 자동 복구, 구버전 공존 및 G01~G07의 나머지 검증은 완료로 간주하지 않는다.

> 후속 12차 실제 DB 기초 검증(2026-09-08): 격리된 로컬 PostgreSQL 16.15에서 0005→0007 동시 전환(독립 풀 3개), 보고서 원문/상태/시각 보존, 새 버전별 고유 제약, 실제 행 잠금 대기와 롤백을 확인했다. 전용 실행/검증 스크립트를 추가하고 서버를 정상 종료했다. G01/G07의 앱 단위 경합·전체 초기화·구버전 공존까지 검증한 것은 아니다. 운영 미변경이며 다음 검증 환경만 준비된 상태다.

> 후속 11차 로컬 수정(2026-09-08): F17의 출제 자료를 승인 계획서·확인 보고서의 고정본/문서 이력으로 연결했다. 현재 초안·주제·역할은 사용하지 않으며 회차별 원본과 학생 ID로 개인 역할을 구분한다. 실제 회차 전환 뒤에도 고정 근거와 원래 역할을 보존한다. 출제 입력→저장 근거, 초안 제외, 미확인 보고서 제외, 제거 학생 기록 보존·출제 제외 포함 전체 240개 테스트와 타입 포함 49개 경로 빌드 통과. F17 로컬 수정/합성 검증 완료이며 운영 미배포다. 실제 PostgreSQL 경쟁(G01/G02), 나머지 G 항목과 F03의 남은 쓰기 회차 계약은 계속 남는다.

> 후속 10차 로컬 수정(2026-09-08): F17의 잘못된 출처 임의 대체와 근거 길이/개수 불일치를 제거했다. 출처·문항 수·학생 구성 검사 후에만 완료 단계/최종 시험을 저장하고 새 출처 정책으로 작업 재사용을 구분한다. 개인 자료가 전혀 없는 표준 대체 문항은 실제 기록과 구별한다. 전체 237개 테스트 통과, 최종 관련 검증/빌드 결과는 PROJECT_STATUS.md 최신 기록을 따른다. 승인 계획/확인 보고서 고정본과 과거 역할 연결은 계속 미완료이며 운영 미배포다.

> 후속 9차 로컬 수정(2026-09-08): F17 중 개인 일지의 회차/원본별 근거 키 충돌을 수정했다. 같은 제목 두 회차의 1차시 각각을 선택한 AI 입력과 저장 근거·제시문 대응 및 이후 원본 변경에도 시험 근거 보존을 합성 검증했다. 추가 탭 키도 설정/응답 ID로 구분한다. 전체 231개 테스트·타입 포함 49개 경로 빌드 통과. F17의 승인 계획/확인 보고서 고정본 선택, 과거 역할 연결, 잘못된 출처 임의 대체와 길이 불일치는 아직 남아 있다. 운영 미배포다.

> 후속 8차 로컬 수정(2026-09-08): F15 일반 계정만 공지 푸시 대상으로 선정한다. F16은 공식 SheetJS 0.20.3으로 갱신하고 업로드 크기·행/열·실제 압축 해제량·worker 시간 예산과 안전한 오류 응답을 적용했다. 전체 62개 파일·230개 테스트, 타입 포함 49개 경로 빌드, standalone 패키지만 사용하는 한글 XLS/XLSX worker 검증 통과. 배포 산출물의 worker 파일 누락도 수정 후 재검증했다. 운영 미배포이며 F17·G 항목은 남는다. 아래 F15/F16 원문은 발견 당시 기록이다.

> 후속 7차 로컬 수정(2026-09-08): F11 계획서/회차 AI 결과의 프롬프트·스키마 버전별 저장/조회/재사용을 분리했다. 마이그레이션 0007로 같은 자료의 구버전과 새 버전 결과를 함께 보존한다. 진행 중인 회차의 구버전은 최신으로 판정하지 않고 판단/전환을 차단한다. 회차 분석의 가림·근거 검사·단일 탐구 지침은 v2로 구분했다. 버전 두 축별 재생성·재사용·원문 보존·구버전 차단 포함 전체 216개 테스트·타입 포함 빌드 통과. 운영 미배포이며 실제 PostgreSQL 전환/구버전 공존과 나머지 F15~F17/G 항목은 남는다.

> 후속 6차 로컬 수정(2026-09-08): F09 회차 이름을 포함한 회차 정의의 식별정보 가림, F12 신규 AI 결과의 빈 근거·미전송 근거 인용 거절을 구현했다. 실제 전송 목록과 검증 대상 목록을 공유한다. 합성 개인정보 가림과 거절 시 분석 무저장/유효 결과 저장, 전체 212개 테스트·타입 포함 빌드 통과. 과거 기록 파서는 보존했으며 F11 버전별 캐시 분리는 후속이므로 기존 캐시를 새 규칙으로 검증한 것은 아니다. 운영 배포와 외부 AI 호출은 하지 않았다.

> 후속 5차 로컬 수정(2026-09-08): F07 과거 개인 일지·사진 읽기 전용 열람을 학생/교사 화면과 조회 API에 연결했다. 새 회차 미승인 및 전체 종료 이후에도 현재 활성 팀원의 본인 기록 열람을 허용하며, 다른 학생 사진/다른 팀 회차/제거된 학생 접근과 과거 저장은 차단한다. 교사용 제거 학생 기록은 보존한다. 전체 211개 테스트·타입 포함 빌드와 합성 Chrome 태블릿 학생/교사 화면 통과. 운영 배포는 하지 않았다. 남은 AI·외부 연동·시험 근거와 실제 PostgreSQL/운영 통합 검증은 후속으로 남는다.

> 후속 4차 로컬 수정(2026-09-08): F06 같은 회차에서 중간→최종 분석→탐구 종료와 이전 분석/판단 읽기 전용 이력을 구현했다. 저장 판단 0건/1건 모두 두 번째 회차 없이 종료하며 같은 회차 판단이 최종 근거에 포함되는 것을 검증했다. 늦은 결과 저장의 회차 종료 검사와 분석 종류 간 작업 소유권도 보완했다. 전체 211개 테스트·타입 포함 빌드·합성 Chrome 태블릿 화면 검증 통과. 배포하지 않았다. F07, 실제 PostgreSQL 경합과 AI/외부 경계 등은 계속 남는다.

> 후속 3차 로컬 수정(2026-09-08): 보고서 검토에 회차·단조 증가 검토 번호·상태·저장 피드백 기대값과 세션→회차→문서 잠금을 적용했다. 오래된 검토/동일 내용 재제출 거절, 거절 시 이력 무변경, 수정 요청 상태의 재전송, 초안 충돌 확인 및 이전 형식 초안 보존을 검증했다. 새 마이그레이션 0006과 모든 보고서 변경 경로의 번호 증가를 포함한다. 전체 209개 테스트·타입 검사와 합성 Chrome 검증 통과. 실제 PostgreSQL 전환/경합·구버전 서버 공존 검증과 F06·F07 등은 남는다. 운영 배포는 하지 않았다. 최종 빌드 결과는 PROJECT_STATUS.md를 따른다.

> 후속 2차 로컬 수정(2026-09-08): F13 보고서 피드백의 사용자·문서·회차·양식별 초안 복구와 실패 재시도, F14 늦은 판단 저장 응답으로 새 초안을 지우는 문제, F05 오래된 분석의 재분석 버튼을 수정했다. 기존 전체 206개 테스트와 추가 재분석 화면 2개, 타입 포함 운영 빌드, Chrome 보고서 초안 및 판단의 늦은 응답/새로고침 보존과 기존 학생·교사 AI 화면을 검증했다. 배포하지 않았으며 운영은 00027-hvc 그대로다. 보고서 검토 대상의 서버 기대값/동시 교사 충돌 보호는 별도 미완료이며 F06·F07 및 나머지 미해결 항목도 계속 남는다. 상세 범위는 PROJECT_STATUS.md 최신 기록을 따른다.

> 후속 기록 경계 수정(2026-09-08, 로컬): F01 완료 회차 복원 차단, F04 계획서·보고서 초안의 회차/양식 분리와 회차 불명 옛 초안의 별도 보존, F08 맞춤 보고서 필드/표 표시를 수정했다. F03 중 문서·일지·주제 선택의 회차 명시 및 문서 잠금·저장·제출의 전환 경합 방어를 보완했다. 기존 편집 Chrome 13개와 회차 초안 분리 2개 통과. 실제 PostgreSQL 경합(G01), AI/준비물/대화의 나머지 회차 쓰기와 F13 보고서 초안 등은 아직 남아 있다. 최초 재현 테스트의 결함 발생 기대값은 수정본의 수락 기준으로 사용하지 않는다. 최신 최종 검증/배포 여부는 PROJECT_STATUS.md를 따른다.

검토일: 2026-09-08 · 대상: 현재 로컬 작업본 · 원래 작업: `01a0714d-4de5-7e43-92ef-404b306168d1`

> 최신 배포: 피드백 재전송 회귀 수정은 `science-inquiry-platform-00027-hvc`에 반영 완료했다. Ready=True·트래픽 100%, health/로그인 200, 기존 실행 설정 보존을 확인했다.

> 피드백 재전송 회귀 수정(2026-09-08): 사용자 확인에서 수정 요청 직후 AI 반영/재전송이 막히는 회귀가 발견됐다. feedback 상태를 교사 화면·검토 API·서비스·AI 고정 제출본 조회에서 지원하고, 기대 제출본/회차/상태/피드백 충돌 검사를 유지했다. 실제 로컬 검토 API와 Chrome 합성 화면에서 추가 편집·AI 항목 반영·동일 제출본 재전송을 확인했다. 전체 203개 테스트·타입 포함 49개 경로 빌드 통과. 사용자의 태블릿 로그인 성공은 확인됐으며 추가 실제 학생 자료 수동 시험은 중단했다. 배포 결과는 `PROJECT_STATUS.md` 최신 기록 참조.

> 배포 완료(2026-09-08): 사용자가 새로 승인한 긴급 계획서 최소 수정은 `science-inquiry-platform-00026-8hg`에 적용돼 트래픽 100%로 제공 중이다. 공개 상태/로그인/manifest 200, 비로그인 교사 계획서 검토·AI 검토 403, 기존 환경·Cloud SQL 연결 보존 확인. 이하의 로컬 전용·운영 미반영·전체 배포 불가 문구는 각 기록 당시의 상태이며, 나머지 구조 발견 전체의 해결을 의미하지 않는다.

> 추가 검증(2026-09-08): 사용자의 새 최소 수정·배포 지시에 따라 별도 Chrome에서 F13 관련 AI 항목 반영·중복 차단·수동 문장 보존·새로고침 초안 복구·전송 실패와 재시도를 확인했다. 합성 DB의 실제 로컬 수정 요청 API 저장값이 초안과 같으며 768/390px 가로 넘침이 없다. AI 결과만 합성 응답으로 대체했고 실제 AI/운영 데이터 시험은 하지 않았다. 전체 203개 테스트·타입 검사·운영 빌드 통과. 실제 학생 기기·IME·절전 및 나머지 구조 발견은 미해결/미확인 상태다. 아래 최초 검토의 배포 불가 판정은 전체 구조 범위에 대한 당시 기록이며, 이번에는 사용자가 긴급 계획서 최소 수정만 우선 배포하도록 별도 지시했다. 배포 결과는 최신 `PROJECT_STATUS.md` 참조.

> 후속 최소 수정 기록(2026-09-08): 아래 본문은 최초 읽기 전용 검토 당시의 기록이다. 이후 사용자가 계획서 검토 우선을 지시해 F10의 해시 불일치를 로컬에서 수정하고, 현재 저장본/제출본과 일치하는 AI 결과를 우선 조회하도록 보완했다. 현재 작업본에 이미 들어온 F02의 기대값 검사와 교사 초안·피드백 반영 코드는 유지했다. 실제 검토 API 처리 함수에서 오래된/누락된 기대값 거절과 재제출·승인을 검증했다. 관련 10개 및 전체 203개 테스트·타입 검사 통과. F13의 실제 화면 복구, 실제 PostgreSQL 경합과 나머지 발견 전체를 해결한 것으로 표시하지 않는다. 운영 반영은 하지 않았으며 상세 범위는 `PROJECT_STATUS.md`의 최신 항목에 있다. 최초 검토 전용 R14는 현재 올바른 동작을 확인하는 정식 회귀 테스트로 대체되었으므로 과거 결함 발생을 기대하는 검토 전용 테스트 전체를 현재 수락 기준으로 사용하지 않는다.

## 판단과 범위

이 절부터의 최초 검토 설명은 **초기 검토 당시의 역사 기록**이다. 이후 수정·검증·배포 사실은 문서 상단 후속 기록과 `PROJECT_STATUS.md`, 남은 완료 조건은 `REMAINING_WORK.md`를 함께 확인한다. 아래의 제품 미변경/198개 검사/미수정 문구를 현재 상태로 사용하지 않는다.

**인수인계서 1번 구조 검토를 완료했다. 현재 작업본에는 기록 보존·승인 대상·회차 경계의 필수 수정이 남아 있다. 새 기능보다 이 경계를 먼저 고쳐야 한다. 배포 가능한 상태로 판정하지 않는다.**

검토 시작 시 Git 상태의 수정 파일 75개와 미추적 파일 77개를 포함한 실제 폴더를 대상으로 했다. 기본 브랜치나 별도 worktree를 검토 대상으로 대체하지 않았다. `src` 205개 파일과 API 처리 파일 44개의 구성을 확인하고, 화면→API→서비스→DB 연결, 아래 핵심 경로와 기존 테스트를 대조했다. 모든 파일의 모든 분기나 권한·경합 조합을 실행했다는 뜻은 아니다. 코드 위치는 이 작업본의 프로젝트 상대 경로와 줄 번호다.

`PROJECT_STATUS.md` 전체, `CURRENT_DECISIONS.md`, `REVIEW_NEXT_STEPS.md`, `AGENTS.md`, 이전 `ARCHITECTURE_REVIEW.md`, 관련 구현 계획과 설치된 Next.js의 Route Handler·데이터 보안 지침을 확인했다. 오래된 설계 문구보다 최신 사용자 결정을 우선했다. 특히 단일 탐구·한 계획 안의 여러 변인 비교·연쇄 탐구를 모두 허용하며, 학생 판단과 후속 회차는 필수가 아니다.

제품 코드·설정·의존성·운영 환경은 수정하지 않았다. 실제 AI, Google Sheet, 푸시, 운영 DB를 호출하지 않았다. 배포, Git 초기화·체크아웃·커밋·푸시도 하지 않았다. 변경 산출물은 이 보고서, 상태 문서의 검토 기록, 외부 자격정보를 격리한 `output/astra-review/` 합성 검증 파일뿐이다. `00025-p9s`의 운영 상태와 과거 복원 검증은 인수인계에 적힌 과거 사실이며 이번에 재조회하지 않았다.

### 이번 검증 증거

| 검증 | 결과 | 해석과 한계 |
|---|---|---|
| 기존 Vitest 전체 | 49개 파일·198개 테스트 통과 | 이번 작업본에서 다시 실행. PostgreSQL의 실제 잠금·격리 수준을 증명하지 않음 |
| TypeScript | `tsc --noEmit --incremental false` 통과 | 타입 검사. 빌드·실제 브라우저 검증을 대체하지 않음 |
| 검토 전용 합성 검증 | 14개 모두 통과 | **현재의 잘못된 동작이 발생함을 확인하는 재현 테스트**이며 수정 완료 테스트가 아님 |
| UI 확인 | 일부 컴포넌트의 정적 렌더링과 코드 경로 확인 | 실제 브라우저 조작·한글 IME·태블릿·OS 복원은 미검증 |
| 제품 파일 무변경 | 소스·스크립트·공개 파일·주요 설정 238개의 SHA-256 전후 비교 | 최종 비교 결과는 아래 인계 기록에 기재 |

합성 검증은 `src/test/setup.ts`의 외부 연결 격리와 메모리 모의 DB를 사용했다. AI 요청 생성과 푸시 대상 선정은 실제 제품 함수를 사용하되 외부 발신 함수는 모의 구현으로 대체했다. 테스트 출력의 사용량·전송 이벤트도 합성 기록이다. 악성 파일 공격이나 실제 모델 응답은 실행하지 않았다.

재현 파일: `output/astra-review/reproduction.test.ts` · 설정: `output/astra-review/vitest.config.mts` · 결과: `output/astra-review/results.json`. 재실행: `node node_modules/vitest/vitest.mjs run --config output/astra-review/vitest.config.mts --reporter=json --outputFile=output/astra-review/results.json`. 이 폴더는 Git 제외 산출물이다. 후속 수정 시 아래 재현 조건을 정식 회귀 테스트로 옮기고 기대값을 올바른 동작으로 바꿔야 한다.

## 현재 구조에서 유지할 부분

단일 Next.js 앱과 서비스 계층을 유지해도 현재 수업 흐름을 수용할 수 있다. 이번 발견 때문에 서비스 분리나 대규모 재작성이 필요하지는 않다. 제출 고정본, 문서 이력, 항목별 충돌 검사, 기기 복구 초안, AI 작업의 만료·소유권, 준비물 전송 예약·영수증, 팀 변경 잠금과 평가 공개 고정본이 이미 구현돼 있다. 과거 문서의 ‘AI 작업 잠금 미구현’ 같은 문구를 현재 결함으로 재사용하지 않았다.

서버 로그인·역할·비밀번호 변경 요구와 여러 서비스의 활성 팀원 검사는 확인했다. 학생/팀장/제거 팀원의 구분, 교사 공동 열람, 동아리 담당의 설정·계획 검토 권한, 마스터 계정 관리는 기존 확정 규칙을 유지해야 한다. 특히 제거된 팀원의 과거 자료 삭제나 일반 교사의 기존 공유 열람 차단을 아래 수정의 해결책으로 사용하면 안 된다. 역할 검사에 **요청이 가리킨 회차·제출본·문서 버전** 검사를 더하는 것이 핵심이다.

## A. 확정 오류 및 수정 필요 사항

P1은 관련 수업 사용·다음 배포 전에 우선 처리할 기록·승인·개인정보·진행 차단 문제, P2는 함께 계획해 수정할 기능 정확성 문제다. 발생 조건이 다른 문제들을 단일 ‘전체 기능 불가’로 확대 해석하지 않는다. ‘합성 재현’, ‘정적 확인’, ‘공급사 보안 공지’를 구분했다.

### F01 · P1 · 완료 회차의 계획서·보고서가 복원으로 다시 변경된다

- **위치:** `src/lib/document-history.ts:104,122,168`, `src/app/api/teacher/plans/review/route.ts:23`, `src/app/api/teacher/reports/review/route.ts`.
- **조건·증거:** R13 합성 재현. 문서 이력을 만든 뒤 최종 분석·전체 탐구 완료를 수행하고 교사 복원 함수를 호출하면, 회차는 `completed`인 채 계획서는 `reapproval_required`, 보고서는 `draft`로 바뀐다. 복원 권한과 이력의 회차 일치는 검사하지만 현재 회차가 활성인지 검사하지 않는다. 마지막 회차는 문서 ID와 회차 ID가 그대로 남으므로 통과한다. 화면의 버튼 숨김만으로 API를 막지 못한다.
- **영향:** 완료 기록과 검토 상태가 달라져 완료 회차의 읽기 전용 보존이 깨진다.
- **최소 수정:** 복원 트랜잭션에서 회차 활성 여부와 요청 회차를 확인하고 회차 종료와 같은 잠금을 사용한다. 기존 교사·팀장 권한과 이력 보존은 유지한다.
- **검증:** 완료 직후 교사·팀장의 직접 복원 요청 거절, 이전 회차 이력 거절, 활성 회차의 정상 복원, 복원과 종료의 PostgreSQL 동시 실행을 확인한다.

### F02 · P1 · 교사가 보지 않은 재제출본에 오래된 승인이 적용된다

- **위치:** `src/components/teacher-team-review.tsx:34`, `src/app/api/teacher/plans/review/route.ts:7`, `src/lib/plan-service.ts:187,244`; 보고서 대응 경로 `teacher-report-review.tsx:21`.
- **조건·증거:** R1 합성 재현. 교사가 제출본 1을 연 상태에서 학생이 수정·재제출하여 제출본 2를 만든 뒤, 기존 화면의 승인 요청을 보내면 2가 승인된다. 요청에는 `planId`만 있고 교사가 확인한 제출본 ID가 없다. 현재 상태 검사와 최신 제출본 조회만으로는 이를 구별하지 못한다. 보고서 요청에도 검토한 버전이 없다는 점은 코드로 확인했으며 동일 재현 시험은 계획서에서 수행했다.
- **영향:** 승인·수정 요청 및 교사의 문장이 검토하지 않은 내용에 연결된다. 예정된 AI 피드백 반영 기능에서도 우선 해결해야 한다.
- **최소 수정:** 검토 화면의 `expectedSubmissionId` 또는 해당 고정본·버전과 `expectedCycleId`를 필수로 전달하고 트랜잭션 안에서 일치할 때만 처리한다. 불일치 시 교사 초안을 보존하고 새 제출본 확인을 안내한다.
- **검증:** 제출본 1의 승인·수정 요청이 제출본 2에 적용되지 않음, 같은 제출본 정상 검토, 교사 두 명의 상충 검토, 오래된 AI 검토에서의 전송을 시험한다.

### F03 · P1 · 이전 화면의 저장 요청이 다음 회차 자료로 들어간다

- **위치:** `src/lib/cycle-workflow.ts:125`, `src/lib/plan-service.ts:64`, `src/app/api/inquiry/plan/route.ts:10`, `src/app/api/inquiry/report/route.ts:8`, `src/lib/journal-service.ts:35,172`, `src/app/api/inquiry/journals/route.ts:17`.
- **조건·증거:** R2·R3 합성 재현. 다음 회차에서 기존 계획서·보고서 행 ID를 재사용한다. 요청에는 원래 회차 ID가 없고 서버는 현재 활성 회차를 선택한다. 양쪽 회차의 선택 항목이 빈 값이면 `expectedValue: ""` 검사도 통과하여 오래된 계획서·보고서 내용이 새 회차에 저장된다. 보고서는 새 회차의 승인·학생 접근 조건도 충족한 상태에서 재현했다. 이전 화면의 미생성 일지 1차시 역시 새 회차에 일지가 없고 접근 조건이 충족되면 새 회차 일지로 생성된다.
- **영향:** 단순 버전 충돌 방어를 우회하는 회차 간 기록 혼입이다. 구버전 요청에서 기대값 생략이 허용되는 경로는 별도 점검도 필요하다.
- **최소 수정:** 문서·일지의 저장·잠금·제출 요청에 원래 회차를 필수로 고정하고, 같은 트랜잭션의 잠금 아래 확인한다. 준비물·대화·주제·AI 요청 등 ‘현재 회차를 추론하는 쓰기’도 동일 계약으로 점검한다. 필수 식별자가 없는 옛 요청은 조용히 새 회차에 배정하지 않는다.
- **검증:** 빈 값/동일 값, 지연 요청, 새 회차 준비 전후, 회차 종료와 저장의 실제 PostgreSQL 경합, 옛 클라이언트 요청을 확인한다.

### F04 · P1 · 기기 복구 초안도 회차를 구분하지 않는다

- **위치:** `src/components/use-document-editor.ts:7,27`, `src/components/inquiry-workspace.tsx:93`, `src/lib/document-drafts.ts`.
- **조건·증거:** R6 합성 재현과 화면 연결 확인. 저장소 키는 사용자·문서 종류·문서 ID만 포함한다. 회차 전환 때 컴포넌트를 다시 만들어도 같은 문서 ID의 이전 초안이 복구된다. 이전 원격 기준값과 새 회차 값이 모두 빈 문자열이면 충돌 표시 없이 편집 초안으로 남는다. F03은 이를 새 회차에 저장할 수 있는 서버 경로까지 보여 준다.
- **영향:** 학생이 새 회차의 글로 오인해 이전 초안을 공유할 수 있다.
- **최소 수정:** 사용자·회차·문서·양식 버전으로 복구 범위를 구분한다. 이전 글은 별도 복구 대상으로 보존하며 자동 삭제하지 않는다. 대기 중 요청도 생성 당시의 문서·회차를 유지해야 한다.
- **검증:** 회차 전환·양식 변경·사용자 전환·늦은 응답을 조합하고, 옛 초안은 보존되지만 새 회차에 자동 삽입되지 않음을 확인한다.

### F05 · P1 · 오래된 회차 분석을 화면에서 다시 실행할 수 없다

- **위치:** `src/components/cycle-analysis-panel.tsx:157,214`.
- **조건·증거:** R12 정적 렌더링 재현. 분석 후 자료가 변경되면 ‘다시 분석해야 한다’고 표시하지만 분석 생성 버튼은 `!active.analysis`일 때만 나온다. 기존 분석이 있으면 최신성 검사로 비활성화된 전환 버튼만 남는다.
- **영향:** 서버가 같은 종류의 새 분석을 지원해도 교사는 정상 화면에서 회차 전환·완료를 진행할 수 없다.
- **최소 수정:** 기존 분석을 보존하면서 최신 자료로 재분석하는 행동을 제공하고, 자료의 승인·확인 조건이 부족하면 충족할 조건을 보여 준다.
- **검증:** 중간·최종 각각 분석 후 자료 변경→재검토→재분석→전환, 재분석 실패 시 기존 결과 보존을 UI와 서비스에서 확인한다.

### F06 · P1 · 중간 분석 선택 후 같은 탐구를 마칠 수 없다

- **위치:** `src/lib/cycle-analysis.ts:364`, `src/lib/cycle-workflow.ts:60`, `src/components/cycle-analysis-panel.tsx:214`.
- **조건·증거:** R4 합성 재현. 중간 분석이 한 번 존재하면 최종 분석으로 변경할 수 없고, 전체 완료는 최종 분석을 요구한다. 같은 탐구를 중간 점검한 뒤 종료하려면 다음 회차를 만들어야 한다. R7의 대조 사례에서는 **첫 회차에서 바로 최종 분석하면 한 회차로 완료할 수 있었다.** 최소 회차 수 자체가 문제는 아니다.
- **영향:** 중간 점검 뒤 같은 탐구의 계속·종료를 허용해야 한다는 최신 기준과 충돌한다. 여러 변인 실험을 불필요한 다음 회차로 유도할 수 있다.
- **최소 수정:** 중간 점검 이력을 유지한 채 같은 회차를 계속하고 최종 정리할 수 있게 분석 종류 고정 규칙과 화면을 함께 바꾼다. 강제 탐구 유형 분류나 여러 보고서 생성을 추가하지 않는다. 과거 구현 계획의 종류 고정 문구도 최신 결정과 맞춘다.
- **검증:** 단일 탐구 직접 완료, 중간 점검 뒤 같은 회차 완료, 여러 변인 비교의 한 보고서 완료, 실제 후속 질문이 생긴 경우에만 다음 회차로 이동하는 시나리오를 확인한다. 실제 AI 표현 평가는 G06에 남긴다.

### F07 · P1 · 지난 회차의 개인 일지를 다시 읽는 경로가 빠져 있다

- **위치:** `src/lib/journal-service.ts:152,288`, `src/app/api/teacher/journals/route.ts`, `src/components/inquiry-workspace.tsx:95`, `src/components/cycle-analysis-panel.tsx:218`.
- **조건·증거:** R5 합성 재현. 교사 조회에 나오던 일지가 다음 회차 시작 후 0건이 되지만 DB 행은 남는다. 조회는 활성/최신 회차 하나를 선택하고 API에 과거 회차 선택자가 없다. 학생도 활성 회차 중심이며 완료 화면의 탐구 여정은 계획서·보고서·준비물·분석을 보여 줄 뿐 개인 일지 원문·성찰·사진 열람을 대체하지 못한다.
- **영향:** 기록 삭제는 아니지만 학생 본인과 교사가 과거 활동을 확인하기 어렵다.
- **최소 수정:** 회차를 명시한 읽기 경로와 화면을 추가하고 쓰기 가능 여부를 분리한다. 본인 일지·교사 열람 범위 및 제거 팀원의 과거 기록 규칙을 유지한다.
- **검증:** 두 회차의 같은 차시 일지·사진 구분, 마지막 회차 완료 후 조회, 다른 학생의 개인 일지 차단, 제거 팀원의 교사 역사 조회와 편집 금지를 확인한다.

### F08 · P1 · 동아리 맞춤 보고서 내용을 교사 검토 화면이 누락한다

- **위치:** `src/components/teacher-report-review.tsx:5,60`.
- **조건·증거:** R11 정적 렌더링 재현. 동아리의 맞춤 필드에 작성한 값은 존재하지만 화면은 고정 `REPORT_FIELDS`만 순회한다. 맞춤 본문 없이도 ‘확인 완료’ 버튼이 나타난다.
- **영향:** 교사가 실제 제출 내용을 보지 못한 채 보고서를 확인 완료할 수 있다.
- **최소 수정:** 해당 문서에 고정된 필드 정의·유형을 사용해 표시한다. 과탐실 기본 양식은 유지하고 과거 양식을 현재 설정으로 덮어 해석하지 않는다.
- **검증:** 기본 양식 및 동아리 텍스트·표·선택 필드, 설정 변경 전후 제출본, 완료 회차의 교사 표시를 확인한다. 실제 원본 보고서·Sheet 구조를 추측해 새 필드를 만들지 않는다.

### F09 · P1 · 회차 제목은 AI 전송 전 개인정보 가림을 우회한다

- **위치:** `src/lib/cycle-analysis.ts:213,236`, `src/lib/cycle-settings.ts`.
- **조건·증거:** R8 합성 재현. 출처 본문은 가리지만 자유 입력 제목이 든 `snapshot.cycle`은 그대로 직렬화한다. 합성 제목에 넣은 학번 형태 문자열과 시험용 이메일이 실제 요청 생성 함수의 AI 입력에 그대로 남았다. 외부 AI 발신은 모의 구현으로 막았다.
- **영향:** 제목에 식별정보를 넣는 경우 본문 보호와 달리 외부 모델 입력에 포함될 수 있다. 실제 학생 정보 유출 여부는 확인하지 않았다.
- **최소 수정:** 전송 객체 전체의 자유 입력 필드에 동일한 개인정보 처리 규칙을 적용한다. 계획서 AI 검토의 회차 정의 처리와 공통화할 수 있다.
- **검증:** 제목·라벨·본문·추가 메타데이터에 합성 이름·학번·연락처를 넣고 최종 발신 직전 payload를 검사한다. 프롬프트 문구만으로 해결하지 않는다.

### F10 · P2 · 새 학생용 AI 계획 검토도 즉시 오래된 결과로 표시된다

- **위치:** `src/lib/plan-snapshots.ts:112`, `src/lib/inquiry-data.ts:141,296,361`.
- **조건·증거:** R14 합성 재현. 학생 AI 검토 직후 아무것도 바꾸지 않고 조회해도 `isCurrent`가 false다. 스냅샷의 `cycleDefinition`과 조회 시 해시를 계산하는 회차 객체의 모양이 다르며 후자에는 `id`가 포함된다.
- **영향:** 최신성 경고를 신뢰하기 어렵고 불필요한 재검토를 유도한다.
- **최소 수정:** 저장과 조회가 같은 정규화 함수를 사용하도록 한다. 과거 고정본을 임의로 다시 쓰지 말고 기존 해시 호환 방식을 정한다.
- **검증:** 직후에는 true, 본문·양식·의미 있는 회차 정의 변경 시 false, 비내용 변경에는 일관된 결과를 확인한다.

### F11 · P2 · AI 완료 결과 재사용이 프롬프트·스키마 버전을 무시한다

- **위치:** `src/lib/plan-ai-review.ts:81,257,296`, `src/lib/cycle-analysis.ts:389,426`, 관련 결과 테이블의 고유 제약.
- **조건·증거:** R9 합성 재현. 기존 검토 결과의 저장된 프롬프트 버전을 과거 값으로 설정한 뒤 현재 검토를 요청해도 기존 결과가 재사용되고 생성 함수는 추가 호출되지 않는다. 작업 키에는 버전이 있어도 완료 결과 조회와 고유 제약은 스냅샷·대상 또는 분석 종류만 본다. 회차 분석도 같은 구조임을 정적으로 확인했다.
- **영향:** 개인정보·교육적 지침·출력 형식을 고쳐도 같은 자료에서는 옛 결과가 남는다.
- **최소 수정:** 결과 조회·저장 고유성에 프롬프트/스키마 버전을 함께 적용하고 과거 결과를 이력으로 유지한다. 이미 적용된 마이그레이션을 고치지 말고 새 번호의 변경을 추가한다.
- **검증:** 동일 자료·동일 버전 재사용, 버전 변경 시 새 결과, 구버전 결과 열람, 동시 요청 중복 방지를 시험한다.

### F12 · P2 · 근거가 없는 AI 사실 진술이 구조 검증을 통과한다

- **위치:** `src/lib/cycle-analysis.ts:33,40,52,59,182,416`; 계획 검토의 필드 참조 검증도 점검 대상.
- **조건·증거:** R7 합성 재현. 발견·제안의 `evidenceIds`가 빈 배열인 결과가 수락된다. 검증은 존재하지 않는 ID를 걸러내지만 빈 배열은 허용한다. 또한 길이 제한으로 실제 전송된 자료가 아니라 전체 출처 목록을 기준으로 검사한다는 점을 코드로 확인했다.
- **영향:** 근거가 없는 사실·활동·역량 추정을 금지한 결정이 서버에서 충분히 보장되지 않는다. ID 존재만으로 내용상 뒷받침 여부까지 검증되지는 않는다.
- **최소 수정:** 사실 진술에는 실제 전송된 출처의 참조를 요구하고, ‘자료가 없어 확인 불가’ 같은 한계 설명은 별도 형태로 허용한다. 계획 검토의 필드 키도 실제 제공한 정의와 대조한다.
- **검증:** 빈 참조·가짜 참조·전송에서 잘린 참조 거절, 정당한 자료 부족 설명 허용, 인용과 주장 일치의 합성 평가를 추가한다.

### F13 · P1 · 교사 피드백 초안에 기기 복구가 없다

- **위치:** `src/components/teacher-team-review.tsx:21,34`, `src/components/teacher-report-review.tsx:15,21`.
- **조건·증거:** 정적 확인. 미전송 피드백은 컴포넌트의 `useState`에만 있고 복구 저장소 연결이 없다. 새로고침·언마운트 후 서버에 있던 이전 피드백으로 돌아간다. 검토 요청의 네트워크 예외를 정리하는 `catch/finally`도 없어 실패 시 busy 상태가 남는 경로가 있다. 이번에는 실제 기기에서 타이핑·이동을 재현하지 않았다.
- **영향:** 교사가 직접 작성한 글이 사라질 수 있다. `[피드백에 반영]`을 이 상태에 붙이면 누적한 AI 항목과 자필 문장도 같은 위험을 갖는다.
- **최소 수정:** 교사·팀·문서·제출본에 묶인 복구 초안과 실패 복구를 먼저 만든다. AI 항목 반영 상태는 AI 결과 ID까지 구분한다. 입력·반영만으로 학생 전송을 실행하지 않는다.
- **검증:** 자필 입력→외부 이동·뒤로가기·새로고침, 다른 팀/제출본 격리, 실패·응답 지연, 명시적 전송 전에는 API·알림이 발생하지 않는지 확인한다. F02와 함께 처리한다.

### F14 · P2 · 판단 저장의 늦은 응답이 더 최근 복구 초안을 지운다

- **위치:** `src/components/cycle-analysis-panel.tsx:121,128,150`.
- **조건·증거:** 정적 확인. A를 저장하는 동안 입력은 계속 가능하여 B를 복구 저장소에 기록할 수 있다. A의 성공 응답은 현재 초안이 A인지 확인하지 않고 저장소 항목을 삭제한다. B가 화면에 남더라도 이후 새로고침 때 복구할 수 없다. 실제 브라우저 지연 주입은 미수행이다.
- **영향:** 선택적 학생 판단의 저장 후 추가 입력이 유실될 수 있다.
- **최소 수정:** 요청에 담은 초안 버전만 완료 처리한다. 그 뒤 편집된 내용은 새 서버 기준 버전과 함께 보존한다. 학생 판단을 회차 종료의 필수 조건으로 바꾸지 않는다.
- **검증:** A 저장 응답 지연→B 입력→A 성공→새로고침에서 B 보존, 실패·충돌·다른 분석 전환을 확인한다.

### F15 · P2 · 전체 공지 푸시 대상에 체험 계정이 포함된다

- **후속 상태(2026-09-08):** 로컬 수정·검증 완료. 전체/학급/팀 수신자 조회에서 체험 계정을 제외하며 비활성·구독 해제·팀에서 제거된 관계의 범위를 모의 발신기로 검증했다. 운영에 발송하거나 배포하지 않았다.
- **위치:** `src/lib/push-notifications.ts:171`.
- **조건·증거:** R10 합성 재현. 활성 체험 계정에 합성 구독을 만들고 전체 학생 공지를 전송하면 모의 발신기에 1건이 전달된다. 수신자 조회에 `account_type = 'standard'` 제한이 없다. 실제 푸시는 발송하지 않았다.
- **영향:** 체험 계정과 실제 수업 알림의 분리 원칙이 지켜지지 않는다.
- **최소 수정:** 수신자 선정에서 체험 계정을 기본 제외한다. 체험 알림 시험이 필요하면 별도 명시적인 시험 범위를 사용한다.
- **검증:** 전체·학급·팀 범위, 비활성·제거·구독 해제·체험 계정의 대상 선정을 외부 발신 없이 검증한다.

### F16 · P1 · 명단 업로드의 xlsx 버전이 알려진 취약 범위에 있다

- **후속 상태(2026-09-08):** 로컬 수정·검증 완료. [공식 배포 안내](https://docs.sheetjs.com/docs/getting-started/installation/nodejs/)의 0.20.3 tarball과 무결성 값을 패키지/잠금 파일에 고정했다. 실제 multipart 바이트(파일 2MB+부가정보 64KiB)와 파일 2MB, 첫 시트 1,000명/머리글 포함 1,001행/32열, ZIP 256항목·실제 해제 총량 16MiB를 제한한다. ZIP 중앙/로컬 크기와 실제 해제량을 대조하고 ZIP64·암호화 파일은 거절한다. 파싱 worker에는 환경변수를 전달하지 않고 10초 타이머와 V8 힙 예산을 적용한다(OS 전체 메모리 격리 아님). 예상하지 않은 예외는 일반 안내로 바꾸고 중복 학번 오류도 학번을 응답하지 않는다. 한글 XLS/XLSX·행 경계·손상/위장/압축 과다·허위 크기·기존 비밀번호/팀 관계 보존 회귀 통과. 시간 초과/오류 종료는 모의 worker로 확인했다. 최초 배포 파일 추적에서 빠진 CommonJS 파일을 명시적으로 포함한 뒤 재빌드하고, standalone 내부 패키지로 실제 worker 파싱도 통과했다. 전체 230개 테스트 통과. 전체 의존성 감사 및 실제 Linux 컨테이너 검증은 이 결과에 포함하지 않는다.
- **위치:** `package.json:27`의 `xlsx` 0.18.5(설치 버전도 확인), `src/lib/roster.ts:49`, `src/app/api/teacher/roster/route.ts:15`.
- **조건·증거:** 공급사 공지와 코드 대조. 교사가 올린 파일을 해당 파서로 읽으며 확장자 검사 외 명시적인 파일 크기·행 수 제한이 없다. 공급사는 0.20.1 이하의 정규식 서비스 거부 취약점과 0.19.2 이하의 악성 파일 읽기 시 prototype pollution을 고지했다. 현재 버전은 둘 다 해당한다. 악성 파일 재현은 하지 않았다. 교사 인증이 필요한 경로이며 익명 원격 코드 실행을 확인했다는 의미가 아니다.
- **영향:** 악성·비정상 명단 파일 처리의 가용성과 객체 안전성에 알려진 위험이 있다. 이번 변경에서 새로 생긴 의존성이라고 주장하지 않는다.
- **최소 수정:** 공급사가 지원하는 배포 방식·수정 버전을 확인해 파서를 교체/업데이트하고 명단 호환성을 검사한다. 파싱 전 크기 제한, 처리 가능한 행·압축 해제량·시간 예산, 사용자용 오류 메시지를 둔다. 이 검토에서는 설치하지 않았다.
- **검증:** 정상 한글 XLS/XLSX 명단, 경계 크기·행 수, 손상/과도한 파일의 통제된 실패, 학생 데이터·내부 경로가 오류에 노출되지 않음을 확인한다.
- **출처(2026-09-08 확인):** [SheetJS CVE-2024-22363](https://cdn.sheetjs.com/advisories/CVE-2024-22363), [SheetJS CVE-2023-30533](https://cdn.sheetjs.com/advisories/CVE-2023-30533). npm에서 단순 최신 설치만 하면 해결된다고 가정하지 않는다.

### F17 · P1 · 시험 근거가 현재 초안과 여러 회차 일지를 잘못 연결할 수 있다

- **최신 후속 상태(2026-09-08, 11차):** 로컬 수정·합성 검증 완료. 승인 제출 스냅샷, 보고서 확인 이력, 회차 종료 시 보존한 승인/확인 원문을 회차별로 선택한다. 현재 승인/확인 상태만 남아 있는 기존 자료는 현재 고정한 사실을 표시하며 과거 시점을 만들지 않는다. 익명 AI 역할이나 현재 학생 순서로 과거 개인 역할을 추정하지 않는다. 원본 고정 ID와 일지/추가 탭 고유 ID가 출제 입력 및 저장 근거에 이어지고, 잘못된 출처는 완료 결과 저장 전에 차단한다. 두 회차·수정 중 초안·미확인 제출 보고서·역할 변경·제거 학생·실제 출제 저장 대응 검증과 전체 240개 테스트/빌드 통과. 아래 부분 완료 표기는 중간 기록이다. PostgreSQL 잠금/명단 경합은 G01/G02의 실제 DB 검증으로 남으며 운영 배포는 실행하지 않았다.
- **후속 상태(2026-09-08):** 부분 수정. `journal.<cycleId|legacy>.<journalId>.<field>`로 키를 구분하고 라벨에 회차·차시를 표시한다. 같은 차시 두 회차의 입력→선택→시험 저장→원본 후속 변경 보존을 검증했다. 제목이 같은 추가 탭도 설정 버전/응답 ID로 키를 구분한다. 승인 계획서·확인 보고서 고정본 선택과 역할 연결은 계속 미완료다. `evidenceFor`의 알 수 없는 키를 첫 근거로 대체하는 동작과 입력보다 짧은 저장 인용문도 후속 제거 대상이며 이번 근거 키 변경만으로 F17을 완료 처리하지 않는다.
- **위치:** `src/lib/exam-service.ts:203,240,284,343`.
- **조건·증거:** 정적 확인. 팀 문항 자료는 승인 고정본 대신 현재 `p.form_data`를 읽고 계획서 승인 상태를 제한하지 않는다. 조회한 `report_status`도 근거 채택에 사용하지 않는다. 개인 일지는 여러 회차를 조회하면서 `cycle_id` 없이 `journal.<차시>.activities` 같은 키를 만든다. 두 회차의 같은 차시는 같은 키가 되고, 이후 `Map`은 앞 자료를 뒤 자료로 덮어 근거를 선택한다. 실제 시험 생성 호출은 하지 않았다.
- **영향:** 승인된 계획서만 사용한다는 시험 설계와 다르고, 문항이 참조한 일지와 교사에게 표시·고정되는 근거가 달라질 수 있다.
- **최소 수정:** 시험의 대상 회차/자료 범위를 명시하고 승인된 계획서 고정본과 사용 가능한 보고서 상태를 선택한다. 개인 근거 키에 회차와 원본 ID를 포함하고 선택된 원문을 그대로 고정한다. 모든 회차마다 별도 시험을 강제하지 않는다.
- **검증:** 승인 후 수정 중 계획, 미제출/확인 보고서, 두 회차의 같은 차시·같은 제목, 제거 팀원 이력과 현재 시험 대상 분리, 생성 입력과 저장 근거의 정확한 대응을 확인한다.

## B. 검증 부족 — 실제 실패로 확정하지 않은 항목

아래는 수정을 했다고 가정하거나 모의 DB 성공으로 닫을 수 없다. 위치·조건·영향·최소 대응·검증을 다음 작업에 남긴다.

### G01 · PostgreSQL 문서 쓰기와 회차 종료의 원자성

- **후속 20차(2026-09-09):** saveDiscussionEntry의 외래 키 대기 이후 완료 회차 삽입을 실제 DB에서 재현해 세션/회차 잠금과 active 재검사를 추가했다. confirmMeeting도 같은 경계에서 현재 회차 원문과 참여자를 확인한다. 전환 선행 시 원격 대화·대면 메모·내용 확인 각각 × start_next/finish_project의 6개 검증 통과. 이전 원문은 보존하고 늦은 새 글/확인은 생성하지 않는다. 저장 선행 시 아직 요약되지 않은 원문의 분석/요약 반영은 G05와 함께 미완료다.
- **후속 16차(2026-09-08):** 계획서 제출/일지 저장 × 다음 회차/탐구 완료 × 양쪽 선행 순서의 추가 8개 통과. 제출 고정본은 기존 회차 pending으로만 생성하고 전환 후 낡은 제출은 고정본을 만들지 않는다. 일지의 활동·관찰·개인 성찰은 해당 회차에만 남는다. 준비물은 외래 키 대기로도 오류가 막히지 않는 것을 실제 DB에서 재현했다. 전환의 미전송 검사 뒤 신청이 대기했다가 완료 회차에 pending으로 삽입됐다. materials.ts 저장 트랜잭션의 세션→회차 잠금/active 재검사로 수정했고 준비물 추가 4개 경합 통과. 입력 회차도 UI/API에 필수로 연결해 요청 시작 전에 이미 전환된 옛 화면을 구분한다. 총 40개 수락 시나리오(15차 28+이번 12)의 증거가 있으며, 별도 material_race_reproduction 결과 2개는 수정 전 결함 재현이다. 승인/확인 완료 방향, 기존 준비물 수정·동시 전송 완료와 경합, 실제 외부 연동/G02의 소속 변화 등은 여전히 남는다.
- **후속 15차(2026-09-08):** 같은 실제 DB 검증 도구를 28개 시나리오로 확대했다. savePlanField/saveReportField, restorePlanRevision/restoreReportRevision, submitReport, reviewPlan(feedback)/reviewReport(feedback) × start_next/finish_project × 양쪽 선행 순서. 각 조합에서 실제 다른 연결의 잠금 대기를 관찰하고 후행 요청의 상태 재검사와 기록 보존을 확인했다. 복원은 교사 권한으로 수행했으며 과거 참여자의 역할과 계획서 주제 동기화를 검증했다. 수정 요청 알림은 성공한 기존 문서/팀에만 생성되고 거절된 요청에는 생성되지 않는다. 계획서 제출, 승인/확인 완료 방향, 일지/준비물의 전환 경합은 미완료이므로 G01 전체 통과로 확대하지 않는다.
- **후속 14차 실제 앱 경합(2026-09-08):** scripts/local-postgres-document-race.mjs의 독립 실제 DB 연결에서 saveReportField와 transitionCycle(start_next/finish_project)을 각각 선행시킨 4개 교차 실행 통과. 세션/회차 잠금 뒤 쿼리 직전에 멈춰 다른 앱 연결의 pg_blocking_pids/Lock 대기를 관찰한다. 전환 선행 시 옛 회차 저장 거절·고정 이력/완료 원문·새 빈 문서 보존, 저장 선행 시 새 내용/draft 보존·회차 active 유지·오래된 분석의 전환 거절을 단언했다. 외부 AI는 주입한 합성 응답으로 대체했으며 결과는 제외 폴더 output/test-infra/postgres-document-race.json. 보고서 필드 저장 외 나머지 조합은 미완료다.
- **후속 검증 환경(2026-09-08):** scripts/local-postgres.ps1 및 scripts/local-postgres-smoke.mjs로 격리된 실제 PostgreSQL 16.15를 준비했다. 실제 행 잠금 대기/해제·롤백 기초 검증은 통과했으나 아래 앱 서비스 교차 실행은 아직 남는다.
- **위치:** `cycle-workflow.ts`, `plan-service.ts`, `report-service.ts`, `document-history.ts`, `cycle-evidence.ts`, `plan-snapshots.ts`(모두 `src/lib/`).
- **조건·영향:** 회차 전환의 세션→회차 잠금과 일반 저장·제출·복원의 검사/쓰기 범위가 일치하지 않는다. 일부 보고서 작업은 여러 쿼리가 하나의 트랜잭션으로 묶이지 않는다. 스냅샷도 여러 테이블을 순차 조회하므로 시점 혼합 가능성을 검증해야 한다. F01·F03은 순차적으로도 재현된 별도 확정 오류다.
- **최소 대응·검증:** 세션·회차·문서 잠금 순서와 스냅샷 시점을 정리한 뒤 실제 PostgreSQL의 독립 연결에서 저장/제출/검토/복원/일지/준비물과 finish/start를 중간 지점에 멈춰 교차 실행한다. 교착·잘못된 완료 상태·스냅샷과 원본 불일치가 없는지 확인한다. 과거 ‘회차 전환끼리 경합 통과’와 구별한다.

### G02 · 팀 변경과 평가 공개의 전체 경쟁 상황

- **후속 18차(2026-09-08):** scripts/local-postgres-evaluation-race.mjs에서 실제 saveSelfEvaluation/savePeerEvaluation과 removeStudent/assignStudent/deactivateStudent를 교차했다. 권한 사전 조회 뒤 소속 변경이 끝나도 옛 세션 평가가 저장되는 7개 조합을 재현했다. writeEvaluation은 평가 회차→세션→학생→팀 잠금 뒤 작성자/피평가자의 활성 일반 계정·현재 같은 팀·평가 학급/동아리 범위를 다시 검사한다. 변경 선행이면 평가 미생성, 저장 선행이면 변경이 실제 Lock 대기 후 진행되며 기존 평가와 과거 팀 관계를 보존하는 14개 검증 통과. 공개는 세션을 학생보다 먼저 잠가 일지/평가 저장과 역순 대기를 피하고 최초 후보 밖의 새 대상·잠그지 않은 새 세션이 발견되면 전체 공개를 롤백한다. 공개 후보 조회 후 새 팀원 배정 시 공개 중단/피드백 확인 후 재공개, 공개 선행 시 제거 작업 대기/2명 고정본 보존/반복 공개 불변의 2개 추가 검증도 통과했다. 결과는 output/test-infra/postgres-evaluation-race-before.json / after.json. 모든 신규 가입/팀 보관/동아리 중복 소속·시험 대상 변경·일지와 공개 교차 조합까지 검증한 것은 아니다.
- **위치:** `src/lib/team-mutation-locks.ts`, `evaluation-service.ts`, `journal-service.ts`, 팀·계정 변경 서비스.
- **조건·영향:** 학생→팀 잠금과 공개 시 후보 재조회는 이미 있으나, 처음 읽은 후보 밖의 신규 참여자, 잠금 전에 확인한 소속, 동시 비활성화까지 안정적인지 이번에 실제 DB로 증명하지 않았다. 잘못된 평가 공개 대상·인원 수 또는 변경 전 권한으로 쓰는 위험이다.
- **최소 대응·검증:** 읽기·권한 재검사·공개 고정본 생성 경계를 표로 만들고 팀 이동/새 가입/제거/비활성화/평가 저장과 공개를 독립 연결에서 교차한다. 현재 팀원 권한과 제거 팀원의 역사 보존을 모두 단언한다. 기존 잠금을 없애거나 전체 학급 직렬화부터 도입하지 않는다.

### G03 · AI 만료 작업의 소유권과 단계 결과 저장

- **후속 19차(2026-09-09):** 공통 소유권 검증에 더해 실제 requestCycleAnalysis의 중간/최종 각각 선행·중복 요청 및 만료 인계 후 늦은 응답 4개를 통과했다. 겹친 요청의 생성 함수 미호출, 중간 후 최종 허용/최종 후 중간 차단, 새 소유자 결과만 남기기 및 같은 결과 ID 재사용을 확인했다. 따라서 아래 ‘실제 중간/최종 서비스 경쟁 미확인’은 이 기록으로 대체한다. 외부 모델 호출 품질/실제 공급자 호출 횟수는 별도 범위이며 F03의 대화 회차 재검사는 남는다.
- **후속 17차(2026-09-08):** scripts/local-postgres-ai-race.mjs로 실제 서비스/독립 PostgreSQL 연결을 사용해 소유권 확인 직후 멈춘 옛 단계 쓰기가 인계 후 저장되는 문제와 다른 요청 키의 옛 작업이 여전히 단계/완료 결과를 쓰는 문제를 재현했다. ownsAiJob에 FOR UPDATE를 추가해 쓰기 트랜잭션 종료까지 권한을 고정하고 beginAiJob의 자원 잠금 아래 만료된 다른 processing 요청의 토큰을 무효화한다. 생성 호출은 잠금 밖에 둔다. 같은/다른 요청 각각에서 인계 선행의 늦은 결과 거절·단계 미생성, 저장 선행의 실제 인계 잠금 대기·인계 전 정상 단계 보존, 저장 실패 롤백 후 새 단계 생성의 6개 시나리오 통과. 옛 토큰의 완료 쓰기도 차단하고 같은 요청의 정상 단계 재사용을 확인했다. 결과는 output/test-infra/postgres-ai-race-before.json / after.json. 기본 실행이 수락 검증이며 --reproduce는 수정 전 오류 기록용이다. 최초 동시 자원 획득, 실제 중간/최종 분석 서비스의 경쟁 및 외부 공급자 호출 횟수까지 입증한 것은 아니다.
- **위치:** `src/lib/ai-jobs.ts`, `plan-ai-review.ts`, `cycle-analysis.ts`.
- **조건·영향:** 만료·소유자 토큰과 완료 시 비교는 있으나 단계 실행의 소유권 확인 SELECT와 결과 INSERT 사이에 인계가 발생하면 옛 실행자의 단계 결과가 남을 수 있는지 확인이 필요하다. 중간·최종은 작업 자원이 달라 최초 동시 요청 시 종류 고정 규칙도 별도 경합 대상이다.
- **최소 대응·검증:** 현재 소유자만 단계 결과를 기록하도록 쓰기 경계에서 조건을 보장한다. PostgreSQL에서 만료 직전/직후 재시도·중단·늦은 모델 응답·서로 다른 분석 종류를 교차한다. 정상 재사용은 유지한다. 외부 공급자 호출까지 정확히 한 번임을 현재 구조만으로 보증하지 않는다.

### G04 · 준비물의 실제 Sheet 계약과 복구 절차

- **위치:** `src/lib/material-sheet-transfer.ts`, `materials.ts`, `cycle-workflow.ts`.
- **조건·영향:** 고정 전송 배치·예약·영수증은 확인했다. 실제 Sheet에서 응답 유실 뒤 영수증 확인, 충돌·권한 오류·중단 복구, 과거 형식과 회차별 배치 위치는 이번에 실행하지 않았다. `team_sections`의 다음 회차 처리와 기존 팀 구역의 보존 정책은 실제 설정 없이는 추정하면 안 된다. 체험 준비물의 미전송 상태가 회차 전환의 미동기화 차단과 충돌하는지도 통합 확인이 필요하다.
- **최소 대응·검증:** 외부 전송 승인 후 별도 시험 Sheet의 실제 구조를 읽어 계약을 확정하고, 성공/응답 유실/재시도/영구 실패/새 회차를 검증한다. 오래된 예약·영수증을 임의 삭제하지 않는 운영 복구 절차를 만든다. [Google batchUpdate 문서](https://developers.google.com/workspace/sheets/api/reference/rest/v4/spreadsheets/batchUpdate)의 일괄 원자성은 앱 DB와의 원자성 또는 이후 공동 편집 불변성을 뜻하지 않는다. 현재는 실제 전송 보류다.

### G05 · 대화와 AI 출처의 회차 귀속·내용 범위

- **위치:** `src/lib/discussions.ts`, `cycle-evidence.ts:165`, `cycle-analysis.ts`의 출처 목록 생성.
- **조건·영향:** 대화 읽기·일별 요약은 세션/날짜 중심이고 회차 분석은 날짜 경계에 의존한다. 같은 날 회차 전환, 일정 변경, 종료 후 늦은 요약이 올바른 회차에 귀속되는지 확인하지 않았다. 준비물은 보존 스냅샷에 있지만 AI 출처 목록의 동등한 근거 항목으로 제공되는지, 이전 회차가 원문 대신 이전 분석 요약으로만 대표돼 주장이 확대되지 않는지도 확인해야 한다.
- **최소 대응·검증:** 대화 원본·요약·회차의 식별 연결과 늦은 요약 처리 규칙을 명시하고 고정본에 실제 포함된 근거를 목록화한다. 동일 날짜 두 회차·미요약 메시지·일정 변경·삭제 아닌 역사 보존·길이 제한을 합성 데이터로 검증한다. 개인 일지의 팀 공개 권한을 확대하지 않는다.

### G06 · 실제 기기·AI 품질·학년도·푸시 네트워크 경계

- **위치:** `src/components/use-document-editor.ts`, `use-form-draft.ts`, 교사 검토 컴포넌트, `src/lib/cycle-analysis.ts:220`, `auth.ts`, `push-notifications.ts`.
- **조건·영향:** 기존 문서 초안·충돌 테스트는 있지만 한글 조합, 모바일 외부 이동/OS 탭 폐기, 저장 공간 차단, 재접속을 실제 학생 기기로 이번에 확인하지 않았다. 최종 분석 프롬프트의 이전 회차·학생 판단 가정은 단일 탐구를 부당하게 평가할 가능성을 실제 응답으로 평가해야 한다. 인증 계정과 업무 조회의 학년도 경계, DNS 사전 검사와 실제 푸시 연결의 주소 일치도 전체 보증되지 않았다.
- **최소 대응·검증:** 실제 기기 접근 시 미저장 글·늦은 저장·여러 기기 충돌부터 확인한다. 실제 AI 호출 전 합성 평가표에 단일/여러 변인/연쇄, 학생 판단 없음, 검증하지 않은 최적 조합, 입력문 속 지시·식별정보를 넣는다. 두 학년도·역할별 자료 격리와 제거 팀원 사례를 모의 시험한다. 푸시는 DNS 응답 변화·일시 해석 실패를 모의 주입해 사설 주소 연결 방지와 정상 구독 보존을 확인한다. 실제 모델·푸시 시험은 별도 승인 전 실행하지 않는다.

### G07 · 초기화·마이그레이션·복구와 입력 한계

- **후속 13차(2026-09-08):** 실제 빈 DB의 스키마 생성 경쟁(3개 중 2개 23505)을 재현해 전체 앱 초기화 잠금을 추가했다. 실제 getDb 앱 프로세스 3개의 동일 초기 자료 확인, 초기 자료 생성 초반/마지막 SQL 실패 시 전체 롤백과 재시도, 잠금 해제 및 실패 연결 정리를 검증했다. 초기 자료 생성은 한 연결의 트랜잭션이며 기존 부분 생성 DB를 임의 정리하지 않는다. 검증 스크립트 `scripts/local-postgres-bootstrap.mjs --app`, `--seed-failure`, `--seed-failure-late`는 표식이 있는 127.0.0.1:55416 전용 합성 DB만 사용한다. 아래 ‘전체 초기 SCHEMA_SQL 동시 실행 미완료’는 12차 당시 상태이며 이번에 검증했다. 구버전 공존·미래 마이그레이션·전체 복구/의존성 감사는 계속 남는다.
- **후속 기초 검증(2026-09-08):** 실제 PostgreSQL에서 0001~0005를 적용한 합성 DB의 보고서 원문/상태/시각을 보존하며 독립 풀 3개가 0006/0007을 한 번씩 적용하는 것을 확인했다. 최종 7개 기록, 보고서 초기 번호 0, AI 프롬프트/스키마 버전 고유 제약 2개를 검증했다. 전체 초기 SCHEMA_SQL 동시 실행과 구버전 공존은 별도 미완료다. [PostgreSQL Windows 배포 안내](https://www.postgresql.org/download/windows/)가 연결한 [EDB 실행 파일 배포처](https://www.enterprisedb.com/download-postgresql-binaries?lang=en)의 Windows 16.15 ZIP(fileid=1260494)을 사용했다. 로컬 ZIP SHA-256은 `5e8afffe67daf949aeeb03b74951f1ec2324e1888f73fbd036ab0e567ab004d9`이다. 내려받은 파일의 재현 식별값이며 공급사 별도 서명을 검증했다는 뜻은 아니다. 실행 파일/클러스터/자격파일/합성 DB 결과는 배포 및 Git 제외 폴더에 있다.
- **위치:** `src/lib/db/index.ts`, `schema.ts`, `migrations.ts`, `src/app/api/teacher/roster/route.ts`, 업로드 API와 오류 응답 경로.
- **조건·영향:** 초기 SCHEMA 실행과 마이그레이션 잠금의 범위가 다르고 schema에는 계정 권한·설정·보고서 생성 등의 데이터 보정도 포함된다. 기존 복원 DB에서 초기화 성공한 과거 기록은 빈 DB 동시 초기화·모든 구버전 앱 호환을 증명하지 않는다. 일부 API의 원래 오류 메시지 반환과 업로드 메모리 예산도 별도 확인이 필요하다. `serverActions.bodySizeLimit`을 Route Handler 업로드 전체 제한으로 간주하면 안 된다.
- **최소 대응·검증:** 빈 DB/현재 DB/이전 스키마 각각에서 다중 초기화와 실패 후 재시도, 예상하지 않은 신규 마이그레이션이 있는 구버전 앱 실행을 격리된 실제 PostgreSQL로 확인한다. 기존 적용 마이그레이션은 수정하지 않는다. 입력 크기·파싱 시간·오류 노출 테스트와 전체 잠금 파일 취약점 검사를 수행한다. 이번 보안 검토는 F16의 확인된 파서 문제이며 전체 의존성 무결성 인증이 아니다. 운영 초기화·헬스 조회·롤백은 실행하지 않았다.

## C. 선택 개선

| 항목 | 위치·발생 조건 | 영향·최소 개선 | 검증 |
|---|---|---|---|
| O01 요청 문맥의 공통화 | 여러 API와 `plan-service.ts`, `report-service.ts`, `journal-service.ts`에 역할·회차·버전 검사 분산 | F01~F04를 고치면서 검증된 사용자/학년도/팀/회차/기대 버전을 전달하는 작은 공통 구조를 도입하면 새 경로의 누락을 줄일 수 있음. 권한을 일괄 강화해 기존 공유 열람을 바꾸지 않음 | 역할·활성/제거·현재/이전 회차의 공통 회귀 표 |
| O02 큰 파일의 책임 분리 | `exam-service.ts`, `evaluation-service.ts`, `inquiry-data.ts`; 정책·SQL·출력 조합이 한곳에 있음 | 즉시 재작성보다 경계 수정 후 출처 수집·정책·결과 저장을 작은 단위로 분리. 성능은 측정 후 조정 | 기존 동작과 SQL 수/응답 시간 비교, 근거·권한 불변 |
| O03 역사 문서의 표기 정리 | `ARCHITECTURE_REVIEW.md`, `implementation_plan.md`의 과거 미구현/분석 종류 고정 문구 | 최신 결정·수정 증거로 연결하되 과거 판단·배포 사실은 삭제하지 않음. 이번에는 상태 요약만 추가 | 실제 코드·검증 날짜·운영 확인 날짜가 구별되는지 문서 대조 |

## 후속 수정 순서와 인계

1. **기록·승인 경계:** F01~F04, F13을 함께 해결한다. 화면/요청/서비스/DB가 같은 회차·제출본을 가리키고 초안이 살아 있어야 한다. G01·G02의 PostgreSQL 경합 검증을 붙인다.
2. **수업 진행·교사 확인:** F05~F08, F14. 단일/여러 변인/연쇄 탐구의 화면·완료 조건을 2번 상세 설계 대조에서 마무리한다. 중간 점검을 다음 회차 강제로 해석하지 않는다.
3. **AI·외부 경계:** F09~F12, F15~F17과 G03~G07. F09의 식별정보 처리와 F16의 취약 파서는 해당 기능을 확대 사용하기 전에 우선 해결한다. 시험 기능 사용 일정이 먼저면 F17을 앞당긴다.
4. **교사 AI ‘피드백에 반영’:** F02·F13 해결 후 구현한다. 항목마다 즉시 자필 문장 뒤에 누적, 반복 클릭 중복 방지, 수동 수정 보존, 교사/팀/제출본/AI 결과별 초안 분리, 명시적 수정 요청 때만 전송한다. 클릭마다 실제 AI 호출하거나 전체 교사 글을 재작성하는 설계를 전제하지 않는다. 버튼 미구현 자체는 이미 합의된 후속 과제이므로 이번 결함으로 중복 집계하지 않았다.
5. 실제 기기·외부 연동 검증은 접근·승인이 준비된 뒤 수행한다. 채팅 새 메시지 알림·메시지별 읽음은 현재 경계 수정 이후 순서를 유지한다. 모든 수정·검증을 마친 뒤에도 새 사용자 배포 지시 없이는 운영에 반영하지 않는다.

원래 작업으로 이어갈 문장:

> 현재 미커밋·미추적 작업본의 1번 구조 검토 결과는 `ASTRA_REVIEW_FINDINGS.md`에 있습니다. F01~F04·F13의 회차/제출본/초안 보존부터 수정하고, F05~F08의 진행·열람 문제와 개인정보·업로드·시험 근거 문제를 후속 처리하세요. 기존 198개 테스트와 타입 검사는 통과했지만 합성 14개는 현 결함 재현 증거이며 수정 완료 증거가 아닙니다. 제품·운영은 이번 검토에서 변경하지 않았습니다. 실제 AI·Sheet·푸시·운영 DB 쓰기·배포·Git 커밋·푸시는 계속 보류하고, 위 보고서의 PostgreSQL·실제 기기·외부 연동 미확인 범위를 완료로 간주하지 마세요.

### 최종 인계 기록

- 제품 관련 238개 파일의 SHA-256 전후 비교 결과: 변경 0개, 누락 0개. 보고서 작성 후 현재 작업본의 소스·스크립트·공개 파일·주요 설정이 검토 시작 때와 같음을 확인했다.
- 원래 작업 `01a0714d-4de5-7e43-92ef-404b306168d1`에 이 보고서를 파일 패널로 여는 요청을 보냈고 도구가 `queued`를 반환했다. 같은 앱 창에서 해당 작업을 열 때 보고서가 표시되도록 대기 중이다. 작업 간 메시지 발송이나 원래 에이전트의 자동 재개를 수행한 것은 아니다. 이어갈 지시는 위 인계 문장을 사용한다.
- `PROJECT_STATUS.md`에는 이번 검토 완료 범위·미확인 범위와 보류 사항만 추가한다. 원래 미커밋 변경과 과거 배포 기록은 유지한다.
