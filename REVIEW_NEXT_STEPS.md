# Astra 전체 구조 검토·후속 수정 인수인계서 (2026-09-08)

현재 운영은 science-inquiry-platform-00025-p9s이다. 추가 배포·운영 변경·Git 커밋·푸시는 보류한다. 이번 문서는 작업 배정 추천이며 모델 변경이나 새 작업 생성, 검토 완료를 뜻하지 않는다.

## 새 작업에서 바로 시작할 일

프로젝트: `C:/Users/user/Desktop/Science qury platfrom`. 사용자가 승인한 첫 작업은 Astra High의 전체 구조 검토다. 우선 검토를 완료하고 근거 있는 문제 목록을 작성한다. 이 단계에서 제품 코드나 운영 데이터를 수정하지 않는다. 로컬 검토 보고서 작성과 외부 자격정보를 격리한 합성 검증은 가능하다.

1. `AGENTS.md`, `PROJECT_STATUS.md` 전체, `CURRENT_DECISIONS.md`, 이 문서를 읽는다. `implementation_plan.md`는 검토 대상 절부터 읽고 충돌 시 범위를 넓힌다. Next.js 코드를 다룰 때 설치된 `node_modules/next/dist/docs/`의 해당 지침을 확인한다.
2. 현재 작업 폴더의 수정·미추적 파일을 포함해 검토한다. Git 기본 브랜치만 보거나 새 worktree가 현재 변경을 포함한다고 가정하지 않는다. 파일 삭제·덮어쓰기·체크아웃·커밋·푸시는 하지 않는다.
3. `ARCHITECTURE_REVIEW.md`는 이전 분석 기록이다. 오래된 미구현·미배포 표기와 후속 완료 기록이 섞여 있으므로 현재 코드·테스트와 대조한다. 이전 결론이나 테스트 개수만으로 안전성을 확정하지 않는다.
4. 아래 검토 범위를 확인한 뒤 `ASTRA_REVIEW_FINDINGS.md`에 확정 오류, 검증 부족, 선택 개선을 나누어 기록한다. 보고서 작성 후에만 상태 문서에 검토 완료 범위와 미확인 범위를 갱신한다.
5. 필수 수정의 우선순위·파일·검증 방법을 다음 구현 작업에 인계한다. 실제 AI·Google Sheet·푸시·운영 DB 시험 호출이나 배포를 검토 완료의 자동 다음 단계로 실행하지 않는다.

## 배포 사실과 검증의 한계

- 앞선 작업에서 이미 `00025-p9s`로 배포했다. 사용자가 이후 추가 배포를 중단하고 Astra 검토를 먼저 하도록 정정했다. 이전 배포 승인을 새 배포에 재사용하지 않는다.
- 당시 기록: PostgreSQL 16 수동 백업 `1788830618056`, 임시 복원본에서 3중 초기화와 Cycle 전환 경합 검증. 최초 잠금 테이블 동시 생성 충돌을 수정했다. 77개 세션 연결과 10개 보호 대상의 행 수·내용 해시 불변성을 확인했다. 임시 인스턴스는 삭제했다.
- 당시 로컬 검증: 49개 테스트 파일·198개 테스트, 타입 검사, 49개 경로 빌드 통과. 배포 후 읽기 전용 감사에서 연결 오류·일지 중복 0건이었다. 이는 이번 인수인계 시점의 새 운영 조회 결과가 아니다.
- 실제 학생 기기·새 AI 기능의 실제 응답 품질·이번 준비물 전송 수정 전체의 실제 Sheet 연동·모든 기능 조합의 PostgreSQL 동시 실행은 완료하지 않았다.
- 운영에는 새로운 스키마가 적용돼 있으므로 과거 앱 이미지로 단순 복귀해도 안전하다고 가정하지 않는다. 복구는 호환성 검토 대상이며 현재 실행 요청이 아니다.

## 전체 구조 검토 범위

- 화면→API→서비스→DB의 권한과 상태 전환: 학생/팀장/제거 팀원/교사/동아리 담당/마스터, 제출 고정본과 승인·재제출, 완료 회차의 읽기 전용 보존.
- 글 보존: 항목·탭·외부 사이트 이동, 늦은 저장 응답, 한글 조합, 재접속, 여러 기기 충돌, 교사 피드백 초안. 서버 공유 저장과 기기 복구 초안을 구분한다.
- 동시 실행: 팀 이동과 평가 공개, 문서 저장과 회차 종료, 잠금 순서, 마이그레이션, 이전 화면·구버전 요청, 전송 응답 유실과 재시도. 모의 DB 결과와 실제 PostgreSQL 결과를 구분한다.
- AI: 근거 스냅샷, 프롬프트·스키마 버전, 학생 식별정보 보호, 입력문 속 지시 처리, 만료·소유권·결과 재사용, 중단 후 재시도, 단일 탐구 및 선택적 학생 판단.
- 준비물·푸시: 대상과 소유권, 고정된 설정, 중복·실패 복구, 과거 전송 이행, 체험 계정 분리. 실제 외부 전송은 실행하지 않는다.
- 업로드 제한·오류 노출·라이브러리 보안·학년도 격리·초기화와 마이그레이션 경계·큰 파일 책임 분리. 추정 기술부채와 재현된 오류를 구분한다.

탐색 시작점: `src/components/use-document-editor.ts`, `teacher-team-review.tsx`, `plan-ai-review-panel.tsx`, `src/lib/plan-ai-review.ts`, `plan-snapshots.ts`, `cycle-workflow.ts`, `cycle-analysis.ts`, `cycle-evidence.ts`, `db/migrations.ts`, `evaluation-service.ts`, `team-mutation-locks.ts`, `material-sheet-transfer.ts`. 파일 존재와 실제 연결 관계는 검토 시 확인한다.

## 작업별 권장 모델

아래는 프로젝트 위험도에 따른 추천이지 모델 간 실측 성능표가 아니다. 플랫폼 내부 학생용 AI 모델을 바꾸라는 결정도 아니다.

| 순서 | 작업 | 추천 모델 | Reasoning |
|---|---|---|---|
| 1 | 전체 구조·권한·기록 보존·Cycle 설계 독립 검토 | Astra | High |
| 2 | 단일/여러 변인/연쇄 탐구의 화면·완료 조건·AI 근거 설계 대조 | Astra | High |
| 3 | 확인된 오류 수정과 회귀 검증 | Sol | High |
| 3 이후 | 교사 AI 검토의 피드백에 반영 기능 구현·회귀 검증 | Sol | High |
| 4 | PostgreSQL 동시 실행·오래된 요청·이전 버전 호환·복구 검증 | Astra | High; 어려운 경합에만 XHigh |
| 5 | 실제 기기·글 보존·외부 연동·교사 화면 검증 및 수정 | Sol | High |
| 6 | 실제 AI 응답의 근거·교육적 적절성 평가 기준과 합성 사례 준비 | Astra | High |
| 7 | 채팅 알림·메시지별 읽음 구현 | Sol | High; 권한·이력 설계는 Astra 검토 |
| 8 | 확정된 문서 정리·반복 검증 실행 | Sol | Medium |
| 9 | 최종 변경 검토·배포 준비 평가 | Astra | High |

실제 AI·Sheet·푸시·운영 쓰기는 현 보류 상태에서 실행하지 않는다. 실제 기기 검증은 기기 접근이 필요하다. 검토 결과가 우선순위를 바꿀 수 있으며 중대한 문제는 새 기능보다 먼저 수정한다.

## 작업 맥락

전체 구조 검토는 같은 프로젝트의 새 읽기 전용 작업을 권장한다. 기존 결론을 그대로 추인하지 않고 현재 코드·검증 증거로 판단한다. 미커밋 파일을 포함한 현재 작업본을 검토해야 하며 기본 브랜치만 검토하면 배포 코드가 누락될 수 있다. 이 작업은 수정·결정 이력의 기준으로 유지하고 검토 결과를 받아 수정한다. 새 작업은 아직 생성하지 않았다.

원래 작업 ID는 `01a0714d-4de5-7e43-92ef-404b306168d1`이다. 사용자는 새 작업의 검토 결과를 원래 작업으로 보내 이어가도록 승인했다. 현재 인수인계 작성 환경에는 새 작업 생성·작업 간 메시지 도구가 없어 실행하지 못했다. 새 작업에서 해당 도구가 제공되면 검토 보고서 경로·주요 발견·다음 수정 범위·배포 보류를 원래 작업에 전달한다. 도구가 없다면 보고서 경로와 이어갈 문장을 사용자에게 제공한다. 전달이나 자동 재개가 실제 성공하기 전에는 성공했다고 보고하지 않는다.

## 교사 AI 검토의 ‘피드백에 반영’ — 최신 확정 요구

사용자가 말한 대상은 학생이 제출한 계획서에 대한 AI 검토 항목이다. AI가 학생 계획서를 대필하거나 제출본을 수정하는 기능이 아니다. 앞서 제안했던 ‘여러 항목을 선택한 뒤 별도 버튼으로 한꺼번에 추가’ 방식은 폐기한다.

1. 각 AI 검토 항목의 `[피드백에 반영]`을 누르면 해당 항목을 학생에게 전달하기 좋은 수정 요청 문장으로 정리하여 교사의 수정 요청 메시지 입력란에 즉시 추가한다.
2. 교사가 이미 작성하거나 수정한 문장은 유지한다. 여러 항목을 반영하면 같은 입력란에 누적한다. 반영을 위한 별도의 일괄 확정 단계는 두지 않는다.
3. 교사는 모인 문장을 읽고 수정·삭제할 수 있다. 실제 `[수정 요청 보내기]`를 눌렀을 때만 기존 권한·상태 검사에 따라 저장·전달·알림이 이루어진다.
4. 반영 버튼 자체는 학생에게 전송하거나 계획서 원문·고정 제출본·승인 상태를 변경하지 않는다. 교사 입력란과 기기 복구 초안의 변경이다.
5. 같은 항목 반복 클릭으로 중복 삽입되지 않도록 하고 반영 여부를 표시한다. 교사가 다듬은 전체 메시지를 다음 반영 때 AI가 다시 써버리지 않는다. 학생에게 필요한 내용만 정리하고 근거 없는 지시를 추가하지 않는다.
6. 초안·반영 상태는 교사/팀/제출본/AI 검토 결과 범위를 구분한다. 다른 팀 이동, 재제출·새 분석, 새로고침, 늦은 응답에서 다른 제출본의 내용과 섞이거나 교사 글이 유실되지 않게 검증한다. 오래된 제출본에 대한 실제 수정 요청은 서버에서 다시 검사한다.

‘정리’의 구체적 구현은 검토 후 결정한다. 기존 구조화 결과에서 문장을 구성할 수 있는지 먼저 확인한다. 클릭마다 실제 AI 호출이나 전체 메시지 재생성을 전제하지 않는다. 현재는 요구사항만 확정했으며 버튼·초안 연결 구현과 검증은 미완료다.

완료 검증 예: 교사 자필 문장 뒤 항목 A·B가 누적됨, A 재클릭 시 중복 없음, B 추가 후 앞 문장 수동 수정 보존, 초안 복구·팀 전환 격리, 클릭만으로 수정 요청 API·학생 알림 없음, 명시적 전송 시 검토한 메시지만 전달, 재제출된 오래된 검토의 전송 차단.

## 물로켓 사례를 반영한 검토 기준

- 근거 대화: `탐구 계획서 수정 포인트` (6a9f6c24-5d5c-83e8-98d7-e59e6fa63620). 사용자: 물로켓은 2차 탐구가 아니라 여러 변인 탐구로 설계한 사례. 이전 AI의 연쇄화 제안을 사용자 확정 의도로 취급하지 않는다.
- 단일 탐구, 하나의 계획 안에서 여러 변인을 각각 비교하는 탐구, 실제 결과가 후속 질문을 만드는 연쇄 탐구를 모두 허용한다. 강제 유형 선택 UI나 새 DB 분류는 검토 전 확정하지 않는다.
- 여러 실험을 자동으로 여러 Cycle로 쪼개거나 새 승인·보고서를 강제하지 않는다. 첫 회차 보고서를 무조건 중간보고서로 표시하지 않는다.
- 최종 분석은 이전 회차·학생 판단이 없어도 실제 자료만으로 성립해야 한다. 후속 회차·학생 판단 부재를 감점 근거로 삼지 않는다.
- 여러 변인의 개별 비교를 검증하지 않은 조합의 최적 조건으로 과장하지 않는다. 연쇄 탐구의 우수성을 일률적으로 전제하지 않는다.
- 현재 transitionCycle은 finish_project에 최소 회차 수를 요구하지 않는다. 그러나 한 회차 최종 종료, 중간 점검 뒤 같은 탐구 계속/종료, UI와 분석 종류 고정 규칙, 프롬프트의 이전 회차 가정을 함께 검증해야 한다. 전체 구현 적합성은 아직 미확인이다.

## 검토 산출물

확정 오류 / 검증 부족 / 선택 개선을 구분한다. 각 항목에 코드 위치, 재현 조건, 사용자 영향, 최소 수정, 검증 방법을 기록한다. 기존 문서의 오래된 미완료 문장만으로 현재 오류라고 결론 내리지 않는다. 198개 테스트와 복원 검증은 모든 경쟁 상황·실제 모델 품질·실제 기기 검증 완료를 뜻하지 않는다.
