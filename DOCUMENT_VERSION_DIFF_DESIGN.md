# 계획서·보고서 두 버전 비교 구현 설계

작성: 2026-09-11 · 상태: 설계 완료, 제품 구현·실행 검증·배포 전

## 1. 기준과 요구의 우선순위

대조한 소스는 사용자가 지정한 `C:/Users/user/.codex/worktrees/b8c4/Science qury platfrom`의 현재 작업본이다. Git HEAD나 이 작업의 최초 Desktop 폴더를 최신 소스로 간주하지 않았다. 기준 문서는 그 폴더의 `DOCUMENT_VERSION_DIFF_HANDOFF.md`, `PROJECT_STATUS.md`, `CURRENT_DECISIONS.md`와 `implementation_plan.md`의 회차·문서 보존/양식 버전 절이다. 배포 현황은 상태 문서에 기록된 79차를 참고했으며 운영 서버를 새로 조회하지 않았다.

**이번 사용자 지시가 인수인계서의 최신 저장본 고정 비교 권장안보다 우선한다.** 같은 팀·같은 문서 종류·같은 탐구 회차에서 A와 B를 각각 선택한다. 최신 저장본은 선택지 하나이며 반드시 포함할 필요가 없다. 지난주 기록과 어제 기록도 비교할 수 있다.

| 구분 | 내용 | 처리 |
| --- | --- | --- |
| 사용자 확정 요구 | 학생·교사 계획서·보고서 공통, 임의의 두 버전, 같은 회차, 변경 항목 우선, 색과 글꼴 표시, 모바일 우선 | 첫 구현 필수 |
| 사용자 확정 보존 요구 | 미저장 초안, 기존 복원 권한, 제출본, 완료 회차 기록 | 조회·비교로 변경하지 않음 |
| 인수인계 권장 중 대체 | 과거 하나와 최신 서버 저장본 고정, `이전 → 최신` 고정 표기 | `기준 A → 비교 B`로 변경 |
| 인수인계 권장 중 채택 제안 | AI 없는 공통 비교, 선택 원문 지연 조회, 변경 구절 강조, 상태/피드백 분리 | 아래 구현안에 반영하되 사용자 개별 확정으로 기록하지 않음 |
| 이번 세부 설계 제안 | 전체 화면형 비교창, A/B 맞바꾸기, 날짜 필터·더 보기, 색상·계산 상한 | 구현을 위한 권장 기본값 |
| 별도 확장 제안 | 계획서의 고정 제출본을 독립 선택지로 추가 | 보존 요구와 선택 기능을 구분; 첫 기본 범위에 필수로 넣지 않음 |

이번 산출물은 설계다. 제품 코드 변경·신규 API·DB 이행·배포를 완료했다는 뜻이 아니다. 아래 새 파일명과 API는 모두 구현 예정 이름이다.

## 2. 현재 소스 대조 결과와 재사용 경계

| 실제 위치 | 확인한 사실 | 설계 반영 |
| --- | --- | --- |
| `src/lib/document-history.ts` | `getDocumentHistory`는 최근 30건 메타데이터만 반환. 사용자 권한 인자가 없음 | 스냅샷 저장 구조 재사용. 원문 API 앞에는 별도 권한 검사. 페이지형 목록 추가 |
| `src/components/document-history-panel.tsx` | 네 화면의 공통 패널. 현재는 작업·작업자·시각·복원 버튼만 존재 | 진입점 확장. 비교와 복원을 별도 동작으로 유지 |
| `src/lib/document-history.ts` | 계획서 이력은 주로 변경 직전 `formData`, 상태, 피드백. 일반 이력에 양식 정의 없음 | 이력 시각을 본문 작성/저장 완료 시각으로 표현하지 않음. 양식 출처 신뢰도 별도 표시 |
| `src/lib/document-history.ts` | 보고서는 `form_data` 위에 `report_fields`를 병합하고 모든 저장 역할의 사용자 ID·설명 및 선택적 양식 정의 보관 | 현재 보고서에도 동일한 병합 우선순위 적용. 역할은 사용자 ID로 대응 |
| `src/components/use-document-editor.ts` | 전체 임시 저장은 변경 항목들을 저장하는 흐름. 하나의 저장 버튼이 여러 이력을 만들 수 있음 | 한 번 클릭당 버전 하나라고 약속하지 않음. 시간 근접 이력을 임의 병합하지 않음 |
| `src/lib/inquiry-data.ts` | 조회 중 보고서 생성, 만료 잠금 정리, 값 변환 포함 | 새 읽기 전용 API에서 `buildInquiryData`/`getInquiryDataForTeam` 전체 호출 금지 |
| `src/lib/inquiry-data.ts` | 잘못된 표/복수선택 JSON을 빈 배열로 바꾸는 기존 화면 처리 | 비교 파서는 별도로 엄격하게 작성. 손상을 삭제/미작성으로 오판하지 않음 |
| `src/lib/plan-snapshots.ts` | 계획서 제출본은 문서 스냅샷·양식·회차 정의를 고정하고 실제 제출 번호 보유 | 저장 이력과 제출본 구분. 조회 중 `createPlanSubmission`/스냅샷 생성 함수 호출 금지 |
| `src/components/teacher-team-review.tsx` | 교사 계획서 본문은 최신 제출 스냅샷을 우선 표시 | 비교의 B를 이 화면의 본문에서 가져오지 않음. 저장본 비교임을 명시 |
| `src/lib/cycle-workflow.ts` | `finish_project`는 완료 상태로 전환하고 문서 행을 남김. `start_next`에서만 `cycle_completed` 이력 기록 후 문서 행 재사용 | 종료 이력이 항상 존재한다는 가정 제거. 완료 대상 해석을 두 경로로 구분 |
| `src/components/inquiry-workspace.tsx` | 전체 완료 시 학생 편집기를 읽기 화면으로 대체 | 편집기 안의 패널만 바꾸면 완료 학생에게 기능 누락. 별도 연결 필수 |
| `src/components/cycle-analysis-panel.tsx` | 과거 회차의 고정 문서·분석을 학생/교사가 열람 | 각 회차 안에 문서 비교 진입점 추가. AI 분석 유무로 비교 버튼을 숨기지 않음 |
| `src/lib/cycle-evidence.ts` | 분석용 보고서 역할은 `memberAlias`만 있고 사용자 ID 없음 | 문서 이력의 사용자 역할과 매핑 금지. 분석 근거를 일반 이력 대용으로 사용하지 않음 |
| `src/components/document-conflicts.tsx` | 공유된 최신 내용과 내 초안의 선택·충돌 해결 기능 | 레이아웃 참고만. 비교창에 `공유 내용 사용`/`내 내용 사용` 동작을 가져오지 않음 |
| `src/components/read-only-form-fields.tsx` | 필드 종류별 읽기 표시, 잘못된 JSON 원문 보존 | 순수 표시 부분을 공통화. 삭제 필드/알 수 없는 키도 표시하도록 확장 |
| `src/lib/document-drafts.ts` | `sameFieldValue`는 JSON 문자열 비교이며 null/빈 문자열을 합침 | 기존 초안 로직은 유지. 객체 키 정렬·자료형 구분이 필요한 diff 동등성으로 직접 재사용하지 않음 |

‘지난 기록 보기’는 대화·개인 일지 기능이다. 검색 UI의 날짜/더 보기 패턴은 참고할 수 있으나, 문서 원문 조회나 동아리 담당 권한을 그대로 복사하지 않는다.

## 3. 사용자가 선택하는 버전의 의미

첫 기본 범위는 현재 저장본, 보관된 문서 변경 이력, 완료 회차의 최종 보존 상태다. 양쪽 선택기는 같은 종류의 선택지를 제공한다.

| 원천 | A/B 선택 | 식별·표시 | 보존/복원 |
| --- | --- | --- | --- |
| 활성 회차 현재 문서 | 모두 가능 | `현재 서버 저장본 · 저장 시각 · 조회 시각` | 조회 시점의 내용 고정 표시. 복원 대상 아님 |
| `document_revisions` | 모두 가능 | `9/10 14:32 항목 수정 직전 · 기록 작업자 …` | 이력 ID로 선택. 원래 복원 가능한 이력만 기존 경로 이용 |
| 다음 회차 시작 전 `cycle_completed` 이력 | 모두 가능 | `이 회차 최종 보존본 · 종료 시각` | 완료 회차이므로 복원 불가 |
| 전체 완료 후 같은 회차에 남은 문서 행 | 모두 가능 | `이 회차 최종 보존본 · 저장 시각/회차 종료 시각` | 읽기 전용. 다음 회차 문서 행은 사용 불가 |
| 계획서 제출 스냅샷 | 추가 확장 제안 | 실제 `제출본 n번`과 고정 스냅샷 ID, 철회/기존 자료 고정 여부 | 직접 복원 메뉴 추가 안 함. 기존 제출·승인 흐름 유지 |
| 브라우저 미저장 초안 | 선택지에서 제외 | `작성 중인 미저장 내용은 비교에 포함되지 않습니다` | 편집기와 기존 복구 저장소에 그대로 유지 |

예: A=9/4 기록, B=9/10 기록 / A=9/4 기록, B=현재 저장본 / A=현재 저장본, B=9/4 기록. A/B 순서에 따라 추가·삭제 의미가 바뀐다. 마지막 예도 허용하고 날짜순으로 몰래 재정렬하지 않는다. 같은 원천을 양쪽에 고르는 것은 UI에서 막고, 다른 기록의 내용이 같으면 정상적인 ‘변경 없음’ 결과다.

이력에 저장된 원문이 있는 시점만 선택할 수 있다. ‘어제’ 검색은 어제 생성된 이력을 찾는 것이며, 어제 자정/특정 시간의 문서를 추정 재구성하는 기능이 아니다. 자동 일별 백업이나 모든 키 입력의 이력도 아니다. 임의 `v1/v2` 번호, 작업자의 본문 전체 작성자 표시, 개인 기여도 점수는 만들지 않는다.

### 완료 회차 해석

1. 문서 ID → 세션 → 팀 관계와 선택한 회차의 세션 일치를 먼저 확인한다. 비교 범위 키는 문서 종류·문서 ID·회차 ID이며, 문서 ID만으로 구분하지 않는다.
2. 선택 회차가 완료됐고 해당 문서 행의 `cycle_id`가 여전히 같으면 전체 완료 경로의 보존 상태를 읽는다. 보고서 필드·역할도 같은 DB 읽기에서 병합한다.
3. 문서 행이 다음 회차로 전환됐다면 선택한 옛 회차의 `cycle_completed` 이력을 사용한다. 원문 후보가 여러 개이며 일치 여부를 입증할 수 없으면 최신 시각 하나를 임의 선택하지 않는다.
4. 최종 보존본이 없거나 손상되면 해당 선택지만 ‘자료 없음/확인 필요’로 표시한다. 정상 과거 이력 두 개가 있으면 그 둘의 비교는 계속 가능하다.
5. 분석용 `cycle_evidence_snapshots`를 최종 문서·실명 역할 복원의 자동 대체재로 쓰지 않는다. 기존 분석 원문 열람은 유지한다. 문서 원천이 없는 경우 새로 추정 생성하지 않는다.

계획서 제출본 선택을 나중에 추가할 경우에도 위 범위 키를 모두 검증한다. 보고서에는 같은 제출 스냅샷 모델이 없다. `submit` 이력은 제출 **전** 상태이며 제출 서비스가 제목 등을 병합할 수 있으므로 그대로 ‘제출 완료본’으로 이름 붙이지 않는다.

## 4. 모바일 우선 공통 화면

권장: 기존 패널의 제목을 `변경 이력 · 버전 비교`로 확장하고 `두 버전 비교` 버튼으로 공통 비교창을 연다. 작은 화면은 전체 화면, 넓은 화면은 넓은 대화상자로 제공한다. 기존 편집기는 마운트 상태로 유지한다. 인수인계의 인라인 펼침도 가능하지만 현재 학생 패널은 좁은 보조 영역이므로 긴 본문/표 비교를 위해 별도 비교창을 권장한다.

화면 배치 예시(합성 내용):

```text
계획서 버전 비교                         [닫기]
과탐실 · 예시 팀 · 1차 탐구
미저장 작성 내용은 비교에 포함되지 않습니다.

기준 A   [9/4 15:20 · 항목 수정 직전   ▾]
                         [A/B 맞바꾸기]
비교 B   [9/10 14:32 · 항목 수정 직전  ▾]
방향: A에서 B로 달라진 내용

[변경 항목 3개]  [전체 항목]
추가 1 · 삭제 1 · 수정 1
[연구 목적] [실험 과정] [팀원 역할]

연구 목적                         [수정]
A · 9/4 …   용액을 10 mL 사용한다.
B · 9/10 …  용액을 20 mL 사용한다.
                (10 삭제 / 20 추가 강조)

[상태·교사 피드백 변화 펼치기]
[A 원문 보기]                  [B 원문 보기]
```

- 처음 열 때 A=최근 과거 이력, B=현재 저장본(완료 회차는 최종 보존본)을 편의상 선택한다. 어느 쪽도 고정하지 않는다. 이력이 하나뿐이면 ‘비교할 다른 기록이 없습니다’와 원문 보기만 제공한다.
- 목록에서 특정 이력의 `비교`를 누르면 그 이력을 A로 미리 선택한다. B도 언제든 다른 과거 이력으로 바꾼다. 서버 원문이 확인되기 전에는 비교 결과를 표시하지 않는다.
- A/B 선택창에는 날짜 필터와 `이전 기록 더 보기`를 제공한다. KST 날짜의 시작 이상/다음 날 시작 미만으로 조회한다. 30개만 보이고 그 이전이 없는 것처럼 안내하지 않는다.
- 결과 기본은 변경 항목만. 그 안에서는 문서의 실제 항목 순서를 유지하고, 사라진 항목도 원래 제목과 위치를 최대한 남긴다. 상단 요약의 항목 버튼을 누르면 해당 항목으로 이동한다.
- 모바일은 항목마다 A 위/B 아래. 넓은 화면은 같은 항목의 A/B를 좌우로 표시한다. 표는 모바일에서 변경 행 카드와 열 이름을 우선 제공하고 원래 표는 별도 펼침 영역에서 가로 스크롤한다. 페이지 전체 가로 스크롤은 발생하지 않아야 한다.
- 추가: 초록 계열 배경/글자 + **굵게** + ‘추가’. 삭제: 빨강 계열 + 취소선 + ‘삭제’. 수정: ‘수정’ 표시와 A의 삭제 구절/B의 추가 구절. 색만으로 구분하지 않는다. 기존 한글 본문 글꼴을 유지하고 굵기/장식으로 의미를 표현한다.
- 내용·역할 변경과 양식/상태/교사 피드백 변경을 별도로 집계한다. 상위 필드 수정 1개에 하위 구절 10개가 있어도 본문 항목 수는 1개다. 역할은 변경된 사용자별 1개로 집계하고 따로 표시한다. 양식만 바뀌었으면 ‘본문 변경 없음 · 양식 변경 있음’으로 구분한다.
- 동일 내용이면 ‘선택한 두 버전의 본문과 팀원 역할이 같습니다’. 비교 불가 항목이 있으면 ‘확인 가능한 항목의 변경 없음 · 확인 필요 n개’로 표시하며 전체 동일이라고 단정하지 않는다.
- 닫기/ESC/뒤로 가기는 비교창만 닫고 열었던 버튼으로 포커스와 스크롤을 돌린다. 작성 중이던 필드의 커서 범위는 유지하되 자동 입력 포커스로 잠금을 새로 획득하지 않는다. 비교창 포커스 순환, 제목 연결, 결과 변경의 짧은 `aria-live` 안내, 44px 수준의 터치 영역, 200% 확대를 검증한다.

## 5. 조회 API와 데이터 계약

새 서비스 `src/lib/document-version-reader.ts`를 쓰고 `document-history.ts`의 기록·복원 서비스는 유지한다. 새 목록·원문/비교 경로는 **읽기 전용**이다. 공통 타입은 서버 전용 의존성을 포함하지 않는 `document-version-types.ts`에 둔다.

### 제안 API

| 경로 | 입력 | 결과 |
| --- | --- | --- |
| `GET /api/document-history/versions` | `documentType`, `documentId`, `cycleId`, 선택 `cursor`, `fromDate`, `toDate` | 권한 범위의 버전 메타데이터, 현재/최종 선택지, `nextCursor`, 한계 안내 |
| `GET /api/document-history/compare` | 같은 범위 키 + `aKind`, `aId`, `bKind`, `bId`, 선택 현재본 기대 지문 | 정규화한 A/B 원문, 각 출처·시각·양식 신뢰도, 복원 가능 원천 표시 |

비교는 반환된 두 문서를 공통 순수 엔진에서 계산한다. 한쪽 원문 보기만 필요하면 같은 조회 서비스의 단일 대상 함수를 사용하는 원문 GET을 추가할 수 있다. 원문을 목록 응답에 넣지 않는다.

- `kind`: `current`, `revision`, `cycle_final`을 첫 단계에 허용한다. `revision`만 필수 이력 ID를 받는다. `cycle_final`은 서버가 해석하고 결과에 실제 원천 ID를 돌려준다. 확장 시 `plan_submission`을 별도 열거값으로 추가한다.
- `VersionRef`는 선택 요청이다. 반환 `ResolvedVersion`에는 `documentType`, `documentId`, `sessionId`, `teamId`, `cycleId`, 실제 `sourceKind/sourceId`, `capturedAt`, 확인 가능한 `documentUpdatedAt`, `eventAt`, `action`, `snapshotSemantics`, `formData`, `roles`, `definition`, `definitionSource`, `issues`를 둔다.
- 이력은 `snapshotSemantics=before_action`, 종료 이력/완료 문서는 `cycle_final` 등으로 구분한다. 시험 근거 고정 이력도 작업 전 상태라는 일반 문구 대신 실제 `exam_evidence_capture` 의미를 표시한다.
- 신선도 지문은 범위·원천·원문·역할·양식·상태·피드백·가능한 저장 번호/시각을 포함한다. 보고서는 `write_version`을 활용하고 계획서는 현재 전용 쓰기 번호가 없으므로 내용/메타데이터 지문으로 확인한다. 기존 AI 스냅샷 해시의 의미를 변경하지 않는다.
- 지문은 원문 확인용이며 인증 수단이 아니다. 브라우저가 보낸 원문을 신뢰하거나 지문 일치만으로 권한을 허용하지 않는다.
- 목록은 `(created_at DESC, id DESC)`의 안정적인 커서 페이지, 기본 30건을 제안한다. 커서에 범위/필터를 묶고 서버에서 검증한다. 현재/최종 선택지는 이력 페이지와 분리한다. 종료 이력과 최종 선택지가 같은 원천이면 하나의 선택 대상으로 취급한다. 같은 시각의 ID 정렬은 화면 순서일 뿐 실제 쓰기 선후의 증거가 아니다.
- 없는 날짜에 가장 가까운 기록을 자동 선택하지 않는다. 누락/손상 원천은 목록에 확인 필요로 남기되 거짓 빈 문서를 만들어 비교하지 않는다.

### 일관된 읽기와 조회 중 변경

권한·회차·두 원천·보고서 필드/역할·양식은 하나의 PostgreSQL 연결에서 `BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ READ ONLY`로 읽고 즉시 종료하는 방식을 제안한다. 기본 READ COMMITTED에서 여러 SELECT만 묶으면 같은 읽기 시점을 보장하지 못한다. 전용 읽기 함수에는 생성·잠금 정리·스냅샷 고정·감사 쓰기 등의 부작용이 없어야 한다. DB 최초 부팅/마이그레이션은 기존 앱 초기화 책임이며 비교 요청에서 새로운 자료 이행을 유발하지 않는다.

기존 `getCurrentUser`로 세션을 검증하고 읽기 안에서도 계정 활성/역할/학년도/비밀번호 변경 필요 및 소속을 확인한다. 반환 직전 현재 세션·접근 권한을 다시 확인해 읽는 사이의 권한 회수가 관찰되면 원문을 폐기한다. 응답 후 이미 브라우저로 전달된 자료를 소급 회수한다고 주장하지 않는다.

처음 반환된 A/B는 클라이언트 메모리에서 고정한다. 기존 4초 대시보드 갱신이 비교 결과를 덮어쓰면 안 된다. 비교 중 새 저장본 존재를 알게 되면 ‘새 저장 내용이 있습니다’만 안내한다. `현재 저장본 다시 불러오기`를 눌렀을 때만 해당 측을 갱신한다.

현재본 B를 고정한 채 A를 바꾸는 추가 요청에는 B의 기대 지문을 함께 보낸다. 서버가 그 B를 더 이상 같은 현재 상태로 확인할 수 없으면 409를 반환하고 기존 표시를 유지한다. 사용자가 새 현재본을 선택해 다시 비교하게 한다. 새로 저장된 B로 몰래 교체하거나 예전 B를 영구 재조회 가능한 버전처럼 약속하지 않는다. A/B 맞바꾸기는 이미 받은 두 원문으로 수행할 수 있다.

빠른 선택 변경은 AbortController와 요청 순번으로 이전 응답을 무시한다. 요청 중 선택과 마지막 성공 결과의 선택을 따로 관리해, 새 선택 제목 아래 옛 결과가 표시되지 않게 한다. 연결 실패/시간 초과는 재시도하고 작성 초안을 건드리지 않는다. 로그인 변경·권한 거절 시 비교 원문 메모리를 정리한다.

응답에는 `Cache-Control: private, no-store`. 캐시/정적 생성에 넣지 않는다. 조회 URL은 범위·원천 ID/필터만 포함하고 본문·이름·피드백은 포함하지 않는다. 브라우저 영구 저장소·분석 로그·외부 AI에 비교 원문을 추가 보관하지 않는다. 잘못된 입력은 400, 미인증/유효하지 않은 계정은 403, 접근 불가/존재하지 않는 범위는 같은 404 안내, 바뀐 현재본은 409로 구분하는 안이다. 예상 밖 오류는 공통 안전 문구로 반환한다.

## 6. 권한과 초안·복원 보존

| 사용자/상태 | 비교 | 복원 |
| --- | --- | --- |
| 현재 학년도 활성 일반 팀원·이전 팀장(현재 팀원) | 기존 문서 열람 범위에서 허용 | 불가 |
| 현재 활성 팀장 | 동일 | 활성 회차의 기존 이력만 기존 서비스로 가능 |
| 제거·다른 팀 이동·비활성 학생, 보관 팀 학생 | 해당 팀 새 조회 차단 | 불가 |
| 유효한 교사 | 기존 공동 문서 열람 범위, 보관 팀/과거 문서 보존 | 기존 활성 회차 복원 범위 유지 |
| 완료 회차 | 권한 있는 사람 읽기 허용 | 교사·팀장 모두 불가 |

교사 문서 열람은 현재 `teacher/team/[teamId]`가 교사 인증 후 팀 문서를 공동 조회하는 범위를 따른다. 동아리 설정·계획 승인·79차 지난 기록 목록의 담당 제한을 비교에 덧붙이지 않는다. 교사 과거 학년도 직접 문서 열람도 목록의 현재 학년도 제한을 근거로 임의 축소하지 않는다. 계정 자체의 현재 유효성 검사는 유지한다.

학생 현재 보고서는 기존 `getStudentStageAccess`의 계획 승인 조건을 기준으로 진입/조회 경계를 대조한다. 완료 회차의 과거 보고서는 새 회차 미승인 때문에 차단하지 않는다. 쓰기 전용 `assertStudentReportAccess`나 `lockDocumentCycle`을 읽기 권한 함수로 직접 재사용하면 완료 회차가 막히므로 별도 읽기 검사가 필요하다. 기존 보고서 쓰기 서비스의 단계 조건과 화면 승인 조건은 같지 않으므로 읽기 함수 통합 과정에서 쓰기 권한까지 바꾸지 않는다.

- 학생 비교창은 `useDocumentEditor`의 `form`, `change`, `saveAll`, `useRemote`, `keepMine`을 호출하지 않는다. 사용자·문서·회차·양식 초안 키와 saved/baseValue/version을 유지한다. 비교 열기·닫기·갱신은 새 편집 잠금 획득을 요청하지 않는다. 기존 편집기의 정상 blur/잠금 해제 동작과는 구분한다.
- 교사 비교창은 `useFormDraft`와 제출본별 피드백 초안에 쓰지 않는다. 조회 뒤 `router.refresh()`를 자동 호출하지 않으며 교사의 현재 검토 대상/승인 기대값을 바꾸지 않는다.
- 복원은 이력 행의 별도 `이 상태로 복원`에 남긴다. 비교창은 원문 확인만 담당하며 A 또는 B 중 무엇을 복원하는지 모호한 버튼을 만들지 않는다. 일반 팀원에게도 비교 버튼은 제공한다.
- 학생의 미저장 상태 복원 차단, 복원 직전 이력 추가, 계획서 주제 동기화, pending 제출 철회, 복원 후 재제출·재검토 규칙을 그대로 사용한다. 완료 회차를 현재 회차에 덮어쓰는 경로는 추가하지 않는다.
- 현재 교사 복원 요청은 학생과 달리 cycleId를 보내지 않지만, 서버가 이력과 현재 문서 회차 일치 및 활성 회차를 검사한다. 이를 즉시 취약점으로 단정하지 않는다. 비교 기능 때문에 복원 대상을 새 계약으로 우회하지 않는다. 교사 복원 전 미전송 피드백의 처리/충돌도 기존 초안 보존 동작으로 회귀 확인한다.

## 7. 양식·본문·표·역할 비교 규칙

### 원문과 자료형

`src/lib/document-diff.ts`는 입력을 변경하지 않는 순수 함수로 만든다. 양쪽 원문과 표시용 정규화를 분리하고, JSON 객체 키만 정렬해 의미 없는 키 순서 변경을 제외한다. 원문 문자열의 공백·줄바꿈·유니코드를 삭제하거나 저장값을 정규화하지 않는다. `0`, `false`, 빈 문자열, null, 키 부재는 구분하며 항목 부재와 ‘미작성’을 별도로 표시한다.

보고서 `table`/`multiple_choice`/`checkbox`는 해당 양식이 입증될 때만 JSON 문자열을 해석한다. 숫자도 정의된 숫자 항목에 한해 유효한 문자열/숫자 동등성을 처리하고 원래 표기는 원문 보기로 남긴다. 일반 문자열 `"false"`, `"0"`, `"[]"`를 임의 변환하지 않는다. 복수선택은 정의상 선택 집합이므로 순서만 다르면 변경 아님; 알 수 없는 배열은 순서를 보존한다. 파싱 실패는 `unknown/invalid` 이슈이며 빈 배열 대체가 아니다.

### 필드와 양식 출처

양쪽 필드 ID와 실제 값 키의 합집합을 비교한다. 필드 라벨 변경, 필드 종류/선택지/열 변경, 값 변경을 별도 기록한다. 제목/안내 heading은 양식 변화이며 학생 본문 수정 수에 합산하지 않는다.

당시 양식은 원천에 고정된 정의가 있으면 그것을 우선한다. 현재 문서는 고정 `config_version_id`의 정의를 사용한다. 과거 계획서 일반 이력처럼 정의가 없을 때 현재 양식/가까운 제출본을 무조건 당시 양식으로 간주하지 않는다. 같은 회차의 양식 불변이 입증되는 경우에만 그 근거를 기록해 사용한다. 입증이 없으면 기본 필드명은 참고 라벨로만 제공하고 ‘당시 양식 확인 불가’ 표시, 알 수 없는 키와 원문을 남긴다. 옛 표의 열을 최신 열만으로 잘라내지 않는다.

### 텍스트

문단/줄 → 단어와 공백 토큰 → 실제 바뀐 짧은 구간의 grapheme 단위 순으로 세분화한다. 한글 결합문자·이모지 ZWJ 시퀀스를 중간 절단하지 않는다. 공백 변화만 있을 때도 알 수 있게 표시하고, 긴 본문에서 앞뒤 문맥과 `문맥 더 보기`를 제공한다. 삭제 텍스트에는 `<del>`, 추가 텍스트에는 `<ins>`와 굵기/라벨을 사용하고 React 텍스트 노드로 렌더링한다. HTML 원문 실행은 허용하지 않는다.

### 표

현재 편집기는 행을 배열 인덱스로 관리하며 행 추가는 `{}`를 붙인다. 안정적 행 ID가 있다고 가정할 수 없다. 열은 정의의 열 ID로 대응한다. 우선 동일한 행의 결정적 대응을 찾고 순서를 보존하는 정렬을 사용한다. 중간에 행 하나를 추가한 경우 뒤의 모든 행을 수정으로 표시하지 않는다. 고유하게 대응되는 동일 행의 위치 변경은 ‘순서 변경’으로 별도 표시한다.

동일 행 앵커 사이에 A/B 행이 하나씩 남고 열 구조가 같은 경우 셀 수정으로 비교할 수 있다. 여러 후보·중복 행·양식 변화로 대응이 모호하면 추측하지 않고 해당 행 추가/삭제 및 ‘행 대응 불확실’을 표시한다. 애매한 유사도만으로 서로 다른 실험 행을 한 행의 수정으로 결합하지 않는다. 원래 표 보기로 항상 확인할 수 있게 한다. 새 행 ID 도입·과거 행 ID 채우기는 첫 구현에 포함하지 않는다.

### 팀원 역할

보고서의 저장된 `userId` 합집합으로만 비교한다. 이름·학번·현재 배열 순서·현재 팀장 표시를 과거 역할의 키로 사용하지 않는다. 제거된 학생의 역할은 원문 그대로 표시하고 현재 제거 상태는 별도 안내한다. 당시 이름/팀장 정보가 보존되지 않았다면 현재 사용자 정보에 ‘현재 표시 정보’라는 의미를 부여하며 과거 명단을 재구성하지 않는다. 사용자 ID를 잃은 분석 alias는 별도 원문이며 역할 비교 불가를 표시한다.

### 계산 상한 제안

초기 제안은 항목당 20,000 grapheme, 정렬 후보 250,000쌍, 누적 계산 200ms를 세밀 강조의 한도로 두고 합성 최대 입력으로 조정한다. 결정적 연산 횟수 상한과 시간 상한을 함께 적용한다. 초과 시 해당 항목의 A/B 원문으로 전환하고 ‘긴 내용은 항목 단위로 표시합니다’라고 안내한다. 원문을 잘라 저장하거나 조용히 생략하지 않는다. 표/긴 본문은 펼침·부분 렌더링을 사용한다. 필요하면 Web Worker에서 계산하고 닫기/선택 변경 시 취소한다.

## 8. 구현 파일과 순서

| 순서 | 파일/작업 | 완료 기준 |
| --- | --- | --- |
| 1 | 새 `document-version-types.ts`, `document-version-reader.ts` | 읽기 권한·범위·원천 해석·보고서 병합·손상 구분·일관된 읽기 |
| 2 | 새 `api/document-history/versions/route.ts`, `compare/route.ts` | 30건 이후 목록, 두 임의 원천, 안전한 오류·no-store·지연 조회 |
| 3 | 새 `document-diff.ts`, 필요 시 `document-diff.worker.ts` | 타입/필드/한글/표/역할의 순수 비교 및 상한 |
| 4 | 새 `document-version-comparison.tsx`, `document-diff-view.tsx`; 기존 `document-history-panel.tsx` 확장 | 두 선택기·맞바꾸기·변경 우선·원문·실패/빈 상태·복원 분리 |
| 5 | `plan-editor.tsx`, `report-editor.tsx`, `teacher-team-review.tsx`, `teacher-report-review.tsx` | 학생/교사 네 경로의 범위 props 연결, 초안/검토 대상 불변 |
| 6 | `inquiry-workspace.tsx`, `read-only-cycle-document.tsx`, `cycle-analysis-panel.tsx` | 전체 완료·이전 완료 회차에도 같은 비교창 진입. 복원 없음 |
| 7 | `read-only-form-fields.tsx`, `src/app/globals.css` | 표시 도우미 재사용, 삭제/알 수 없는 필드 보존, 모바일/접근성 |
| 8 | 새 검증 파일 + 기존 문서/초안/회차 회귀, 안내 문서 | 아래 수락 기준 통과 및 상태 기록 |

추가 테이블 없이 첫 범위가 가능하다. 기존 문서/이력/제출 스냅샷을 조회하며 비교를 위해 새 버전을 저장하지 않는다. 페이지 조회 성능이 부족할 때만 실제 실행 계획을 보고 `(document_type, document_id, cycle_id, created_at, id)` 계열 인덱스를 점진적 마이그레이션으로 검토한다. 기존 적용 마이그레이션 수정이나 과거 스냅샷 재작성은 하지 않는다.

Next 구현 시 기준 폴더에 설치된 가이드를 따른다. 확인한 `node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md`는 `route.ts`의 GET과 기본 비캐시 동작을 설명한다. 민감 문서 응답에는 별도로 no-store를 명시하고 구현 직전 필요한 라우트/클라이언트 가이드를 다시 확인한다.

## 9. 수락 검증

모든 새 시험은 합성 자료로 수행한다. 아래는 앞으로 실행할 검사이며 이번 설계 단계의 통과 결과가 아니다.

| 영역 | 필수 사례 | 기대 결과 |
| --- | --- | --- |
| 핵심 두 버전 | 최신↔과거, 어제↔지난주, 역방향, 동일 원천, 내용 동일인 다른 이력 | 양쪽 자유 선택, 역방향 추가/삭제 반전, 동일 내용 정상 표시 |
| 탐색 | 31건 이상, 같은 시각 여러 이력, 날짜 경계, 페이지 중 새 저장, 필터 변경 | 누락/중복 최소화, 커서 범위 일치, KST 날짜 정확 |
| 원천 의미 | 필드별 저장 여러 건, 제출 전 상태, 시험 근거 고정, 복원 전 상태 | 임의 버전 번호/작성자 귀속 없음, 작업 의미 표시 |
| 현재본 고정 | 조회 후 팀원 저장, A 변경 중 B 지문 변경, 빠른 연속 선택, 늦은 응답 | 화면 조용한 교체 없음, 409/명시적 갱신, 이전 응답 무시 |
| 회차 | 같은 문서 ID의 다른 회차 조합, start_next/finish_project 전후 및 동시 읽기 | 다른 회차 결합 차단, 옛 원문/새 문서 분리, 두 종료 경로 지원 |
| 데이터 타입 | 0/false/null/빈 문자열/없는 키, 객체 키 순서, 선택값 배열, 잘못된 JSON | 자료형 의미 보존, 손상을 삭제/빈 문서로 오판하지 않음 |
| 텍스트·표 | 한글·이모지·공백·줄바꿈·긴 본문·HTML 문자열, 중간 행 삽입·이동·중복·열 삭제 | 안전한 렌더링, 문자 보존, 보수적 대응, 상한 시 원문 유지 |
| 양식 | 정의 없음, 옛 필드 삭제/라벨·kind 변경, 같은 회차 양식 출처 입증 실패 | 과거 값 숨김 없음, 신뢰도/한계 표시 |
| 보고서 역할 | form_data/report_fields 충돌, 역할만 수정, 제거 학생, alias만 남은 원천 | 병합 우선순위 일치, ID별 비교, 원문 보존/추정 금지 |
| 권한 | 일반 팀원/팀장/이전 팀장, 제거·이동·비활성·보관·학년도·세션 취소, 공동 교사/비담당/마스터 | 기존 열람/복원 범위 보존, 두 원천 모두 검사, 원문/존재 여부 유출 차단 |
| 조회 부작용 | 비교 전후 문서·이력·제출·역할·승인·완료 기록의 행/내용 대조 | 업무 데이터 불변, 조회에서 생성/저장/제출/잠금 획득 없음 |
| 초안 | 학생 글/표/역할, 교사 피드백, 열기/닫기/맞바꾸기/재조회/외부 복귀/새로고침/저장 중 추가 입력 | 기존 복구 초안과 기준값 유지, 커서/스크롤 유지, 자동 저장/전송 없음 |
| 복원 회귀 | 팀장/교사 정상 복원, 일반/완료 회차 차단, pending 제출 철회, 제거 학생 역할 | 기존 복원 서비스/감사·재검토·원문 보존 |
| 실제 화면 | 320/390px, 태블릿, PC, 키보드/ESC/뒤로, 확대·스크린리더, 큰 표 | 페이지 넘침 없음, 색 없이도 식별, 포커스 복귀, 조작 가능 |

권장 테스트 파일: `document-diff.test.ts`, `document-version-reader.test.ts`, `document-version-access.test.ts`, 비교 API 테스트와 `scripts/document-version-comparison-regression.mjs`. 기존 `document-history.test.ts`, `document-cycle-regression.test.ts`, `document-save-regression.test.ts`, `teacher-review-access.test.ts`, `scripts/editor-regression.mjs`, `scripts/document-cycle-browser-regression.mjs`의 의미 있는 회귀를 함께 실행한다. 실제 PostgreSQL에서 읽기 일관성·회차 전환/권한 변경 교차를 확인하고, pg-mem 통과를 실제 잠금/격리 검증으로 확대하지 않는다.

구현 후 집중 검사 → 타입 검사 → 전체 회귀 → 운영 빌드 → 합성 브라우저 확인 순으로 수행하고 실패/미확인 범위를 기록한다. 실제 휴대폰의 새 비교 화면 확인은 별도이며 기존 앱 이용 가능 확인을 반복 요구하지 않는다. 설계 단계에서는 제품 검사를 재실행하지 않았다. 배포와 Git 작업은 이번 설계 범위에 포함되지 않는다.

## 10. 다음 작업의 시작점

첫 구현은 **현재/이력/회차 최종본 중 두 원천 선택 + 페이지형 이력 조회 + 공통 비교창 + 네 화면과 완료 회차 연결**을 하나의 사용자 기능으로 마감한다. 제출본 독립 선택은 확장 가능하게 설계하되 기본 비교가 최신본에 묶이지 않도록 한다. 인수인계서의 기존 권장안은 과거 기록으로 유지하고, 이번 사용자 확정 사항과 이 문서의 세부 제안을 구분해 후속 구현한다.
