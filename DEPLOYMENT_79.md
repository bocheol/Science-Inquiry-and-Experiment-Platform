# 지난 기록 보기 배포 결과 — 2026-09-10

**배포 완료.** 학생·교사 대시보드의 `지난 기록 보기`를 기존 Cloud Run 서비스에 반영했다. Git 커밋·푸시는 사용하지 않고 현재 로컬 제품 파일을 감사한 뒤 Cloud Build로 이미지를 만들었다.

사이트: [비공개 운영 기록에 보존]

## 배포 결과

- Cloud Build: `[비공개 운영 기록에 보존]`, `SUCCESS`
- 이미지: `[비공개 운영 기록에 보존]`
- 새 리비전: `[비공개 운영 기록에 보존]`
- 최종 상태: `Ready=True`, 트래픽 100%, 임시 태그 0개
- 직전 정상 리비전: `[비공개 운영 기록에 보존]`

## 검증

- 배포 입력은 193개 제품 파일, 1.3MiB였다. 새 기록 기능 파일 3개가 포함됐고 `output/`, 테스트, 스크립트, 문서, 환경 파일, 비밀 파일은 제외됐다. 포함 파일의 비밀키 패턴 후보는 0개였다.
- 잠금 파일 291개가 Cloud Build 공급망 정책을 통과했다. Next.js 16.3.3 Linux 빌드, TypeScript 검사, 50개 경로 생성이 성공했다.
- 배포 전 최신 Cloud SQL 자동 백업 `[비공개 운영 기록에 보존]`이 `SUCCESSFUL`임을 확인했다. 이번 기능은 DB 스키마 변경이나 운영 데이터 쓰기를 추가하지 않는다.
- 새 리비전을 먼저 0% 트래픽으로 배포했다. 이미지 외 런타임 spec 해시는 기존과 같았고 기존 `00030-vet`의 트래픽 100%가 유지됐다.
- 0% 리비전과 전환 뒤 공개 주소에서 각각 9개 검사를 통과했다. `/api/health`, 로그인, manifest, 서비스 워커는 200이고 새 `/api/past-records` 및 교사·AI 관리 API의 미인증 접근은 403이었다.
- 최종 전환 직후 `[비공개 운영 기록에 보존]`의 ERROR 이상 로그는 0건이었다. 이는 확인 시점 결과이며 장기 무오류 보장은 아니다.
- 제품 변경 기준 전체 회귀 100개 파일·568개, 지난 기록 집중 5개, 로컬 타입 검사와 프로덕션 빌드는 배포 직전 로컬 검증 결과를 유지한다.

## 실패와 범위

첫 0% 단계 명령은 빌드 로그 줄바꿈에서 옮긴 digest에 문자 하나가 중복되어 이미지 형식 검사에서 즉시 거절됐다. 리비전·트래픽·운영 설정은 바뀌지 않았다. Artifact Registry에서 실제 digest를 다시 읽은 뒤 정상 배포했다.

실제 학생 계정으로 로그인하거나 학생 원문을 조회·수정하지 않았다. 운영 Google Sheet 쓰기, 외부 푸시 시험, Git 초기화·커밋·푸시는 수행하지 않았다. 실제 iOS·Android 기기의 새 기능 확인과 기록량이 매우 큰 경우의 검색 성능은 별도 확인 사항이다.
